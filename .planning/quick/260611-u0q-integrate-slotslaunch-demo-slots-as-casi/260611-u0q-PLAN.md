---
phase: quick-260611-u0q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/integrations/slotslaunch/__init__.py
  - backend/app/integrations/slotslaunch/client.py
  - backend/app/integrations/slotslaunch/schemas.py
  - backend/app/integrations/slotslaunch/service.py
  - backend/app/integrations/slotslaunch/router.py
  - backend/app/main.py
  - backend/tests/integrations/slotslaunch/__init__.py
  - backend/tests/integrations/slotslaunch/test_casino_router.py
  - .env.example
  - docker-compose.yml
  - frontend/src/lib/casino.ts
  - frontend/src/app/casino/page.tsx
  - frontend/src/app/casino/casino-grid.tsx
  - frontend/src/app/casino/game-launcher.tsx
  - frontend/src/components/player-nav.tsx
  - frontend/src/proxy.ts
autonomous: true
requirements: [CASINO-01, CASINO-02, CASINO-03]
user_setup:
  - service: slotslaunch
    why: "Demo slots catalog + iframe game launcher (Casino demo section)"
    env_vars:
      - name: SLOTSLAUNCH_TOKEN
        source: "Already present in .env.local (domain-bound to app.xprediction.online). Activate the free SlotsLaunch plan to light up the catalog; no code change needed when active."

must_haves:
  truths:
    - "An authenticated player can open /casino from the player nav and see a grid of demo slot thumbnails when the SlotsLaunch subscription is active"
    - "While the subscription is inactive, /casino shows a friendly 'Casino demo not available yet' empty state instead of an error or a blank/zero grid"
    - "Clicking a game opens a fullscreen iframe launcher loading only that one game (no autoplay/preload of iframes from the grid)"
    - "If the iframe fails to load (e.g. 403 from localhost), the launcher shows a graceful fallback message, not a broken frame, and this is not treated as a bug"
    - "The SlotsLaunch token is never hardcoded in source nor sent to the browser as a raw env var; the browser only receives backend-composed iframe URLs"
    - "The catalog is cached server-side in Redis (~6-12h TTL) so repeated /casino loads do not re-hit the upstream quota"
  artifacts:
    - path: "backend/app/integrations/slotslaunch/client.py"
      provides: "Async httpx client for GET https://slotslaunch.com/api/games with Origin header + token, detects the inactive-subscription error body"
      contains: "class SlotsLaunchClient"
    - path: "backend/app/integrations/slotslaunch/service.py"
      provides: "Catalog service: Redis-cached fetch + normalization into {status, games[]} with composed iframe URLs"
      contains: "async def get_catalog"
    - path: "backend/app/integrations/slotslaunch/router.py"
      provides: "Public GET /api/v1/casino/games endpoint returning {status: active|inactive, games[]}"
      contains: "casino_router"
    - path: "backend/tests/integrations/slotslaunch/test_casino_router.py"
      provides: "Hermetic tests (mocked upstream via pytest_httpx) for active, inactive, and upstream-error cases"
      contains: "def test_"
    - path: "frontend/src/app/casino/page.tsx"
      provides: "Casino (demo) Server Component page: grid + inactive empty state + fullscreen launcher entry"
      min_lines: 30
    - path: "frontend/src/app/casino/game-launcher.tsx"
      provides: "Client fullscreen iframe launcher with onError fallback (mirrors LiveFullscreenHost overlay)"
      contains: "iframe"
  key_links:
    - from: "frontend/src/lib/casino.ts"
      to: "/api/v1/casino/games"
      via: "fetch with apiBase() (cache: no-store)"
      pattern: "api/v1/casino/games"
    - from: "backend/app/integrations/slotslaunch/client.py"
      to: "https://slotslaunch.com/api/games"
      via: "httpx GET with token param + Origin header"
      pattern: "slotslaunch\\.com/api/games"
    - from: "frontend/src/components/player-nav.tsx"
      to: "/casino"
      via: "DESTINATIONS nav entry"
      pattern: "/casino"
    - from: "frontend/src/proxy.ts"
      to: "casino"
      via: "PLAYER_PROTECTED matcher + config.matcher"
      pattern: "casino"
---

<objective>
Add a "Casino (demo)" section to XPredict that surfaces SlotsLaunch demo slots:
a backend catalog proxy (Redis-cached, token-from-env) exposing
`GET /api/v1/casino/games`, and a frontend `/casino` page with a thumbnail grid
plus a fullscreen iframe game launcher. The SlotsLaunch account subscription is
NOT yet active today, so the integration MUST degrade gracefully — backend returns
`{status: "inactive", games: []}` and the frontend shows a friendly empty state —
and MUST light up with zero code changes once the free plan is activated.

Purpose: complete a credible "casino demo" surface for sales, reusing the
established external-integration (livebets/polymarket) and catalog-page patterns
without any new DB tables.

Output: a `slotslaunch` integration package (client/service/schemas/router) wired
into `main.py`, a public catalog endpoint, a `/casino` page + nav entry, the
`SLOTSLAUNCH_TOKEN` wired through settings + `.env.example` + docker-compose, and
hermetic backend tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Verified API facts (ground truth — DO NOT re-research):
# - Catalog: GET https://slotslaunch.com/api/games?token={TOKEN}&page=N&per_page=N
#   (per_page max 150). MUST send header `Origin: https://app.xprediction.online`
#   (token is domain-bound to that domain).
# - Response: {"data":[{id,name,slug,url,thumb,provider,type,...}],"meta":{...}}.
#   Each game's `url` IS the embeddable URL; canonical iframe form is
#   `https://slotslaunch.com/iframe/{game_id}?token={TOKEN}`.
# - INACTIVE state (current): HTTP 200 body `{"error":"Your Slots Launch subscription is not active"}`;
#   iframe endpoint returns 403. Detect the `error` key → structured inactive response.
# - Each iframe load consumes 1 quota request — load ONLY on explicit click, never preload.
# - Cache the catalog server-side (Redis, ~6-12h TTL); the API is "for synchronization, not live data".
# - SLOTSLAUNCH_TOKEN already in .env.local (gitignored) — NEVER hardcode/commit it.
# - Local-dev: token bound to app.xprediction.online → iframe may 403 from localhost;
#   launcher must show a graceful fallback (NOT a bug).

# Backend analog — the external-API integration to mirror (public, no-auth variant):
@backend/app/integrations/polymarket/client.py
# Backend analog — package layout + router mount + dependency-override test seam:
@backend/app/integrations/livebets/router.py
# Settings pattern (append keys; never os.getenv elsewhere):
@backend/app/core/config.py
# Public, UNAUTHENTICATED catalog router + main.py mount:
@backend/app/catalog/router.py
# Hermetic router-test pattern (ASGITransport + dependency_overrides, no Docker):
@backend/tests/integrations/livebets/test_livebets_router.py

# Frontend analog — catalog fetch helper (apiBase() split, no-store):
@frontend/src/lib/catalog.ts
# Frontend analog — catalog Server-Component page (grid + empty/error states):
@frontend/src/app/markets/page.tsx
# Frontend analog — fullscreen overlay host (reuse for the launcher):
@frontend/src/app/live/shared.tsx
# Nav entry list + route auth gate:
@frontend/src/components/player-nav.tsx
@frontend/src/proxy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend SlotsLaunch proxy — settings, client, service, public router, tests</name>
  <files>
    backend/app/core/config.py,
    backend/app/integrations/slotslaunch/__init__.py,
    backend/app/integrations/slotslaunch/client.py,
    backend/app/integrations/slotslaunch/schemas.py,
    backend/app/integrations/slotslaunch/service.py,
    backend/app/integrations/slotslaunch/router.py,
    backend/app/main.py,
    backend/tests/integrations/slotslaunch/__init__.py,
    backend/tests/integrations/slotslaunch/test_casino_router.py,
    .env.example,
    docker-compose.yml
  </files>
  <behavior>
    - Active: upstream returns 200 with a `data` array → service returns status="active" and a list of normalized games, each with id, name, provider, thumb, and a backend-composed iframe_url of the form https://slotslaunch.com/iframe/{id}?token={token}. The raw token NEVER appears in any field other than the composed iframe_url.
    - Inactive: upstream returns 200 with body {"error": "...subscription is not active"} → service returns status="inactive", games=[] (NOT an HTTP error). The endpoint responds 200.
    - Upstream failure (network/timeout/non-200/garbage body): service returns status="inactive", games=[] (degrade gracefully — a demo surface must never 500 the player). The endpoint responds 200.
    - Token unset: settings default is None; service returns status="inactive", games=[] without attempting the call.
    - Caching: a second call within TTL does NOT re-invoke the upstream client (served from Redis); a cold Redis falls back to an in-request live fetch.
    - Endpoint GET /api/v1/casino/games is UNAUTHENTICATED (mirrors public_catalog_router — no auth dependency).
  </behavior>
  <action>
    Append `SLOTSLAUNCH_TOKEN: str | None = None`, `SLOTSLAUNCH_API_BASE: str = "https://slotslaunch.com"`, `SLOTSLAUNCH_ORIGIN: str = "https://app.xprediction.online"`, and `SLOTSLAUNCH_CACHE_TTL_SECONDS: int = 43200` (12h) to the `Settings` class in config.py, in a new commented section. Never read these via os.getenv — only through `get_settings()`.

    Create `backend/app/integrations/slotslaunch/` package mirroring the `polymarket`/`livebets` layout:
    - `client.py`: a `SlotsLaunchClient` with a lazy `httpx.AsyncClient` singleton (copy the structure from polymarket/client.py — bounded `httpx.Limits`, `httpx.Timeout(15.0, connect=5.0)`, tenacity retry only on `(httpx.NetworkError, httpx.TimeoutException)` with `reraise=True`). Add a `fetch_games(per_page: int = 150)` coroutine that GETs `{SLOTSLAUNCH_API_BASE}/api/games` with params `token={SLOTSLAUNCH_TOKEN}`, `per_page`, and header `Origin: {SLOTSLAUNCH_ORIGIN}` (the domain-bound-token requirement). Return the parsed JSON dict. Do NOT raise on the inactive-subscription 200 body — return it as-is so the service can branch on the `error` key. NEVER log the token (the structlog scrubber covers token-like keys, but also never pass it into a log event).
    - `schemas.py`: pydantic v2 models — `CasinoGame {id: str, name: str, provider: str | None, thumb: str | None, iframe_url: str}` and `CasinoCatalog {status: Literal["active","inactive"], games: list[CasinoGame]}`. iframe_url is the backend-composed URL; the raw token is never a standalone field.
    - `service.py`: a `CasinoService` (or module-level coroutines) with `async def get_catalog(redis, client) -> CasinoCatalog`. Logic: if `settings.SLOTSLAUNCH_TOKEN` is None → return inactive. Try Redis GET of key `casino:catalog` (JSON) first; on hit, parse and return. On miss, call `client.fetch_games()`; if the response dict has an `error` key → inactive (do NOT cache inactive, so it lights up immediately on activation). Otherwise map each `data` item to a `CasinoGame` composing `iframe_url = f"{SLOTSLAUNCH_API_BASE}/iframe/{item['id']}?token={token}"`, build the active `CasinoCatalog`, SET it in Redis with `SLOTSLAUNCH_CACHE_TTL_SECONDS` TTL, and return. Wrap the whole upstream branch so ANY exception (network, timeout, KeyError, JSON error) degrades to `CasinoCatalog(status="inactive", games=[])` — a demo surface must never 500.
    - `router.py`: `casino_router = APIRouter(prefix="/api/v1", tags=["casino"])` with `@casino_router.get("/casino/games", response_model=CasinoCatalog)` — UNAUTHENTICATED (no auth dep), matching the public_catalog_router convention. Use a `get_slotslaunch_client()` dependency (returns `SlotsLaunchClient()`) and the existing `get_redis` dependency so tests override the client via `app.dependency_overrides`. IMPORTANT: this router OMITS `from __future__ import annotations` — same FastAPI/Annotated/Depends startup constraint documented at the top of catalog/router.py and livebets/router.py. Do NOT add the future-import.

    Mount the router in `backend/app/main.py`: add `from app.integrations.slotslaunch.router import casino_router` to the deferred-import block (the `# noqa: E402` group near the other routers) and `app.include_router(casino_router)` alongside `public_catalog_router`.

    Add `SLOTSLAUNCH_TOKEN=` (placeholder, empty) with a short comment to `.env.example` in a new "Casino demo (SlotsLaunch)" section, noting it's domain-bound and already set in .env.local. In `docker-compose.yml`, add `SLOTSLAUNCH_TOKEN: ${SLOTSLAUNCH_TOKEN:-}` to the `backend:` service `environment:` block (the same place `LIVEBETS_API_KEY` is enumerated — NOT the shared `x-backend-env` anchor, since it's an optional per-service secret like the live-bets keys).

    Tests in `backend/tests/integrations/slotslaunch/test_casino_router.py`: mirror `test_livebets_router.py` — use `httpx.ASGITransport(app=app, raise_app_exceptions=False)`, an autouse `app.dependency_overrides.clear()` fixture, and override `get_slotslaunch_client` with a `FakeSlotsLaunchClient`. Cover: (a) active fetch → 200 status="active" with games whose iframe_url contains `/iframe/` and the composed token; (b) inactive body `{"error": ...}` → 200 status="inactive" games=[]; (c) client raises (network) → 200 status="inactive" games=[]. Mock/override Redis so the cache path is exercised but hermetic (override `get_redis` with a fake/in-memory dict-backed double, OR assert behavior with a miss). Do NOT hit the real SlotsLaunch API.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integrations/slotslaunch/ -x -q</automated>
  </verify>
  <done>
    `GET /api/v1/casino/games` returns 200 in all three cases; active returns composed iframe_urls (token in iframe_url only), inactive/error return `{status:"inactive",games:[]}`; token wired through Settings + .env.example + docker-compose with no hardcoded value; new tests pass and never hit the network.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend Casino (demo) page — fetch helper, grid, fullscreen launcher, nav + route gate</name>
  <files>
    frontend/src/lib/casino.ts,
    frontend/src/app/casino/page.tsx,
    frontend/src/app/casino/casino-grid.tsx,
    frontend/src/app/casino/game-launcher.tsx,
    frontend/src/components/player-nav.tsx,
    frontend/src/proxy.ts
  </files>
  <action>
    Create `frontend/src/lib/casino.ts` mirroring `lib/catalog.ts`: the same `apiBase()` server/browser split, and a `fetchCasinoGames()` that GETs `${apiBase()}/api/v1/casino/games` with `cache: "no-store"` and returns the typed `CasinoCatalog` ({ status: "active" | "inactive"; games: CasinoGame[] }) where `CasinoGame = { id: string; name: string; provider: string | null; thumb: string | null; iframe_url: string }`. On a non-ok response, return `{ status: "inactive", games: [] }` (degrade gracefully — never throw into the page).

    Create `frontend/src/app/casino/page.tsx` as an async Server Component (mirror `markets/page.tsx`): fetch the catalog server-side; if `status === "inactive"` OR `games.length === 0`, render a friendly empty state card ("Casino demo not available yet — slots will appear here once the demo is live") — NOT an error, NOT a blank/zero grid (v1.1 Fase C error contract: a degraded state must read as intentional). Otherwise render `<CasinoGrid games={...} />`. Use the existing page shell + Card components and on-brand chrome.

    Create `frontend/src/app/casino/casino-grid.tsx` ("use client"): a responsive thumbnail grid. Each tile shows `thumb` (next/image or a plain img with object-cover; fall back to a name placeholder when thumb is null) + name + provider. Clicking a tile opens the `GameLauncher` for THAT game only — do NOT render any iframe in the grid itself (each iframe load costs 1 upstream quota request; load only on explicit click). Manage the selected-game state here and pass it down.

    Create `frontend/src/app/casino/game-launcher.tsx` ("use client"): a fullscreen overlay launcher mirroring `LiveFullscreenHost` in `live/shared.tsx` — `fixed inset-0 z-50 ... bg-black`, a close affordance (X) that clears selection, and a single `<iframe src={game.iframe_url}>` (allow fullscreen; sized to fill the overlay). Add an `onError` handler AND a visible fallback message ("This demo game couldn't load here — it may be restricted to the live domain. Try again on the deployed site.") shown when the iframe errors, so a localhost 403 reads as a graceful fallback, not a broken frame or a bug. The iframe src already carries the token (SlotsLaunch's documented domain-bound model) — that is acceptable; the frontend never sees the raw env var, only the backend-composed URL.

    Add a `{ href: "/casino", label: "Casino" }` entry to the `DESTINATIONS` array in `frontend/src/components/player-nav.tsx` (place it after "Live"). The existing `isActive` prefix logic already handles `/casino`.

    Update `frontend/src/proxy.ts`: add `casino` to the `PLAYER_PROTECTED` regex alternation and add `"/casino/:path*"` to `config.matcher`, so the Casino section sits behind player auth like the other app destinations.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit && pnpm exec next lint --dir src/app/casino --dir src/lib --dir src/components</automated>
  </verify>
  <done>
    `/casino` is in the nav (after Live) and gated by player auth; the page renders a thumbnail grid when active and a friendly "not available yet" empty state when inactive/empty; clicking a tile opens a fullscreen iframe loading only that game with an onError fallback; no iframes load from the grid; typecheck + lint pass. The whole flow lights up unchanged once SlotsLaunch is activated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XPredict backend → SlotsLaunch API | Outbound call carrying the domain-bound `SLOTSLAUNCH_TOKEN`; untrusted JSON response crosses back in |
| Backend → browser | Backend composes the iframe URL (token embedded per SlotsLaunch's documented model); the raw env var never reaches the client bundle |
| Browser → SlotsLaunch iframe | Third-party game content rendered in a sandboxed `<iframe>` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u0q-01 | Information Disclosure | `SLOTSLAUNCH_TOKEN` in source/logs | mitigate | Read only via `get_settings()`; placeholder-only in `.env.example`; never logged (structlog scrubber + never passed into a log event); real value lives in gitignored `.env.local` |
| T-u0q-02 | Information Disclosure | Raw token in client bundle | mitigate | Backend composes `iframe_url`; frontend env never carries the token. Token appears only inside the composed iframe `src` (SlotsLaunch's documented domain-bound model — accepted by design) |
| T-u0q-03 | Denial of Service | SlotsLaunch quota exhaustion (each iframe + catalog call costs quota) | mitigate | Redis-cache the catalog (~12h TTL); never preload/autoplay grid iframes — load only on explicit user click |
| T-u0q-04 | Denial of Service | Upstream outage / inactive subscription crashing the player surface | mitigate | Service degrades ANY upstream failure (incl. inactive body, network, garbage) to `{status:"inactive",games:[]}` and returns 200; frontend renders a friendly empty state, never a 500/blank |
| T-u0q-05 | Tampering | Malicious/garbage upstream JSON | mitigate | Parse into pydantic `CasinoGame`/`CasinoCatalog`; any parse error falls into the graceful-inactive branch |
| T-u0q-SC | Tampering | npm/pip installs | accept | No new packages — `httpx`/`pytest-httpx` already in `backend/pyproject.toml`; frontend reuses existing deps. Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/integrations/slotslaunch/ -x -q` — green (active / inactive / upstream-error all hermetic, no network).
- `cd frontend && pnpm exec tsc --noEmit` — no type errors.
- Manual (optional, post-activation): with `SLOTSLAUNCH_TOKEN` active, `/casino` shows a populated grid; clicking a game opens the fullscreen iframe. With the token blank/inactive, `/casino` shows the friendly empty state and the backend endpoint returns `{status:"inactive",games:[]}`.
- `grep` the codebase: the literal token value appears NOWHERE in tracked source (only `SLOTSLAUNCH_TOKEN=` placeholder in `.env.example`).
</verification>

<success_criteria>
- `GET /api/v1/casino/games` exists, is public, and returns `{status, games[]}` — `active` with composed iframe URLs when the subscription is live, `inactive`/empty (HTTP 200) otherwise. Never 500s on upstream failure.
- Catalog is Redis-cached server-side; grid iframes are never preloaded.
- `SLOTSLAUNCH_TOKEN` is wired through `Settings` + `.env.example` (placeholder) + docker-compose, never hardcoded or committed.
- `/casino` appears in the player nav (after Live), is auth-gated, renders a thumbnail grid (active) or a friendly empty state (inactive), and launches games in a fullscreen iframe with an onError fallback.
- The whole surface lights up with zero code changes when the SlotsLaunch free plan is activated.
- Backend tests pass hermetically (mocked upstream); frontend typechecks + lints.
</success_criteria>

<output>
Create `.planning/quick/260611-u0q-integrate-slotslaunch-demo-slots-as-casi/260611-u0q-SUMMARY.md` when done.
</output>

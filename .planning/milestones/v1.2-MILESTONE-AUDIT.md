---
milestone: v1.2
milestone_name: Credible Catalog
audited: 2026-06-06
status: tech_debt
scores:
  requirements: 29/29
  phases: 6/6
  integration: 12/12
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 13-multi-outcome-model-catalog-indexes
    items:
      - "Pre-existing ORM↔Alembic autogenerate drift (9 diffs in OTHER subsystems; NOT phase-13-introduced; phase-13 objects are zero-drift) — deferred to an autogenerate-hygiene chore (13-deferred-items.md)"
  - phase: 14-curated-per-category-gamma-sync
    items:
      - "status: human_needed — 2 live verifications outstanding (redbeat schedule reload on deploy; live Gamma tag_id drift re-check)"
      - "WR-02 event volume24hr is float-derived Decimal (accepted exception to string→Decimal money rule; soft curation threshold, not a payout)"
      - "WR-01 blank-conditionId child could collapse a 2-child event onto the standalone path (non-blocking)"
      - "IN-01..04: per-candidate GammaClient in detect path, hand-inlined detect lock, magic slug-truncation numbers, stale docstrings"
  - phase: 15-event-settlement-house-resolve-void-mirrored-verify
    items:
      - "Pitfall 6: re-resolve-after-reverse (settle:{bet_id}:{leg} idempotency-key collision) explicitly scoped out + documented in reverse_event docstring; no test"
      - "IN-01/IN-02: opaque scalar_one() error context; test-helper dedup (info)"
  - phase: 16-catalog-event-api-house-event-crud
    items:
      - "Pitfall-6 DEFERRED design comment carried forward (event_service.py:417)"
  - phase: 17-catalog-browse-ui-event-detail-admin-event-ops
    items:
      - "P2 stretch deferred (combined chart, live odds on all rows, featured shelf)"
      - "edit-mode resolution_criteria field + dedicated admin event-list endpoint deferred (backend additions)"
      - "22 house-pattern eslint warnings (CI-accepted)"
nyquist:
  compliant_phases: []
  partial_phases: [13, 14, 15, 16, 17]
  missing_phases: [18]
  overall: "No phase Nyquist-compliant — all VALIDATION sign-offs left at draft/false (phase 18 has none). Documentation-hygiene gap: VERIFICATION.md files report tests authored + passing on every phase; the validation sign-off checkboxes were simply never flipped."
human_verify:
  - phase: 14
    items:
      - "redbeat schedule reload on deploy (top-25 entry stops firing, events poll starts)"
      - "live Gamma tag_id drift re-verify (the 7-tag allow-list still maps)"
post_merge_fixes:
  note: "During the v1.2 close review, several phase-13/14 LOW items were already FIXED on main and are not outstanding."
  commits:
    - "ce625e4 fix(14): widowed market_groups guard (event SAVEPOINT) + regression test"
    - "9e6110d fix(14): GammaClient inside try — events lock can't leak (resolves the WR-03 lock-structure fragility)"
    - "29c6a44 test(13): MarketGroup slug + partial-unique negative coverage"
    - "e627224 ci(backend): drop paths filter so the required `backend` check always reports"
---

# Milestone Audit — v1.2 "Credible Catalog"

**Audited:** 2026-06-06 · **Phases:** 13–18 (6) · **Status:** `tech_debt` (no blockers; deferred debt + sign-off hygiene)

## Verdict

**v1.2 achieved its definition of done.** All 29 P1 requirements are satisfied across three independent sources (phase VERIFICATION + requirements traceability + summaries), cross-phase integration is fully wired (12/12) and all three end-to-end flows complete (3/3), with **zero unsatisfied requirements, zero orphans, and zero anti-patterns** in production code. The milestone is **not** `passed`-clean only because of standing, non-blocking deferred debt and a uniform Nyquist sign-off gap (below) — none of which blocks completion.

> **Definition of done (ROADMAP v1.2):** the operator can offer a *credible* catalog of prediction markets (Polymarket mirror + house multi-outcome events) with correct settlement and admin ops, under their brand. Delivered: model → Gamma sync → event settlement → catalog API → browse/admin UI → demo seed.

## Requirements coverage — 29/29 satisfied

| Group | REQ-IDs | Phase | Status |
|-------|---------|-------|--------|
| Events model/UX | EVT-01..07 | 13 / 14 / 15 / 17 | ✅ 7/7 |
| Catalog sync | CAT-01..06 | 14 | ✅ 6/6 |
| Browse | BRW-01..06 | 16 / 17 | ✅ 6/6 |
| Admin event ops | EVA-01..06 | 15 / 16 | ✅ 6/6 |
| Demo seed | DEMO-01..04 | 18 | ✅ 4/4 |

- **satisfied: 29/29 · partial: 0 · unsatisfied: 0 · orphaned: 0**
- *Doc-format caveat:* phases 17 & 18 don't use the `requirements-completed` YAML field (17 summaries are prose; 18 has no SUMMARY — completion is asserted in `18-VERIFICATION.md` frontmatter). Under a strict literal 3-source reading, those 9 REQs (EVT-02/03/04/05, BRW-06, EVA-01/02, DEMO-01..04) score *partial*. All carry a passing VERIFICATION + `[x]` in REQUIREMENTS, so the verdict does not flip either way. → doc-hygiene pass recommended.

## Per-phase verification

| Phase | Status | Critical gaps | Anti-patterns | Nyquist sign-off |
|-------|--------|---------------|---------------|------------------|
| 13 model + indexes | passed (4/4) | none | none | false (draft) |
| 14 Gamma sync | **human_needed** (11/11) | none | none | false (draft) |
| 15 event settlement | passed (13/13) | none | none | false (draft) |
| 16 catalog API + CRUD | passed (18/18) | none | none | false (draft) |
| 17 browse/admin UI | passed (tsc/eslint/188 vitest/build) | none | none | no frontmatter |
| 18 seed/demo | passed (DEMO-01..04, 19 tests) | none | none | **MISSING** |

Phase 14's `human_needed` = all 11 must-haves verified statically; held only for 2 live runtime checks (see `human_verify`). Not a gap.

## Integration & E2E (gsd-integration-checker: 12/12 wiring, 3/3 flows)

- **Wiring confirmed:** both new routers mounted in `main.py`; Gamma sync (14) is the first writer of `market_groups`; catalog API (16) reads `MarketGroup` + `derive_event_status`; event router (16) calls `EventService` (15); seed (18) drives the **merged** service layer (no reimplemented settlement); frontend `lib/catalog.ts` + `lib/admin-events-api.ts` hit the real routes; TS types transcribe the Pydantic schemas field-for-field.
- **E2E flows complete:** (1) admin create → catalog → resolve/void/reverse → derived status reflects; (2) player browse → event detail → per-outcome independent odds → bet on a child; (3) seed → all 4 states across categories → renders.
- **Defining invariant holds:** per-outcome YES bars are rendered independently and **never summed to 100%** at every render site (`event-card.tsx`, `outcome-row.tsx`).
- No orphaned exports, no route without a consumer, no field drift. **Verdict: integration solid.**

## Tech debt (non-blocking, deferred)

See `tech_debt` frontmatter. Highlights to carry into the backlog / next milestone:
- **Phase 14 live checks** (redbeat reload, Gamma tag_id drift) — only closeable on a real deploy.
- **Phase 15 Pitfall 6** — re-resolve-after-reverse needs a per-bet settlement epoch in the idempotency key (out of scope here).
- **Phase 14 `volume24hr` float→Decimal** — accepted soft-threshold exception to the strict money rule.
- **Phase 13 autogenerate drift** — pre-existing, other subsystems; one hygiene chore.
- **Already fixed on main during this close** (see `post_merge_fixes`): widowed-group guard, events-lock leak, phase-13 negative tests, CI required-check.

## Nyquist coverage — sign-off hygiene gap

**No phase is marked `nyquist_compliant: true`.** All VALIDATION.md files (13–16) read `nyquist_compliant: false` / `status: draft`; phase 17's is a prose plan with no frontmatter; phase 18 has none. This is a **documentation-hygiene gap, not skipped work** — every phase's VERIFICATION.md reports tests authored and passing. Optional remediation before/after archive: run `/gsd-validate-phase {13..18}` to flip the sign-offs, or accept as-is and track.

## Recommendation

Verdict `tech_debt` with **no blockers** → **proceed to `/gsd-complete-milestone v1.2`** (accept debt, carry the items above into the backlog). Optionally do a quick doc-hygiene pass first (align phase 17/18 summary frontmatter + flip Nyquist sign-offs).

_Auditor: Claude (gsd-audit-milestone orchestrator) — phase verifications aggregated + cross-phase integration checked._

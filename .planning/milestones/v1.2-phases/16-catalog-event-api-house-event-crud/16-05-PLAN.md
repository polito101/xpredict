---
phase: 16-catalog-event-api-house-event-crud
plan: 05
type: execute
wave: 3
depends_on: ["16-02", "16-04"]
files_modified:
  - backend/app/main.py
  - backend/tests/catalog/test_wiring.py
  - backend/tests/markets/test_public_router.py
autonomous: true
requirements: [BRW-01, BRW-02, BRW-03, BRW-04, BRW-05, EVA-01, EVA-02]
tags: [wiring, router-registration, main, back-compat, legacy-markets, deferred-import]

must_haves:
  truths:
    - "public_catalog_router and event_admin_router are registered in app/main.py via the deferred-import include_router block"
    - "After wiring, GET /api/v1/catalog, /api/v1/categories, /api/v1/events/{slug}, POST/PATCH /admin/events, and the resolve/void/reverse routes are all reachable on the live app"
    - "The legacy GET /api/v1/markets still returns a flat list[MarketListItem] (back-compat preserved, isinstance(body, list) holds)"
  artifacts:
    - path: "backend/app/main.py"
      provides: "deferred-import registration of public_catalog_router + event_admin_router"
      contains: "public_catalog_router"
    - path: "backend/tests/catalog/test_wiring.py"
      provides: "live-app route-presence smoke test for the new catalog + event-admin routes + legacy /markets still a list"
      min_lines: 20
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/catalog/router.py"
      via: "deferred import + app.include_router(public_catalog_router)"
      pattern: "include_router\\(public_catalog_router\\)"
    - from: "backend/app/main.py"
      to: "backend/app/settlement/event_router.py"
      via: "deferred import + app.include_router(event_admin_router)"
      pattern: "include_router\\(event_admin_router\\)"
---

<objective>
Wire the two new routers into the application and prove back-compat. Register `public_catalog_router` (plan 16-02) and `event_admin_router` (plans 16-03/16-04) in `app/main.py` via the existing deferred-import `include_router` block, add a live-app route-presence smoke test, and reinforce the legacy `GET /api/v1/markets` back-compat assertion (the endpoint must still return a flat `list[MarketListItem]`).

Purpose: This is the final glue that makes every Phase-16 endpoint reachable on the running app, plus the explicit guard that adding the new surfaces did not change the legacy markets contract (a success criterion). It is isolated to `main.py` + tests so it never collides with the router-authoring plans (it runs last, wave 3, after both router files exist).
Output: `app/main.py` (two added imports + two `include_router` calls), `tests/catalog/test_wiring.py` (new), and a reinforced `tests/markets/test_public_router.py` back-compat assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-RESEARCH.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the catalog + event-admin routers in main.py (deferred import)</name>
  <files>backend/app/main.py</files>
  <read_first>
    - MODIFY: backend/app/main.py — the deferred-import block (lines 180-187, each import carries `# noqa: E402`) and the include_router block (lines 189-203). Existing reference: `from app.markets.router import admin_market_router, public_market_router  # noqa: E402` (line 186) + `app.include_router(public_market_router)` (line 197) + `app.include_router(settlement_admin_router)` (line 201)
    - SOURCE OF TRUTH: backend/app/catalog/router.py (plan 16-02) exports public_catalog_router; backend/app/settlement/event_router.py (plans 16-03/16-04) exports event_admin_router
    - PATTERNS: lines 348-363 (the exact registration lines to add); RESEARCH lines 649-657 (Router registration)
  </read_first>
  <action>
    In backend/app/main.py, inside the deferred-import block (after line 186, keeping the existing grouping), add two imports each ending with the `# noqa: E402` marker: import public_catalog_router from app.catalog.router, and import event_admin_router from app.settlement.event_router. Then inside the include_router block (alongside the existing app.include_router(public_market_router) and app.include_router(settlement_admin_router) calls), add app.include_router(public_catalog_router) and app.include_router(event_admin_router). Use the deferred-import (post-app-construction, `# noqa: E402`) placement exactly like every existing router — this avoids the circular-import problem. Change nothing else in main.py (do not reorder or remove existing registrations).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/catalog/test_wiring.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/main.py contains `from app.catalog.router import public_catalog_router` and `from app.settlement.event_router import event_admin_router`, each with `# noqa: E402`.
    - backend/app/main.py contains `app.include_router(public_catalog_router)` and `app.include_router(event_admin_router)`.
    - The imports sit in the deferred block (after the app is constructed) like every other router import.
    - No existing `include_router` call or import in main.py is removed or reordered (`git diff` shows only the two added imports + two added include_router lines).
    - `cd backend && uv run pytest tests/catalog/test_wiring.py --co` collects without error (the app imports cleanly with both routers wired).
  </acceptance_criteria>
  <done>Both new routers are registered via the deferred-import include_router block; the app imports cleanly and exposes the catalog + event-admin routes.</done>
</task>

<task type="auto">
  <name>Task 2: Live-app wiring smoke test + legacy /markets back-compat assertion</name>
  <files>backend/tests/catalog/test_wiring.py, backend/tests/markets/test_public_router.py</files>
  <read_first>
    - CREATE: backend/tests/catalog/test_wiring.py (new). MODIFY: backend/tests/markets/test_public_router.py (reinforce the existing back-compat assertion only — do NOT change the legacy endpoint or its other tests)
    - DEPENDS ON: backend/tests/catalog/conftest.py (the api AsyncClient fixture) from plan 16-01
    - ANALOG (route-presence + back-compat): backend/tests/markets/test_public_router.py — the existing `assert isinstance(body, list)` on GET /api/v1/markets (line 86); the api fixture + markers
    - SOURCE OF TRUTH: backend/app/main.py (Task 1) — the wired routes to assert present
  </read_first>
  <action>
    Create backend/tests/catalog/test_wiring.py: import app from app.main; a test test_new_routes_registered that builds the set of route paths from app.routes and asserts every Phase-16 path is present — /api/v1/catalog, /api/v1/categories, /api/v1/events/{slug}, /admin/events, /admin/events/{group_id}, /admin/events/{group_id}/resolve, /admin/events/{group_id}/void, /admin/events/{group_id}/reverse. Add test_catalog_endpoint_live (using the api fixture): GET /api/v1/catalog with no filters returns 200 and isinstance(body, list) on the live wired app (an empty DB returns 200 + []). Then in backend/tests/markets/test_public_router.py, confirm (and if absent, add) an explicit back-compat test that GET /api/v1/markets returns 200 and isinstance(body, list) and that wiring the new routers did not break the legacy contract — keep the legacy endpoint untouched; only the test assertion is reinforced. Reuse the catalog conftest api fixture + the parent conftest engine. Per-module run.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/catalog/test_wiring.py tests/markets/test_public_router.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/catalog/test_wiring.py tests/markets/test_public_router.py -x` exits 0.
    - test_new_routes_registered asserts all eight Phase-16 route paths are present on the live app.
    - test_catalog_endpoint_live asserts GET /api/v1/catalog returns 200 + a list on the wired app.
    - tests/markets/test_public_router.py still asserts `isinstance(body, list)` on GET /api/v1/markets (back-compat); the legacy endpoint and its other tests are unchanged.
  </acceptance_criteria>
  <done>A wiring smoke test proves every Phase-16 route is reachable on the live app and the legacy /markets endpoint still returns a flat list — back-compat verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app wiring | Registering routers changes the reachable attack surface; the new routes carry their own auth (admin endpoints gated; public reads intentionally open). |
| legacy contract | The legacy `/markets` endpoint must not change shape (back-compat); a behavior regression here breaks existing clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-16 | Tampering | wiring the new routers accidentally alters the legacy /markets contract | mitigate | the back-compat test asserts `GET /api/v1/markets` still returns a flat list; the legacy endpoint is not edited (add new surfaces only). |
| T-16-17 | Spoofing | a new admin route registered without its auth dependency slipping through | mitigate | admin gating is enforced in the router modules (plans 16-03/16-04) where `Depends(current_active_admin)` lives; this plan only registers them — the auth-gate negative tests in 16-03/16-04 remain the guard, and the wiring test confirms the routes exist (their auth is asserted upstream). |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero new deps — RESEARCH §Package Legitimacy Audit "not applicable"). |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/catalog/test_wiring.py tests/markets/test_public_router.py -x` green.
- app/main.py: two added deferred imports (`# noqa: E402`) + two added `include_router` calls; nothing else changed.
- The eight Phase-16 routes are present on the live app; legacy `/markets` still returns a flat list.
- This plan is the ONLY one that edits app/main.py (wave 3, after both router files exist) — no concurrent main.py edit.
</verification>

<success_criteria>
- Both new routers are registered via the deferred-import block; every Phase-16 endpoint (catalog reads, categories, event detail, admin create/edit, admin resolve/void/reverse) is reachable on the running app.
- The legacy `GET /api/v1/markets` endpoint still works for back-compat (flat `list[MarketListItem]`, `isinstance(body, list)`).
</success_criteria>

<output>
Create `.planning/phases/16-catalog-event-api-house-event-crud/16-05-SUMMARY.md` when done
</output>

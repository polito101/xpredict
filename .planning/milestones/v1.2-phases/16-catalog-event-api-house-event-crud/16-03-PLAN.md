---
phase: 16-catalog-event-api-house-event-crud
plan: 03
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - backend/app/settlement/event_schemas.py
  - backend/app/settlement/event_router.py
  - backend/app/settlement/event_service.py
  - backend/tests/settlement/test_event_router.py
autonomous: true
requirements: [EVA-01, EVA-02]
tags: [admin, house-event, create, edit-lock, exists-bets, http-423, binary-trigger, admin-auth]

must_haves:
  truths:
    - "POST /admin/events creates one MarketGroup(source=HOUSE) + N child binary YES/NO markets in one request transaction, each group_item_title=label with YES seeded at initial_odds"
    - "A body with <2 outcomes is rejected with 422 (EVT-07: grouping needs >=2)"
    - "Each child is exactly YES + NO — the binary-only trigger is never tripped (no 3rd outcome)"
    - "PATCH /admin/events/{group_id} edits metadata/outcomes only while no child has a bet; after the first bet -> HTTP 423 (predicate = EXISTS(bets), NOT the dead bet_count column)"
    - "Both endpoints require an admin Bearer (current_active_admin); no Bearer -> 401"
  artifacts:
    - path: "backend/app/settlement/event_schemas.py"
      provides: "OutcomeInput, CreateEventRequest (outcomes min_length=2), UpdateEventRequest, EventCreatedResponse — extra=forbid, odds gt=0 lt=1, money as JSON strings"
      contains: "min_length=2"
      min_lines: 30
    - path: "backend/app/settlement/event_router.py"
      provides: "event_admin_router (prefix /admin/events): POST '' (create), PATCH /{group_id} (edit-lock)"
      contains: "current_active_admin"
      min_lines: 50
    - path: "backend/app/settlement/event_service.py"
      provides: "EventService.create_house_event (group + N YES/NO children, slug retry), event_has_bets helper, update_house_event"
      contains: "create_house_event"
    - path: "backend/tests/settlement/test_event_router.py"
      provides: "EVA-01 create + EVA-02 edit-lock-423 + auth-gate 401 coverage"
      min_lines: 60
  key_links:
    - from: "backend/app/settlement/event_router.py"
      to: "backend/app/auth/deps.py"
      via: "Depends(current_active_admin) on every endpoint"
      pattern: "current_active_admin"
    - from: "backend/app/settlement/event_service.py"
      to: "markets / outcomes tables"
      via: "MarketGroup + per-child YES/NO Outcome inserts, binary-trigger-safe"
      pattern: "group_item_title"
    - from: "backend/app/settlement/event_router.py"
      to: "bets table"
      via: "EXISTS bets edit-lock over child ids"
      pattern: "event_has_bets"
---

<objective>
Build the admin house-event create + edit-lock HTTP surface (EVA-01, EVA-02): a new `event_admin_router` (prefix `/admin/events`) with `POST ""` (create one `MarketGroup` + N child binary YES/NO markets in one request transaction) and `PATCH /{group_id}` (edit metadata/outcomes pre-bet only, HTTP 423 after the first bet via `EXISTS(bets)`), the `EventService.create_house_event` service path, and the request/response schemas. Integration tests cover create (>=2 enforced, binary-trigger-safe), the edit-lock 423, and the admin-auth 401 gate.

Purpose: This is the admin authoring surface for house multi-outcome events — the mirror of the existing market create/edit extended to the group-of-binaries shape, with the financial-integrity edit-lock derived from real bets (never the dead `bet_count`).
Output: `app/settlement/event_schemas.py` (new), `app/settlement/event_router.py` (new, create+PATCH only — resolve/void/reverse added by plan 16-04), `app/settlement/event_service.py` (extended), `tests/settlement/test_event_router.py`. Router registration in `main.py` is plan 16-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-RESEARCH.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-PATTERNS.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-CONTEXT.md
@.claude/skills/spike-findings-xpredict/SKILL.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Event request/response schemas + EventService.create_house_event</name>
  <files>backend/app/settlement/event_schemas.py, backend/app/settlement/event_service.py</files>
  <read_first>
    - CREATE: backend/app/settlement/event_schemas.py (new). MODIFY: backend/app/settlement/event_service.py (append create_house_event + edit-lock/update helpers; the resolve/void/reverse methods already exist — do NOT touch them)
    - ANALOG (create body): backend/app/markets/schemas.py — MarketCreate (lines 67-81: title Field(min_length=1, max_length=500); initial_odds_yes Decimal Field(gt=0, lt=1); deadline_must_be_future validator); the extra="forbid" rule
    - ANALOG (DecimalStr + request shape): backend/app/settlement/schemas.py — DecimalStr (lines 16-19), ResolveMarketRequest ConfigDict(extra="forbid") + Field(min_length=1) (lines 22-28)
    - SOURCE OF TRUTH (YES/NO child body + slug-retry to replicate): backend/app/markets/service.py — create_market (lines 38-111): the slug-retry loop (range(3) + begin_nested + flush; except IntegrityError -> nested.rollback() + session.expunge(market); lines 44-69) and the YES+NO Outcome seeding (71-85); generate_slug import (line 15)
    - SOURCE OF TRUTH (group-create idiom + columns): backend/app/integrations/polymarket/adapter.py _upsert_market_group MarketGroup value set (~341-378); backend/app/markets/models.py MarketGroup (~200-275), Market (group_id ~178, group_item_title ~182, resolution_criteria NOT NULL ~75), generate_slug (line 33)
    - SOURCE OF TRUTH (audit): backend/app/markets/service.py AuditService.record (lines 99-108) -> event_type event.created
    - HARD CONSTRAINTS: binary-only trigger trg_binary_outcomes_only (migration 0003:170-192) — exactly YES+NO per child; Market.resolution_criteria NOT NULL; RESEARCH Pattern 5 (lines 337-391)
  </read_first>
  <behavior>
    - CreateEventRequest with 2 outcomes validates; 1 outcome raises a pydantic validation error (endpoint returns 422).
    - OutcomeInput.initial_odds outside (0,1) raises a validation error.
    - create_house_event(session, admin_id, body) inserts one MarketGroup(source=HOUSE, unique slug) then per outcome a child Market (group_id set, group_item_title=label, OPEN, shared deadline, resolution_criteria supplied-or-synthesized) + exactly its YES (seeded at initial_odds) and NO (1 - initial_odds) outcomes.
    - One event.created audit row is written; the function returns the group.
  </behavior>
  <action>
    Create backend/app/settlement/event_schemas.py (MAY keep from __future__ import annotations) defining: OutcomeInput (extra="forbid"; label Field(min_length=1, max_length=100); initial_odds Decimal Field(gt=0, lt=1)); CreateEventRequest (extra="forbid"; title Field(min_length=1, max_length=500); category str | None Field(default=None, max_length=100); deadline datetime with a future-deadline field_validator mirroring MarketCreate.deadline_must_be_future; resolution_criteria str | None Field(default=None, max_length=2000); slug str | None Field(default=None, max_length=100); outcomes list[OutcomeInput] Field(min_length=2)); UpdateEventRequest (extra="forbid"; all-optional title/category/deadline plus an optional whole-list outcomes list[OutcomeInput] | None replace — document add/remove as a pre-bet whole-list replace per RESEARCH Pattern 6); EventCreatedResponse and EventDetailResponse (from_attributes; id, title, slug, category, source, deadline, plus per-child outcome rows carrying YES price as a JSON string via DecimalStr or field_serializer when_used="json"). Mirror RESEARCH lines 618-647. Then APPEND to backend/app/settlement/event_service.py (below the existing resolve/void/reverse code, never editing it): a module-level async function event_has_bets(session, group_id) returning the scalar of select(exists().where(Bet.market_id.in_(select(Market.id).where(Market.group_id == group_id)))) (import Bet from app.bets.models and exists from sqlalchemy); EventService.create_house_event (classmethod, keyword-only admin_id: UUID and body) inserting MarketGroup(title=body.title, source=MarketSourceEnum.HOUSE.value, category=body.category, slug=body.slug or generate_slug(body.title)) inside the begin_nested()+IntegrityError slug-retry idiom copied from create_market:44-69 (raise HTTPException(409) after 3 attempts), flush for group.id, then per outcome insert child Market(question= the label or f-string of title+label [discretion], slug=generate_slug(oc.label), resolution_criteria=body.resolution_criteria or a synthesized per-child default, deadline=body.deadline, category=body.category, source=HOUSE, status=OPEN, group_id=group.id, group_item_title=oc.label), flush, then add_all the YES(initial_odds) + NO(Decimal("1") - initial_odds) Outcome pair, flush; write one AuditService.record(actor=f"user:{admin_id}", event_type="event.created", payload with group_id/title/child_count); session.commit(); return group; EventService.update_house_event (classmethod, keyword-only group_id and body) mutating title/category/deadline and replacing children pre-bet when body.outcomes is provided (the 423 guard runs at the endpoint before this), then commit and return the group. Define a module-level synthesized default resolution_criteria constant (Market.resolution_criteria is NOT NULL). Use Decimal for all odds; never float. Do NOT modify resolve_event/void_event/reverse_event or their helpers.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/settlement/test_event_router.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/settlement/event_schemas.py contains `min_length=2` on the outcomes list and `Field(gt=0, lt=1)` on initial_odds, with `extra="forbid"` on every request model.
    - backend/app/settlement/event_service.py contains `def create_house_event`, `def event_has_bets`, and `def update_house_event`; `cd backend && uv run pytest tests/settlement/test_event_router.py --co` collects without ImportError.
    - The create path references both `label="YES"` and `label="NO"` per child and no path adds a 3rd outcome (binary trigger never trips).
    - event_has_bets contains `exists(` and references `Bet.market_id`; it does NOT contain `bet_count`.
    - The new code writes `event_type="event.created"` exactly once and calls `session.commit()` once.
    - `git diff` of event_service.py shows only additions; the existing resolve_event/void_event/reverse_event bodies are unchanged.
  </acceptance_criteria>
  <done>The event create/update service path builds a group + N binary YES/NO children (binary-trigger-safe, slug-retry, Decimal odds, one audit row) plus the EXISTS(bets) edit-lock helper; request schemas enforce >=2 outcomes and bounded odds.</done>
</task>

<task type="auto">
  <name>Task 2: event_admin_router — POST /admin/events (create) + PATCH /{group_id} (edit-lock 423)</name>
  <files>backend/app/settlement/event_router.py</files>
  <read_first>
    - CREATE: backend/app/settlement/event_router.py (new)
    - ANALOG (admin router decl + admin gate + docstring): backend/app/settlement/router.py — the from __future__ omission comment (lines 16-18), router decl + current_active_admin/User imports (lines 27-46), the resolve_market handler shape (lines 59-96)
    - ANALOG (CRITERIA_LOCKED 423): backend/app/markets/service.py update_market (lines 128-143) — the 423-when-locked shape (event predicate is EXISTS(bets), NOT bet_count)
    - SOURCE OF TRUTH: backend/app/settlement/event_service.py (Task 1) create_house_event / update_house_event / event_has_bets; backend/app/settlement/event_schemas.py CreateEventRequest / UpdateEventRequest / response models; backend/app/auth/deps.py current_active_admin; backend/app/auth/models.py User; backend/app/db/session.py get_async_session
    - RESEARCH: Pattern 6 edit-lock (lines 393-419); PATTERNS lines 304-321 (EXISTS not bet_count); Shared Patterns admin-auth + 423
  </read_first>
  <action>
    Create backend/app/settlement/event_router.py. OMIT `from __future__ import annotations` (FastAPI 3.13 Annotated-Depends gotcha — Pitfall 5; copy the load-bearing comment from settlement/router.py:16-18 explaining why it is absent). Use runtime imports for User and AsyncSession. Declare event_admin_router = APIRouter(prefix="/admin/events", tags=["admin-events"]) (bare /admin prefix, mirroring the settlement anomaly — plan 16-04 extends this same router). Add a POST "" route (response_model=EventCreatedResponse, status_code=201) with params admin Annotated[User, Depends(current_active_admin)], session Annotated[AsyncSession, Depends(get_async_session)], body CreateEventRequest: capture admin_id = admin.id; group = await EventService.create_house_event(session, admin_id=admin_id, body=body); reload the group with its children + outcomes (a small local selectinload read, or an EventService read helper) and return the response. Add a PATCH "/{group_id}" route (response_model=EventDetailResponse) with params group_id UUID, body UpdateEventRequest, admin, session: load the group (404 if missing); if await event_has_bets(session, group_id) is True raise HTTPException(status_code=status.HTTP_423_LOCKED, detail={"code": "EVENT_LOCKED", "reason": "Event outcomes/metadata cannot be changed after a bet has been placed"}); else group = await EventService.update_house_event(session, group_id=group_id, body=body) and return it. Both endpoints carry Depends(current_active_admin) (no override in the router — the real gate 401s without a Bearer). Add __all__ = ["event_admin_router"]. Do NOT register the router in main.py (plan 16-05). Do NOT add resolve/void/reverse here (plan 16-04).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/settlement/test_event_router.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/settlement/event_router.py does NOT contain `from __future__ import annotations` and carries the comment explaining the omission.
    - The file contains `APIRouter(prefix="/admin/events"`, a `@event_admin_router.post("")`, and a `@event_admin_router.patch("/{group_id}")`.
    - The PATCH handler calls `event_has_bets` and raises `status.HTTP_423_LOCKED` with detail code `EVENT_LOCKED`; it does NOT reference `bet_count`.
    - Both routes declare `Depends(current_active_admin)`; neither sets a dependency override.
    - The PATCH handler returns 404 when the group is missing (loads it first).
    - `cd backend && uv run pytest tests/settlement/test_event_router.py --co` collects without error (router imports cleanly under the FastAPI Annotated-Depends resolver).
  </acceptance_criteria>
  <done>The event admin router exposes create + edit under /admin/events with the admin Bearer gate, the EXISTS(bets)→423 edit-lock, and a 404 on a missing group; it imports without the future-annotations footgun and leaves room for plan 16-04 to add the settle routes.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — create (EVA-01), edit-lock 423 (EVA-02), auth-gate 401</name>
  <files>backend/tests/settlement/test_event_router.py</files>
  <read_first>
    - CREATE: backend/tests/settlement/test_event_router.py (new)
    - DEPENDS ON (import, do NOT redefine): backend/tests/catalog/_factories.py (make_event / place_bet_on_child / admin_override / seed_admin) from plan 16-01 — import via the catalog test package, OR mirror the same seed helpers locally if a cross-package import is awkward
    - ANALOG (admin client + auth-gate + bets seeding): backend/tests/settlement/test_settlement_router.py — the api AsyncClient fixture (lines 60-64), _Admin override class + _admin(user_id) helper (lines 99-100), the real-401 negative (no override -> 401, lines 169-175), _bets_table autouse (lines 47-51), _seed_wallet (lines 116-134), _place via BetService.place_bet (lines 145-155)
    - SOURCE OF TRUTH (behaviors): 16-RESEARCH.md Validation Architecture Test Map (lines 731-736) — test_create_event / test_edit_lock_after_bet / test_auth_gate; the binary-trigger + >=2 constraints (Pattern 5)
  </read_first>
  <action>
    Create backend/tests/settlement/test_event_router.py with the settlement markers (pytest.mark.integration + asyncio loop_scope="session"), the api AsyncClient fixture (or reuse the catalog conftest's), the _bets_table autouse, and the dependency-override-clear autouse. Cover: test_create_event_creates_group_and_children (override current_active_admin to a seeded admin; POST /admin/events with >=2 outcomes; assert 201, the response carries the group + N child outcome rows, and a DB read confirms one MarketGroup(source=HOUSE) + N children each with exactly a YES+NO outcome pair, group_item_title set, YES current_odds == the requested initial_odds as a string); test_create_event_rejects_single_outcome (POST with 1 outcome -> 422); test_create_event_rejects_bad_odds (initial_odds >= 1 -> 422); test_edit_lock_pre_bet_succeeds (create, then PATCH metadata with no bet -> 200 and the change persists); test_edit_lock_after_bet_returns_423 (create, seed a ledger-backed bet on one child via place_bet_on_child, then PATCH -> 423 with detail code EVENT_LOCKED); test_patch_missing_group_404 (PATCH a random uuid -> 404); test_create_requires_admin (no override / no Bearer -> POST /admin/events returns 401); test_patch_requires_admin (no Bearer -> PATCH returns 401). Money/odds assertions check the JSON value is a string. Use the parent conftest async_session for DB seeding/verification. Per-module run only (Windows-worktree-safe).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/settlement/test_event_router.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/settlement/test_event_router.py -x` exits 0.
    - test_create_event_creates_group_and_children asserts exactly one YES + one NO outcome per child (binary-trigger-safe) and that YES current_odds equals the requested initial_odds (as a string).
    - test_create_event_rejects_single_outcome asserts 422 (EVT-07 >=2 enforcement).
    - test_edit_lock_after_bet_returns_423 seeds a real (ledger-backed) bet and asserts 423 with detail code EVENT_LOCKED (EVA-02; EXISTS(bets), not bet_count).
    - test_create_requires_admin and test_patch_requires_admin both assert status_code 401 (admin gate; no override set).
    - The edit-lock test drives the lock via a seeded bet — it does NOT manipulate any bet_count column.
  </acceptance_criteria>
  <done>Integration tests prove house-event create (group + N binary children, >=2 enforced, binary-trigger-safe), the EXISTS(bets)→423 edit-lock after the first bet, the pre-bet edit success, the 404-on-missing, and the 401 admin gate on both endpoints — all green per-module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → /admin/events (Bearer) | Untrusted create/edit input crosses here; must be admin-gated. |
| event service → markets/outcomes/bets | Writes N children + outcomes; the binary trigger + edit-lock guard integrity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Elevation of Privilege | POST/PATCH /admin/events reachable without auth | mitigate | `Depends(current_active_admin)` on both endpoints; `test_create_requires_admin` / `test_patch_requires_admin` assert 401 with no Bearer. |
| T-16-07 | Tampering | editing an event with live bets (financial integrity) | mitigate | `event_has_bets` EXISTS(bets) over all child ids → HTTP 423; the lock spans every child; `test_edit_lock_after_bet_returns_423`. |
| T-16-08 | Tampering | malformed create body (missing/extra fields, <2 outcomes, out-of-range odds, 3rd outcome) | mitigate | `extra="forbid"`, `Field(min_length=2)` on outcomes, `Field(gt=0, lt=1)` on initial_odds; the binary-only DB trigger backstops a 3rd outcome; tests assert 422 on bad input. |
| T-16-09 | Repudiation | a created house event with no audit trail | mitigate | `create_house_event` writes one `event.created` audit row (actor = the admin id). |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero new deps — RESEARCH §Package Legitimacy Audit "not applicable"). |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/settlement/test_event_router.py -x` green (create, >=2 enforcement, bad-odds 422, edit-lock 423, pre-bet edit, 404-missing, 401 admin gate).
- event_service.py: new additions only; existing resolve/void/reverse unchanged; `event_has_bets` uses EXISTS over bets, never bet_count.
- event_router.py: no `from __future__ import annotations`; both routes admin-gated; PATCH returns 423 via HTTP_423_LOCKED.
- No edit to app/main.py in this plan (registration is plan 16-05, sequenced to avoid collision with plans 16-02/16-04).
</verification>

<success_criteria>
- EVA-01: POST /admin/events creates a house multi-outcome event (title, category, N outcomes each with a group_item_title label + initial odds) — one group + N binary YES/NO children in one request transaction, binary-trigger-safe, >=2 enforced.
- EVA-02: PATCH /admin/events/{group_id} edits outcomes/metadata only while zero bets; locks after the first bet → HTTP 423 (EXISTS(bets), mirrors ADM-07).
</success_criteria>

<output>
Create `.planning/phases/16-catalog-event-api-house-event-crud/16-03-SUMMARY.md` when done
</output>

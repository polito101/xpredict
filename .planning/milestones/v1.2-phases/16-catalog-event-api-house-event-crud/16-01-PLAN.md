---
phase: 16-catalog-event-api-house-event-crud
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/catalog/__init__.py
  - backend/tests/catalog/conftest.py
  - backend/tests/catalog/_factories.py
autonomous: true
requirements: [BRW-01, BRW-02, BRW-03, BRW-04, BRW-05]
tags: [catalog, test-scaffold, nyquist-wave-0, testcontainers, httpx, asgitransport]

must_haves:
  truths:
    - "A catalog test package exists with an httpx AsyncClient + ASGITransport fixture reusing the conftest engine testcontainer"
    - "A seed-factory module can create standalone markets and ≥2-child events in open / partially-resolved / resolved / void states with ledger-backed bets"
    - "Every Wave-1+ catalog/event test can import the shared client + factories without redefining infra"
  artifacts:
    - path: "backend/tests/catalog/__init__.py"
      provides: "catalog test package marker"
    - path: "backend/tests/catalog/conftest.py"
      provides: "api AsyncClient fixture (ASGITransport, raise_app_exceptions=False) + autouse engine requirement + dependency_overrides cleanup"
      min_lines: 25
    - path: "backend/tests/catalog/_factories.py"
      provides: "seed helpers: make_market, make_event (group + N binary YES/NO children), place_bet_on_child, resolve_child"
      min_lines: 40
  key_links:
    - from: "backend/tests/catalog/conftest.py"
      to: "backend/tests/conftest.py"
      via: "engine / async_session fixture reuse"
      pattern: "engine"
    - from: "backend/tests/catalog/conftest.py"
      to: "app.main"
      via: "ASGITransport(app=app)"
      pattern: "ASGITransport"
---

<objective>
Create the Wave-0 test scaffolding for Phase 16 per `16-VALIDATION.md`: a `tests/catalog/` package with a shared httpx `AsyncClient` + `ASGITransport` fixture and a seed-factory module that builds the catalog row shapes every later plan asserts against (standalone markets; ≥2-child events in open / partially-resolved / resolved / void states; ledger-backed bets for the edit-lock and settle tests).

Purpose: Nyquist validation requires every later task to have an automated `uv run pytest` target. This plan creates the test infrastructure those targets depend on, so Waves 1–3 write behavior tests (not boilerplate) and stay Windows-worktree-safe (per-module runs).
Output: `tests/catalog/__init__.py`, `tests/catalog/conftest.py`, `tests/catalog/_factories.py`. No production code, no app behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-VALIDATION.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the tests/catalog package + shared AsyncClient fixture</name>
  <files>backend/tests/catalog/__init__.py, backend/tests/catalog/conftest.py</files>
  <read_first>
    - CREATE: backend/tests/catalog/__init__.py (new), backend/tests/catalog/conftest.py (new)
    - ANALOG (copy fixture shapes verbatim): backend/tests/settlement/test_settlement_router.py — `api` AsyncClient fixture (`ASGITransport(app=app, raise_app_exceptions=False)`, lines 60-64), `pytestmark = [pytest.mark.integration, pytest.mark.asyncio(loop_scope="session")]` (line 36), `_require_testcontainer(engine)` autouse (lines ~42-45), `_clear_overrides` autouse (lines ~54-57)
    - SOURCE OF TRUTH (do NOT redefine): backend/tests/conftest.py — the `engine` / `async_session` testcontainer fixtures (engine fixture ~line 137; `pytest.importorskip` Docker guard ~line 124) that this package's conftest reuses
    - app import target: `from app.main import app`
  </read_first>
  <action>
    Create `backend/tests/catalog/__init__.py` (empty package marker).
    Create `backend/tests/catalog/conftest.py` defining: (1) `pytestmark = [pytest.mark.integration, pytest.mark.asyncio(loop_scope="session")]`; (2) an `api` fixture (`@pytest_asyncio.fixture(loop_scope="session")`) yielding `httpx.AsyncClient(transport=httpx.ASGITransport(app=app, raise_app_exceptions=False), base_url="http://test")` — mirror `test_settlement_router.py:60-64` byte-for-byte; (3) an autouse `_require_testcontainer(engine)` fixture that just returns the conftest `engine` (forces the testcontainer Postgres up); (4) an autouse `_clear_overrides` fixture that yields then calls `app.dependency_overrides.clear()`. Reuse the root `backend/tests/conftest.py` `engine`/`async_session` fixtures by NOT redefining them (pytest discovers the parent conftest). Import `app` from `app.main`. This conftest module is a test fixture file — it MAY keep `from __future__ import annotations` (the FastAPI router omission rule applies only to routers, not tests).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/catalog -x -q --co</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/catalog/__init__.py` exists.
    - `backend/tests/catalog/conftest.py` contains `ASGITransport` and `app.dependency_overrides.clear()`.
    - `cd backend && uv run pytest tests/catalog --co -q` collects (no import error) — exits 0 even with zero tests yet (collection-only).
    - The conftest does NOT redefine `engine` or `async_session` (those stay in the parent `backend/tests/conftest.py`).
  </acceptance_criteria>
  <done>The catalog test package imports cleanly and exposes the `api` client + autouse testcontainer/override fixtures; `uv run pytest tests/catalog --co` collects without error.</done>
</task>

<task type="auto">
  <name>Task 2: Seed-factory module for markets + multi-state events + ledger-backed bets</name>
  <files>backend/tests/catalog/_factories.py</files>
  <read_first>
    - CREATE: backend/tests/catalog/_factories.py (new)
    - ANALOG (reuse seeding idioms): backend/tests/settlement/test_settlement_router.py — `_bets_table` (lines 47-51, creates the bets table for sqlite-less testcontainer), `_seed_wallet` (lines 116-134, ledger-backed via `WalletService.recharge`), `_place` via `BetService.place_bet` (lines 145-155), `_Admin` override class (lines 99-100)
    - ANALOG (event seeding): backend/tests/settlement/test_event_service.py — `_seed_house_event` synthesizer (group + N binary YES/NO children) + the committed-session helpers + `_assert_ledger_clean` spike-004 gate (`_reconcile_async(...)["drift_count"] == 0`)
    - SOURCE OF TRUTH (child YES/NO body): backend/app/markets/service.py — `create_market` YES/NO outcome seeding (lines 71-85); each child is exactly YES + NO (binary-only trigger `trg_binary_outcomes_only`)
    - SOURCE OF TRUTH (group + child stamp): backend/app/markets/models.py — `MarketGroup` columns (id/title/source/category/slug ~200-275), `Market.group_id` + `Market.group_item_title` (~178/182), `generate_slug` (line 33)
    - SOURCE OF TRUTH (resolve a child for partial/resolved/void states): backend/app/settlement/service.py — `SettlementService.resolve_market` (looped per child); OR direct status set + winning_outcome_id for non-financial state setup
  </read_first>
  <action>
    Create `backend/tests/catalog/_factories.py` with async helper functions (each taking an `AsyncSession`): `make_market(session, *, question, category=None, status="OPEN", source="HOUSE", deadline=None, volume=Decimal("0"))` → inserts a standalone `Market` (`group_id=None`) + its YES/NO `Outcome` pair (mirror `markets/service.py:71-85`, never a 3rd outcome) and returns it; `make_event(session, *, title, category=None, n_outcomes=3, labels=None, deadline=None, source="HOUSE")` → inserts one `MarketGroup` (source=HOUSE, `slug=generate_slug(title)`) + N child binary YES/NO markets each stamped `group_id`/`group_item_title`, returns `(group, children)`; `place_bet_on_child(session, child, user_id, *, outcome="YES", stake=Decimal("10"))` → seeds a ledger-backed wallet (mirror `_seed_wallet` via `WalletService.recharge`) then a `Bet` row so `EXISTS(bets)` flips (drives the EVA-02 edit-lock 423); `resolve_child(session, child, *, winner_label)` and `void_child` / helpers sufficient to drive a group to each `derive_event_status` state (open: no child resolved; partially_resolved: ≥1 resolved + ≥1 open; resolved: all resolved with one YES winner; void: all resolved no YES winner). Set child `Market.status` + `winning_outcome_id` consistently with `event_service.py:_derive_status` ChildStatus construction (`func.upper(label)=="YES"`). Include an `_Admin` override helper (`id` attribute only) and an `admin_override(user_id)` that sets `app.dependency_overrides[current_active_admin]`. Provide a `seed_admin(session)` that inserts a real superuser row for the cases wanting a true Bearer (mirror `tests/markets/test_public_router.py:27-40`). Use `Decimal`, never float, for every money/odds value. This is a test helper module — `from __future__ import annotations` is allowed.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/catalog -x -q --co</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/catalog/_factories.py` exists and imports cleanly (`uv run pytest tests/catalog --co` still collects, exit 0).
    - The module exposes `make_market`, `make_event`, `place_bet_on_child`, and per-state event helpers (open/partial/resolved/void).
    - `make_event` creates exactly YES + NO per child (no factory path adds a 3rd outcome → binary trigger never trips).
    - Every monetary/odds literal in the file is a `Decimal` (no float literals on money/odds).
    - `place_bet_on_child` seeds a ledger-backed wallet via `WalletService.recharge` (not a raw balance write) so later spike-004 `drift_count==0` assertions stay valid.
  </acceptance_criteria>
  <done>The factory module provides reusable seeding for standalone markets, ≥2-child events in all four derived states, and ledger-backed bets; later plans import it instead of redefining seed logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app (ASGITransport) | In-process; no network. Tests exercise the real router/auth stack via `app.main:app`. |
| test factories → DB (testcontainer) | Real Postgres 16; factories write rows the production model already supports. No privileged escalation path introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-00 | Tampering | seed factories writing non-ledger-backed balances | mitigate | `place_bet_on_child` recharges via `WalletService.recharge` (ledger-backed), preserving the spike-004 `drift_count==0` invariant later tests assert |
| T-16-00b | Repudiation | tests masking the real 401 admin gate by leaving an override set | mitigate | autouse `_clear_overrides` clears `app.dependency_overrides` after every test so the auth-gate negative tests in 16-03/16-04 hit the real `current_active_admin` |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new deps per RESEARCH §Package Legitimacy Audit — "not applicable"); nothing to slopcheck |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/catalog --co -q` collects with zero errors (package + conftest + factories all import).
- No production module under `backend/app/` is modified by this plan (scaffolding only).
- `git diff --stat` shows only files under `backend/tests/catalog/`.
</verification>

<success_criteria>
- The `tests/catalog/` package exists with the shared `api` AsyncClient fixture and autouse testcontainer/override fixtures.
- `_factories.py` can seed standalone markets, ≥2-child events in open/partial/resolved/void states, and ledger-backed bets — all `Decimal`, binary-trigger-safe.
- Waves 1–3 can import these without redefining infrastructure.
</success_criteria>

<output>
Create `.planning/phases/16-catalog-event-api-house-event-crud/16-01-SUMMARY.md` when done
</output>

---
phase: 15-event-settlement-house-resolve-void-mirrored-verify
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - backend/app/settlement/event_service.py
  - backend/tests/settlement/test_event_service.py
  - backend/tests/settlement/test_event_mirrored.py
autonomous: true
requirements: [EVA-05, EVA-06]
must_haves:
  truths:
    - "reverse_event loops SettlementService.reverse_settlement over every already-settled child on a fresh session, restoring pre-settlement state and reopening each child"
    - "A reverse is idempotent — a second reverse finds no SETTLED bets and is a no-op"
    - "A child whose winner already spent the winnings hits CHECK(balance>=0) and rolls back THAT child only; siblings already reversed stay reversed"
    - "reverse_event writes an event.reversed audit row in its own transaction (mirrors STL-07)"
    - "reverse_event rejects a source=POLYMARKET group (admin read-only; EVA-06)"
    - "A mirrored (POLYMARKET) market_group's children auto-settle through the UNCHANGED detect_polymarket_resolutions path — verified, not rebuilt (EVA-06)"
    - "spike-004 _reconcile_async drift_count == 0 after reverse / partial-reverse / replay and after the mirrored verify"
  artifacts:
    - path: "backend/app/settlement/event_service.py"
      provides: "EventService.reverse_event classmethod (loop reverse_settlement per settled child on fresh sessions + event.reversed audit + mirrored-reject)"
      contains: "async def reverse_event"
    - path: "backend/tests/settlement/test_event_service.py"
      provides: "Reverse integration tests (restore / idempotent / per-child balance floor / audit) appended to the Plan-02 suite"
      contains: "reverse_event"
    - path: "backend/tests/settlement/test_event_mirrored.py"
      provides: "EVA-06 verify: detect_polymarket_resolutions settles a mirrored event's children with zero new settlement code + EventService mirrored-reject on reverse"
      contains: "_run_detect_resolutions"
  key_links:
    - from: "backend/app/settlement/event_service.py"
      to: "app.settlement.service.SettlementService.reverse_settlement"
      via: "per-settled-child loop on a fresh _get_session_maker() session"
      pattern: "SettlementService\\.reverse_settlement"
    - from: "backend/tests/settlement/test_event_mirrored.py"
      to: "app.integrations.polymarket.tasks._run_detect_resolutions"
      via: "session_override/redis_override injection over a POLYMARKET market_group's children (verify, unchanged)"
      pattern: "_run_detect_resolutions"
---

<objective>
Complete Phase 15 with the EVENT REVERSE path (EVA-05) and the MIRRORED verify (EVA-06). Add `EventService.reverse_event` — a loop of the UNCHANGED `SettlementService.reverse_settlement` over a house event's already-settled child markets, ONE FRESH session per child (the per-child `CHECK(balance>=0)` floor rolls back that child alone; siblings already reversed stay reversed — another reason for Option A), idempotent, with one additional `event.reversed` audit row, mirroring STL-07. Then a NEW test module `test_event_mirrored.py` proves EVA-06: a `source=POLYMARKET` `market_group`'s children auto-settle through the existing `detect_polymarket_resolutions` Celery path with ZERO new settlement code, and `EventService` refuses to touch mirrored groups.

EVA-06 is a VERIFY deliverable, not new code: the mirrored auto-settle path (`_run_detect_resolutions` -> `SettlementService.resolve_market(actor_user_id=None)` per market) already exists and is unchanged. This plan verifies it end-to-end against synthesized mirrored event children using the task's existing `session_override`/`redis_override` test seam — it does NOT call, modify, or re-route that task, and does NOT alter the beat schedule or Redis locks.

Purpose: Close EVA-05/EVA-06 by reuse + verification, completing the event-settlement layer (resolve/void/reverse + derived status + mirrored verify) with the spike-004 integrity invariant green on every path.
Output: `EventService.reverse_event` (extends the Wave-2 module), reverse integration tests (appended to the Wave-2 suite), and `test_event_mirrored.py` (the EVA-06 verify + reverse mirrored-reject).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-CONTEXT.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-RESEARCH.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-PATTERNS.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-VALIDATION.md
@.claude/skills/spike-findings-xpredict/SKILL.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventService.reverse_event — fresh-session-per-settled-child loop over reverse_settlement, per-child balance floor, mirrored-reject, event.reversed audit (EVA-05)</name>
  <files>backend/app/settlement/event_service.py</files>
  <read_first>
    - backend/app/settlement/event_service.py  (the Wave-2 module — EXTEND with reverse_event; reuse the existing _load_group_with_children / mirrored-reject / justification-guard / event-audit helpers)
    - backend/app/settlement/service.py  (lines 252-385 `reverse_settlement` — signature at :253-261, opens its OWN `async with session.begin()` at :277, posts INVERSE transfers append-only, flips SETTLED->PENDING, `mark_unresolved`, writes `settlement.reversed` audit; returns int count; idempotent (no SETTLED bets => no-op); winner-spent-winnings => CHECK(balance>=0) rollback of that call. DO NOT modify.)
    - backend/app/settlement/market_port.py  (mark_unresolved — called inside reverse to reopen the child; pass the SAME HouseMarketResolveAdapter())
    - backend/app/settlement/router.py  (lines 185-199 — the action-THEN-audit "separate begin()" analog for event.reversed)
    - backend/app/core/audit/service.py  (AuditService.record — event.reversed row, actor convention)
    - backend/app/db/session.py  (_get_session_maker — fresh session per child)
    - backend/app/settlement/constants.py  (reverse_idempotency_key + the Pitfall-6 deferred note at :66-69 — re-resolve-after-reverse reuses settle:{bet_id}:{leg} keys and collides on 23505; scope reverse to restore+audit ONLY)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-PATTERNS.md  (CORE IDIOM — reverse loops reverse_settlement; event audit idiom)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-RESEARCH.md  (EVA-05 contract; Pitfall 3 per-child balance floor; Pitfall 6 deferred re-resolve gap)
  </read_first>
  <behavior>
    - reverse_event(group_id, justification, actor_user_id): loads group+children; raises if source==POLYMARKET; raises if justification blank; loops reverse_settlement per already-settled child, each on a FRESH session, best-effort (a child hitting CHECK(balance>=0) is caught and recorded, siblings already reversed stay reversed); writes one event.reversed audit row.
    - Idempotent: a second reverse_event over the same group finds no SETTLED bets per child and is a no-op (no double-refund).
    - Mirrored (source=POLYMARKET) group -> raise ValueError (EVA-06).
    - Blank/whitespace justification -> raise ValueError.
  </behavior>
  <action>
    EXTEND `backend/app/settlement/event_service.py` by adding `EventService.reverse_event` (a classmethod, keyword-only, `actor_user_id: UUID | None = None`) mirroring `SettlementService.reverse_settlement`'s shape (service.py:253-261). REUSE the Wave-2 helpers: `_load_group_with_children` (selectinload — dodges `lazy="raise"`), the mirrored-reject gate (`group.source == MarketSourceEnum.POLYMARKET.value` => raise), and the non-blank justification guard (`not justification.strip()` => raise). NO `winning_outcome_id` parameter (reverse needs none — `reverse_settlement` finds SETTLED bets by status).

    - Build `ordered_children` = every child market id (order by `market.id` for determinism; winner-first ordering is irrelevant for reverse). Reverse does not need the YES/NO mapping (it reads SETTLED bets directly).
    - Loop EXACTLY per the 15-PATTERNS "CORE IDIOM — reverse loops reverse_settlement": `session_maker = _get_session_maker()`; `resolver = HouseMarketResolveAdapter()`; for each `child_market_id` -> `async with session_maker() as child_session:` call `SettlementService.reverse_settlement(child_session, market_id=child_market_id, market_resolver=resolver, justification=justification, actor_user_id=actor_user_id)`; on `except Exception` append the child id to `failed` and `continue` (a winner who spent winnings hits `CHECK(balance>=0)` and rolls back THAT child only — Pitfall 3; siblings already reversed stay reversed). NEVER chain two `reverse_settlement` calls in one `with`/`begin()` (Pitfall 1 — the 23505 dangling-tx landmine; the same reason resolve uses fresh sessions).
    - After the loop, write ONE `event.reversed` audit row in its own tx (same idiom as resolve/void): `async with session_maker() as audit_session, audit_session.begin(): await AuditService.record(audit_session, actor=f"user:{actor_user_id}" if actor_user_id is not None else "system", event_type="event.reversed", payload={"group_id": str(group_id), "child_count": ..., "children_reversed": ..., "children_failed": [str(x) for x in failed], "justification": justification})`.
    - Return the same summary dataclass shape used by resolve/void (group_id, child_count, the count reversed, failed list, and the derived status via `derive_event_status` — after a full reverse every child reopens to CLOSED so the event derives back to `"open"`).

    SCOPE GUARD (Pitfall 6 — known deferred limitation): reverse is "restore pre-settlement state + audit" ONLY (mirrors STL-07). Do NOT add re-resolution-after-reverse support; the `settle:{bet_id}:{leg}` idempotency keys would collide on 23505 (constants.py:66-69). Add a short code comment in `reverse_event` flagging this deferred gap so it is not mistaken for a Phase-15 bug.

    DO NOT: modify `service.py`/`adapters.py`; DELETE/UPDATE ledger entries (reverse is append-only compensating transfers, inside the unchanged `reverse_settlement` — WAL-06); wrap all child reversals in one tx (would roll back ALL on any one floor hit — Pitfall 3).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.settlement.event_service import EventService; assert hasattr(EventService, 'reverse_event'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `event_service.py` contains `async def reverse_event(` (classmethod, keyword-only, `actor_user_id: UUID | None = None`, NO `winning_outcome_id` param)
    - The loop calls `SettlementService.reverse_settlement(` inside `async with session_maker() as` ... — a FRESH session per child (grep `SettlementService.reverse_settlement` + `_get_session_maker`)
    - `reverse_event` reuses the mirrored-reject (`MarketSourceEnum.POLYMARKET`) and blank-justification (`justification.strip`) guards
    - An `event.reversed` audit row is written in its own `begin()` (grep `event.reversed`)
    - A code comment flags the Pitfall-6 deferred re-resolve-after-reverse limitation
    - `event_service.py` performs NO DELETE/UPDATE of ledger rows and does not re-implement compensating-transfer logic (delegates to reverse_settlement)
  </acceptance_criteria>
  <done>`EventService.reverse_event` composes `SettlementService.reverse_settlement` over a house event's settled children on per-child fresh sessions, idempotent, with per-child balance-floor isolation, a mirrored-reject gate, and an `event.reversed` audit row — restore+audit only, re-resolve-after-reverse explicitly out of scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reverse integration tests — restore / idempotent / per-child balance floor / audit, with spike-004 gate (EVA-05)</name>
  <files>backend/tests/settlement/test_event_service.py</files>
  <read_first>
    - backend/app/settlement/event_service.py  (reverse_event — Task 1)
    - backend/tests/settlement/test_event_service.py  (the Wave-2 suite — APPEND reverse tests; reuse `_seed_house_event`, `_seed_wallet`, `_balance`, `_assert_ledger_clean`, the resolve helpers)
    - backend/tests/settlement/test_resolve_market.py  (lines 602-720 — the reverse_settlement analogs: `test_reverse_settlement_restores_pre_settlement_state` :602, `_writes_audit` :648, `_is_idempotent` :677, `_is_atomic_on_failure` :703 — the per-child balance-floor rollback pattern)
    - backend/app/wallet/reconcile.py  (lines 61-85 `_reconcile_async` — the spike-004 gate, drift_count==0)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-PATTERNS.md  (integrity-check idiom; "scope reverse to restore + audit only" note)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-RESEARCH.md  (Pitfall 3 balance floor; Pitfall 6 — do NOT write a resolve->reverse->re-resolve success test)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-VALIDATION.md  (Wave 0 reverse surface + spike-004 sign-off)
  </read_first>
  <behavior>
    - test_reverse_event_restores_pre_settlement_state: resolve a 3-child event, then reverse_event -> winner/loser balances return to pre-settlement values, each child's SETTLED bets flip back to PENDING, each child reopens (CLOSED); integrity drift_count==0; event derives back to "open".
    - test_reverse_event_is_idempotent: reverse, then reverse again -> second pass reverses nothing (no SETTLED bets), no double-refund; drift_count==0.
    - test_reverse_event_per_child_balance_floor: one winner spends winnings before reverse so that child hits CHECK(balance>=0) and rolls back alone -> that child id surfaced in failed, sibling children reversed successfully; drift_count==0.
    - test_reverse_event_writes_audit: an event.reversed audit row exists with group_id + justification.
  </behavior>
  <action>
    APPEND reverse tests to `backend/tests/settlement/test_event_service.py` (do NOT create a new file — reuse the Wave-2 committed-session helpers and the `_seed_house_event` synthesizer). Mirror `test_resolve_market.py:602-720` at the event level.

    - `test_reverse_event_restores_pre_settlement_state`: capture pre-settlement balances; `EventService.resolve_event(...)`; assert settled; then `EventService.reverse_event(group_id=..., justification="reverse for test", actor_user_id=...)`; assert each user's balance equals its pre-settlement value, each child's bets are back to PENDING (use `_bets_for_user`), each child reopened; `_assert_ledger_clean()` (drift_count==0); and `derive_event_status` over the reopened children yields `"open"`.
    - `test_reverse_event_is_idempotent`: after the restore, call `reverse_event` AGAIN; assert no balance change and the result reverses-count is 0; `_assert_ledger_clean()`. (No-SETTLED-bets no-op — mirrors `test_reverse_settlement_is_idempotent` :677.)
    - `test_reverse_event_per_child_balance_floor`: resolve; for ONE winner, move their winnings out of the wallet (a committed transfer/debit to a sink, mirroring how the per-market floor test forces a winner to have spent — see `test_reverse_settlement_is_atomic_on_failure` :703) so reversing that child would drive the wallet negative; call `reverse_event`; assert that child id is in the result's `children_failed`, the OTHER children reversed successfully (their winners' balances restored / bets back to PENDING), and `_assert_ledger_clean()` reports `drift_count == 0` for the consistent accounts. This proves per-child rollback isolation (Pitfall 3 — Option A).
    - `test_reverse_event_writes_audit`: assert an `event.reversed` audit row exists for the group (query `AuditLog` by `event_type == "event.reversed"` and payload `group_id`), carrying the justification.

    Each reverse/partial-reverse/replay test ends with `_assert_ledger_clean()` (the spike-004 cross-cutting invariant — 15-VALIDATION). Do NOT write any resolve -> reverse -> RE-resolve test expecting success (the deferred Pitfall-6 23505 idempotency-key collision; out of scope).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/settlement/test_event_service.py -k "reverse" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/settlement/test_event_service.py -k reverse -x` exits 0 (Linux CI is the source of truth; the Windows worktree may flake)
    - A restore test asserts each user's balance returns to its pre-settlement value AND bets flip SETTLED->PENDING after `reverse_event`
    - An idempotent-reverse test asserts the second reverse is a no-op (no double-refund)
    - A per-child balance-floor test asserts the failed child is surfaced in `children_failed` while sibling children reverse successfully
    - Every reverse-path test calls `reconcile._reconcile_async(...)` and asserts `drift_count == 0`
    - An `event.reversed` audit assertion is present
    - NO resolve->reverse->re-resolve success test (Pitfall-6 deferred gap untouched)
  </acceptance_criteria>
  <done>The reverse integration suite is green: pre-settlement state restored, idempotent re-reverse a no-op, per-child balance-floor rollback isolated, `event.reversed` audited, and spike-004 `drift_count == 0` on every reverse path.</done>
</task>

<task type="auto">
  <name>Task 3: EVA-06 mirrored verify — detect_polymarket_resolutions settles a mirrored event's children (verify, unchanged) + EventService reverse mirrored-reject</name>
  <files>backend/tests/settlement/test_event_mirrored.py</files>
  <read_first>
    - backend/app/integrations/polymarket/tasks.py  (lines 380-518 `_run_detect_resolutions` — accepts `session_override`/`redis_override` (:382-383); settles `source=POLYMARKET` markets whose UMA is resolved + grace elapsed via `SettlementService.resolve_market(actor_user_id=None)` (:489-496). VERIFY this; do NOT modify it or the beat schedule/locks.)
    - backend/tests/polymarket/test_detect_resolution.py  (THE analog — `test_grace_period_triggers_resolution` :113 and `test_integration_proposed_not_settled` :235 show the `_run_detect_resolutions(redis_override=redis, session_override=session)` injection idiom + the fake-redis + committed-session setup; `test_reversal_after_auto_settlement` :310)
    - backend/app/settlement/event_service.py  (EventService.reverse_event/resolve_event mirrored-reject gate under test)
    - backend/tests/settlement/test_event_service.py  (reuse `_seed_house_event` / committed-session helpers if importable; the mirrored seed sets source=POLYMARKET)
    - backend/app/integrations/polymarket/adapter.py  (sync_events / `_map_winning_outcome_id` — how mirrored market_groups + stamped children + title-case "Yes"/"No" labels are written; reference for synthesizing a realistic mirrored event)
    - backend/app/markets/models.py  (MarketGroup source=POLYMARKET + child Market source=POLYMARKET, group_id stamping, uma_resolved_at / deadline fields the detect path reads)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-PATTERNS.md  ("Mirrored auto-settle path (EVA-06 — VERIFY, do not rebuild)" + mirrored-reject test pattern)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-RESEARCH.md  (EVA-06; Runtime State Inventory — mirrored data are the verification fixtures; the detect path is unchanged)
  </read_first>
  <action>
    Create `backend/tests/settlement/test_event_mirrored.py` (integration, testcontainers) proving EVA-06 — that mirrored event children auto-settle through the EXISTING `detect_polymarket_resolutions` with NO new settlement code, and that `EventService` refuses mirrored groups. Mirror `tests/polymarket/test_detect_resolution.py` for the detect-path setup (the `_run_detect_resolutions(redis_override=fake_redis, session_override=committed_session)` injection idiom; use the project's `fake_redis` fixture / a fakeredis instance as that test does). Use the committed-session model (Pitfall 5) and the `engine` autouse + `_bets_table` fixtures as in the other settlement tests.

    - `test_mirrored_event_children_auto_settle_via_detect` (the EVA-06 core verify): synthesize a `source=POLYMARKET` `MarketGroup` + N child `Market` rows (`source=POLYMARKET`, title-case `"Yes"`/`"No"` outcomes to mirror real Gamma data — Pitfall 2), each with a past `deadline` and an already-elapsed `uma_resolved_at` so the detect path's grace gate passes (mirror the grace setup in `test_grace_period_triggers_resolution` :113). Place bets on the children. Stub/route the Gamma fetch the same way `test_detect_resolution.py` does (the existing test already arranges the parsed-resolved response — follow its exact mechanism, e.g. its `GammaClient` monkeypatch / fixture). Run `_run_detect_resolutions(redis_override=..., session_override=...)` and assert the mirrored children settled (bets flipped, markets RESOLVED) WITHOUT any call into `EventService`. End with `_assert_ledger_clean()` (drift_count==0). Add a comment: this is VERIFY-ONLY — `detect_polymarket_resolutions` is unchanged; `EventService` is never invoked on the mirrored path.
    - `test_event_service_rejects_mirrored_reverse`: seed a `source=POLYMARKET` `MarketGroup`; `with pytest.raises(ValueError): await EventService.reverse_event(group_id=mirrored_group_id, justification="x")`. (Complements the Plan-02 resolve/void mirrored-reject tests; together they cover all three EventService methods refusing POLYMARKET groups — EVA-06 admin-read-only.)

    If the Gamma-stub mechanism in `test_detect_resolution.py` cannot be reused cleanly for a multi-child group, keep the verify minimal but real: at minimum drive ONE mirrored child of a `market_group` through `_run_detect_resolutions` end-to-end and assert it settles, proving the mirrored event-child path needs no new settlement code (EVA-06). Do NOT alter `tasks.py`, the beat schedule, or the Redis lock keys.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/settlement/test_event_mirrored.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/settlement/test_event_mirrored.py -x` exits 0 (Linux CI is the source of truth)
    - The file calls `_run_detect_resolutions(` with `session_override`/`redis_override` and asserts a mirrored (`source=POLYMARKET`) market_group's child(ren) settle (bets flipped / market RESOLVED) — WITHOUT invoking `EventService`
    - The mirrored verify ends asserting `reconcile._reconcile_async(...)` `drift_count == 0`
    - A `pytest.raises(ValueError)` test asserts `EventService.reverse_event` rejects a `source=POLYMARKET` group
    - `backend/app/integrations/polymarket/tasks.py` is NOT modified (verify-only — no diff to the detect task, beat schedule, or lock keys)
    - Mirrored child outcomes use title-case `"Yes"`/`"No"` labels (mirrors real Gamma data; exercises the case-insensitive mapping)
  </acceptance_criteria>
  <done>EVA-06 is proven: a mirrored event's children auto-settle via the unchanged `detect_polymarket_resolutions` path with zero new settlement code, `EventService` rejects mirrored groups on reverse (and resolve/void via Plan 02), and the ledger stays drift-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin actor -> EventService.reverse_event | A reverse carries an actor + justification; the HTTP/admin gate is Phase 16. At this layer the service records an actor, refuses a blank justification, and refuses a mirrored group. |
| EventService.reverse_event -> ledger | Each child reverse posts compensating (inverse) transfers and can hit `CHECK(balance>=0)`; integrity must hold (`drift_count == 0`) and a floor hit must roll back that child alone. |
| detect_polymarket_resolutions -> UMA-owned mirrored events | The oracle path (unchanged) is the ONLY settle path for mirrored events; `EventService` must refuse them. This boundary is verified, not modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Tampering | Double-refund via repeated `reverse_event` | mitigate | Inherited idempotency: `reverse_settlement` finds SETTLED bets by status, so a second reverse is a no-op (no SETTLED bets). `test_reverse_event_is_idempotent` asserts no balance change + `drift_count == 0`. |
| T-15-09 | Tampering | Ledger integrity broken by a multi-child reverse (one child's floor hit corrupting siblings) | mitigate (HIGH) | Per-child ACID transactions (Option A): a `CHECK(balance>=0)` floor hit rolls back THAT child only; siblings already reversed stay reversed (append-only compensating transfers, WAL-06). `test_reverse_event_per_child_balance_floor` + `_reconcile_async` `drift_count == 0` assert it. |
| T-15-10 | Elevation / Tampering | Admin reversing a mirrored (UMA-owned) event | mitigate (HIGH) | `reverse_event` mirrored-reject gate (`source == POLYMARKET` => raise); `test_event_service_rejects_mirrored_reverse` asserts the raise. Mirrored events settle ONLY via the unchanged oracle path. |
| T-15-11 | Repudiation | Denial of who reversed an event and why | mitigate | One immutable `event.reversed` audit row (actor + group_id + justification + counts) in its own tx, in addition to the per-child `settlement.reversed` rows. `test_reverse_event_writes_audit` asserts it. |
| T-15-12 | Tampering | Mirrored event-child settlement silently broken / re-routed through new (unaudited) code | mitigate | EVA-06 is VERIFY-ONLY: `test_event_mirrored.py` drives the UNCHANGED `detect_polymarket_resolutions` over a mirrored `market_group`'s children and asserts they settle with `drift_count == 0`; `tasks.py` is not modified (acceptance criterion: no diff). The mirrored path keeps its existing `actor_user_id=None` + DETECT_LOCK_KEY behavior. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (zero new dependency — 15-RESEARCH "Package Legitimacy Audit: Not applicable"). slopcheck moot; no `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/settlement/test_event_service.py tests/settlement/test_event_mirrored.py tests/settlement/test_derive_event_status.py -x -q` GREEN per-module (trust Linux CI for the full suite).
- spike-004 `_reconcile_async` `drift_count == 0` after reverse / partial-reverse / replay and after the mirrored verify (15-VALIDATION cross-cutting invariant).
- `reverse_event` composes `reverse_settlement` on per-child FRESH sessions; a floor hit rolls back that child only; an `event.reversed` audit row is written.
- All three `EventService` methods reject `source=POLYMARKET` groups; mirrored children auto-settle via the UNCHANGED `detect_polymarket_resolutions` (verified — `tasks.py` not modified).
- No re-resolve-after-reverse path/test (Pitfall-6 deferred gap respected, flagged in code).
</verification>

<success_criteria>
- EVA-05: A house event resolution can be reversed via compensating ledger entries (mirrors STL-07), idempotent, per-child balance-floor isolated, audit-logged; spike-004 integrity green.
- EVA-06: Mirrored (Polymarket) event children auto-settle through the existing `detect_polymarket_resolutions` path (verified, not rebuilt), and mirrored events stay admin-read-only in `EventService` (reject on resolve/void/reverse) except the existing emergency force-settle.
- The Phase-15 event-settlement layer is complete: resolve + void + reverse + derived status + mirrored verify, all with the double-entry integrity invariant green.
</success_criteria>

<output>
Create `.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-03-SUMMARY.md` when done.
</output>

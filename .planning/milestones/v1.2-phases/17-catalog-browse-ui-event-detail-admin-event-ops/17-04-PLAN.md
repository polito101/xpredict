---
phase: 17-catalog-browse-ui-event-detail-admin-event-ops
plan: 04
type: execute
wave: 4
depends_on: ["17-01"]
files_modified:
  - frontend/src/components/admin/event-form.tsx
  - frontend/src/components/admin/event-detail-admin-actions.tsx
  - frontend/src/components/admin/resolve-event-dialog.tsx
  - frontend/src/components/admin/void-event-dialog.tsx
  - frontend/src/components/admin/reverse-event-dialog.tsx
  - frontend/src/app/admin/events/page.tsx
  - frontend/src/app/admin/events/new/page.tsx
  - frontend/src/app/admin/events/[slug]/page.tsx
  - frontend/src/components/admin/admin-nav.tsx
  - frontend/src/components/admin/event-form.test.tsx
  - frontend/src/components/admin/resolve-event-dialog.test.tsx
autonomous: true
requirements: [EVA-01, EVA-02, EVA-03, EVA-04, EVA-05, BRW-06]
tags: [admin, event-ops, useFieldArray, two-step-confirm, server-preview, edit-lock-423, white-label]

must_haves:
  truths:
    - "EventForm has a dynamic useFieldArray outcomes editor (min 2, label + initial_odds in (0,1)); create posts CreateEventRequest, edit posts UpdateEventRequest (whole-list replace); a 423 EVENT_LOCKED disables the outcomes editor with a locked helper"
    - "Resolve/Void/Reverse dialogs use the SERVER two-step: open -> confirm:false preview (winners/losers/settled_children_to_reverse + projected_status) -> operator confirm -> confirm:true execute; justification mandatory (trim>=1)"
    - "Admin events list reads the PUBLIC catalog filtered type:event && source:HOUSE (no admin list endpoint); manage page loads via fetchEvent(slug) (id == group_id); admin-nav has an Events link"
    - "All admin event surfaces are brand-compliant; destructive confirms use the destructive button (not brand-colored)"
  artifacts:
    - path: "frontend/src/components/admin/event-form.tsx"
      provides: "create/edit event form with useFieldArray outcomes + 423 lock"
      min_lines: 120
    - path: "frontend/src/components/admin/event-detail-admin-actions.tsx"
      provides: "status-gated action host (edit form + resolve/void/reverse dialogs + router.refresh)"
      min_lines: 60
    - path: "frontend/src/components/admin/resolve-event-dialog.tsx"
      provides: "outcome Select + justification + server two-step preview/execute"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/admin/event-form.tsx"
      to: "frontend/src/lib/admin-events-api.ts"
      via: "createEvent/updateEvent + isEventLockedError(423)"
    - from: "frontend/src/components/admin/resolve-event-dialog.tsx"
      to: "frontend/src/lib/admin-events-api.ts"
      via: "resolveEvent({confirm:false}) preview then resolveEvent({confirm:true}) execute"
    - from: "frontend/src/app/admin/events/page.tsx"
      to: "frontend/src/lib/catalog.ts"
      via: "fetchCatalog() filtered type:event && source:HOUSE (no admin list endpoint)"
---

<objective>
Build the admin house-event operations UI (EVA-01..05): the create/edit `EventForm` with a dynamic outcomes editor + 423 edit-lock, the resolve/void/reverse dialogs using the backend's server-driven two-step confirm (preview → execute), the action-host island, the three `/admin/events*` pages (list from the filtered public catalog, create, manage), and the nav link. Clones `market-form.tsx`, `resolve-market-dialog.tsx`, `market-detail-actions.tsx`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: EventForm (create/edit, useFieldArray outcomes, 423 lock)</name>
  <files>frontend/src/components/admin/event-form.tsx, frontend/src/components/admin/event-form.test.tsx</files>
  <analog>frontend/src/components/admin/market-form.tsx (RHF+zod+Form*+Loader2+sonner+422)</analog>
  <action>
    Create `event-form.tsx` (`"use client"`) `{mode:"create"|"edit"; groupId?:string; initialValues?:EventFormValues}`. zod schema: `title` (1..500), `category` (optional ≤100), `deadline` (future), `resolution_criteria` (optional ≤2000), `outcomes` array (min 2) each `{label:string(1..100), initial_odds:string matching /^0?\.\d+$|^\d+(\.\d+)?$/ with Number in (0,1)}`. Use `useFieldArray({control, name:"outcomes"})`; render each outcome row: label `Input` + odds `Input` (`inputMode="decimal"`, helper "0–1, e.g. 0.5") + a remove `X` `Button` (disabled when `fields.length<=2`). An "Add outcome" `Button` (`Plus` icon) appends `{label:"",initial_odds:""}`. On submit: create → `createEvent({title, category|undefined, deadline, resolution_criteria|undefined, outcomes})` → toast "Event created." → `router.push(`/admin/events/${created.slug}`)`; edit → `updateEvent(groupId, {title, category, deadline, outcomes})` → toast "Event updated." → `router.refresh()`. Catch: decode the thrown `API error: <status>` — `isEventLockedError(err)` (423) → set `locked` state true (disable the whole outcomes editor + show "Outcomes lock once the event has a bet.") + toast "Outcomes are locked once the event has a bet."; 401/403 → session-expired toast; else generic create/save failure toast. When `locked` (or edit-mode passes a `locked` initial), the outcomes editor + Add button are `disabled`. `event-form.test.tsx`: render create mode; assert ≥2 outcome rows by default, "Add outcome" appends a row, remove disabled at 2; mock `createEvent` and assert the submit body has `outcomes.length>=2` + the title; simulate `updateEvent` throwing `Error("API error: 423")` and assert the locked helper renders + outcomes disabled.
  </action>
  <verify><automated>cd frontend && pnpm exec vitest run src/components/admin/event-form.test.tsx</automated></verify>
  <acceptance_criteria>
    - Dynamic outcomes (min 2) via useFieldArray; add/remove with the 2-floor.
    - Create posts CreateEventRequest; edit posts UpdateEventRequest (outcomes whole-list).
    - A 423 disables the outcomes editor + shows the locked helper.
    - Test green; brand-compliant (no hardcoded hue; default/destructive buttons only).
  </acceptance_criteria>
  <done>The event create/edit form with a dynamic, lockable outcomes editor exists and is tested.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve/Void/Reverse dialogs (server two-step preview/execute)</name>
  <files>frontend/src/components/admin/resolve-event-dialog.tsx, frontend/src/components/admin/void-event-dialog.tsx, frontend/src/components/admin/reverse-event-dialog.tsx, frontend/src/components/admin/resolve-event-dialog.test.tsx</files>
  <analog>frontend/src/components/admin/resolve-market-dialog.tsx + reverse-settlement-dialog.tsx (two-step, stays-open, role=alert, isSessionExpiredError)</analog>
  <action>
    `resolve-event-dialog.tsx` (`"use client"`) `{open; onOpenChange; groupId; outcomes:{label;yes_outcome_id}[]; onResolved}`. State: `winningOutcomeId`, `justification`, `preview:EventActionResponse|null`, `submitting`, `validationError`. On open (effect): reset all. A "Preview" step: when the operator picks an outcome + justification and clicks "Preview impact", call `resolveEvent(groupId,{winning_outcome_id, justification:justification.trim(), confirm:false})` → store `preview` (show "{winners} winning, {losers} losing → {projected_status}"); validation: outcome required + `justification.trim().length>=1` (else `role="alert"`). Then a `destructive` "Confirm resolve" calls `resolveEvent(...,{confirm:true})` → toast "Event resolved." → `onResolved()` → close. Dialog stays open during submit; `isSessionExpiredError`→session toast; else "Couldn't resolve the event. Please try again." `void-event-dialog.tsx`: same but no outcome Select (justification only), `voidEvent`, preview "All {losers} positions settle NO → void", toast "Event voided." `reverse-event-dialog.tsx`: justification only, `reverseEvent`, preview "{settled_children_to_reverse} settled outcomes will be reversed → open", reverse-copy-guard body ("restores pre-settlement state… does not re-open for a clean re-resolution"), toast "Event settlement reversed." `resolve-event-dialog.test.tsx`: mock `@/lib/admin-events-api`; assert clicking Preview calls `resolveEvent` with `confirm:false` and renders winners/losers; assert Confirm calls with `confirm:true`; assert empty justification blocks the call with a `role="alert"`.
  </action>
  <verify><automated>cd frontend && pnpm exec vitest run src/components/admin/resolve-event-dialog.test.tsx</automated></verify>
  <acceptance_criteria>
    - Each dialog does the server two-step: `confirm:false` preview (renders projected impact) then `confirm:true` execute.
    - Justification mandatory (`role="alert"` on empty); session-expired handled.
    - Reverse dialog uses the reverse-copy-guard.
    - Test green.
  </acceptance_criteria>
  <done>The three event settlement dialogs implement the server-driven two-step confirm with previews and mandatory justification.</done>
</task>

<task type="auto">
  <name>Task 3: Action host + admin pages + nav link</name>
  <files>frontend/src/components/admin/event-detail-admin-actions.tsx, frontend/src/app/admin/events/page.tsx, frontend/src/app/admin/events/new/page.tsx, frontend/src/app/admin/events/[slug]/page.tsx, frontend/src/components/admin/admin-nav.tsx</files>
  <analog>market-detail-actions.tsx + app/admin/markets/{page,new,[id]}.tsx + admin-nav.tsx</analog>
  <action>
    `event-detail-admin-actions.tsx` (`"use client"`) `{event:EventDetail}`: derive gating from `event.status` — `open`/`partially_resolved` → Resolve + Void buttons; `resolved`/`partially_resolved` → Reverse button (partially_resolved shows both resolve-ish and reverse — keep Resolve+Void for open/partial and Reverse for resolved/partial). `mirrored = event.source !== "HOUSE"` → hide all mutate actions (mirrored events are read-only; show a note). Build `initialValues` for `<EventForm mode="edit" groupId={event.id} initialValues={...}>` from `event` (title, category, deadline→datetime-local, resolution_criteria omitted unless available, outcomes from `event.outcomes` map to `{label, initial_odds: yes_price}`). Mount the 3 dialogs (open state each) wired to `event.id` (groupId) + `event.outcomes` (resolve Select: label→yes_outcome_id) with `onResolved/onVoided/onReversed = () => router.refresh()`. `app/admin/events/page.tsx` (Server Component, `dynamic="force-dynamic"`): `fetchCatalog()` then filter `type==="event" && source==="HOUSE"` → a simple list/table (title link → `/admin/events/{slug}`, category, derived-ish status, outcome count); top-right "Create event" `Link`→`/admin/events/new`; empty → "No house events yet."; degrade to empty on fetch failure. `app/admin/events/new/page.tsx` → `<EventForm mode="create"/>` in a `max-w-6xl … py-12` shell with an H1 "Create event". `app/admin/events/[slug]/page.tsx` (Server Component): `const {slug}=await params`; `fetchEvent(slug)` (try/catch → "Event not found" block); header (title + status) + `<EventDetailAdminActions event={event}/>`. Edit `admin-nav.tsx`: add `{href:"/admin/events", label:"Events"}` to the LINKS array (keep the active-highlight convention).
  </action>
  <verify><automated>cd frontend && pnpm exec tsc --noEmit && cd frontend && pnpm exec eslint "src/app/admin/events/**" src/components/admin/event-form.tsx src/components/admin/event-detail-admin-actions.tsx src/components/admin/resolve-event-dialog.tsx src/components/admin/void-event-dialog.tsx src/components/admin/reverse-event-dialog.tsx src/components/admin/admin-nav.tsx</automated></verify>
  <acceptance_criteria>
    - The action host gates Resolve/Void/Reverse off the derived status, hides mutations for mirrored events, hosts the edit form + dialogs, and refreshes after a mutation.
    - The list page reads the filtered public catalog; the manage page loads via fetchEvent(slug); admin-nav has the Events link.
    - Typecheck + lint clean for all new/edited admin files.
  </acceptance_criteria>
  <done>The admin event-ops surface (list/create/manage + gated actions + nav) is wired end-to-end against the contract.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|--------|-------------|------------|
| admin_jwt reaching client JS | mitigate | reads happen in the `"use server"` `lib/admin-events-api.ts` only; client calls the async fns. |
| Unauthenticated mutate | mitigate | `proxy.ts` `/admin/:path*` gate + backend `current_active_admin` Bearer; mirrored events hidden + backend 409. |
| Justification XSS / blank | mitigate | rendered as escaped React text; `trim().length>=1` client gate mirrors backend `min_length=1`. |
| Wrong prefix | mitigate | bare `/admin/events` via Plan-01 (URL-tested). |
| package installs | accept | zero new deps. |
</threat_model>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` clean; `pnpm exec eslint` clean for all admin event files.
- `cd frontend && pnpm exec vitest run src/components/admin/event-form.test.tsx src/components/admin/resolve-event-dialog.test.tsx` green.
</verification>

<success_criteria>
- EVA-01: create a house multi-outcome event (title, category, N≥2 outcomes each with a label + initial odds).
- EVA-02: edit while zero bets; outcomes lock (423) after the first bet.
- EVA-03/04/05: resolve / void / reverse via two-step confirm + mandatory justification (server preview→execute).
- BRW-06: brand-compliant admin surfaces.
</success_criteria>

<output>Create 17-04-SUMMARY.md when done.</output>
</content>

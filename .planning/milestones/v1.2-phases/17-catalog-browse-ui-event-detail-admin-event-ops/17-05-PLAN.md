---
phase: 17-catalog-browse-ui-event-detail-admin-event-ops
plan: 05
type: execute
wave: 5
depends_on: ["17-02", "17-03", "17-04"]
files_modified:
  - frontend/src/components/player-nav.tsx
  - frontend/src/components/catalog/event-card.tsx
  - frontend/src/components/event/event-detail-view.tsx
  - frontend/src/components/event/outcome-row.tsx
autonomous: true
requirements: [BRW-06]
tags: [white-label-sweep, integration, brand-audit, validation, nav]

must_haves:
  truths:
    - "Every new Phase-17 surface (catalog browse, event card, event detail rows/bars, admin event forms/dialogs) uses --brand-* tokens; a grep finds no hardcoded indigo/emerald/sky hex in the new files (BRW-06)"
    - "The player nav 'Markets' link still resolves to the catalog browse at /; events are reachable from the catalog"
    - "Full local gate is green: pnpm typecheck + lint + vitest; next build --webpack succeeds (Turbopack flake tolerated locally)"
  artifacts:
    - path: "frontend/src/components/player-nav.tsx"
      provides: "verified Markets->/ link (the catalog browse)"
      min_lines: 1
  key_links:
    - from: "frontend/src/components/player-nav.tsx"
      to: "frontend/src/app/page.tsx"
      via: "Markets link -> / (the catalog browse)"
---

<objective>
Final integration + the BRW-06 white-label sweep: audit every new surface for brand-token compliance (no hardcoded hue), verify the player nav reaches the catalog/events, and run the full local gate (typecheck + lint + vitest + webpack build). This is the cross-cutting closeout that makes BRW-06 a verified truth and proves the surfaces integrate.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: BRW-06 brand sweep + nav verification</name>
  <files>frontend/src/components/player-nav.tsx (verify/adjust), all Phase-17 new files (audit)</files>
  <analog>app/layout.tsx brand cascade + globals.css @theme inline</analog>
  <action>
    Grep every new Phase-17 file for hardcoded brand hues (`#4f46e5`, `#6366f1`, `indigo-`, `sky-`, `emerald-` used as a PRIMARY accent — emerald/red as won/lost/destructive semantic colors are allowed per the Phase-12 palette; the target is stray BRAND-accent hardcoding). Replace any primary-accent hardcode with `bg-/text-/border-/ring-brand-primary` or `var(--brand-primary,…)`. Confirm: the active category chip, the selected OutcomeRow ring, and the per-outcome YES bars all route through the brand token. Read `player-nav.tsx`; confirm the "Markets" entry links to `/` (the catalog browse). If a dedicated "Events" entry is warranted it is optional (events are reachable from the catalog grid); do NOT add nav clutter unless the existing nav clearly expects it. Make only the minimal edits the audit surfaces.
  </action>
  <verify><automated>cd frontend && pnpm exec eslint src && cd frontend && pnpm exec tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - No new file hardcodes a primary brand hue; brand accents use the tokens.
    - The player nav "Markets" link resolves to `/` (the catalog browse).
    - Lint + typecheck clean.
  </acceptance_criteria>
  <done>BRW-06 is a verified truth across all new surfaces; nav reaches the catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Full local validation gate</name>
  <files>(no source change — validation)</files>
  <action>
    Run the full frontend gate: `cd frontend && pnpm exec tsc --noEmit` (typecheck), `pnpm exec eslint src` (lint), `pnpm exec vitest run` (all tests), `pnpm exec next build --webpack` (build — Turbopack flakes on the Windows worktree per [[xprediction-frontend-local-validation]], so use the webpack builder locally; the Linux CI `frontend` job is authoritative). Fix any failure (typecheck/lint/test) and re-run until green. If `next build --webpack` hits a known worktree-only flake (pnpm symlink / Sentry), record it and rely on CI; a genuine build error must be fixed.
  </action>
  <verify><automated>cd frontend && pnpm exec tsc --noEmit && cd frontend && pnpm exec vitest run</automated></verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` clean.
    - `pnpm exec vitest run` all green.
    - `pnpm exec eslint src` clean.
    - `next build --webpack` succeeds (or a documented worktree-only flake deferred to CI).
  </acceptance_criteria>
  <done>The full local gate is green; the phase is ready for code review + verification + CI.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` clean.
- `cd frontend && pnpm exec eslint src` clean.
- `cd frontend && pnpm exec vitest run` all green.
- Grep audit: no stray primary-brand hex in any Phase-17 file.
</verification>

<success_criteria>
- BRW-06: every new catalog/browse/event/admin surface respects the operator's `--brand-*` tokens (verified by audit + the runtime palette-swap note in UI-review).
- The phase integrates and the full local gate is green.
</success_criteria>

<output>Create 17-05-SUMMARY.md when done.</output>
</content>

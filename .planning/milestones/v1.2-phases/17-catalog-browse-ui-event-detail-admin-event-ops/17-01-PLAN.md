---
phase: 17-catalog-browse-ui-event-detail-admin-event-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/catalog.ts
  - frontend/src/lib/admin-events-types.ts
  - frontend/src/lib/admin-events-api.ts
  - frontend/src/lib/__tests__/catalog.test.ts
  - frontend/src/lib/__tests__/admin-events-api.test.ts
autonomous: true
requirements: [EVT-02, EVT-03, EVT-04, EVT-05, BRW-06, EVA-01, EVA-02, EVA-03, EVA-04, EVA-05]
tags: [data-layer, catalog-client, admin-events, prefix-split, money-strings, server-actions]

must_haves:
  truths:
    - "lib/catalog.ts mirrors lib/api.ts (apiBase server/browser split + cache:no-store); fetchCatalog builds GET /api/v1/catalog?q&category&status&sort; fetchEvent throws EventNotFound on 404; fetchCategories returns string[]"
    - "catalogMarketToMarketItem adapter maps a type:market CatalogItem to the MarketItem shape MarketCard consumes (deadline ?? '' guards formatDeadline)"
    - "lib/admin-events-api.ts is 'use server', forwards admin_jwt as Bearer, and targets the BARE /admin/events prefix (NO /api/v1); resolve/void/reverse carry the confirm flag; 423 is surfaced as EventLockedError/status"
    - "lib/admin-events-types.ts holds all event request/response TS types (a use-server module exports only async fns); money/odds typed as string"
  artifacts:
    - path: "frontend/src/lib/catalog.ts"
      provides: "CatalogItem, CatalogOutcome, EventDetail, EventOutcomeRead types + fetchCatalog/fetchEvent/fetchCategories + EventNotFound + catalogMarketToMarketItem"
      min_lines: 90
    - path: "frontend/src/lib/admin-events-api.ts"
      provides: "createEvent/updateEvent/resolveEvent/voidEvent/reverseEvent server actions (bare /admin/events, Bearer, confirm flag, 423 surfaced)"
      min_lines: 70
    - path: "frontend/src/lib/admin-events-types.ts"
      provides: "event request/response types"
      min_lines: 40
  key_links:
    - from: "frontend/src/lib/catalog.ts"
      to: "frontend/src/lib/api.ts"
      via: "apiBase() pattern + cache:no-store + typed throw (EventNotFound mirrors MarketNotFound) + reuse formatVolume/formatDeadline"
    - from: "frontend/src/lib/admin-events-api.ts"
      to: "frontend/src/lib/admin-markets-api.ts"
      via: "bearerHeader() admin_jwt->Bearer + adminApiFetch throw-on-non-2xx; per-call full bare /admin/events path"
---

<objective>
Build the Phase-17 data layer: the public catalog read client (`lib/catalog.ts`), the admin event Server Actions (`lib/admin-events-api.ts`) + their shared types (`lib/admin-events-types.ts`), and two URL-contract unit tests. This is the typed seam every Phase-17 UI surface fetches through. No backend changes — it consumes the merged Phase-16 contract.
Output: 3 lib modules + 2 test files. Mirrors `lib/api.ts` (public reads) and `lib/admin-markets-api.ts` (admin writes) exactly.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: lib/catalog.ts (public catalog client + types + adapter)</name>
  <files>frontend/src/lib/catalog.ts</files>
  <analog>frontend/src/lib/api.ts (apiBase, cache:no-store, MarketNotFound, formatVolume/formatDeadline, MarketItem/MarketOutcome)</analog>
  <action>
    Create `lib/catalog.ts`. Define types from the Phase-16 contract: `PublicCatalogStatus = "open"|"closing_soon"|"resolved"`, `CatalogSort = "volume"|"closing_soonest"|"newest"`, `EventDerivedStatus = "open"|"partially_resolved"|"resolved"|"void"`, `CatalogOutcome {label:string; yes_outcome_id:string|null; yes_price:string}`, `CatalogItem {type:"market"|"event"; id; slug; title; category:string|null; source:string; status:PublicCatalogStatus; deadline:string|null; volume:string; created_at:string; outcomes:CatalogOutcome[]}`, `EventOutcomeRead {label; yes_outcome_id:string|null; yes_price:string; market_id:string; child_slug:string; child_status:string}`, `EventDetail {id; slug; title; category:string|null; source:string; status:EventDerivedStatus; deadline:string|null; created_at:string; outcomes:EventOutcomeRead[]}`. Copy `apiBase()` from `lib/api.ts` (same server/browser split). Implement `fetchCatalog(params:{q?;category?;status?;sort?}): Promise<CatalogItem[]>` building a `URLSearchParams` (omit empty/undefined) → `GET ${apiBase()}/api/v1/catalog?…` `{cache:"no-store"}`, throw on non-ok. `class EventNotFound extends Error` (mirror MarketNotFound). `fetchEvent(slug): Promise<EventDetail>` → `GET /api/v1/events/{encodeURIComponent(slug)}`, 404→throw EventNotFound, throw on other non-ok. `fetchCategories(): Promise<string[]>` → `GET /api/v1/categories`. Add pure `catalogMarketToMarketItem(item:CatalogItem): MarketItem` (import MarketItem from ./api): map `question:item.title`, `deadline:item.deadline ?? ""`, `bet_count:0`, `volume_24hr:"0"`, `source_url:null`, `outcomes:item.outcomes.map(o=>({id:o.yes_outcome_id ?? "", label:o.label, initial_odds:o.yes_price, current_odds:o.yes_price}))`. Re-export `formatVolume`/`formatDeadline` from ./api or import where needed.
  </action>
  <verify><automated>cd frontend && pnpm exec tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - `lib/catalog.ts` contains `fetchCatalog`, `fetchEvent`, `fetchCategories`, `EventNotFound`, `catalogMarketToMarketItem`.
    - Money/odds fields (`yes_price`, `volume`) typed `string`.
    - `fetchEvent` throws `EventNotFound` on 404; `fetchCatalog` builds `/api/v1/catalog` with a query string from non-empty params only.
    - Adapter passes `deadline ?? ""` (never null) to the MarketItem shape.
  </acceptance_criteria>
  <done>The public catalog client + types + adapter exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: lib/admin-events-types.ts + lib/admin-events-api.ts (admin event server actions)</name>
  <files>frontend/src/lib/admin-events-types.ts, frontend/src/lib/admin-events-api.ts</files>
  <analog>frontend/src/lib/admin-markets-api.ts (use-server Bearer-forward) + frontend/src/lib/admin-markets-types.ts</analog>
  <action>
    Create `lib/admin-events-types.ts`: `OutcomeInput {label:string; initial_odds:string}`, `CreateEventRequest {title; category?:string|null; deadline:string; resolution_criteria?:string|null; slug?:string|null; outcomes:OutcomeInput[]}`, `UpdateEventRequest {title?; category?:string|null; deadline?; outcomes?:OutcomeInput[]}`, `EventChildRead {market_id; label; slug; status; yes_outcome_id:string|null; yes_price:string}`, `EventCreatedResponse {id; title; slug; category:string|null; source; deadline:string|null; outcomes:EventChildRead[]}` (alias `EventDetailResponse`), `ResolveEventRequest {winning_outcome_id; justification; confirm?:boolean}`, `VoidEventRequest {justification; confirm?:boolean}`, `ReverseEventRequest {justification; confirm?:boolean}`, `EventActionResponse {preview:boolean; group_id:string; child_count:number; winners?:number|null; losers?:number|null; settled_children_to_reverse?:number|null; children_settled?:number|null; children_failed?:string[]|null; projected_status:string}`, and `EVENT_LOCKED = "EVENT_LOCKED"` + a helper `isEventLockedError(err): boolean` (matches `/\b423\b/` on the thrown message). Create `lib/admin-events-api.ts` (`"use server"`): copy `getBackendUrl`, `bearerHeader` (reads `admin_jwt`), `adminApiFetch<T>` from `admin-markets-api.ts` verbatim (throw `API error: <status>` on non-2xx). Then: `createEvent(body:CreateEventRequest): Promise<EventCreatedResponse>` → `POST /admin/events`; `updateEvent(groupId, body:UpdateEventRequest): Promise<EventDetailResponse>` → `PATCH /admin/events/{groupId}`; `resolveEvent(groupId, body:ResolveEventRequest): Promise<EventActionResponse>` → `POST /admin/events/{groupId}/resolve`; `voidEvent(groupId, body:VoidEventRequest)` → `…/void`; `reverseEvent(groupId, body:ReverseEventRequest)` → `…/reverse`. ALL bare `/admin/events…` (NO `/api/v1`). `Content-Type: application/json` + `JSON.stringify(body)` on each. (The 423 lock surfaces via the thrown `API error: 423`, decoded by `isEventLockedError` in the form.)
  </action>
  <verify><automated>cd frontend && pnpm exec tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - `admin-events-api.ts` starts with `"use server"` and exports only async functions.
    - Every event URL is bare `/admin/events…` (no `/api/v1`).
    - resolve/void/reverse pass the `confirm` flag through the body.
    - All shared types live in `admin-events-types.ts`.
  </acceptance_criteria>
  <done>Admin event Server Actions + types exist, bare-prefixed, Bearer-forwarded, confirm-flag-aware, and typecheck.</done>
</task>

<task type="auto">
  <name>Task 3: URL-contract tests (catalog + admin-events)</name>
  <files>frontend/src/lib/__tests__/catalog.test.ts, frontend/src/lib/__tests__/admin-events-api.test.ts</files>
  <analog>frontend/src/lib/__tests__/admin-markets-api.test.ts + auth.test.ts (fetch-mock style)</analog>
  <action>
    `catalog.test.ts`: `vi.spyOn(globalThis,"fetch")` returning `new Response(JSON.stringify([...]),{status:200})`; assert `fetchCatalog({q:"x",category:"Sports",status:"open",sort:"newest"})` calls a URL containing `/api/v1/catalog` with `q=x&category=Sports&status=open&sort=newest` (and that empty params are omitted); assert `fetchEvent("s")` 404 → rejects with `EventNotFound`; assert `fetchCategories()` hits `/api/v1/categories`; assert `catalogMarketToMarketItem` maps title→question, yes_price→YES current_odds, null deadline→"". `admin-events-api.test.ts` (clone admin-markets-api.test): `vi.hoisted` cookie store with `admin_jwt`, `vi.mock("next/headers")`, `vi.spyOn(globalThis,"fetch")`, `process.env.BACKEND_URL="http://backend.test"`; assert `createEvent` POSTs `http://backend.test/admin/events` (bare, NO /api/v1) with `Authorization: Bearer`; assert `resolveEvent(gid,{winning_outcome_id,justification,confirm:false})` POSTs `/admin/events/{gid}/resolve` with `confirm:false` in the JSON body, and `confirm:true` passes through; assert a 423 response makes `updateEvent` reject with a message matching `/423/` (so `isEventLockedError` is true).
  </action>
  <verify><automated>cd frontend && pnpm exec vitest run src/lib/__tests__/catalog.test.ts src/lib/__tests__/admin-events-api.test.ts</automated></verify>
  <acceptance_criteria>
    - Both test files pass.
    - The admin test asserts the BARE `/admin/events` prefix (no `/api/v1`) and the `confirm` flag passthrough.
    - The catalog test asserts `EventNotFound` on 404 and the query-string build.
  </acceptance_criteria>
  <done>URL-contract tests lock the prefix split, the confirm flag, EventNotFound, and the adapter.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` clean.
- `cd frontend && pnpm exec vitest run src/lib/__tests__/catalog.test.ts src/lib/__tests__/admin-events-api.test.ts` green.
- `admin-events-api.ts` contains no `/api/v1` substring for any event URL.
</verification>

<success_criteria>
- The typed seam (catalog reads + admin event writes) exists and is contract-tested, unblocking all Phase-17 UI plans (17-02..17-05).
</success_criteria>

<output>Create 17-01-SUMMARY.md when done.</output>
</content>

---
phase: 14-curated-per-category-gamma-sync
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - backend/app/integrations/polymarket/adapter.py
  - backend/tests/polymarket/test_adapter.py
autonomous: true
requirements: [CAT-04, EVT-07, CAT-06]
nyquist_compliant: true

must_haves:
  truths:
    - "sync_events on the multi-outcome event writes ONE market_groups row (source_event_id='538337', category='Crypto') and 3 child markets each stamped group_id=that row, category='Crypto', and group_item_title in {'64,000','66,000','68,000'}."
    - "A len==1 event (after conditionId dedup) writes NO market_groups row; its single market is standalone (group_id IS NULL) with category populated (EVT-07)."
    - "sync_events is idempotent: replaying the same event does not create a second market_groups row (ON CONFLICT (source, source_event_id))."
    - "Every child routes through GammaMarket._derive_status (inherited) — status is never set from closed=true alone; this plan does NOT settle."
    - "sync_top25 still works — its per-market body now delegates to the extracted _upsert_one_market with group_id=None, category=None (back-compat)."
  artifacts:
    - path: "backend/app/integrations/polymarket/adapter.py"
      provides: "_upsert_one_market (extracted), _upsert_market_group, sync_events"
      contains: "async def sync_events"
    - path: "backend/tests/polymarket/test_adapter.py"
      provides: "sync_events grouping / EVT-07 / category / dedup / idempotency integration tests"
      contains: "test_sync_events_groups_multi_outcome"
  key_links:
    - from: "backend/app/integrations/polymarket/adapter.py::sync_events"
      to: "market_groups (pg_insert ON CONFLICT (source, source_event_id))"
      via: "_upsert_market_group using ix_market_groups_source_source_event_id"
      pattern: "on_conflict_do_update[\\s\\S]*source_event_id"
    - from: "backend/app/integrations/polymarket/adapter.py::sync_events"
      to: "markets.group_id / markets.category / markets.group_item_title"
      via: "_upsert_one_market stamps the child"
      pattern: "_upsert_one_market"
---

<objective>
Build the write path: extract the per-market upsert body from `sync_top25` into `_upsert_one_market(session, parsed, *, group_id, category)`, then build `sync_events()` on top — `_upsert_market_group()` (parent `market_groups` upsert on the Phase-13 partial-unique) + the child stamp + within-event conditionId dedup + the EVT-07 standalone branch + the slug-collision fallback. This is the first writer of `market_groups` (the Phase-13 seam). Consumes `GammaEvent` from 14-01 (Wave 2 → depends_on 14-01). Owns ONLY `adapter.py` + its test.

Purpose: Implements CAT-04 (populate `Market.category` + `MarketGroup.category`), EVT-07 (len==1 stays standalone, no group), and the CAT-06 data-layer contract (never WRITE an empty category — suppression itself is a Phase-16 read). Reuses the proven ON CONFLICT idempotent upsert; adds ZERO new status code paths.
Output: `adapter.py` gains the extracted helper + 2 new methods; `test_adapter.py` gains grouping/EVT-07/category/dedup/idempotency integration tests (testcontainers — run PER-MODULE per the Windows-worktree policy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-curated-per-category-gamma-sync/14-CONTEXT.md
@.planning/phases/14-curated-per-category-gamma-sync/14-RESEARCH.md
@.planning/phases/14-curated-per-category-gamma-sync/14-VALIDATION.md

# The body to extract + the ON CONFLICT pattern + changed_markets bookkeeping
@backend/app/integrations/polymarket/adapter.py
# The parsers this consumes (GammaEvent / GammaEventMarket) — 14-01 output
@backend/app/integrations/polymarket/schemas.py
# MarketGroup ORM + the partial-unique index + Market.group_id/category/group_item_title + generate_slug/_slugify
@backend/app/markets/models.py
# Integration test harness (async_session fixture, TestAdapterIntegration patterns)
@backend/tests/polymarket/test_adapter.py
# Phase-13 dependency context: market_groups is empty; THIS phase is its first writer
@.planning/phases/13-multi-outcome-model-catalog-indexes/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract _upsert_one_market from sync_top25 (with group_id + category stamp); keep sync_top25 green</name>
  <read_first>
    - backend/app/integrations/polymarket/adapter.py (FULL): `sync_top25` (lines 163-299) — the deadline parse (182-188), the `slug = f"pm-{parsed.slug}"[:100]` rule (193), `description` fallback (194), the `market_values` dict (199-211), the `pg_insert(Market).on_conflict_do_update(index_elements=["source","source_market_id"], index_where=Market.source_market_id.isnot(None), set_={...})` (213-226), the post-upsert `select(Market)` by (source, source_market_id) (229-235), the YES/NO outcome upsert loop with `current_odds` change tracking → `market_deltas` (243-277), `flush()` + `self.changed_markets.append(...)` (279-285), and the `except IntegrityError: await session.rollback(); continue` (291-297). Imports already present: `pg_insert`, `IntegrityError`, `UUID`, `generate_slug`, `Market`, `Outcome`, `format_odds`.
    - 14-RESEARCH.md Pattern 2 (lines 300-350) — the exact `_upsert_one_market(session, parsed, *, group_id, category)` signature + the 3 NEW market_values keys (category, group_id, group_item_title via `getattr(parsed, "group_item_title", None)`) + the 3 NEW `set_` excluded entries.
    - backend/app/markets/models.py: `Market.category` (line 76, String(100) nullable), `Market.group_id` (176-181, FK market_groups ondelete SET NULL), `Market.group_item_title` (182, Text nullable).
  </read_first>
  <action>
    Add `async def _upsert_one_market(self, session, parsed, *, group_id: UUID | None, category: str | None) -> bool` containing the EXACT body currently inside the `sync_top25` per-market `try:` (deadline parse, slug, description, market upsert, post-upsert select, YES/NO outcome loop, flush, changed_markets bookkeeping). Add THREE keys to `market_values`: `"category": category`, `"group_id": group_id`, `"group_item_title": getattr(parsed, "group_item_title", None)`. Add the matching THREE entries to the `on_conflict_do_update` `set_`: `"category": stmt.excluded.category`, `"group_id": stmt.excluded.group_id`, `"group_item_title": stmt.excluded.group_item_title`. Return `True` after the successful flush, `False` on the `IntegrityError` (rollback + return False, NOT `continue`). Then rewrite `sync_top25` to loop and call `if await self._upsert_one_market(session, parsed, group_id=None, category=None): synced += 1` after the per-raw parse+ValidationError guard (the parse/ValidationError-skip stays in `sync_top25`; the DB body moves into the helper). The realtime `self.changed_markets` recording stays inside `_upsert_one_market` so both paths publish identically. Do NOT change `detect_resolution`, `fetch_active_markets`, `fetch_market`, or `_map_winning_outcome_id`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/polymarket/test_adapter.py -x</automated>
  </verify>
  <acceptance_criteria>
    - adapter.py contains `async def _upsert_one_market(` with keyword-only `group_id` and `category` params.
    - adapter.py `market_values` (inside the helper) contains `"category":`, `"group_id":`, and `"group_item_title":`.
    - The `on_conflict_do_update` `set_` inside the helper contains `stmt.excluded.category`, `stmt.excluded.group_id`, `stmt.excluded.group_item_title`.
    - `sync_top25` body calls `self._upsert_one_market(` with `group_id=None, category=None`.
    - `cd backend && uv run pytest tests/polymarket/test_adapter.py -x` exits 0 (existing sync_top25 integration tests stay green — back-compat).
  </acceptance_criteria>
  <done>The shared upsert helper exists, stamps category/group_id/group_item_title, and sync_top25 delegates to it with nulls — legacy behavior byte-equivalent.</done>
</task>

<task type="auto">
  <name>Task 2: Add _upsert_market_group + sync_events (grouping, EVT-07, conditionId dedup, slug-collision fallback)</name>
  <read_first>
    - 14-RESEARCH.md Pattern 2 "sync_events" + "_upsert_market_group" (lines 352-403) — the exact loop: within-event conditionId dedup, `if len(children) == 1: _upsert_one_market(children[0], group_id=None, category); continue` (EVT-07), else `group_id = _upsert_market_group(...)` then stamp each child; the parent upsert `pg_insert(MarketGroup).on_conflict_do_update(index_elements=["source","source_event_id"], index_where=MarketGroup.source_event_id.isnot(None), set_={"title":..,"category":..,"updated_at":..})` then `select(MarketGroup.id)` by (source, source_event_id).
    - 14-RESEARCH.md Pitfall 6 (lines 554-558) — `MarketGroup.slug` is `String(100) UNIQUE`; ON CONFLICT is on `(source, source_event_id)` NOT slug, so a slug clash across DIFFERENT events raises IntegrityError → fall back to a suffixed slug (mirror `generate_slug`'s `-{uuid hex[:6]}`).
    - backend/app/markets/models.py: `class MarketGroup` (200-260) — `title: Text NOT NULL` (240), `source` (241-245), `source_event_id: String(200) nullable` (246), `category: String(100) nullable` (247), `slug: String(100) UNIQUE NOT NULL` (248-253); the partial-unique `ix_market_groups_source_source_event_id` (224-230). `generate_slug` / `_slugify` (33-36).
    - backend/app/markets/enums.py via adapter import — `MarketSourceEnum.POLYMARKET.value`.
  </read_first>
  <action>
    Add `async def _upsert_market_group(self, session, ev: GammaEvent, category: str) -> UUID`: build `slug = f"pm-evt-{_slugify(ev.title, max_length=80)}"[:100]` with a `generate_slug(ev.title)` fallback if the slugified title is empty; values dict = `source=MarketSourceEnum.POLYMARKET.value`, `source_event_id=ev.id`, `title=ev.title`, `slug=slug`, `category=category`; `pg_insert(MarketGroup).on_conflict_do_update(index_elements=["source","source_event_id"], index_where=MarketGroup.source_event_id.isnot(None), set_={"title": excluded.title, "category": excluded.category, "updated_at": datetime.now(UTC)})`; on `IntegrityError` from a slug collision (different event, same `pm-evt-` slug), retry once with a `-{uuid4().hex[:6]}`-suffixed slug (Pitfall 6); then `select(MarketGroup.id).where(source==POLYMARKET, source_event_id==ev.id)` → `scalar_one()`. Add `async def sync_events(self, session, events: list[GammaEvent], *, category: str) -> int`: for each event, dedup children within the event by `condition_id` (skip falsy/duplicate condition_ids); if `len(children) == 1` → `_upsert_one_market(children[0], group_id=None, category=category)` and `continue` (EVT-07, NO group row); else `group_id = await self._upsert_market_group(session, ev, category)` and stamp every child via `_upsert_one_market(child, group_id=group_id, category=category)`; accumulate `synced` from the helper's True returns; return `synced`. Import `GammaEvent` (and `MarketGroup`, `_slugify`/`generate_slug`) at the top of adapter.py. Do NOT write a category onto a group/child when there are no qualifying events — sync_events is only ever called with a non-empty curated list per category (CAT-06: never persist an empty category; suppression is a Phase-16 read). Never derive status outside the inherited `GammaMarket._derive_status` (spike-002).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/polymarket/test_adapter.py -x</automated>
  </verify>
  <acceptance_criteria>
    - adapter.py contains `async def sync_events(` and `async def _upsert_market_group(`.
    - sync_events contains the `if len(` … `== 1` EVT-07 branch calling `_upsert_one_market(` with `group_id=None`.
    - `_upsert_market_group` uses `on_conflict_do_update` with `index_elements=["source", "source_event_id"]` and has a uuid-suffixed slug fallback path (`uuid4().hex` referenced).
    - adapter.py imports `GammaEvent` and `MarketGroup`.
    - `cd backend && uv run pytest tests/polymarket/test_adapter.py -x` exits 0 (Task 3 supplies the grouping/EVT-07 assertions).
  </acceptance_criteria>
  <done>sync_events writes one group + N stamped children for multi-outcome events, a standalone market for len==1 (no group), is idempotent, and survives a slug collision.</done>
</task>

<task type="auto">
  <name>Task 3: Add sync_events integration tests (grouping, EVT-07, category, dedup, idempotency)</name>
  <read_first>
    - backend/tests/polymarket/test_adapter.py (lines 1-55): imports, `_integration_marks`-style `@pytest.mark.integration` + `@pytest.mark.asyncio(loop_scope="session")`, the `async_session` fixture usage, `TestAdapterIntegration` patterns.
    - 14-RESEARCH.md "sync_events integration test" (lines 601-666) — the exact assertions: `n == 3`; the `select(MarketGroup).where(source_event_id=="538337")` group with `category=="Crypto"`; 3 children via `select(Market).where(group_id==grp.id)` all `category=="Crypto"` with `group_item_title` set {"64,000","66,000","68,000"}; the single-market test asserting NO group row for source_event_id "108634" and the lone market (source_market_id "958443") `group_id is None and category=="Politics"`; the idempotency replay asserting `len(grps)==1`.
    - The fixtures: backend/tests/fixtures/gamma/{events_multi_outcome,events_single_market}.json. The 14-01 conftest fixtures `gamma_events_multi` / `gamma_events_single` are available (Wave-1 dependency).
  </read_first>
  <action>
    In test_adapter.py add an integration block using `async_session` + `gamma_events_multi`/`gamma_events_single`. `test_sync_events_groups_multi_outcome`: parse the fixture into `GammaEvent`s, `n = await adapter.sync_events(async_session, events, category="Crypto")`, assert `n==3`, one `MarketGroup` (source_event_id=="538337", category=="Crypto"), 3 children under `group_id` all category=="Crypto" with `{k.group_item_title for k in kids} == {"64,000","66,000","68,000"}`. `test_sync_events_single_market_no_group`: sync the single fixture with category="Politics"; assert NO MarketGroup row for source_event_id=="108634" (scalar_one_or_none() is None) and the lone Market (source_market_id=="958443") has `group_id is None and category=="Politics"` (EVT-07). `test_sync_events_idempotent`: call sync_events twice with the multi fixture; assert exactly 1 MarketGroup for source_event_id=="538337". Use the actual ids present in the on-disk fixtures (read them — if an id differs from the research sample, assert against the real fixture value and note it in the SUMMARY). Mark all `@pytest.mark.integration` + `@pytest.mark.asyncio(loop_scope="session")`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/polymarket/test_adapter.py -k sync_events -x</automated>
  </verify>
  <acceptance_criteria>
    - test_adapter.py contains `test_sync_events_groups_multi_outcome`, `test_sync_events_single_market_no_group`, `test_sync_events_idempotent`.
    - `cd backend && uv run pytest tests/polymarket/test_adapter.py -k sync_events -x` exits 0 — proves: 1 group + 3 stamped children with category + group_item_title (SC#1/SC#3); len==1 → no group, standalone with category (EVT-07/SC#4); idempotent replay (1 group).
    - Windows-worktree policy honored: PER-MODULE run only; the full `pytest tests/` suite is NOT used as the local gate.
  </acceptance_criteria>
  <done>Grouping, the EVT-07 standalone path, category population, conditionId dedup, and idempotency are proven by integration tests against the live-captured fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed GammaEvent → DB writes (markets, market_groups) | Curated-but-externally-sourced data is upserted into internal tables; integrity + idempotency must hold |

## STRIDE Threat Register

(Applicable ASVS: V5 Input Validation. No auth/session/access-control — internal Celery write path, no user input.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | duplicate event / child rows from cross-fetch overlap | mitigate | `pg_insert ... on_conflict_do_update` on the Phase-13 partial-unique `(source, source_event_id)` (groups) and `(source, source_market_id)` (children) — replay is idempotent; within-event conditionId dedup drops duplicate children before upsert. |
| T-14-09 | Denial of Service | slug collision aborts a whole group + siblings | mitigate | `IntegrityError` on `ix_market_groups_slug` is caught and retried with a uuid-suffixed slug (Pitfall 6) — one event's slug clash cannot block its siblings or the cycle. |
| T-14-10 | Elevation of Privilege (data) | child stored RESOLVED inside an OPEN event (settle on price/closed alone) | mitigate | Every child routes through the inherited `GammaMarket._derive_status` (spike-002 truth table: closed=true alone → CLOSED, never RESOLVED). This plan writes status only; it does NOT settle (Phase 15). |
| T-14-11 | Repudiation | silent partial write on child IntegrityError | accept | `_upsert_one_market` rolls back the failing child and returns False (logged `gamma.upsert_conflict`); the cycle continues. Matches the proven `sync_top25` behavior; per-child loss is logged, low risk. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/polymarket/test_adapter.py -x` exits 0 (back-compat sync_top25 + new sync_events).
- `cd backend && uv run pytest tests/polymarket/test_adapter.py -k sync_events -x` exits 0 (grouping/EVT-07/category/idempotency).
- Windows-worktree policy: PER-MODULE integration runs only; the Linux-CI `backend` job (full `pytest tests/` + ruff + mypy) is the phase gate.
</verification>

<success_criteria>
- sync_events writes the `market_groups` seam for the first time: 1 group + N stamped children (category + group_id + group_item_title) for multi-outcome events.
- A len==1 event stays standalone with NO group row (EVT-07); category is populated on mirrored rows for the first time (CAT-04).
- Replay is idempotent; slug collisions degrade gracefully; status always flows through `_derive_status` (no new path; no settlement).
</success_criteria>

<output>
Create `.planning/phases/14-curated-per-category-gamma-sync/14-03-SUMMARY.md` when done.
</output>

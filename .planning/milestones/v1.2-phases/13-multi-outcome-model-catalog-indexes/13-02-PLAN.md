---
phase: 13-multi-outcome-model-catalog-indexes
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - backend/tests/markets/test_migration_0011.py
  - backend/tests/markets/test_models.py
autonomous: true
requirements: [EVT-01]

must_haves:
  truths:
    - "test_migration_0011.py proves 0011 applies, is reversible, chains off 0010_phase12_resolution_stakes as the single head, enables pg_trgm, and creates all 6 indexes with correct GIN gin_trgm_ops opclass and partial WHERE"
    - "test_models.py proves MarketGroup round-trips a parent with >=2 children via selectinload, Market.group is lazy=raise, and a group_id IS NULL standalone market is unchanged"
    - "The full markets suite plus existing bets/settlement suites stay green (SC#2 zero-behavior-change proof) and money-lint stays green"
  artifacts:
    - path: "backend/tests/markets/test_migration_0011.py"
      provides: "Migration introspection: apply, reversibility, chain/down_revision, pg_trgm, all 6 indexes incl. GIN opclass + partial WHERE via pg_indexes.indexdef"
      contains: "ix_markets_question_trgm"
      min_lines: 60
    - path: "backend/tests/markets/test_models.py"
      provides: "MarketGroup round-trip (>=2 children) + Market.group lazy=raise + group_id IS NULL regression"
      contains: "MarketGroup"
  key_links:
    - from: "backend/tests/markets/test_migration_0011.py"
      to: "ix_market_groups_source_source_event_id partial WHERE"
      via: "pg_indexes.indexdef raw-DDL introspection"
      pattern: "pg_indexes"
    - from: "backend/tests/markets/test_models.py"
      to: "MarketGroup.markets"
      via: "selectinload round-trip"
      pattern: "selectinload\\(MarketGroup.markets\\)"
---

<objective>
Provide the automated Nyquist verification for every Phase 13 success criterion. This is the Wave 0 test layer for the schema+ORM shipped in Plan 13-01: a NEW migration-introspection test and an extension to the existing ORM test module. After this plan, every SC (1, 2, 3, 4) and EVT-01 has an automated `uv run pytest` proof.

Purpose: Prove the migration applies + reverses + chains correctly, pg_trgm + all 6 indexes exist with the right opclass/partial-WHERE, the `MarketGroup` relationship round-trips, and existing standalone binary markets are byte-for-byte unchanged.
Output: `backend/tests/markets/test_migration_0011.py` (NEW — first migration test under `tests/markets/`); additions to `backend/tests/markets/test_models.py` (MarketGroup round-trip + `Market.group` lazy="raise" + `group_id IS NULL` regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-multi-outcome-model-catalog-indexes/13-RESEARCH.md
@.planning/phases/13-multi-outcome-model-catalog-indexes/13-PATTERNS.md
@.planning/phases/13-multi-outcome-model-catalog-indexes/13-VALIDATION.md
@.planning/phases/13-multi-outcome-model-catalog-indexes/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_migration_0011.py (apply + reversibility + chain + pg_trgm + 6 indexes)</name>
  <files>backend/tests/markets/test_migration_0011.py</files>
  <read_first>
    - backend/tests/markets/test_migration_0011.py (the NEW file you create — first migration test under tests/markets/)
    - backend/tests/auth/test_migration_0002.py (PATTERNS Analog A — pytestmark integration+asyncio loop_scope="session"; get_columns/get_foreign_keys via inspect+conn.run_sync at lines 12-22, 58-66, 180-194; migration-chain assertion via ScriptDirectory.get_revision at lines 41-50)
    - backend/tests/wallet/test_migration_0003.py (PATTERNS Analog B — raw text() DDL introspection + begin_nested() savepoint + _sqlstate helper at lines 38-40, 173-196)
    - backend/tests/conftest.py (the `engine` AsyncEngine fixture — runs upgrade head fresh per session; `async_session` outer-transaction/savepoint discipline)
    - backend/alembic/versions/0011_phase13_market_groups.py (the migration under test — index names, FK name, down_revision; created in 13-01)
    - 13-RESEARCH.md Pattern 3 (introspection test, GIN/partial caveat A4) + Pattern 4 (reversibility harness A5)
    - 13-PATTERNS.md section "test_migration_0011.py" (full adaptation list of assertions)
  </read_first>
  <action>
    Create `backend/tests/markets/test_migration_0011.py`. Start with `from __future__ import annotations`, then `import pytest`, `from sqlalchemy import inspect, text`, `from sqlalchemy.engine import Inspector`, `from sqlalchemy.ext.asyncio import AsyncEngine`. Set `pytestmark = [pytest.mark.integration, pytest.mark.asyncio(loop_scope="session")]` (clone from `test_migration_0002.py`). The `engine` fixture (from `conftest.py`) is already migrated to head, so the schema under test exists.

    Write these async tests (each takes `engine: AsyncEngine`, opens `async with engine.connect() as conn`, and uses `await conn.run_sync(...)` for `inspect`-based checks or `await conn.execute(text(...))` for raw DDL):
    1. `test_market_groups_table_columns` — `inspect(sync_conn).get_columns("market_groups")`; assert `{"id","title","source","source_event_id","category","slug","created_at","updated_at","tenant_id"}.issubset(col_names)`. (SC#1)
    2. `test_markets_has_group_columns` — `get_columns("markets")`; assert `{"group_id","group_item_title"}.issubset(col_names)`. (SC#1)
    3. `test_markets_group_id_fk_set_null` — `inspect(sync_conn).get_foreign_keys("markets")`; pick the FK whose `constrained_columns` contains `group_id`; assert `referred_table == "market_groups"` and `fk.get("options", {}).get("ondelete") == "SET NULL"`. (SC#1 + the T-13-01 financial-safety mitigation assertion)
    4. `test_pg_trgm_enabled` — `await conn.execute(text("SELECT 1 FROM pg_extension WHERE extname = 'pg_trgm'"))`; assert `scalar_one_or_none() == 1`. (SC#3)
    5. `test_gin_trgm_indexes_have_opclass` — for each of `ix_market_groups_title_trgm` and `ix_markets_question_trgm`, `SELECT indexdef FROM pg_indexes WHERE indexname = :name`; assert `"gin" in ddl.lower()` and `"gin_trgm_ops" in ddl.lower()`. Read raw `pg_indexes.indexdef`, NOT `get_indexes()` (the structured introspection does not reliably surface the opclass — RESEARCH A4). (SC#3)
    6. `test_partial_unique_has_where_clause` — `indexdef` for `ix_market_groups_source_source_event_id`; assert `"unique" in ddl.lower()` and `"where" in ddl.lower()` and `"source_event_id" in ddl.lower()`. (SC#3)
    7. `test_btree_catalog_indexes_exist` — query `pg_indexes` for `ix_market_groups_category`, `ix_markets_category`, `ix_markets_status_volume_24hr`, `ix_odds_snapshots_outcome_id_snapshot_at`; assert each row exists. Also assert the pre-existing `ix_odds_snapshots_outcome_id` is STILL present (the composite is additive — SC#3, regression guard).
    8. `test_chain_down_revision` (mark with `-k chain`-matchable name) — `from alembic.config import Config; from alembic.script import ScriptDirectory`; `cfg = Config("alembic.ini")`; `s = ScriptDirectory.from_config(cfg)`; assert `s.get_revision("0011_phase13_market_groups").down_revision == "0010_phase12_resolution_stakes"` and `len(s.get_heads()) == 1` and `s.get_heads()[0] == "0011_phase13_market_groups"`. (SC#1 chain — verifies the single-head + correct down_revision, the Pitfall 2 guard)
    9. Reversibility (SC#1) — implement per RESEARCH A5: prefer a true `command.downgrade(cfg, "-1")` then `command.upgrade(cfg, "head")` cycle run on an ISOLATED connection/config (NOT the shared session `engine`, which must stay at head — isolate so you don't disturb the session-scoped schema other tests rely on). If that proves flaky under the shared session loop, fall back to a static assertion that the `downgrade()` body is non-empty and drops every object `upgrade()` created (parse the migration module's source, assert each `create_index`/`create_table`/`add_column` has a matching `drop_*`). Pick the harness that stays green deterministically and document the choice in a test docstring.

    Use `begin_nested()` savepoints for any statement expected to raise (Pitfall 4) — though most assertions here are read-only introspection.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/markets/test_migration_0011.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/markets/test_migration_0011.py -x` passes (Docker daemon up for testcontainers PG16)
    - `cd backend && uv run pytest tests/markets/test_migration_0011.py -k chain -x` passes — asserts `down_revision == "0010_phase12_resolution_stakes"` and exactly one head named `0011_phase13_market_groups`
    - `cd backend && uv run pytest tests/markets/test_migration_0011.py -k index -x` passes — covers GIN `gin_trgm_ops`, partial `WHERE source_event_id`, and the 4 B-tree indexes via `pg_indexes.indexdef`
    - GIN opclass + partial-WHERE are verified via raw `pg_indexes.indexdef` (not `get_indexes()`)
    - FK assertion proves `markets.group_id` ondelete is `SET NULL` (T-13-01 mitigation is test-enforced)
    - Reversibility is proven either by a true downgrade->upgrade cycle on an isolated connection OR a static downgrade-mirrors-upgrade assertion (SC#1)
  </acceptance_criteria>
  <done>
    `test_migration_0011.py` exists and passes, automating SC#1 (apply + reversibility + chain/down_revision/single-head) and SC#3 (pg_trgm + all 6 indexes with correct opclass/partial-WHERE + existing single-column odds index retained).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend test_models.py — MarketGroup round-trip + lazy=raise + group_id IS NULL regression</name>
  <files>backend/tests/markets/test_models.py</files>
  <read_first>
    - backend/tests/markets/test_models.py (the file you extend — TestMarketCreation selectinload round-trip lines 106-119; TestLazyRaise lines 192-202; TestCheckConstraints begin_nested lines 137-152; the line-14 import to extend)
    - backend/tests/markets/conftest.py (the `sample_market` fixture + required Market kwargs at lines 61-69: question, slug=generate_slug(...), resolution_criteria, category, source, status, deadline)
    - backend/app/markets/models.py (MarketGroup + Market.group/group_id/group_item_title shipped in 13-01 — the symbols under test)
    - 13-RESEARCH.md Pattern 4 (round-trip + lazy=raise test bodies)
    - 13-PATTERNS.md section "test_models.py" (the three additions: round-trip SC#4, lazy=raise SC#4, group_id IS NULL regression SC#2) + Shared Patterns "Savepoint discipline"
  </read_first>
  <action>
    Extend `backend/tests/markets/test_models.py`. Add `MarketGroup` to the existing `from app.markets.models import ...` line (line 14). Use the existing `async_session` fixture and the `@_async` class decorator / `selectinload` patterns already in the file. Build child markets with the FULL required kwargs from the `conftest.py` sample (`question`, `slug=generate_slug(question)`, `resolution_criteria`, `source=MarketSourceEnum.HOUSE.value`, `status=MarketStatus.OPEN.value`, `deadline=datetime.now(UTC) + timedelta(days=1)`) plus `group_id` and `group_item_title`.

    Add three things:
    1. `MarketGroup` round-trip (SC#4): in a test, create one `MarketGroup(title=..., source="HOUSE", slug=generate_slug(...))`, `async_session.add(grp)`, `await async_session.flush()`. Then create >=2 child `Market`s with `group_id=grp.id` and distinct `group_item_title` (e.g. "Candidate A", "Candidate B"), `add_all`, `flush`. Then `stmt = select(MarketGroup).where(MarketGroup.id == grp.id).options(selectinload(MarketGroup.markets))`; load via `(await async_session.execute(stmt)).scalar_one()`; assert `len(loaded.markets) == 2` and `{m.group_item_title for m in loaded.markets} == {"Candidate A", "Candidate B"}`.
    2. `Market.group` lazy="raise" (SC#4): mirror `TestLazyRaise` — load a `Market` (one of the children, or a fresh standalone) WITHOUT eager-loading `group`, then assert accessing `.group` raises `sqlalchemy.exc.InvalidRequestError` with `match="lazy='raise'"`.
    3. `group_id IS NULL` standalone regression (SC#2): assert the existing `sample_market` fixture (a standalone binary market created without any group) has `group_id is None`, and still round-trips its `outcomes` via `selectinload(Market.outcomes)` exactly as before (2 outcomes, labels {"YES","NO"}) — proving the additive columns did not alter the standalone path.

    Wrap any statement expected to raise an `IntegrityError`/`DBAPIError` in a `begin_nested()` savepoint (Pitfall 4) so one abort does not cascade across the session-scoped suite. Do NOT modify existing tests — only add.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/markets/test_models.py -k "group or lazy" -x && cd backend && uv run pytest tests/markets/test_models.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/markets/test_models.py -k group -x` passes — MarketGroup loads exactly 2 children via `selectinload(MarketGroup.markets)` with the expected `group_item_title` set (SC#4)
    - A test asserts `Market.group` access without eager-load raises `InvalidRequestError` matching `lazy='raise'` (SC#4)
    - A test asserts a standalone `sample_market` has `group_id is None` and still round-trips its 2 outcomes unchanged (SC#2)
    - `cd backend && uv run pytest tests/markets/test_models.py -x` passes in full (existing `TestBinaryOnlyTrigger`/`TestCheckConstraints` still green — EVT-01 additive)
    - No existing test in the file was modified or removed
  </acceptance_criteria>
  <done>
    `test_models.py` carries a `MarketGroup` round-trip (>=2 children, SC#4), a `Market.group` lazy="raise" assertion (SC#4), and a `group_id IS NULL` standalone regression (SC#2); the full markets test module passes; existing binary-only/CHECK tests remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No production code, no endpoint, no input handling. Zero added runtime attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 (verification) | Tampering / DoS (data loss) | `Market.group_id` FK ondelete | mitigate | This plan TEST-ENFORCES the Plan 13-01 mitigation: `test_migration_0011.py::test_markets_group_id_fk_set_null` asserts `options.ondelete == "SET NULL"`, so a regression to CASCADE (which would let a group delete cascade-destroy financial child rows) fails CI. |
| T-13-02 | Tampering | future search-term SQL injection | transfer | Not exercised here (no query written in Phase 13). Forward constraint owned by Phase 16. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package install in this phase. Legitimacy gate N/A. |
</threat_model>

<verification>
Per-task: `cd backend && uv run pytest tests/markets/ -x` + `cd backend && uv run python scripts/lint_money_columns.py`. Per-wave merge: `cd backend && uv run pytest tests/markets/ tests/bets/ tests/settlement/` (SC#2 zero-behavior-change proof — the existing bets/settlement suites re-run green against the additive schema). Phase gate: full `cd backend && uv run pytest` green + money-lint green before `/gsd-verify-work`. Requires a running Docker daemon (testcontainers PG16); if Docker is down, defer per 13-VALIDATION.md and re-run when up.
</verification>

<success_criteria>
- SC#1 (full automated): `test_migration_0011.py` proves apply + reversibility + chain (`down_revision == "0010_phase12_resolution_stakes"`, single head).
- SC#2 (full automated): `group_id IS NULL` standalone market round-trips unchanged + existing `tests/markets/`, `tests/bets/`, `tests/settlement/` suites stay green.
- SC#3 (full automated): pg_trgm + all 6 indexes verified via `pg_indexes.indexdef` (GIN `gin_trgm_ops`, partial `WHERE`, the 4 B-trees, existing single-column odds index retained).
- SC#4 (full automated): `MarketGroup` round-trips a parent with >=2 children via `selectinload`; `Market.group` is `lazy="raise"`.
- EVT-01: full markets module + bets + settlement suites green; money-lint green.
</success_criteria>

<output>
Create `.planning/phases/13-multi-outcome-model-catalog-indexes/13-02-SUMMARY.md` when done.
</output>

---
phase: 03-wallet-double-entry-ledger
plan: 06
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - backend/app/wallet/reconcile.py
  - backend/app/celery_app.py
  - backend/tests/wallet/test_reconcile.py
autonomous: true
requirements: [PLT-09]
must_haves:
  truths:
    - "A nightly Celery task reconcile_wallets computes SUM(entries) per account and compares it to accounts.balance"
    - "When the ledger is clean (zero drift) the task logs at INFO"
    - "When drift is present the task logs at CRITICAL and a Sentry event is captured"
    - "The task is registered on the Celery app and scheduled via RedBeat (nightly 03:00 UTC)"
    - "The task body is sync and wraps the async reconciliation in asyncio.run (Celery 5.5 has no native async)"
  artifacts:
    - path: "backend/app/wallet/reconcile.py"
      provides: "reconcile_wallets Celery task + async reconciliation logic"
      contains: "reconcile_wallets"
    - path: "backend/tests/wallet/test_reconcile.py"
      provides: "SC#7 — clean ledger logs INFO; injected drift logs CRITICAL + Sentry capture"
      contains: "CRITICAL"
  key_links:
    - from: "backend/app/celery_app.py"
      to: "reconcile-wallets-nightly schedule entry"
      via: "beat_schedule.update with crontab(hour=3, minute=0)"
      pattern: "reconcile"
    - from: "backend/app/wallet/reconcile.py"
      to: "SUM(entries) vs accounts.balance"
      via: "per-account aggregate compared to the cached balance"
      pattern: "SUM|sum|func.sum"
    - from: "backend/app/wallet/reconcile.py"
      to: "Sentry"
      via: "sentry_sdk.capture_message/capture on drift"
      pattern: "sentry"
---

<objective>
Ship the nightly reconciliation safety net (SC#7 / PLT-09): a Celery task `reconcile_wallets` that
computes `SUM(credit) - SUM(debit)` over `entries` per account and compares it to the denormalized
`accounts.balance` cache. Clean (zero drift) logs at INFO; any drift logs at CRITICAL via structlog
AND captures a Sentry event. Scheduled via RedBeat (nightly 03:00 UTC — Claude's Discretion) by
appending to the existing empty `celery_app.conf.beat_schedule`.

Per RESEARCH Pattern 6 + Pitfall 5: Celery 5.5 has no native async, so the task body is sync and
wraps the async DB work in `asyncio.run()`; the task module must be importable by the worker so it
registers (mirror the existing `sentry_test_task` on `celery_app`).

Purpose: the balance-cache-vs-ledger drift detector (WAL-06 "balance is a reconciled cache"; PITFALLS
#4 invariant-check job) that proves the double-entry truth holds over time and alerts loudly if it
ever diverges.
Output: `app/wallet/reconcile.py`, the RedBeat schedule entry in `app/celery_app.py`, and the SC#7 test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-wallet-double-entry-ledger/03-CONTEXT.md
@.planning/phases/03-wallet-double-entry-ledger/03-RESEARCH.md
@.planning/phases/03-wallet-double-entry-ledger/03-VALIDATION.md
@.planning/spikes/_lib/harness.py
@.planning/phases/03-wallet-double-entry-ledger/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconcile_wallets Celery task + async reconciliation + RedBeat schedule entry</name>
  <files>backend/app/wallet/reconcile.py, backend/app/celery_app.py</files>
  <read_first>
    - backend/app/celery_app.py (the FILE BEING MODIFIED — beat_schedule = {} line 44 to append to; how sentry_test_task is defined on celery_app lines 133-140; beat_scheduler = redbeat.RedBeatScheduler line 42; task_default_queue 'default' lines 48-50; the worker/beat sentry init signals)
    - .planning/phases/03-wallet-double-entry-ledger/03-RESEARCH.md (Pattern 6 RedBeat schedule + crontab(hour=3,minute=0) + the asyncio.run gotcha lines 339-360; Pitfall 5 task reachability + wrong-loop lines 425-429)
    - .planning/spikes/_lib/harness.py (_measure lines 414-437 — the SUM(CASE WHEN direction='credit' THEN amount ELSE -amount END) per-account aggregate to reuse)
    - backend/app/wallet/models.py + constants.py (Account, Entry — from 03-01)
    - backend/app/db/session.py (the async sessionmaker — open a fresh session inside asyncio.run; the task must NOT share the FastAPI event loop)
    - backend/app/core/sentry.py (how Sentry is initialized per service; the capture API to call on drift)
    - backend/app/core/logging.py (structlog logger usage — get a logger and log INFO/CRITICAL with structured fields)
  </read_first>
  <behavior>
    - reconcile_wallets() (sync task) runs the async reconciliation via asyncio.run and returns a small summary (e.g., {accounts_checked, drift_count}).
    - For each account: ledger_sum = SUM(credit) - SUM(debit) over its entries; drift = balance - ledger_sum.
    - If all drifts are zero: log a single INFO line (e.g., reconcile_clean, accounts_checked=N).
    - If any account drifts: log CRITICAL per drifting account (account_id, balance, ledger_sum, drift) AND call sentry_sdk.capture_message/capture_exception so an alert fires.
    - The task is registered on celery_app (importable name app.wallet.reconcile.reconcile_wallets) and present in beat_schedule under 'reconcile-wallets-nightly' with a crontab(hour=3, minute=0) schedule.
  </behavior>
  <action>
    Create `app/wallet/reconcile.py`:
    - `@celery_app.task(name="app.wallet.reconcile.reconcile_wallets")` decorating a SYNC
      `def reconcile_wallets() -> dict` that calls `asyncio.run(_reconcile_async())` and returns its summary
      (Pattern 6 — Celery 5.5 has no native async; do NOT write `async def` task body).
    - `async def _reconcile_async() -> dict`: open a fresh `AsyncSession` from `_get_session_maker()`; for
      each account, compute `ledger_sum` with `func.coalesce(func.sum(case((Entry.direction == 'credit',
      Entry.amount), else_=-Entry.amount)), 0)` grouped by `account_id` (reuse the harness `_measure` SQL
      shape); join/compare to `Account.balance`. Collect `(account_id, balance, ledger_sum, drift)` where
      `drift != 0`. Log clean at INFO (`reconcile_clean`, accounts_checked) when no drift; for each drifting
      account log `structlog ... .critical("wallet_ledger_drift", account_id=..., balance=..., ledger_sum=...,
      drift=...)` AND `sentry_sdk.capture_message(f"wallet ledger drift on {account_id}", level="error")`
      (or capture_exception of a constructed drift error). Return
      `{"accounts_checked": N, "drift_count": len(drifts)}`. Use `Decimal` throughout; never float.

    Append the RedBeat schedule in `app/celery_app.py` (do NOT overwrite the dict — Phases 2-9 share it):
    `celery_app.conf.beat_schedule.update({"reconcile-wallets-nightly": {"task":
    "app.wallet.reconcile.reconcile_wallets", "schedule": crontab(hour=3, minute=0)}})` with
    `from celery.schedules import crontab`. Ensure the task module is imported by the worker so it
    registers — add an explicit import of `app.wallet.reconcile` where the celery app finalizes tasks
    (mirror however sentry_test_task is reachable), or set `celery_app.autodiscover_tasks(["app.wallet"])`.
    Keep the existing signals/heartbeat untouched.
  </action>
  <verify>
    <automated>uv run --directory backend ruff check app/wallet/reconcile.py app/celery_app.py && uv run --directory backend python -c "from app.celery_app import celery_app; import app.wallet.reconcile as r; assert 'reconcile-wallets-nightly' in celery_app.conf.beat_schedule; assert 'app.wallet.reconcile.reconcile_wallets' in celery_app.tasks; print('task + schedule OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `reconcile_wallets` is a SYNC task (no `async def` body) wrapping `asyncio.run(_reconcile_async())`.
    - `celery_app.conf.beat_schedule` contains `reconcile-wallets-nightly` with a `crontab(hour=3, minute=0)` schedule.
    - The task is registered: `app.wallet.reconcile.reconcile_wallets` is in `celery_app.tasks`.
    - The reconciliation computes per-account `SUM(credit) - SUM(debit)` and compares to `balance` using `Decimal`.
    - ruff clean; the module + celery app import without error.
  </acceptance_criteria>
  <done>The reconcile_wallets task exists, is registered + scheduled nightly via RedBeat, and computes ledger-vs-cache drift in a sync-wrapped async body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#7 reconciliation test — clean logs INFO; injected drift logs CRITICAL + Sentry</name>
  <files>backend/tests/wallet/test_reconcile.py</files>
  <read_first>
    - backend/tests/test_sentry_test_task.py (how the existing Celery task is invoked in a test + how Sentry capture is asserted/mocked)
    - backend/tests/test_sentry_init.py (the Sentry init/capture mocking pattern)
    - backend/tests/wallet/conftest.py (seeded ledger / funded wallet fixtures — from 03-01)
    - backend/tests/conftest.py (engine/async_session; the DATABASE_URL is pointed at the testcontainer)
    - .planning/phases/03-wallet-double-entry-ledger/03-RESEARCH.md (SC#7 test: clean -> INFO; injected drift -> CRITICAL + sentry; line 586; Environment Availability note — the task LOGIC is integration-testable by calling it directly)
    - backend/app/wallet/reconcile.py (the task + _reconcile_async under test — from Task 1)
    - .planning/research/PITFALLS.md (Pitfall 4 invariant-check job line 132 — SUM(ledger) == balance, alert on divergence)
  </read_first>
  <behavior>
    - test_reconcile_clean_logs_info: seed a wallet whose balance equals SUM(entries) (e.g., via an opening transfer); run the reconciliation; assert drift_count == 0 and an INFO-level reconcile_clean log was emitted (capture via caplog/structlog capture); no Sentry capture.
    - test_reconcile_injected_drift_logs_critical: seed a wallet, then directly mutate accounts.balance to a value that diverges from SUM(entries) (a raw UPDATE — accounts.balance is mutable, unlike the immutable ledger); run the reconciliation; assert drift_count >= 1, a CRITICAL wallet_ledger_drift log was emitted with the account_id, AND sentry_sdk.capture_message/capture_exception was called (mock/patch sentry and assert called).
    - test_reconcile_returns_summary: the task returns {accounts_checked, drift_count} matching the seeded state.
  </behavior>
  <action>
    Create `tests/wallet/test_reconcile.py` with `pytestmark = [pytest.mark.integration,
    pytest.mark.asyncio(loop_scope="session")]` for the DB-backed cases. Because the task body uses
    `asyncio.run`, prefer testing `_reconcile_async()` directly with the test session/engine (await it),
    OR call the sync `reconcile_wallets()` where the event loop allows — the cleaner path is to await
    `_reconcile_async()` against the testcontainer DB so it shares the session-scoped loop. Seed a clean
    wallet (balance == SUM(entries)) for the clean case. For the drift case, run a raw
    `UPDATE accounts SET balance = balance + 1 WHERE id = :id` (the ledger entries stay immutable, so the
    cache now diverges) then reconcile.

    Assert logs by capturing structlog output (use the project's structlog test capture, or `caplog` with
    the stdlib bridge) and asserting the event name + level. Patch Sentry with
    `monkeypatch.setattr` on the `sentry_sdk.capture_message` (or capture_exception) used by reconcile.py and
    assert it is called exactly on the drift case and NOT on the clean case.
  </action>
  <verify>
    <automated>uv run --directory backend pytest tests/wallet/test_reconcile.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_reconcile_clean_logs_info GREEN: zero drift, INFO log, NO Sentry capture — SC#7 clean path.
    - test_reconcile_injected_drift_logs_critical GREEN: injected drift -> CRITICAL log with account_id AND Sentry capture called — SC#7 alert path / PLT-09.
    - test_reconcile_returns_summary GREEN: {accounts_checked, drift_count} matches the seeded state.
    - The drift is injected by mutating accounts.balance (mutable cache), proving the ledger entries remain the source of truth.
  </acceptance_criteria>
  <done>SC#7 is proven: clean reconciliation logs INFO; injected drift logs CRITICAL and fires a Sentry alert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger truth vs cached balance | Reconciliation is the detector that the denormalized cache has not silently diverged |
| scheduled task -> observability | A silent (unregistered / wrong-loop) task would hide drift forever |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Tampering / Integrity | accounts.balance silently diverges from the ledger | mitigate | nightly SUM(entries) vs balance; drift -> CRITICAL + Sentry (SC#7 / PLT-09); test injects synthetic drift |
| T-03-22 | Denial of Service / silent failure | task unregistered or runs in the wrong event loop (silent no-op) | mitigate | sync task wraps asyncio.run; module imported so it registers; verify asserts task in celery_app.tasks (Pitfall 5) |
| T-03-23 | Information Disclosure | drift summary leaks balances to logs | accept | structured logs are operator-internal; account_id + amounts are non-PII play-money values; same posture as Phase 1 audit logs |
</threat_model>

<verification>
- `uv run --directory backend pytest tests/wallet/test_reconcile.py -q` green (needs Docker).
- `uv run --directory backend pytest -m "not integration" -q` stays green (celery app imports with the new task + schedule).
- `uv run --directory backend ruff check app/wallet/reconcile.py app/celery_app.py` clean.
- `reconcile-wallets-nightly` present in `beat_schedule`; task in `celery_app.tasks`.
</verification>

<success_criteria>
- Nightly `reconcile_wallets` computes SUM(entries) vs accounts.balance per account — WAL-06 / PLT-09.
- Clean -> INFO; drift -> CRITICAL + Sentry alert — SC#7.
- Sync task wraps async work (asyncio.run); registered + scheduled via RedBeat at 03:00 UTC.
- Synthetic-drift test proves the alert path fires.
</success_criteria>

<output>
Create `.planning/phases/03-wallet-double-entry-ledger/03-06-SUMMARY.md` when done.
</output>

---
phase: 11-hardening-operator-demo-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - .github/workflows/security-scan.yml
  - .zap/rules.tsv
autonomous: false
requirements: [PLT-07]
must_haves:
  truths:
    - "A CI job runs bandit on backend/app and fails only on HIGH severity"
    - "A CI job runs pnpm audit on frontend and fails only on HIGH (--audit-level high)"
    - "A CI job runs OWASP ZAP baseline against the booted API reaching /auth/* and public market routes, gated to high-severity via .zap/rules.tsv"
    - "Every [ASSUMED] tool install is approved at a blocking human-verify checkpoint before first use"
  artifacts:
    - path: ".github/workflows/security-scan.yml"
      provides: "SC#4 bandit + pnpm-audit + pip-audit + ZAP baseline jobs (HIGH-only)"
      contains: "severity-level high"
    - path: ".zap/rules.tsv"
      provides: "ZAP baseline false-positive suppression / HIGH-only gate"
      min_lines: 5
    - path: "backend/pyproject.toml"
      provides: "bandit + pip-audit in the dev dependency group (not runtime)"
      contains: "bandit"
  key_links:
    - from: ".github/workflows/security-scan.yml"
      to: "backend/app"
      via: "uv run bandit -r app/ --severity-level high"
      pattern: "bandit -r app"
    - from: ".github/workflows/security-scan.yml"
      to: ".zap/rules.tsv"
      via: "zaproxy/action-baseline rules_file_name"
      pattern: "rules_file_name"
user_setup: []
---

<objective>
SC#4 — Add a `security-scan` CI workflow that runs four scanners against the staging-style build,
each gated to HIGH severity so the green base is not broken on day one:
  - `bandit` (Python SAST) over `backend/app`
  - `pip-audit` (Python dependency CVE audit) over the backend lockfile
  - `pnpm audit --audit-level high` (frontend dependency CVE audit)
  - `zaproxy/action-baseline` (OWASP ZAP passive DAST) against the booted API, reaching `/auth/*`
    and the public market routes, with `.zap/rules.tsv` suppressing non-actionable passive alerts.

Purpose: This is the "Demo Trap" security-scan gate (PITFALLS.md §"Run security scan: bandit, npm
audit, gitleaks, OWASP ZAP"). `gitleaks` is already green (3-tier, Phase 1) and is NOT re-added.

Output: ONE new workflow + a ZAP rules file + `bandit`/`pip-audit` added to the backend DEV
dependency group (regenerates `uv.lock` dev-only). No runtime business dependency. The three
existing CI workflows are NOT modified (constraint 4).

Tool versions are `[ASSUMED]` (slopcheck was unavailable at research time). Every install sits
behind a BLOCKING `checkpoint:human-verify` so a bad version cannot silently break CI (T-11-02-SC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-operator-demo-gate/11-CONTEXT.md
@.planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy checkpoint for bandit + pip-audit + ZAP action ([ASSUMED] — slopcheck unavailable)</name>
  <read_first>
    - .planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md  (§"Package Legitimacy Audit" — all rows are [ASSUMED] because slopcheck was unavailable; §"Standard Stack" — pinned versions: bandit 1.9.4, pip-audit 2.10.0, zaproxy/action-baseline v0.14.0)
    - backend/pyproject.toml  (CONFIRM the `[dependency-groups]` / `dev` group shape these tools will be added to — NOT the runtime `[project] dependencies`)
  </read_first>
  <action>
    PAUSE for a blocking human gate — do NOT install or reference any tool yet. Present the three
    `[ASSUMED]` items (bandit, pip-audit, zaproxy/action-baseline) and their proposed pins for
    legitimacy confirmation per `<how-to-verify>` below. This gate is NEVER auto-approved (slopcheck
    was unavailable, so registry existence alone does not confer legitimacy). Block plan Task 2 and
    Task 4 until explicit approval + confirmed pins arrive.
  </action>
  <what-built>
    Nothing is installed yet. This checkpoint exists BECAUSE `slopcheck` was unavailable at research
    time, so per the package-legitimacy protocol every tool is treated as `[ASSUMED]` and a human
    must confirm legitimacy + pins before first use. The three tools to confirm:
      - bandit  (PyPI; pin 1.9.4) — github.com/PyCQA/bandit
      - pip-audit  (PyPI; pin 2.10.0) — github.com/pypa/pip-audit
      - zaproxy/action-baseline  (GitHub Action; pin v0.14.0) — github.com/zaproxy/action-baseline
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/bandit/ — confirm it is the PyCQA-maintained package, the
       latest version is consistent with `1.9.4` (or supply the current pin), and downloads are high.
    2. Open https://pypi.org/project/pip-audit/ — confirm it is PyPA-maintained and `2.10.0` (or the
       current pin) is valid.
    3. Open https://github.com/zaproxy/action-baseline/releases — confirm `v0.14.0` (or the current
       tag) exists and the repo is the official OWASP ZAP action.
    4. If any pin is stale, provide the corrected version for the executor to use.
  </how-to-verify>
  <resume-signal>Type "approved" with confirmed pins (e.g. "approved: bandit 1.9.4, pip-audit 2.10.0, action-baseline v0.14.0"), or supply corrected pins / "reject" with reason.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add bandit + pip-audit to the backend dev dependency group</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <read_first>
    - backend/pyproject.toml  (the `[dependency-groups]` table — add to the `dev` group, mirroring how ruff/mypy/pytest dev tools are declared; NEVER add to `[project] dependencies` runtime deps)
    - backend/uv.lock  (regenerated by `uv add --dev`; commit the dev-group delta)
    - .planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md  (§"Standard Stack → Installation" — `uv add --dev bandit pip-audit` ONLY after the checkpoint gate; these are CI/dev tools, not runtime)
  </read_first>
  <action>
    After the Task-1 checkpoint approves the pins, run `uv add --dev bandit pip-audit` from the
    `backend/` directory using the approved versions (e.g. `uv add --dev "bandit==1.9.4" "pip-audit==2.10.0"`
    if exact pins were confirmed; otherwise the latest approved). This adds them to the `dev`
    dependency group in `backend/pyproject.toml` and regenerates `backend/uv.lock` (dev-group entries
    only — NO runtime dependency changes). Verify nothing landed in the runtime `[project]
    dependencies` table. Do NOT touch any other dependency.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/backend" && uv run bandit --version && uv run pip-audit --version</automated>
  </verify>
  <acceptance_criteria>
    - `uv run bandit --version` and `uv run pip-audit --version` both succeed (tools resolve).
    - `grep -A40 'dependency-groups' backend/pyproject.toml` shows `bandit` and `pip-audit` under the
      `dev` group.
    - `bandit` and `pip-audit` do NOT appear in the runtime `[project] dependencies` table
      (grep the `[project]` section — absent).
    - `git diff backend/uv.lock` shows only additive dev-group package entries (no runtime dep churn).
  </acceptance_criteria>
  <done>bandit + pip-audit resolve via uv in the dev group; runtime deps and uv.lock runtime entries are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: .zap/rules.tsv — ZAP baseline HIGH-only gate</name>
  <files>.zap/rules.tsv</files>
  <read_first>
    - .planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md  (§"Pattern 2: ZAP baseline scoped to specific paths"; §"Common Pitfalls → Pitfall 3" — the unauthenticated baseline will see 401/403 on /bets/*; suppress 401-as-finding noise so the gate stays focused on HIGH on the public + auth surface)
    - .gitleaks.toml  (NOTE: `.zap/rules.tsv` is NOT under the docs/ allowlist; it contains only ZAP rule IDs + WARN/IGNORE/FAIL tokens — no secrets — so gitleaks will not flag it)
  </read_first>
  <action>
    Create `.zap/rules.tsv` — the tab-separated ZAP baseline rule-action file. Each row is
    `<ruleId>\t<action>\t<optional comment>` where action is one of `WARN`/`IGNORE`/`FAIL`. The
    intent: keep the gate at HIGH only — downgrade or IGNORE the known passive informational alerts a
    baseline scan always emits on an unauthenticated API so they do not break the build. Include rows
    that IGNORE the common non-actionable baseline alerts (e.g. the informational ZAP rule IDs for
    "X-Content-Type-Options header missing" only if confirmed non-applicable, "Storable/Cacheable
    Content", "Information Disclosure - Suspicious Comments") AND a header comment block explaining
    that authenticated `/bets/*` DAST is deferred to v2 (Pitfall 3 / RESEARCH A4) and that the
    baseline primarily exercises the public market routes + the high-value unauthenticated `/auth/*`
    endpoints. Use REAL ZAP passive rule IDs (the action file format keys on numeric rule IDs); if a
    specific ID is uncertain, prefer leaving it to default WARN rather than inventing an ID, and note
    in the SUMMARY that the rule list is the initial seed to be tuned on the first scan. Use literal
    TAB characters between columns (ZAP requires TABs, not spaces).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f .zap/rules.tsv && grep -cP '\t' .zap/rules.tsv</automated>
  </verify>
  <acceptance_criteria>
    - `.zap/rules.tsv` exists and contains at least one TAB-separated rule row (`grep -cP '\t'` >= 1).
    - The file contains a comment line documenting that authenticated `/bets/*` DAST is a v2 deferral.
    - Every data row's action token is one of `WARN`, `IGNORE`, or `FAIL` (no other tokens).
  </acceptance_criteria>
  <done>.zap/rules.tsv exists, is tab-separated, suppresses non-actionable baseline noise, and documents the v2 authenticated-DAST deferral.</done>
</task>

<task type="auto">
  <name>Task 4: security-scan.yml CI workflow (bandit + pip-audit + pnpm audit + ZAP baseline, HIGH-only)</name>
  <files>.github/workflows/security-scan.yml</files>
  <read_first>
    - .github/workflows/backend-ci.yml  (MIRROR: checkout@v4, setup-python@v5 `3.12`, `pip install uv`, `uv sync --frozen`, `working-directory: backend`. DO NOT MODIFY.)
    - .github/workflows/frontend-ci.yml  (MIRROR for the pnpm job: `pnpm/action-setup@v4` version 9.15.0, `actions/setup-node@v4` node 20 + pnpm cache, `pnpm install --frozen-lockfile`, `working-directory: frontend`. DO NOT MODIFY.)
    - docker-compose.yml  (the ZAP job boots the API the same way as the dry-run: `docker compose up -d --wait`, API at `http://localhost:8000`; needs the same staging-style `SECRET_KEY` / `ADMIN_JWT_PUBLIC_SECRET` placeholders)
    - .planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md  (§"Pattern 1" — bandit `--severity-level high`; §"Pattern 2" — `zaproxy/action-baseline@v0.14.0` with `target: http://localhost:8000`, `rules_file_name: .zap/rules.tsv`, `allow_issue_writing: false`, `fail_action: true`; §"Anti-Patterns" — NEVER fail on MEDIUM/LOW/INFO; §"Pitfall 4" — no `services:` for db/redis)
  </read_first>
  <action>
    Create `.github/workflows/security-scan.yml`. `name: security-scan`; trigger on `pull_request:`
    and `push: branches: [main]`; `permissions: contents: read`. Four independent jobs (so a single
    scanner failure is isolated):
    (A) job `bandit` (ubuntu-latest, timeout 15): checkout@v4 -> setup-python@v5 `3.12` ->
        `pip install uv` -> `working-directory: backend` `uv sync --frozen` ->
        `working-directory: backend` `uv run bandit -r app/ --severity-level high`. The
        `--severity-level high` flag makes bandit FAIL only on HIGH (Anti-Pattern: never on MEDIUM/LOW).
    (B) job `pip-audit` (ubuntu-latest, timeout 15): same python/uv setup ->
        `working-directory: backend` `uv run pip-audit` (audits the backend deps against the advisory
        DB). If pip-audit's default exit-on-any-finding is too noisy, scope it with the documented
        ignore flags so it gates on actionable CVEs; document the exact flag choice in the SUMMARY.
    (C) job `pnpm-audit` (ubuntu-latest, timeout 15): checkout@v4 -> `pnpm/action-setup@v4` version
        9.15.0 -> setup-node@v4 node 20 + pnpm cache (cache-dependency-path frontend/pnpm-lock.yaml)
        -> `working-directory: frontend` `pnpm install --frozen-lockfile` -> `working-directory:
        frontend` `pnpm audit --audit-level high` (FAIL only on HIGH).
    (D) job `zap-baseline` (ubuntu-latest, timeout 20): checkout@v4 -> write the same ephemeral
        staging-style `.env` as plan 11-01 (ENVIRONMENT=staging + placeholder SECRET_KEY +
        ADMIN_JWT_PUBLIC_SECRET + empty SENTRY_DSN; do NOT log it) -> `docker compose up -d --wait`
        -> `zaproxy/action-baseline@<approved pin>` with `target: 'http://localhost:8000'`,
        `rules_file_name: '.zap/rules.tsv'`, `cmd_options: '-a'`, `allow_issue_writing: false`,
        `fail_action: true` -> `if: always()` `docker compose down -v`. Seed the spider toward the
        auth + public market routes via the target; document in the SUMMARY that `/bets/*`
        authenticated traversal is the v2 deferral (Pitfall 3).
    Pin every action by the version confirmed in Task 1 / already used in the existing CI files. Do
    NOT add `services:` for Postgres/Redis. Do NOT modify backend-ci.yml / frontend-ci.yml /
    security.yml (constraint 4).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "import yaml; d=yaml.safe_load(open('.github/workflows/security-scan.yml')); print('jobs:', sorted(d['jobs'].keys()))"</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid YAML; the automated check prints four jobs including `bandit`, `pnpm-audit`,
      and `zap-baseline` (and `pip-audit` if retained).
    - `grep -c 'severity-level high' .github/workflows/security-scan.yml` >= 1 (bandit HIGH gate).
    - `grep -c 'audit-level high' .github/workflows/security-scan.yml` >= 1 (pnpm HIGH gate).
    - `grep -c 'rules_file_name' .github/workflows/security-scan.yml` >= 1 AND the value is
      `.zap/rules.tsv`.
    - The ZAP job action ref matches the pin approved in Task 1 (e.g. `zaproxy/action-baseline@v0.14.0`).
    - The workflow contains NO top-level `services:` block (Pitfall 4).
    - backend-ci.yml / frontend-ci.yml / security.yml are unchanged (`git diff --stat` — constraint 4).
  </acceptance_criteria>
  <done>security-scan.yml is valid YAML, runs all four scanners HIGH-only, references .zap/rules.tsv + the approved ZAP pin, and leaves the three existing CI files untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> booted API (ZAP) | The ZAP baseline crawls the unauthenticated attack surface (`/auth/*`, public market routes) |
| package registry -> backend dev env | Newly installed `[ASSUMED]` tools (bandit, pip-audit) + the ZAP action cross a supply-chain boundary |
| CI runner -> generated `.env` (ZAP job) | Staging-style secrets in the runner filesystem; must not be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-SC | Tampering | bandit / pip-audit / action-baseline installs | mitigate | BLOCKING `checkpoint:human-verify` (Task 1) confirms each `[ASSUMED]` tool on its registry + pins the version before first use; never auto-approved (slopcheck unavailable) |
| T-11-02-01 | Information Disclosure | false-negative scan coverage | accept | Baseline ZAP is passive + unauthenticated; `/bets/*` authenticated DAST is an explicit v2 deferral (RESEARCH A4); the high-value `/auth/*` unauthenticated surface IS covered |
| T-11-02-02 | Information Disclosure | ZAP-job `.env` secrets in CI logs | mitigate | `.env` generated in-job, never echoed; gitignored; throwaway placeholders only |
| T-11-02-03 | Denial of Service | scanners breaking the green base on non-HIGH noise | mitigate | bandit `--severity-level high`, pnpm `--audit-level high`, ZAP `.zap/rules.tsv` IGNORE list — every gate is HIGH-only (RESEARCH Anti-Patterns / Pitfall 2) |
| T-11-02-04 | Tampering | SQL injection / passive web vulns on the API | mitigate | bandit SAST over `backend/app` + ZAP baseline over the booted API surface the standard SQLi / header issues for triage |
</threat_model>

<verification>
- `uv run bandit --version` and `uv run pip-audit --version` resolve (dev group).
- security-scan.yml + prod-migration-dry-run.yml are valid YAML; .zap/rules.tsv is tab-separated.
- `git diff --stat` confirms backend-ci.yml / frontend-ci.yml / security.yml unchanged.
- On the PR run: all four security-scan jobs complete; any HIGH finding (and only HIGH) fails its
  job (observable as the green/red check on the PR — the phase-gate verification).
- The Task-1 checkpoint is recorded as approved with confirmed pins before any install ran.
</verification>

<success_criteria>
SC#4 satisfied: bandit (no HIGH Python findings), pip-audit + pnpm audit (no HIGH dependency CVEs),
and OWASP ZAP baseline against `/auth/*` + public market routes (no HIGH findings) run in CI, all
HIGH-gated, with every `[ASSUMED]` tool install approved at a blocking human checkpoint — and the
three existing CI workflows untouched. `gitleaks` (already green) is unchanged.
</success_criteria>

<output>
Create `.planning/phases/11-hardening-operator-demo-gate/11-02-SUMMARY.md` when done.
</output>

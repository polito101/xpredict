---
phase: 11-hardening-operator-demo-gate
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/app/wallet/page.tsx
  - frontend/src/app/portfolio/page.tsx
  - frontend/src/app/markets/[slug]/page.tsx
  - frontend/src/app/(auth)/layout.tsx
  - frontend/src/components/market-list.tsx
  - frontend/src/components/market-card.tsx
  - frontend/src/components/order-entry-form.tsx
autonomous: false
requirements: [PLT-07]
must_haves:
  truths:
    - "The player home, market detail, bet flow, portfolio, wallet history, and auth pages render with no horizontal scroll from 360px to 768px"
    - "All interactive controls are thumb-reachable and text is readable at 360px"
    - "Fixes are Tailwind class / container-width changes only — no component restructure, no prop change, no data-flow change"
  artifacts:
    - path: "frontend/src/app/wallet/page.tsx"
      provides: "responsive wallet history (no overflow at 360px)"
      contains: "sm:"
    - path: "frontend/src/app/portfolio/page.tsx"
      provides: "responsive portfolio cards (no overflow at 360px)"
      contains: "sm:"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/components/market-list.tsx"
      via: "responsive market grid"
      pattern: "grid"
user_setup: []
---

<objective>
SC#1 (PLT-07) — Responsive QA pass on the player-facing UI from 360px to 768px: home, market detail,
bet flow, portfolio, wallet history, and auth. Audit each page at the target widths and apply
CSS/layout-only fixes so there is no horizontal scroll and every control is thumb-reachable and
readable. Admin UI is desktop-only-acceptable (out of scope per PLT-07).

Purpose: PLT-07 is the only formal requirement of Phase 11 ("Player-facing UI is fully responsive on
mobile browsers (>=360px width)"). Tailwind v4 is already mobile-first and the pages already use the
right primitives (`max-w-6xl mx-auto px-4 sm:px-6`, responsive grids), so this is an AUDIT-AND-PATCH
pass, not a rebuild. CONSTRAINT 1 + RESEARCH Pitfall 6: fixes are Tailwind class changes
(`flex-col`->`sm:flex-row`, add `sm:` prefixes, `overflow-x-auto` wrappers, `min-w-0`, `truncate`)
and container-width tweaks ONLY — NO prop changes, NO new components, NO data-flow changes (a
"responsive" diff that adds/removes props or splits components has crossed the refactor line).

Output: Tailwind-class edits to the player pages + the shared market/order components. No backend
change, no new file, no logic change. Visual correctness is verified by a human at the target widths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-operator-demo-gate/11-CONTEXT.md
@.planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md
@frontend/src/app/page.tsx
@frontend/src/app/wallet/page.tsx
@frontend/src/app/portfolio/page.tsx
@frontend/src/app/markets/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive audit + CSS/layout-only fixes across the six player surfaces</name>
  <files>frontend/src/app/page.tsx, frontend/src/app/wallet/page.tsx, frontend/src/app/portfolio/page.tsx, frontend/src/app/markets/[slug]/page.tsx, frontend/src/app/(auth)/layout.tsx, frontend/src/components/market-list.tsx, frontend/src/components/market-card.tsx, frontend/src/components/order-entry-form.tsx</files>
  <read_first>
    - frontend/src/app/page.tsx  (home — already `w-full max-w-6xl mx-auto px-4 sm:px-6 py-12`; check the market grid in market-list.tsx for column behavior at 360px)
    - frontend/src/app/wallet/page.tsx  (wallet — `max-w-2xl ... px-6`; the transaction history `<li className="flex items-center justify-between py-3">` rows with amount strings can overflow at 360px; `px-6` lacks the `sm:` mobile reduction; the balance `CardTitle` + currency may wrap awkwardly — candidate fixes: `px-4 sm:px-6`, `min-w-0` + `truncate` on the kind/reason column, `gap` tightening, `flex-wrap` on the amount row)
    - frontend/src/app/portfolio/page.tsx  (portfolio — `max-w-2xl ... px-6`; Card rows with `CardContent className="flex items-center justify-between"` and stake/odds in `CardDescription` can overflow; candidate fixes mirror wallet: `px-4 sm:px-6`, `flex-wrap`/`min-w-0`, allow the P&L to wrap under the label on narrow widths)
    - frontend/src/app/markets/[slug]/page.tsx  (market detail — `grid grid-cols-1 lg:grid-cols-3` already stacks below lg; the sticky order panel `lg:sticky lg:top-8` is fine; CHECK the header `flex flex-wrap items-center gap-3` (question + source badge) and the price-history chart container fit at 360px without horizontal scroll; the chart parent must keep `min-w-0` so Recharts ResponsiveContainer does not force overflow)
    - frontend/src/app/(auth)/layout.tsx  (auth shell — confirm the form container is width-capped + centered with mobile padding; auth pages are simple forms, usually already fine — verify no fixed-width input rows overflow)
    - frontend/src/components/market-list.tsx + market-card.tsx  (the home grid + card — confirm the grid is `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` style mobile-first and cards use `min-w-0`/`truncate` for long questions; long market questions are the most likely 360px overflow source)
    - frontend/src/components/order-entry-form.tsx  (the bet flow — the stake input + outcome select + confirm button; confirm inputs are full-width on mobile and the confirm dialog content fits 360px)
    - .planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md  (section "Pitfall 6" — fixes are Tailwind-class only, no prop/component changes; section "State of the Art" — Tailwind v4 default breakpoints sm=640 md=768 lg=1024, mobile-first; no tailwind.config exists, do not create one)
  </read_first>
  <action>
    Audit each of the six player surfaces at 360 / 390 / 414 / 768px (Chrome DevTools device mode is
    the dev tool; the human round-trip in Task 2 is the authoritative visual check). For each
    overflow / cramped-control / unreadable-text issue found, apply a TAILWIND-CLASS or
    CONTAINER-WIDTH fix ONLY:
      - Replace bare `px-6` page padding with `px-4 sm:px-6` on wallet + portfolio (match the home
        page convention) so 360px gets tighter gutters.
      - Add `min-w-0` + `truncate` (or `break-words`) to flex children holding long strings (market
        question, transaction reason, currency-amount rows) so they shrink instead of forcing
        horizontal scroll.
      - Convert tight horizontal `justify-between` rows that overflow into `flex-wrap` (or
        `flex-col sm:flex-row`) on narrow widths where the amount/P&L should drop below its label.
      - Ensure the home market grid is mobile-first single-column then `sm:`/`lg:` multi-column
        (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` shape) — adjust only if it is not already.
      - Ensure the market-detail price-history chart parent keeps `min-w-0` so Recharts
        ResponsiveContainer cannot push the grid past the viewport.
      - Ensure order-entry inputs/buttons are full-width on mobile (`w-full`) and the confirm dialog
        content fits 360px (Radix dialog is responsive by default; add `max-w-[calc(100vw-2rem)]` only
        if it overflows).
    Touch ONLY the files in `files_modified`. Do NOT change any prop signature, add/remove a
    component, alter a fetch, or edit `frontend/src/app/layout.tsx` (the root layout footer is owned by
    plan 11-04 — do not touch it here; that is the exclusive-ownership boundary that keeps these two
    Wave-1 plans conflict-free). If a surface is already correct at all widths, leave it unchanged and
    record "no fix needed — verified at 360/768" in the SUMMARY.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/frontend" && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` and `pnpm build` both exit 0 (the `next build` app-graph typecheck; the
      pre-existing orphan `middleware.test.ts` repo-wide failure DEF-FE-01 is out of scope per STATE.md).
    - Every diff hunk is a Tailwind `className` change or a container-width change — `git diff` shows
      NO changed prop signatures, NO added/removed component imports, NO changed fetch/`await` calls
      (the reviewer confirms the diff is presentation-only; this is the CONSTRAINT-1 / Pitfall-6 gate).
    - `frontend/src/app/layout.tsx` is NOT in the diff (exclusive-ownership boundary with plan 11-04).
    - The wallet + portfolio pages now use `px-4 sm:px-6` (or an equivalent mobile-first gutter) —
      grep shows `sm:` present in both.
    - Files outside `files_modified` are unchanged (`git status`).
  </acceptance_criteria>
  <done>The six player surfaces are presentation-only-patched for 360-768px; build + typecheck pass; the diff is provably CSS/layout-only and does not touch the root layout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual responsive QA at 360-768px (the authoritative check)</name>
  <read_first>
    - .planning/phases/11-hardening-operator-demo-gate/11-VALIDATION.md  (section "Manual-Only Verifications" — responsive QA is a real-device/viewport visual check at the listed widths; "no horizontal scroll, thumb-reachable")
    - the diff from Task 1  (the surfaces and the specific fixes applied)
  </read_first>
  <action>
    PAUSE for a blocking human gate. Present the Task-1 diff + the list of surfaces and ask the human
    to run the visual QA per `<how-to-verify>` at 360 / 390 / 414 / 768px. Block phase closure until
    all six surfaces pass with no horizontal scroll and thumb-reachable controls, OR the human reports
    a surface+width+issue for a follow-up CSS-only fix (re-run Task 1 for that fix, then re-present).
  </action>
  <what-built>
    Task 1 applied Tailwind-class/container-width fixes to the six player surfaces (home, market
    detail, bet flow, portfolio, wallet history, auth). Responsive correctness is inherently visual
    and cannot be unit-asserted (RESEARCH / VALIDATION: manual-verify). This checkpoint is the
    authoritative QA pass.
  </what-built>
  <how-to-verify>
    Run the frontend (dev server or the built app) and, in browser device mode (and ideally one real
    mobile browser — iOS Safari or Android Chrome), check each surface at 360, 390, 414, and 768px:
      1. Home (/) — market list: no horizontal scroll; long market questions wrap/truncate; cards
         stack to one column at 360px.
      2. Market detail (/markets/[slug]) — question header, resolution criteria, price-history chart,
         and order panel all fit; chart does not force horizontal scroll; order panel stacks below the
         content at narrow widths.
      3. Bet flow (order-entry form + confirm dialog) — inputs full-width, confirm dialog fits 360px,
         buttons thumb-reachable.
      4. Portfolio (/portfolio) — open + settled cards: stake/odds/P&L readable, no overflow.
      5. Wallet history (/wallet) — balance + transaction rows: amounts readable, no overflow.
      6. Auth (/login, /register, /forgot-password, /reset-password, /verify-email) — forms centered,
         inputs full-width, no overflow.
    Confirm NO horizontal scroll on any surface at any tested width and all controls are reachable.
  </how-to-verify>
  <resume-signal>Type "approved" if all six surfaces pass 360-768px, or list the surface + width + issue for a follow-up CSS-only fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | SC#1 is CSS/layout-only on already-shipped pages; it introduces no new data path, endpoint, or input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | a "responsive" change accidentally un-hiding or reordering data | mitigate | Fixes are Tailwind-class only; the reviewer confirms no prop/data-flow change (CONSTRAINT 1 / Pitfall 6), so no previously-gated content becomes exposed |
| T-11-05-02 | Tampering | scope creep into a component refactor | mitigate | Acceptance criteria forbid prop/component/fetch changes; the diff must be presentation-only; the root layout is explicitly off-limits (owned by 11-04) |
| T-11-05-SC | Tampering | npm/pip installs | accept | This plan adds NO package installs (Tailwind classes only; no new dep, no `tailwind.config`); nothing to gate |
</threat_model>

<verification>
- `pnpm typecheck` + `pnpm build` exit 0; the diff is provably CSS/layout-only (reviewer-confirmed).
- `frontend/src/app/layout.tsx` is not in the diff (boundary with 11-04).
- Phase-gate: the Task-2 visual-QA checkpoint passes — no horizontal scroll and thumb-reachable
  controls on all six player surfaces at 360 / 390 / 414 / 768px (human).
</verification>

<success_criteria>
SC#1 / PLT-07 satisfied: the player home, market detail, bet flow, portfolio, wallet history, and
auth surfaces render with no horizontal scroll and thumb-reachable, readable controls from 360px to
768px — achieved with CSS/layout-only changes, verified visually by a human, with no feature change.
</success_criteria>

<output>
Create `.planning/phases/11-hardening-operator-demo-gate/11-05-SUMMARY.md` when done.
</output>

---
phase: 11-hardening-operator-demo-gate
plan: 06
type: execute
wave: 2
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - docs/LOOKS-DONE-CHECKLIST.md
autonomous: false
requirements: [PLT-07]
must_haves:
  truths:
    - "Every PITFALLS.md 'Looks Done But Isn't' item is recorded as VERIFIED (with evidence) or DEFERRED (with reason + owner)"
    - "Wallet/ledger/concurrency items are documented as verify-only against existing tests and Pol's separate track — never re-implemented"
    - "Items closed by Phase 11 workstreams (dry-run, security scan, Sentry alerts) cite those deliverables as evidence"
  artifacts:
    - path: "docs/LOOKS-DONE-CHECKLIST.md"
      provides: "SC#2 — executed audit table, one row per checklist item, each VERIFIED or DEFERRED"
      contains: "VERIFY"
      min_lines: 50
  key_links:
    - from: "docs/LOOKS-DONE-CHECKLIST.md"
      to: ".github/workflows/prod-migration-dry-run.yml"
      via: "demo-trap / dry-run evidence citation"
      pattern: "prod-migration-dry-run"
    - from: "docs/LOOKS-DONE-CHECKLIST.md"
      to: ".github/workflows/security-scan.yml"
      via: "secrets/security-scan evidence citation"
      pattern: "security-scan"
user_setup: []
---

<objective>
SC#2 — Execute the PITFALLS.md "Looks Done But Isn't" checklist as a DOCUMENTED audit: every item is
ticked with evidence or recorded as an approved deferral with a reason and owner.

Purpose: PITFALLS.md §"How to verify you haven't fallen into the trap" requires running the entire
"Looks Done But Isn't" checklist as a gate before any operator demo. Per CONSTRAINT 3, the
wallet/ledger/concurrency items are VERIFY-ONLY (documented against existing tests + Pol's separate
backend-test-isolation track) — they are NEVER re-implemented here. This is the synthesis artifact of
Phase 11: it cites the dry-run (SC#3 / plan 11-01), security scan (SC#4 / plan 11-02), and Sentry
alert runbook (SC#5 / plan 11-03) as evidence, which is why this plan runs in Wave 2 after those
deliverables exist.

Output: ONE markdown audit table under `docs/`. No code change, no test change, no re-implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-operator-demo-gate/11-CONTEXT.md
@.planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md
@.planning/phases/11-hardening-operator-demo-gate/11-01-PLAN.md
@.planning/phases/11-hardening-operator-demo-gate/11-02-PLAN.md
@.planning/phases/11-hardening-operator-demo-gate/11-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/LOOKS-DONE-CHECKLIST.md (executed audit, one row per item)</name>
  <files>docs/LOOKS-DONE-CHECKLIST.md</files>
  <read_first>
    - .planning/research/PITFALLS.md  (the "Looks Done But Isn't" checklist, lines 443-479 — ~33 items across Wallet/Ledger, Bet placement, Settlement, Polymarket, Auth, Audit log, Admin, Self-bet ban, CORS, Secrets, Tenant scoping, Branding, Observability, Backups, Monitoring, Decimal precision, Timezones, Regulatory, Demo-trap. EVERY item becomes one audit row.)
    - .planning/phases/11-hardening-operator-demo-gate/11-CONTEXT.md  (CONSTRAINT 3 — wallet/ledger/concurrency items are VERIFY-ONLY, coordinated with Pol's separate track; NEVER re-implement; the residual `test_50_concurrent_overdraft` / DEF-03-01 is Pol's, out of scope)
    - .planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md  (section "Code Examples → SC#2 checklist execution artifact" — the Markdown table shape; section "Security Domain" — the ASVS/STRIDE status column references; the existing-invariant facts: CORS explicit-origin in main.py, audit-log immutability trigger, NUMERIC(18,4) money, gitleaks 3-tier green, money-as-string serialization, TIMESTAMPTZ)
    - .planning/STATE.md  (the Decisions log — concrete evidence for many rows: WAL-08 CHECK(balance>=0) DB-verified (03-01), 50-concurrent-overdraft drift-0 gate (03-02), idempotent settlement (Phase 5), closed-vs-resolved guard (Phase 6 SC#7), reconcile_wallets drift->CRITICAL+Sentry (03-06), CSV-injection escaping (08-02), audit-log read-only + trigger (08-02), branding-from-data (Phase 10), money-as-string SP-1 throughout)
    - .planning/phases/11-hardening-operator-demo-gate/11-01-PLAN.md, 11-02-PLAN.md, 11-03-PLAN.md  (the Wave-1 deliverables to cite: dry-run workflow + guard, security-scan workflow, Sentry alert runbook)
  </read_first>
  <action>
    Create `docs/LOOKS-DONE-CHECKLIST.md` (English) — the executed audit. Header: purpose (the SC#2
    gate), date, and a legend for the Result column: `VERIFIED` (evidence cited), `DEFERRED`
    (reason + owner), `VERIFY-ONLY (Pol track)` (constraint-3 items confirmed by existing tests, not
    re-implemented). Then a table with one ROW PER PITFALLS checklist item, columns:
    `# | Item | Class | Result | Evidence`.
      - Class is one of: `VERIFY (do here)`, `VERIFY-ONLY (Pol track)`, `CLOSED BY PHASE 11`,
        `DEFERRED`.
      - Evidence is a concrete file/test/commit/workstream reference, e.g.:
          - Wallet SUM(ledger)==balance -> `reconcile_wallets` clean on seed (`backend/app/wallet/reconcile.py`, plan 03-06); VERIFY-ONLY (Pol track).
          - CHECK(balance>=0) is a DB constraint -> migration 0003 / STATE 03-01; VERIFY-ONLY (Pol track).
          - Money columns NUMERIC -> WAL-05 money-lint CI step (backend-ci.yml); VERIFIED.
          - Concurrent bet no-negative-balance -> 50-concurrent overdraft drift-0 gate (plan 03-02);
            VERIFY-ONLY (Pol track) — NOTE the residual `test_50_concurrent_overdraft` isolation debt is
            Pol's DEF-03-01, out of scope.
          - Settlement idempotent / reversal -> Phase 5 SC#6 + reversal path; VERIFY-ONLY.
          - Polymarket schema-validate + settle-on-resolved-not-closed -> Phase 6 SC#6/SC#7 guard; VERIFIED.
          - Auth: reset invalidates sessions / logout revokes / rate-limit / email-enumeration ->
            Phase 2 SC + ZAP `/auth/*` (plan 11-02); VERIFIED.
          - Audit log append-only -> Phase 1 trigger + REVOKE, viewer read-only (08-02); VERIFIED.
          - Self-bet ban -> Phase 5 self-bet firewall; VERIFY.
          - CORS not `*`/no dev origin -> `main.py` explicit-origin allow_origins; VERIFIED.
          - Secrets / gitleaks + `.env.local` gitignored -> gitleaks 3-tier green + new `security-scan.yml`
            (plan 11-02); CLOSED BY PHASE 11.
          - tenant_id on every table -> PLT-01 ghost column; VERIFIED.
          - Branding from data not hardcoded -> Phase 10 runtime theming; VERIFIED.
          - Observability: Sentry backend+frontend + alert on >X/min -> 4 alert rules runbook
            (plan 11-03); CLOSED BY PHASE 11 (round-trip = Pol manual-verify per the runbook).
          - Decimal serialized as string -> money-as-string SP-1 throughout; VERIFIED.
          - Timezones TIMESTAMPTZ -> schema; VERIFIED.
          - Demo-trap prod-migration dry-run -> `prod-migration-dry-run.yml` + `check_no_dev_config.sh`
            (plan 11-01); CLOSED BY PHASE 11.
          - Postgres metrics / slow-query log -> DEFERRED (owner: infra; reason: not in v1 operator-demo
            scope — observability is Sentry-based; document as a known gap).
          - Backups: backup -> drop -> restore -> verify, PITR -> DEFERRED (owner: Pol/infra; reason:
            no managed-DB backup harness in this repo; staging/Railway backup test is an external op —
            see the Task-2 checkpoint).
          - Regulatory: ToS forbids transfer/redemption/monetary-value + legal review + geo-block ->
            scaffold shipped (plan 11-04 docs/regulatory.md + ToS placeholder); legal review DEFERRED
            (owner: external Spanish counsel — gating dependency per STATE.md).
    EVERY item must be either VERIFIED/CLOSED with evidence or DEFERRED with reason + owner — no blank
    rows (CONSTRAINT: "every box ticked or a documented and approved deferral"). Do NOT re-implement or
    re-test any wallet/ledger/concurrency item — cite the existing evidence (CONSTRAINT 3). Do NOT
    modify any source/test file (this is a doc-only audit).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f docs/LOOKS-DONE-CHECKLIST.md && grep -Ec 'VERIFIED|VERIFY-ONLY|DEFERRED|CLOSED BY PHASE 11' docs/LOOKS-DONE-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/LOOKS-DONE-CHECKLIST.md` exists and is >= 50 lines.
    - It has one audit row per PITFALLS "Looks Done But Isn't" item (>= 28 rows; count the
      Result-token occurrences via the grep >= 28).
    - It cites `prod-migration-dry-run` AND `security-scan` AND the Sentry alert runbook as evidence
      (the two `key_links` patterns plus a Sentry reference resolve via grep).
    - Every wallet/ledger/concurrency row is marked `VERIFY-ONLY (Pol track)` and references existing
      tests/migrations — NONE is re-implemented (grep confirms no such row says "fixed"/"re-implemented").
    - Every row has a non-empty Result and Evidence cell (no blank rows; reviewer spot-checks).
    - `git diff --stat` shows ONLY `docs/LOOKS-DONE-CHECKLIST.md` changed (no source/test edits).
  </acceptance_criteria>
  <done>The audit table records every checklist item as VERIFIED/CLOSED (with evidence) or DEFERRED (with reason + owner), wallet/ledger/concurrency are verify-only against Pol's track, and no source/test was touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sign off the audit — confirm deferrals + the genuinely-manual items (backup restore, Sentry round-trip)</name>
  <read_first>
    - docs/LOOKS-DONE-CHECKLIST.md  (the executed audit from Task 1 — the deferrals and their owners)
    - docs/runbooks/sentry-alerts.md  (the SC#5 runbook — the Sentry round-trip sign-off this checklist's "Observability" row depends on)
    - .planning/STATE.md  (Blockers — the counsel-review + Pol's backend-test-isolation track context)
  </read_first>
  <action>
    PAUSE for a blocking human gate. Present the executed audit and ask Pol to confirm every item is
    VERIFIED/CLOSED with evidence or DEFERRED with an acceptable reason + owner, that the
    wallet/ledger/concurrency rows are VERIFY-ONLY against the existing tests + Pol's separate track
    (constraint 3 — not re-implemented), and to acknowledge the genuinely-external deferrals
    (backup-restore/PITR, the Sentry round-trip, the counsel review). Block phase closure until Pol
    accepts the audit as the SC#2 gate or flags an item that must be closed before the demo.
  </action>
  <what-built>
    Task 1 produced the executed "Looks Done But Isn't" audit. A few items are genuinely external and
    cannot be machine-closed: (a) the backup -> drop -> restore -> verify + PITR test (no in-repo
    backup harness; a staging/managed-DB operation), and (b) the Sentry alert round-trip (covered by
    plan 11-03's runbook, needs the real DSN). This checkpoint asks the human (Pol) to confirm each
    deferral is acceptable for the operator-demo gate and that the cross-referenced manual-verify
    items (Sentry round-trip, counsel review) are tracked.
  </what-built>
  <how-to-verify>
    1. Read `docs/LOOKS-DONE-CHECKLIST.md`. Confirm every item is VERIFIED/CLOSED (with evidence) or
       DEFERRED with an acceptable reason + owner.
    2. Confirm the wallet/ledger/concurrency rows are VERIFY-ONLY against existing tests + your
       separate backend-test-isolation track (constraint 3) — not re-implemented here.
    3. Acknowledge the genuinely-external deferrals for the demo gate: the backup-restore/PITR test
       (infra op) and the Sentry alert round-trip (tracked in `docs/runbooks/sentry-alerts.md`,
       plan 11-03) and the Spanish-counsel ToS review (plan 11-04).
    4. Approve the audit as the SC#2 gate, or flag any deferral that must be closed before the demo.
  </how-to-verify>
  <resume-signal>Type "approved — audit accepted, deferrals acknowledged", or list any item that must be closed before the operator demo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit doc -> demo go/no-go | A falsely-green audit row would let an unverified invariant ship to an operator demo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Repudiation | a checklist item marked VERIFIED without real evidence | mitigate | Every row requires a concrete file/test/commit/workstream citation; the Task-2 human checkpoint signs off; blank rows are forbidden by the acceptance criteria |
| T-11-06-02 | Tampering | re-implementing a wallet/ledger item (violating constraint 3 + risking the green base) | mitigate | Wallet/ledger/concurrency rows are VERIFY-ONLY, citing existing tests/migrations + Pol's track; acceptance criteria forbid "fixed"/"re-implemented" on those rows; `git diff` must show docs-only |
| T-11-06-03 | Information Disclosure | a deferred gap (backups/metrics) silently forgotten | mitigate | Deferrals carry an explicit reason + owner and are surfaced at the Task-2 checkpoint for Pol's go/no-go decision |
| T-11-06-SC | Tampering | npm/pip installs | accept | This plan adds NO package installs (a single markdown audit); nothing to gate |
</threat_model>

<verification>
- `docs/LOOKS-DONE-CHECKLIST.md` exists (>=50 lines), one row per checklist item, each VERIFIED/CLOSED
  (with evidence) or DEFERRED (reason + owner), citing the dry-run + security-scan + Sentry runbook.
- `git diff --stat` confirms docs-only (no source/test re-implementation).
- Phase-gate: the Task-2 checkpoint records Pol's sign-off on the audit + its deferrals.
</verification>

<success_criteria>
SC#2 satisfied: the "Looks Done But Isn't" checklist is executed in full — every box ticked with
evidence or recorded as an approved deferral with reason + owner; wallet/ledger/concurrency items are
verify-only against existing tests and Pol's separate track (never re-implemented); and the Phase 11
workstreams (dry-run, security scan, Sentry alerts, regulatory scaffold) are cited as the closing
evidence.
</success_criteria>

<output>
Create `.planning/phases/11-hardening-operator-demo-gate/11-06-SUMMARY.md` when done.
</output>

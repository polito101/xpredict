---
phase: 06-polymarket-sync-catalog-replication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/integrations/polymarket/__init__.py
  - backend/app/integrations/polymarket/client.py
  - backend/app/integrations/polymarket/schemas.py
  - backend/app/integrations/polymarket/adapter.py
  - backend/app/markets/models.py
  - backend/app/markets/enums.py
  - backend/app/core/config.py
  - backend/alembic/versions/0004_phase6_polymarket_sync.py
  - backend/tests/polymarket/__init__.py
  - backend/tests/polymarket/conftest.py
  - backend/tests/polymarket/test_schemas.py
  - backend/tests/polymarket/test_client.py
  - backend/tests/polymarket/test_adapter.py
  - backend/tests/fixtures/gamma/active_market.json
  - backend/tests/fixtures/gamma/closed_not_resolved.json
  - backend/tests/fixtures/gamma/disputed_market.json
  - backend/tests/fixtures/gamma/resolved_market.json
autonomous: true
requirements:
  - MKT-05
  - MKT-06

must_haves:
  truths:
    - "PolymarketAdapter passes MarketSource Protocol isinstance check"
    - "PolymarketAdapter is registered in REGISTRY under POLYMARKET key"
    - "GammaMarket parser decodes stringified JSON fields (outcomes, outcomePrices, clobTokenIds)"
    - "GammaMarket parser converts volume/liquidity strings to Decimal (never float)"
    - "A market with closed=true and umaResolutionStatus=proposed does NOT map to RESOLVED"
    - "A market with closed=true and umaResolutionStatus=resolved and clear winner maps to RESOLVED"
    - "GammaClient retries on httpx.NetworkError and httpx.TimeoutException with exponential backoff"
    - "Migration 0004 adds volume and volume_24hr columns to markets table"
    - "Unique partial index on (source, source_market_id) WHERE source_market_id IS NOT NULL exists"
  artifacts:
    - path: "backend/app/integrations/polymarket/__init__.py"
      provides: "Module init with PolymarketAdapter registration"
      contains: "register_source"
    - path: "backend/app/integrations/polymarket/client.py"
      provides: "GammaClient with httpx + tenacity retry"
      contains: "GammaClient"
    - path: "backend/app/integrations/polymarket/schemas.py"
      provides: "GammaMarket Pydantic v2 parser with state machine"
      contains: "_derive_status"
    - path: "backend/app/integrations/polymarket/adapter.py"
      provides: "PolymarketAdapter implementing MarketSource Protocol"
      contains: "PolymarketAdapter"
    - path: "backend/alembic/versions/0004_phase6_polymarket_sync.py"
      provides: "Migration adding volume columns and unique index"
      contains: "ix_markets_source_source_market_id"
  key_links:
    - from: "backend/app/integrations/polymarket/adapter.py"
      to: "backend/app/integrations/market_source.py"
      via: "implements MarketSource Protocol"
      pattern: "MarketSource"
    - from: "backend/app/integrations/polymarket/__init__.py"
      to: "backend/app/integrations/market_source.py"
      via: "register_source(MarketSourceEnum.POLYMARKET, ...)"
      pattern: "register_source"
    - from: "backend/app/integrations/polymarket/schemas.py"
      to: "backend/app/markets/enums.py"
      via: "_derive_status returns MarketStatus values"
      pattern: "MarketStatus"
---

<objective>
Build the PolymarketAdapter integration layer: Gamma API client with retry, Pydantic v2 parser
with spike-002-validated state machine, and migration adding volume columns to the markets table.
Prove Protocol conformance and parser correctness with comprehensive tests using VCR fixtures.

This plan establishes the data foundation that Plan 02 (Celery tasks) and Plan 03 (frontend) consume.
No Celery tasks, no API changes, no frontend work -- pure integration plumbing + proof of correctness.

Covers MKT-05 (partial: client and adapter infrastructure), MKT-06 (partial: schema for snapshot support).
Per D-01 through D-04, the adapter stores source_market_id and condition_id for reverse lookup and
persists markets in DB even after they fall out of the top-25 (per D-03, D-04).

Purpose: Isolate the highest-risk integration work (Gamma API parsing, closed-vs-resolved state machine)
in a plan with zero external dependencies, enabling parallel execution of subsequent plans.

Output: 4 new production modules, 1 migration, 16+ VCR fixture tests, Protocol conformance verified.
</objective>

<execution_context>
@C:\Users\pobom\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pobom\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polymarket-sync-catalog-replication/06-CONTEXT.md
@.planning/phases/06-polymarket-sync-catalog-replication/06-RESEARCH.md
@.planning/phases/06-polymarket-sync-catalog-replication/06-PATTERNS.md
@.claude/skills/spike-findings-xpredict/references/polymarket-integration.md
@backend/app/integrations/market_source.py
@backend/app/markets/models.py
@backend/app/markets/enums.py
@backend/app/core/config.py
@backend/app/db/types.py
@backend/tests/conftest.py
@backend/tests/markets/conftest.py
@backend/tests/markets/test_protocol.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration, Settings, ORM columns, VCR fixtures, and GammaMarket parser</name>
  <files>
    backend/alembic/versions/0004_phase6_polymarket_sync.py,
    backend/app/core/config.py,
    backend/app/markets/models.py,
    backend/app/integrations/polymarket/__init__.py,
    backend/app/integrations/polymarket/schemas.py,
    backend/tests/fixtures/gamma/active_market.json,
    backend/tests/fixtures/gamma/closed_not_resolved.json,
    backend/tests/fixtures/gamma/disputed_market.json,
    backend/tests/fixtures/gamma/resolved_market.json,
    backend/tests/polymarket/__init__.py,
    backend/tests/polymarket/conftest.py,
    backend/tests/polymarket/test_schemas.py
  </files>
  <read_first>
    backend/app/core/config.py,
    backend/app/markets/models.py,
    backend/app/markets/enums.py,
    backend/app/db/types.py,
    backend/alembic/versions/0003_phase4_markets.py,
    .claude/skills/spike-findings-xpredict/references/polymarket-integration.md,
    .claude/skills/spike-findings-xpredict/sources/002-polymarket-gamma-parser/fixtures/active_market.json,
    .claude/skills/spike-findings-xpredict/sources/002-polymarket-gamma-parser/fixtures/closed_not_resolved.json,
    .claude/skills/spike-findings-xpredict/sources/002-polymarket-gamma-parser/fixtures/disputed_market.json,
    .claude/skills/spike-findings-xpredict/sources/002-polymarket-gamma-parser/fixtures/resolved_market.json,
    backend/tests/conftest.py,
    backend/tests/markets/conftest.py
  </read_first>
  <action>
    1. **Migration 0004_phase6_polymarket_sync.py**: Create Alembic migration with
       revision="0004_phase6_polymarket_sync", down_revision="0003_phase4_markets".
       Add two columns to the "markets" table: `volume Numeric(18, 4) NOT NULL DEFAULT 0`
       and `volume_24hr Numeric(18, 4) NOT NULL DEFAULT 0` using op.add_column with
       server_default=sa.text("0"). Create partial unique index
       `ix_markets_source_source_market_id` on columns ["source", "source_market_id"]
       with `postgresql_where=sa.text("source_market_id IS NOT NULL")`.
       Follow header pattern from 0003 migration: `from __future__ import annotations`,
       TENANT_DEFAULT constant, import sa + op.

    2. **Settings**: Append a Phase 6 block to the Settings class in config.py after the
       Phase 2 block. Add four fields:
       - GAMMA_API_BASE_URL: str = "https://gamma-api.polymarket.com"
       - POLYMARKET_POLL_INTERVAL_SECONDS: int = 30
       - POLYMARKET_SNAPSHOT_INTERVAL_SECONDS: int = 300
       - POLYMARKET_LOCK_TTL_SECONDS: int = 25

    3. **ORM columns**: Add to the Market class in models.py two new columns after
       `condition_id`: `volume: Mapped[Money]` and `volume_24hr: Mapped[Money]`, both
       using the Money import from app.db.types (which is Annotated[Decimal,
       mapped_column(Numeric(18, 4), nullable=False)]). Add server_default="0" and
       default=Decimal("0") on both columns.

    4. **VCR fixtures**: Copy the 4 JSON fixture files from
       .claude/skills/spike-findings-xpredict/sources/002-polymarket-gamma-parser/fixtures/
       into backend/tests/fixtures/gamma/. Create backend/tests/fixtures/gamma/ directory.

    5. **GammaMarket parser (schemas.py)**: Create
       backend/app/integrations/polymarket/schemas.py. Port the spike-002 GammaMarket
       Pydantic v2 model. Key elements:
       - model_config toggled by ENVIRONMENT env var: extra="forbid" when is_dev,
         extra="allow" otherwise (read from get_settings().is_dev).
         Use a module-level function _gamma_model_config() -> ConfigDict that checks
         get_settings().is_dev. Call it at class definition time.
       - Fields: id (str), question (str), slug (str, default=""),
         condition_id (str, Field alias="conditionId", default=""),
         outcomes_raw (list[str], Field alias="outcomes", default_factory=list),
         outcome_prices_raw (list[str], Field alias="outcomePrices", default_factory=list),
         clob_token_ids (list[str], Field alias="clobTokenIds", default_factory=list),
         volume_str (str, Field alias="volume", default="0"),
         liquidity_str (str, Field alias="liquidity", default="0"),
         volume_24hr (float | None, Field alias="volume24hr", default=None),
         closed (bool, default=False),
         uma_resolution_status (str | None, Field alias="umaResolutionStatus", default=None),
         end_date_raw (str | None, Field alias="endDate", default=None),
         description (str, default="").
       - field_validator("outcomes_raw", "outcome_prices_raw", "clob_token_ids",
         mode="before") using the spike-002 parse_stringified_json_list pattern.
       - _safe_decimal(value) module-level helper: returns Decimal("0") for None,
         wraps Decimal(str(value)) with InvalidOperation fallback to Decimal("0").
       - Computed properties: volume -> _safe_decimal(self.volume_str),
         liquidity -> _safe_decimal(self.liquidity_str),
         volume_24hr_decimal -> _safe_decimal(self.volume_24hr) if not None else Decimal("0").
       - model_validator(mode="after") that calls _derive_status and stores result
         in self.internal_status.
       - _derive_status function: port from spike-002 but return MarketStatus values
         (OPEN, CLOSED, RESOLVED) instead of InternalMarketStatus. Import MarketStatus
         from app.markets.enums. The function takes closed: bool, uma_status: str | None,
         outcome_prices: list[str] and returns MarketStatus. Also store the raw
         uma_resolution_status for Phase 7 to use.

    6. **Module init**: Create backend/app/integrations/polymarket/__init__.py as an
       empty file (registration happens after adapter is created in Task 2).

    7. **Test infrastructure**: Create backend/tests/polymarket/__init__.py (empty).
       Create backend/tests/polymarket/conftest.py with:
       - A load_gamma_fixture(name: str) -> dict helper that reads JSON from
         backend/tests/fixtures/gamma/{name}.json.
       - Fixtures: gamma_active, gamma_closed_not_resolved, gamma_disputed, gamma_resolved
         that call load_gamma_fixture with the appropriate filename.

    8. **test_schemas.py**: Create backend/tests/polymarket/test_schemas.py with:
       - TestGammaMarketParser class with tests for all 4 VCR fixtures.
       - test_active_market: parse gamma_active, assert internal_status == MarketStatus.OPEN,
         assert volume is Decimal, assert len(outcomes_raw) == 2.
       - test_closed_not_resolved: parse gamma_closed_not_resolved, assert
         internal_status == MarketStatus.CLOSED (NOT RESOLVED). This is the CRITICAL
         closed-vs-resolved safety test.
       - test_disputed_market: parse gamma_disputed, assert internal_status == MarketStatus.OPEN.
       - test_resolved_market: parse gamma_resolved, assert internal_status == MarketStatus.RESOLVED.
       - test_stringified_json_parsing: manually construct input with stringified JSON
         string for outcomes field, assert it parses to list.
       - test_decimal_volume_not_float: parse active fixture, assert isinstance(volume, Decimal).
       - test_missing_uma_status: construct input without umaResolutionStatus key,
         assert uma_resolution_status is None and internal_status is OPEN.
       - Use pytestmark = [pytest.mark.unit].
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/polymarket/test_schemas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/alembic/versions/0004_phase6_polymarket_sync.py exists with revision="0004_phase6_polymarket_sync" and down_revision="0003_phase4_markets"
    - `grep -c "volume" backend/alembic/versions/0004_phase6_polymarket_sync.py` returns >= 2
    - `grep -c "ix_markets_source_source_market_id" backend/alembic/versions/0004_phase6_polymarket_sync.py` returns >= 1
    - backend/app/core/config.py contains GAMMA_API_BASE_URL, POLYMARKET_POLL_INTERVAL_SECONDS, POLYMARKET_SNAPSHOT_INTERVAL_SECONDS, POLYMARKET_LOCK_TTL_SECONDS
    - backend/app/markets/models.py Market class has volume: Mapped[Money] and volume_24hr: Mapped[Money]
    - 4 JSON fixtures exist in backend/tests/fixtures/gamma/
    - backend/app/integrations/polymarket/schemas.py contains GammaMarket class with _derive_status function
    - `cd backend && uv run pytest tests/polymarket/test_schemas.py -x -q` passes all 7+ tests
    - test_closed_not_resolved asserts MarketStatus.CLOSED (not RESOLVED) for closed=true + uma=proposed
    - test_resolved_market asserts MarketStatus.RESOLVED for closed=true + uma=resolved + clear winner
  </acceptance_criteria>
  <done>
    Migration, Settings, ORM columns, GammaMarket parser, and parser tests all pass.
    The closed-vs-resolved state machine is proven correct via VCR fixtures.
    Decimal handling verified for volume fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: GammaClient, PolymarketAdapter, Protocol registration, and integration tests</name>
  <files>
    backend/app/integrations/polymarket/client.py,
    backend/app/integrations/polymarket/adapter.py,
    backend/app/integrations/polymarket/__init__.py,
    backend/tests/polymarket/test_client.py,
    backend/tests/polymarket/test_adapter.py
  </files>
  <read_first>
    backend/app/integrations/market_source.py,
    backend/app/integrations/polymarket/schemas.py,
    backend/app/markets/models.py,
    backend/app/markets/enums.py,
    backend/app/markets/service.py,
    backend/app/db/session.py,
    backend/app/core/config.py,
    backend/tests/markets/test_protocol.py,
    backend/tests/markets/conftest.py,
    backend/tests/polymarket/conftest.py,
    .planning/phases/06-polymarket-sync-catalog-replication/06-RESEARCH.md
  </read_first>
  <action>
    1. **GammaClient (client.py)**: Create backend/app/integrations/polymarket/client.py.
       - Class GammaClient with lazy httpx.AsyncClient singleton pattern (mirror
         app/db/session.py lazy factory).
       - Constructor stores no state except _client: httpx.AsyncClient | None = None.
       - _get_client() method: creates httpx.AsyncClient if None or is_closed.
         base_url from get_settings().GAMMA_API_BASE_URL. timeout=httpx.Timeout(15.0,
         connect=5.0). limits=httpx.Limits(max_connections=10, max_keepalive_connections=5).
       - fetch_top_markets(limit: int = 25) -> list[dict]: decorated with
         @retry(retry=retry_if_exception_type((httpx.NetworkError, httpx.TimeoutException)),
         stop=stop_after_attempt(3), wait=wait_exponential_jitter(initial=1, max=10, jitter=2),
         reraise=True). Calls GET /markets with params active=true, closed=false,
         order=volume24hr, limit=str(limit). Calls resp.raise_for_status(). Returns resp.json().
         Logs structlog event on success with market_count.
       - fetch_market_by_id(market_id: str) -> dict | None: same retry decorator.
         Calls GET /markets/{market_id}. Returns resp.json() on 200, None on 404.
         Reraises on other errors.
       - close() method: aclose() the httpx client if open.
       - Module-level structlog logger: log = structlog.get_logger().
       - Import pattern: from __future__ import annotations at top.

    2. **PolymarketAdapter (adapter.py)**: Create backend/app/integrations/polymarket/adapter.py.
       - Class PolymarketAdapter implementing all three MarketSource Protocol methods.
       - fetch_active_markets(session: AsyncSession, *, limit: int = 25) -> list[Market]:
         Query markets WHERE source == POLYMARKET AND status == OPEN, selectinload outcomes,
         order_by volume_24hr desc, limit(limit).
       - fetch_market(session: AsyncSession, market_id: UUID) -> Market | None:
         Query by id with selectinload outcomes + odds_snapshots. Identical pattern to
         HouseAdapter.fetch_market.
       - detect_resolution(session: AsyncSession, market_id: UUID) -> ResolutionResult | None:
         Return None in Phase 6 (Phase 7 implements real resolution detection).
       - sync_top25(session: AsyncSession, raw_markets: list[dict]) -> int:
         Iterate raw_markets, parse each with GammaMarket.model_validate(raw).
         For each parsed market, build values dict with source=POLYMARKET,
         source_market_id=parsed.id, condition_id=parsed.condition_id,
         question=parsed.question, slug=generate_slug(parsed.question),
         status=parsed.internal_status.value, volume=parsed.volume,
         volume_24hr=parsed.volume_24hr_decimal,
         deadline=parsed end_date or fallback to 30 days from now,
         resolution_criteria=parsed.description or "Resolution via Polymarket UMA oracle",
         plus outcomes data.
         Use PostgreSQL INSERT ... ON CONFLICT (source, source_market_id) DO UPDATE
         (sqlalchemy.dialects.postgresql.insert). On conflict update: question, status,
         volume, volume_24hr, updated_at. Do NOT overwrite slug, created_at, bet_count.
         For outcomes: upsert YES and NO outcomes with current_odds from outcome_prices.
         Log structlog event market.synced per market.
         Return count of markets synced.
       - Import generate_slug from app.markets.models.

    3. **Module init (__init__.py)**: Update the empty __init__.py to import
       PolymarketAdapter and call register_source(MarketSourceEnum.POLYMARKET,
       PolymarketAdapter()). Follow the exact pattern from market_source.py line 86.

    4. **test_client.py**: Create backend/tests/polymarket/test_client.py.
       - Use pytest + unittest.mock to mock httpx.AsyncClient.
       - test_single_batch_call: Mock the client, call fetch_top_markets(25), assert
         exactly 1 GET request was made (not 25 per-market calls). This proves MKT-05
         rate-limit compliance.
       - test_retry_on_network_error: Mock client to raise httpx.NetworkError on first
         call, succeed on second. Assert fetch_top_markets returns data (retry worked).
       - test_retry_on_timeout: Mock client to raise httpx.TimeoutException on first call,
         succeed on second. Assert success.
       - test_gives_up_after_3_attempts: Mock to always raise NetworkError. Assert
         httpx.NetworkError is raised after 3 attempts (tenacity reraise).
       - Use pytestmark = [pytest.mark.unit].

    5. **test_adapter.py**: Create backend/tests/polymarket/test_adapter.py.
       - test_protocol_conformance: assert isinstance(PolymarketAdapter(), MarketSource).
       - test_registry_lookup: import get_adapter, call get_adapter(MarketSourceEnum.POLYMARKET),
         assert isinstance result is PolymarketAdapter.
       - test_upsert_idempotent (integration, requires async_session): Call sync_top25
         with 2 sample raw market dicts twice. Assert Market.query count is exactly 2
         (no duplicates).
       - test_fetch_active_markets (integration): Insert a POLYMARKET market via
         sync_top25, then call fetch_active_markets. Assert result contains the market.
       - test_detect_resolution_returns_none: Call detect_resolution, assert None.
       - Integration tests use pytestmark = [pytest.mark.integration, pytest.mark.asyncio(loop_scope="session")].
       - Unit tests use pytestmark = [pytest.mark.unit].
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/polymarket/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/integrations/polymarket/client.py contains GammaClient class with @retry decorator
    - backend/app/integrations/polymarket/adapter.py contains PolymarketAdapter with fetch_active_markets, fetch_market, detect_resolution, sync_top25
    - backend/app/integrations/polymarket/__init__.py contains register_source(MarketSourceEnum.POLYMARKET, ...)
    - `cd backend && uv run python -c "from app.integrations.polymarket import PolymarketAdapter; from app.integrations.market_source import MarketSource; assert isinstance(PolymarketAdapter(), MarketSource)"` exits 0
    - test_protocol_conformance passes (isinstance check)
    - test_registry_lookup passes (get_adapter returns PolymarketAdapter)
    - test_single_batch_call passes (exactly 1 HTTP request, not 25)
    - test_upsert_idempotent passes (double sync = zero duplicates)
    - `cd backend && uv run pytest tests/polymarket/ -x -q` passes all tests
  </acceptance_criteria>
  <done>
    GammaClient fetches with retry, PolymarketAdapter implements MarketSource Protocol,
    adapter is registered in REGISTRY, upserts are idempotent. All parser + client +
    adapter tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gamma API -> Backend | Untrusted external JSON crosses into Pydantic parser |
| Redis -> Backend | Lock state read/written by Celery tasks (Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | GammaMarket parser | mitigate | Pydantic strict field validation; extra='forbid' in dev catches injected fields; extra='allow' + structured warning log in staging/prod; all Decimal from strings, never float |
| T-06-02 | Denial of Service | GammaClient | mitigate | tenacity stops after 3 attempts; httpx.Timeout(15s connect=5s); max_connections=10 limit prevents connection exhaustion |
| T-06-03 | Information Disclosure | GammaClient logs | mitigate | structlog with scrub_secrets processor (Phase 1); no API keys (Gamma is public); response body not logged raw |
| T-06-04 | Tampering | Market status state machine | mitigate | _derive_status truth table from spike-002 with 4 VCR fixture tests; closed=true alone NEVER maps to RESOLVED |
| T-06-SC | Tampering | pip/npm installs | mitigate | No new PyPI packages (all already in pyproject.toml); frontend shadcn components added in Plan 03 via official registry |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/polymarket/ -x -q` passes all tests
- `cd backend && uv run alembic heads` shows single head at 0004
- `cd backend && uv run python -c "from app.integrations.polymarket import PolymarketAdapter"` imports cleanly
- `cd backend && uv run ruff check app/integrations/polymarket/` passes
- `cd backend && uv run mypy app/integrations/polymarket/ --ignore-missing-imports` passes
</verification>

<success_criteria>
- PolymarketAdapter passes MarketSource Protocol conformance (isinstance check green)
- GammaMarket parser handles all 4 VCR fixture types correctly
- closed=true + umaResolutionStatus=proposed maps to CLOSED (not RESOLVED)
- GammaClient retries on network/timeout errors, stops after 3 attempts
- Migration 0004 adds volume, volume_24hr columns and partial unique index
- Upsert on (source, source_market_id) is idempotent (double sync = zero duplicates)
- All tests in tests/polymarket/ pass
</success_criteria>

<output>
Create `.planning/phases/06-polymarket-sync-catalog-replication/06-01-SUMMARY.md` when done
</output>

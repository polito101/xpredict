---
phase: 10-admin-kpi-dashboard-configurable-branding
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - backend/app/admin/kpi_service.py
  - backend/app/admin/kpi_schemas.py
  - backend/app/admin/kpi_router.py
  - backend/app/main.py
  - backend/tests/admin/_helpers.py
  - backend/tests/admin/test_kpi.py
autonomous: true
requirements: [ADD-02, ADD-03]
must_haves:
  truths:
    - "GET /api/v1/admin/dashboard/kpis (admin-gated) returns all five cards: 24h volume, DAU, active markets, pending resolutions, house P&L today + cumulative."
    - "House P&L is net (settle_loss credit to house_revenue) minus (settle_winnings debit from house_promo) with reverse_* netted; a reversal returns P&L to its pre-settlement value."
    - "DAU is the distinct UNION of bettors (bets.created_at) and player logins (audit_log auth.session_started); admin logins are excluded; the count dedups a user who both bet and logged in."
    - "Pending resolutions counts markets with deadline < now and status NOT IN (RESOLVED, CANCELLED, DRAFT); active markets counts status = OPEN."
    - "The DAU window query param (24h|7d|30d, default 24h) changes the DAU and chart window; an out-of-allowlist value 422s before the service runs."
    - "Money fields (volume_24h, house_pnl_today, house_pnl_cumulative, bucket volume) serialize as strings, never JSON floats; a negative P&L renders as a negative string."
  artifacts:
    - path: "backend/app/admin/kpi_service.py"
      provides: "read-only KPI aggregates over bets/markets/entries+transfers/audit_log"
      contains: "house_pnl"
    - path: "backend/app/admin/kpi_schemas.py"
      provides: "KpiResponse + VolumeBucket with MoneyStr money fields"
      contains: "MoneyStr"
    - path: "backend/app/admin/kpi_router.py"
      provides: "GET /api/v1/admin/dashboard/kpis?window= gated by current_active_admin"
      exports: ["kpi_router"]
  key_links:
    - from: "backend/app/admin/kpi_service.py"
      to: "settlement transfer-kind constants"
      via: "TRANSFER_SETTLE_LOSS / TRANSFER_SETTLE_WINNINGS / reverse_* (no hardcoded strings)"
      pattern: "TRANSFER_SETTLE"
    - from: "backend/app/admin/kpi_service.py"
      to: "auth.session_started audit event"
      via: "DAU login proxy filtered on the real emitted event name"
      pattern: "auth.session_started"
    - from: "backend/app/main.py"
      to: "kpi_router"
      via: "app.include_router"
      pattern: "include_router"
---

<objective>
Slice C (backend) — KPI aggregation service + admin dashboard endpoint. Ships one read-only KpiService computing all five cards (24h bet volume, DAU, active markets, pending resolutions, house P&L today + cumulative) + the 30-day daily volume chart buckets, plus the admin-gated GET /api/v1/admin/dashboard/kpis?window= endpoint and a 30-day synthetic bet seed helper for chart verification. Implements D-01, D-02, D-03, D-04, D-05, D-06; delivers ADD-02 + ADD-03 (backend).

CRITICAL — the two ROADMAP SC#2 formulas are WRONG against the real schema and MUST NOT be copied verbatim (RESEARCH §Flagged Unknowns 1 & 2): there is no house_expense account (P&L is net settle_loss − settle_winnings by transfer kind), and the login event is auth.session_started (NOT auth.login_*) while bets emit no audit event (so DAU MUST UNION bets + logins).

This plan shares backend/app/main.py with Plan 10-01 (both add include_router calls), so it is sequenced into Wave 2 after 10-01 to keep exclusive file ownership; the KPI logic itself is independent of branding.

Purpose: the operator 5-second health pulse. Plan 10-04 (KPI dashboard page) consumes this endpoint.
Output: app/admin/kpi_{service,schemas,router}.py, main.py wiring, the 30-day seed helper, and a Wave-0 integration test asserting the P&L net (incl. reversal), the DAU UNION, the pending predicate, active markets, 24h volume, and money-as-string on the wire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-admin-kpi-dashboard-configurable-branding/10-CONTEXT.md
@.planning/phases/10-admin-kpi-dashboard-configurable-branding/10-RESEARCH.md
@.planning/phases/10-admin-kpi-dashboard-configurable-branding/10-PATTERNS.md
@backend/app/settlement/constants.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — 30-day synthetic seed helper + failing KPI integration test</name>
  <read_first>
    - backend/tests/admin/_helpers.py (ENTIRE FILE) — `seed_user`, `auth`, `client`, `ADMIN_PASSWORD`, `cleanup_user`, and the existing raw-SQL committed-seed style; extend it with a bet/settlement seeder.
    - backend/app/settlement/service.py (the resolve_market signature + the reverse path) and backend/app/settlement/constants.py (the exact transfer kinds) — to drive a real settlement in the P&L test seam rather than hand-posting ledger rows.
    - backend/app/bets/models.py (stake, user_id, created_at) and backend/app/markets/models.py + enums.py (status, deadline, source) — the columns the seeders write.
    - backend/app/core/audit/service.py + backend/app/auth/router.py around the auth.session_started emission — to seed a login audit row for the DAU test.
    - 10-RESEARCH.md §Validation Architecture (Test Seams: House P&L, DAU, Pending resolutions) + §Other Code-Grounded KPI Findings (the exact predicates) + the Code Examples (House P&L + DAU ORM).
  </read_first>
  <behavior>
    - House P&L: seed bets on a house market, resolve via SettlementService.resolve_market, assert the KPI house_pnl_cumulative equals (sum of loser stakes swept to house_revenue) minus (sum of winner net winnings paid from house_promo). Then reverse the settlement and assert house_pnl_cumulative returns to the pre-settlement value (reverse_* netting). A more-losers market yields positive P&L; an all-winners market yields negative P&L.
    - DAU: seed (a) a user who only placed a bet, (b) a user who only has an auth.session_started audit row, (c) a user who did both → assert DAU = 3 (UNION dedups). Seed an admin auth.admin_login_started row → assert it is NOT counted. Assert window=24h vs 7d vs 30d changes the count when activity is spread across days.
    - Pending resolutions: seed markets across the (deadline past/future) × (status) matrix → assert only past-deadline markets with status NOT IN (RESOLVED, CANCELLED, DRAFT) are counted; a DRAFT past-deadline market is excluded.
    - Active markets: assert COUNT(status == OPEN). 24h volume: assert SUM(bets.stake) within the last 24h, COALESCE 0 when none. Money fields assert as strings on the raw HTTP JSON body.
    - window query param: GET with window=bogus → 422 (FastAPI Literal/pattern) before the service runs.
  </behavior>
  <action>
    Extend backend/tests/admin/_helpers.py with a synthetic-data seeder: a function that inserts N bets across a configurable created_at span (the 30-day fixture — used by both the chart bucket assertion and the volume/DAU windows), a helper to seed a house market with a given status + deadline, and a helper to insert an auth.session_started (and auth.admin_login_started) audit row for a given user id via AuditService.record or a direct committed INSERT mirroring the existing seed style. Create backend/tests/admin/test_kpi.py with `pytestmark = [pytest.mark.integration, pytest.mark.asyncio(loop_scope="session")]` and the test functions named per the RESEARCH test map (test_house_pnl, test_dau, test_pending_resolutions, plus active-markets + 24h-volume + money-as-string + window-422 cases). Use begin_nested() savepoints around any statement expected to raise (the project savepoint discipline). Tests MUST fail RED (no endpoint yet). Do NOT implement the service/router in this task.
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/admin/test_kpi.py -x</automated>
  </verify>
  <acceptance_criteria>
    - test_kpi.py collects cleanly and the seed helpers import without error.
    - All KPI tests FAIL RED (404 / endpoint-missing), not on a seed/import error.
  </acceptance_criteria>
  <done>The 30-day synthetic seeder + KPI integration tests exist, collect, and fail RED against the not-yet-built endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: KpiService (5 aggregates + 30d buckets) + KPI schemas → GREEN</name>
  <read_first>
    - backend/app/wallet/service.py `get_transactions` lines ~436-488 — the entries→transfers join (`select(...).select_from(Entry).join(Transfer, Entry.transfer_id == Transfer.id)`) to reuse VERBATIM for the House P&L sum, plus the `select(func.count()).select_from(...)` shape.
    - backend/app/core/audit/router.py lines ~59-71 — the count-over-subquery shape for the DAU UNION.
    - backend/app/settlement/constants.py — TRANSFER_SETTLE_LOSS, TRANSFER_SETTLE_WINNINGS, TRANSFER_REVERSE_LOSS, TRANSFER_REVERSE_WINNINGS (import these — do NOT hardcode strings).
    - backend/app/wallet/constants.py — HOUSE_REVENUE_ACCOUNT_ID, HOUSE_PROMO_ACCOUNT_ID (for the index-friendly account-constrained arm).
    - backend/app/admin/schemas.py — the `from app.wallet.schemas import MoneyStr` import (money-as-string, CI money-lint enforced).
    - backend/app/markets/schemas.py PriceHistoryResponse/PricePoint lines ~152-178 — the chart-bucket list shape.
    - 10-RESEARCH.md §Flagged Unknown 1 (the LOCKED House P&L SQL/ORM, Strategy B kind-filtered net flow) + §Flagged Unknown 2 (the LOCKED DAU UNION ORM) + the Code Examples block.
  </read_first>
  <action>
    Create backend/app/admin/kpi_schemas.py: `from app.wallet.schemas import MoneyStr`; VolumeBucket(BaseModel) with day: datetime + volume: MoneyStr; KpiResponse(BaseModel) with volume_24h: MoneyStr, daily_active_users: int, active_markets: int, pending_resolutions: int, house_pnl_today: MoneyStr, house_pnl_cumulative: MoneyStr, volume_buckets: list[VolumeBucket] (<=30 daily points).

    Create backend/app/admin/kpi_service.py (from __future__ is fine — service, not router). Implement read-only async functions on an AsyncSession:
    - house_pnl(session, lo=None, hi=None) → Decimal: build `revenue = coalesce(sum(case(...)))` over (TRANSFER_SETTLE_LOSS → Entry.amount, TRANSFER_REVERSE_LOSS → -Entry.amount, else 0) and `expense = coalesce(sum(case(...)))` over (TRANSFER_SETTLE_WINNINGS → Entry.amount, TRANSFER_REVERSE_WINNINGS → -Entry.amount, else 0); select (revenue - expense) from Entry join Transfer on Entry.transfer_id == Transfer.id where Transfer.kind.in_ the four settlement/reversal kinds; apply Entry.created_at >= lo (omit for cumulative) and < hi. The account-constrained arm (e.account_id == HOUSE_REVENUE_ACCOUNT_ID for revenue, == HOUSE_PROMO_ACCOUNT_ID for expense) is the recommended index-friendly form. Return Decimal.
    - dau(session, window_hours) → int: lo = now(UTC) - timedelta(hours=window_hours); UNION select(Bet.user_id).where(Bet.created_at >= lo) with select(cast(func.split_part(AuditLog.actor, ":", 2), PG_UUID)).where(AuditLog.event_type == "auth.session_started", AuditLog.occurred_at >= lo, AuditLog.actor.like("user:%")); count over the union subquery. EXCLUDE admin logins (filter event_type == auth.session_started only, A2).
    - active_markets(session) → int: COUNT(Market WHERE status == MarketStatus.OPEN).
    - pending_resolutions(session) → int: COUNT(Market WHERE deadline < now() AND status NOT IN (RESOLVED, CANCELLED, DRAFT)) — single deadline column for both sources (A3 excludes DRAFT).
    - volume_24h(session) → Decimal: COALESCE(SUM(Bet.stake), 0) WHERE Bet.created_at >= now()-24h (use bets.stake, NOT markets.volume).
    - daily_volume_buckets(session, window_hours) → list[(datetime, Decimal)]: `date_trunc('day', Bet.created_at)` GROUP BY day ORDER BY day over the window (30d for the chart).
    - A top-level get_kpis(session, window) that runs all of the above. "Today" boundary uses UTC `date_trunc('day', now())` for lo and now() for hi (A1, documented in a comment).
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe scripts/lint_money_columns.py; .venv/Scripts/python.exe -c "import app.admin.kpi_service, app.admin.kpi_schemas"</automated>
  </verify>
  <acceptance_criteria>
    - kpi_service.py imports the four TRANSFER_* settlement kinds and the two HOUSE_*_ACCOUNT_ID constants — `grep -E "TRANSFER_SETTLE|TRANSFER_REVERSE|HOUSE_(REVENUE|PROMO)_ACCOUNT_ID" app/admin/kpi_service.py` returns matches; there is NO literal `house_expense` anywhere in the file.
    - The DAU query filters on the literal `auth.session_started` and NOT on `auth.login_started` — `grep "auth.session_started" app/admin/kpi_service.py` matches; `grep "auth.login_started" app/admin/kpi_service.py` returns nothing.
    - kpi_schemas + kpi_service import cleanly; money-lint exits 0; KpiResponse money fields are MoneyStr.
  </acceptance_criteria>
  <done>KpiService computes all five aggregates + the 30-day buckets using the corrected (kind-filtered) P&L and the (bets UNION auth.session_started) DAU; schemas serialize money as strings; module imports clean; money-lint passes.</done>
</task>

<task type="auto">
  <name>Task 3: Admin KPI router (window param, admin-gated) + main.py wiring → all KPI tests GREEN</name>
  <read_first>
    - backend/app/core/audit/router.py lines ~20-22 (NO from __future__) + the admin-gated GET with a `Query(...)` param (`pattern=` / Literal) shape.
    - backend/app/admin/router.py — the admin prefix + Depends(current_active_admin) on every endpoint.
    - backend/app/main.py lines ~180-197 — the deferred-import block + include_router ordering (NOTE: Plan 10-01 added branding routers here in Wave 1; append the KPI import/include without disturbing those lines).
    - 10-PATTERNS.md section backend/app/admin/kpi_router.py (the GET /api/v1/admin/dashboard/kpis?window= shape) + section main.py.
  </read_first>
  <action>
    Create backend/app/admin/kpi_router.py (NO from __future__ import annotations). Declare kpi_router = APIRouter(prefix="/api/v1/admin/dashboard", tags=["admin-dashboard"]).
    - GET /kpis → admin: Annotated[User, Depends(current_active_admin)] + session: Annotated[AsyncSession, Depends(get_async_session)] + window: a typed query param constrained to Literal["24h","7d","30d"] defaulting to "24h" (an out-of-allowlist value 422s before the service runs). Translate the window to window_hours (24 / 168 / 720), call kpi_service.get_kpis, and return KpiResponse. Log the total query time at INFO via structlog (D-01 observability — makes "measurably slow" observable for the caching-revisit decision).
    Modify backend/app/main.py: add the kpi_router import to the deferred-import block and app.include_router(kpi_router) in the admin group (Plan 10-01 already added the branding routers in Wave 1 — append, do not rewrite the existing branding include lines).
  </action>
  <verify>
    <automated>cd backend; .venv/Scripts/python.exe -m pytest tests/admin/test_kpi.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All test_kpi.py tests pass GREEN (P&L net incl. reversal, DAU UNION with admin excluded, pending predicate incl. DRAFT exclusion, active markets, 24h volume, money-as-string on the wire, window=bogus → 422).
    - app/admin/kpi_router.py does NOT contain `from __future__ import annotations` (`grep -L` returns the filename).
    - The window param is a Literal/pattern-constrained query param defaulting to 24h.
  </acceptance_criteria>
  <done>The admin KPI endpoint is wired and admin-gated; the window param is validated; every KPI integration test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser (Bearer) to /api/v1/admin/dashboard/kpis | Authenticated admin read of platform aggregates. |
| KPI service to ledger/bets/markets/audit tables | Read-only aggregate queries; no writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Elevation | a player reaches the KPI endpoint | mitigate | `Depends(current_active_admin)` on GET /kpis; the SC#6 negative test in Plan 10-01 establishes the player→403 pattern; the KPI route shares the same gate (the verifier confirms the gate is present). |
| T-10-08 | Information Disclosure | KPI payload leaks per-user PII | mitigate | KPIs are aggregates/counts only — DAU returns a COUNT, never user ids; no per-row data crosses the boundary. KpiResponse has no identity fields. |
| T-10-09 | Tampering | window query param SQL injection / out-of-range interval | mitigate | window is a `Literal["24h","7d","30d"]` query param → 422 on any other value; the interval is derived from a fixed map, never string-interpolated into SQL. |
| T-10-10 | Tampering (data correctness) | wrong P&L via house_expense or raw balances; wrong DAU via logins-only | mitigate | P&L uses the kind-filtered net-flow query (no house_expense account exists); DAU UNIONs bets + auth.session_started. The reversal-nets-to-zero and bet-only-bettor-counted tests are the correctness sentinels (highest-value tests). |
| T-10-DOS | Denial of Service | unindexed range scans on a large bets table | accept | At single-tenant demo volumes the range scans are trivially fast (RESEARCH A5); an index on bets(created_at)/audit_log(occurred_at) is a cheap optional follow-up, not a blocker. INFO timing log makes a real slowdown observable. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH §Package Legitimacy Audit — not applicable; zero new deps). No install task; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd backend; .venv/Scripts/python.exe -m pytest tests/admin/test_kpi.py -x` — all GREEN (the P&L-reversal and bet-only-DAU cases are the correctness sentinels).
- `cd backend; .venv/Scripts/python.exe scripts/lint_money_columns.py` — exits 0.
- `grep -c house_expense backend/app/admin/kpi_service.py` — returns 0 (the non-existent account is never referenced).
- `grep -c "auth.session_started" backend/app/admin/kpi_service.py` — returns >=1; `grep -c "auth.login_started" backend/app/admin/kpi_service.py` — returns 0.
- `grep -L "from __future__ import annotations" backend/app/admin/kpi_router.py` — returns the filename.
</verification>

<success_criteria>
- ADD-02: GET /api/v1/admin/dashboard/kpis returns the five cards with the CORRECTED formulas (P&L net by kind incl. reversal; DAU UNION bets+logins, admins excluded; pending predicate; active markets; 24h volume); money as strings, negative P&L valid.
- ADD-03 (backend): the endpoint returns <=30 daily volume buckets (date_trunc day) for the chart; a 30-day synthetic seed exists for render verification in Plan 10-04.
- D-05 window toggle: 24h/7d/30d accepted; default 24h; bogus → 422.
- Single Alembic head untouched (no migration in this plan).
</success_criteria>

<output>
Create `.planning/phases/10-admin-kpi-dashboard-configurable-branding/10-02-SUMMARY.md` when done.
</output>

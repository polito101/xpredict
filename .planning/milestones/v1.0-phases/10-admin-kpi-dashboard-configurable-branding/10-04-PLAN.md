---
phase: 10-admin-kpi-dashboard-configurable-branding
plan: 04
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - frontend/src/app/admin/page.tsx
  - frontend/src/components/admin/kpi-card.tsx
  - frontend/src/components/admin/dau-window-toggle.tsx
  - frontend/src/components/admin/volume-chart.tsx
  - frontend/src/components/admin/admin-nav.tsx
  - frontend/src/lib/kpi-api.ts
  - frontend/src/lib/kpi-types.ts
  - frontend/src/components/admin/volume-chart.test.tsx
  - frontend/src/components/admin/kpi-card.test.tsx
  - frontend/src/components/admin/admin-default-route.tsx
  - frontend/src/components/admin/kpi-dashboard.tsx
autonomous: true
requirements: [ADD-01, ADD-02, ADD-03]
must_haves:
  truths:
    - "Logging in lands the admin on /admin showing the five KPI cards (24h volume, DAU, active markets, pending resolutions, House P&L today + all-time) — not the user list."
    - "The /admin landing depends on the EXISTING adminLoginAction → redirect(\"/admin\") in frontend/src/lib/auth.ts (SC#1); this plan does NOT add a redirect — it makes /admin BE the dashboard, so the existing post-login redirect lands there."
    - "The DAU card has a 24h/7d/30d toggle (default 24h) that refetches the KPI endpoint with ?window= and updates the DAU value and the chart."
    - "The volume-over-time chart renders daily buckets for 30 days via Recharts in a fixed h-64 parent; a fresh deploy (<1 bucket) shows the 'No activity yet' empty state at the same height, never a blank/broken axis."
    - "Money KPI values render as strings (never parseFloat for storage); a negative House P&L shows in red-500, positive in emerald-600 (covered by an automated kpi-card.test.tsx)."
    - "The admin nav has a leading Dashboard link (/admin, exact-match active) and a Branding link (/admin/branding); a sessionStorage default-route flag marks /admin as the admin landing across sessions."
  artifacts:
    - path: "frontend/src/app/admin/page.tsx"
      provides: "KPI dashboard replacing the placeholder landing"
      contains: "force-dynamic"
    - path: "frontend/src/components/admin/volume-chart.tsx"
      provides: "Recharts AreaChart in h-64 parent + <1-bucket empty state"
      contains: "ResponsiveContainer"
    - path: "frontend/src/components/admin/kpi-card.tsx"
      provides: "KpiCard + KpiGrid (5 cards, responsive)"
      contains: "KpiGrid"
    - path: "frontend/src/components/admin/kpi-card.test.tsx"
      provides: "vitest covering KpiCard color logic (negative red-500 / positive emerald-600) + money-from-string display"
      contains: "emerald-600"
    - path: "frontend/src/components/admin/admin-nav.tsx"
      provides: "Dashboard (leading) + Branding nav links with exact-match active for /admin"
      contains: "Dashboard"
  key_links:
    - from: "frontend/src/app/admin/page.tsx"
      to: "GET /api/v1/admin/dashboard/kpis"
      via: "use-server adminApiFetch Bearer-forwarded fetch"
      pattern: "dashboard/kpis"
    - from: "frontend/src/components/admin/volume-chart.tsx"
      to: "Recharts react-is pin"
      via: "ResponsiveContainer in a fixed h-64 parent (react-is untouched)"
      pattern: "h-64"
    - from: "frontend/src/components/admin/dau-window-toggle.tsx"
      to: "?window= refetch"
      via: "client toggle re-calls the KPI action"
      pattern: "window"
---

<objective>
Slice C (frontend) — KPI dashboard. Replaces the placeholder /admin landing with the operator 5-second health pulse: five KpiCards (24h volume, DAU + inline 24h/7d/30d toggle, active markets, pending resolutions, House P&L today + all-time), a Recharts AreaChart of 30-day daily volume with a 30-day-empty empty state, the admin-nav Dashboard (leading) + Branding links, and a sessionStorage default-route flag. Implements D-01, D-02, D-06, D-11, the UI-SPEC dashboard contract, and ADD-01 + ADD-02 + ADD-03 (frontend). Consumes Plan 10-02's GET /api/v1/admin/dashboard/kpis.

End-to-end: an operator logs in, the EXISTING adminLoginAction (frontend/src/lib/auth.ts) redirects to /admin, and BECAUSE this plan makes /admin the dashboard, the operator lands on the live pulse + chart; toggling the DAU window refetches; a fresh deploy shows zeros + the chart empty state, not a broken page.

Purpose: ADD-01 makes the dashboard the landing; ADD-02/03 surface the metrics + chart. This plan OWNS admin-nav.tsx (adds both Dashboard and Branding links so Plan 10-03's branding page is reachable).
Output: the dashboard page + a client kpi-dashboard wrapper + KpiCard/KpiGrid + DauWindowToggle + VolumeChart (+empty state) + admin-nav links + sessionStorage flag + use-server KPI lib + types + a Recharts not-blank smoke test + a KpiCard color-logic test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-admin-kpi-dashboard-configurable-branding/10-CONTEXT.md
@.planning/phases/10-admin-kpi-dashboard-configurable-branding/10-PATTERNS.md
@.planning/phases/10-admin-kpi-dashboard-configurable-branding/10-UI-SPEC.md
@frontend/src/lib/admin-api.ts
@frontend/src/components/admin/admin-nav.tsx
@frontend/src/components/price-history-chart.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Recharts not-blank smoke test + empty-state test for VolumeChart</name>
  <read_first>
    - frontend/src/components/price-history-chart.test.tsx (ENTIRE FILE) — copy the vitest setup VERBATIM: the ResizeObserver stub, the getBoundingClientRect stub, the react-is sentinel assertion (an `svg path` with a recharts class renders → react-is matches React 19), and the empty-state copy + window-toggle assertions.
    - frontend/src/components/price-history-chart.tsx — the structure the test mirrors (so the VolumeChart props/shape match).
    - 10-UI-SPEC.md section Copywriting Contract (Chart title "Bet volume — last 30 days", empty heading "No activity yet", empty body "Volume appears here as players place bets.") + section Component Inventory (VolumeChart, VolumeChartEmptyState) — the EXACT strings the test asserts.
  </read_first>
  <behavior>
    - With >=1 daily bucket of data, VolumeChart renders a non-blank Recharts area path (the react-is sentinel: an `svg path` with a recharts class exists) inside the h-64 parent.
    - With <1 bucket (empty), VolumeChart renders VolumeChartEmptyState at the same h-64 height with the "No activity yet" heading + "Volume appears here as players place bets." body — no Recharts blank axis.
    - The DAU window toggle exposes 24h/7d/30d buttons with the active one aria-pressed; default 24h.
  </behavior>
  <action>
    Create frontend/src/components/admin/volume-chart.test.tsx. Copy the price-history-chart.test.tsx browser-API stubs (ResizeObserver + getBoundingClientRect) so Recharts measures a non-zero box under jsdom. Assert the not-blank path with data, the empty state with no data (exact UI-SPEC copy), and the 24h/7d/30d toggle buttons. Tests MUST fail RED (components not built). Do NOT implement the chart in this task.
  </action>
  <verify>
    <automated>cd frontend; corepack pnpm vitest run src/components/admin/volume-chart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and is collected by vitest with the ResizeObserver/getBoundingClientRect stubs in place.
    - Tests FAIL RED (VolumeChart module not found / not built), not on a stub/setup error.
  </acceptance_criteria>
  <done>volume-chart.test.tsx exists with the react-is sentinel + empty-state assertions and fails RED against the not-yet-built chart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: use-server KPI lib + types + KpiCard/KpiGrid (+ color-logic test) + DauWindowToggle + VolumeChart → chart + kpi-card tests GREEN</name>
  <read_first>
    - frontend/src/lib/admin-api.ts (ENTIRE FILE) — the `"use server"` adminApiFetch (admin_jwt cookie → Bearer); add fetchKpis(window) calling GET /api/v1/admin/dashboard/kpis?window= the same way (resolves RESEARCH A6 — reuse this exact forwarding).
    - frontend/src/components/price-history-chart.tsx (ENTIRE FILE) — copy the "use client", the WindowToggle (lines ~46-75: flex gap-1, h-11, variant/aria-pressed), the ResponsiveContainer-in-h-64 structure, and the <2-points empty state (here <1 bucket) at the same height; swap LineChart→AreaChart, probability→volume, X axis ts→day.
    - frontend/src/components/price-history-chart.test.tsx (ENTIRE FILE) — the vitest + @testing-library/react + jsdom render/assert style to MIRROR for kpi-card.test.tsx.
    - frontend/src/components/admin/recharge-form.tsx — the shadcn import style + tabular-nums for numeric values.
    - 10-UI-SPEC.md section Component Inventory (KpiCard label text-sm text-zinc-500 + value text-2xl font-semibold tabular-nums; KpiGrid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6), section Color (positive P&L emerald-600, negative red-500; chart stroke/fill var(--brand-primary) or emerald-600), section Copywriting (every card label + chart title + empty copy + the zero-value rule "$0.0000" not N/A), section Spacing, section Typography, section Responsive Contract.
    - 10-PATTERNS.md section volume-chart.tsx, section dau-window-toggle.tsx, section lib/branding-api.ts + KPI helpers.
  </read_first>
  <behavior>
    - KpiCard color logic (asserted in kpi-card.test.tsx, mirroring price-history-chart.test.tsx): a House P&L card with a NEGATIVE value (e.g. "-12.5000") renders the value element with the `red-500` color class; a POSITIVE (or zero) value (e.g. "340.0000" / "0.0000") renders with `emerald-600`.
    - Money is displayed FROM the string prop (display-only formatting); the stored/passed value is never coerced with parseFloat into a number for storage — the money field type stays string end to end (kpi-types.ts).
    - Fresh-deploy zeros render as a real "0" / "$0.0000", never "N/A".
  </behavior>
  <action>
    Create frontend/src/lib/kpi-types.ts (plain types): VolumeBucket { day: string; volume: string }, KpiResponse { volume_24h, daily_active_users, active_markets, pending_resolutions, house_pnl_today, house_pnl_cumulative: string; volume_buckets: VolumeBucket[] } (money fields are strings on the wire — never number).
    Add fetchKpis(window: "24h"|"7d"|"30d") in a `"use server"` module (frontend/src/lib/kpi-api.ts mirroring admin-api.ts) that forwards the admin_jwt Bearer to GET /api/v1/admin/dashboard/kpis?window=.

    Create frontend/src/components/admin/kpi-card.tsx: KpiCard (Card + CardHeader/CardContent; label text-sm text-zinc-500, value text-2xl font-semibold tabular-nums; money rendered from the string via display-only formatting, never stored as float; the House P&L card shows Today + All-time sub-captions and colors the value emerald-600 if >=0 else red-500) and KpiGrid (grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 of the five cards; fresh-deploy zeros render as real 0 / $0.0000, NOT N/A).
    Create frontend/src/components/admin/kpi-card.test.tsx (mirror price-history-chart.test.tsx): render KpiCard with a negative House P&L value and assert the value element carries the `red-500` class; render with a positive value and assert `emerald-600`; render with "0.0000" and assert it shows "$0.0000" (not N/A) with `emerald-600`; assert the displayed money text matches the string prop (no parseFloat-for-storage round-trip). This test guards the color logic that SC#2/UI-SPEC requires.
    Create frontend/src/components/admin/dau-window-toggle.tsx: DauWindowToggle copied from WindowToggle (flex gap-1 role=group, each Button size="sm" variant secondary-if-active/ghost, aria-pressed, className h-11 + font-semibold-if-active), windows ["24h","7d","30d"], default 24h, lifting the selected window to the parent via an onChange prop.
    Create frontend/src/components/admin/volume-chart.tsx ("use client"): VolumeChart copied from price-history-chart.tsx — a fixed-height parent div className "h-64 w-full" wrapping ResponsiveContainer width 100% height 100%, an AreaChart with series key volume + X axis day, stroke/fill var(--brand-primary) (fallback emerald-600 #059669), money string parsed for DISPLAY ONLY inside data.map (parseFloat/Math.round), and a VolumeChartEmptyState at the same h-64 with the exact UI-SPEC copy when buckets.length < 1. Title "Bet volume — last 30 days". Do NOT touch the react-is pnpm.overrides pin.
  </action>
  <verify>
    <automated>cd frontend; corepack pnpm vitest run src/components/admin/volume-chart.test.tsx src/components/admin/kpi-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - volume-chart.test.tsx passes GREEN (non-blank Recharts path with data + empty state at h-64 with the exact copy + the 24h/7d/30d toggle).
    - kpi-card.test.tsx passes GREEN: negative House P&L renders `red-500`, positive (and zero) renders `emerald-600`, "0.0000" shows "$0.0000" not N/A, and the displayed money equals the string prop (no parseFloat for storage) — `grep -E "red-500|emerald-600" frontend/src/components/admin/kpi-card.test.tsx` matches.
    - VolumeChart wraps ResponsiveContainer in an h-64 parent — `grep -F "h-64" frontend/src/components/admin/volume-chart.tsx` matches.
    - Money KPI fields are typed as string in kpi-types.ts (no number-typed money).
  </acceptance_criteria>
  <done>The KPI lib/types + KpiCard/KpiGrid + DauWindowToggle + VolumeChart exist; the Recharts not-blank + empty-state test AND the KpiCard color-logic test are GREEN; react-is pin untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Replace /admin landing with the dashboard + admin-nav links + sessionStorage default-route flag → build GREEN</name>
  <read_first>
    - frontend/src/app/admin/users/page.tsx (ENTIRE FILE) — the async Server Component + `export const dynamic = "force-dynamic"` + the `mx-auto max-w-6xl px-6 py-12` shell + try/catch degrade; the analog for the new /admin dashboard page.
    - frontend/src/app/admin/page.tsx (the current placeholder being replaced — preserve the shell width).
    - frontend/src/lib/auth.ts (the `adminLoginAction` near the bottom — note the `redirect("/admin")` at the end; SC#1's landing relies on THIS existing redirect, which this plan does NOT modify) + frontend/src/app/admin/login/admin-login-form.tsx (binds the form to `adminLoginAction`). Read these to confirm the post-login redirect already targets `/admin` so the dashboard becomes the landing without any new redirect.
    - frontend/src/components/admin/admin-nav.tsx (ENTIRE FILE) — the LINKS array (currently Users + Audit log, Markets a disabled span) and the active/inactive cn(...) styling; this plan OWNS this file.
    - 10-UI-SPEC.md section Interaction Contract (Login→landing; the sessionStorage default-route flag is a UX hint NEVER an auth gate; admin nav adds Dashboard leading + Branding; DAU toggle refetch; KPI load error copy "Couldn't load dashboard metrics. Refresh the page to try again.") + section Copywriting (Page H1 "Dashboard", subtext "Your platform at a glance.").
    - 10-PATTERNS.md section admin/page.tsx, section admin-nav.tsx (the exact-match note: `/admin` active needs `pathname === "/admin"`, not startsWith).
  </read_first>
  <action>
    Create frontend/src/components/admin/kpi-dashboard.tsx ("use client"): a KpiDashboard wrapper that takes the initial server-fetched KpiResponse, owns the DAU window state (default 24h), renders the DauWindowToggle (onChange refetches via fetchKpis and updates state — wraps the action in a transition/loading flag), and composes KpiGrid + VolumeChart from the current KpiResponse.

    Replace frontend/src/app/admin/page.tsx with an async Server Component: `export const dynamic = "force-dynamic"`, the `mx-auto max-w-6xl px-6 py-12` shell, H1 "Dashboard" + subtext "Your platform at a glance.", awaiting fetchKpis("24h") inside try/catch (on failure render the KPI-load-error copy "Couldn't load dashboard metrics. Refresh the page to try again.", NOT a crash). Render <KpiDashboard initial={kpis} /> and <AdminDefaultRoute />. This makes /admin BE the dashboard so the existing adminLoginAction → redirect("/admin") (frontend/src/lib/auth.ts) lands the operator here (SC#1) — do NOT add or change any redirect.

    Modify frontend/src/components/admin/admin-nav.tsx: prepend `{ href: "/admin", label: "Dashboard" }` as the LEADING entry and add `{ href: "/admin/branding", label: "Branding" }`; the disabled "Markets" span may stay or be removed. CRITICAL: the `/admin` link active-match must be EXACT (`pathname === "/admin"`) — a startsWith("/admin/") check would mark Dashboard active on every sub-route; the other links keep the existing startsWith behavior. Reuse the existing active/inactive cn(...) styling unchanged.

    Create frontend/src/components/admin/admin-default-route.tsx ("use client"): a tiny client component (mounted by the dashboard page) that sets a sessionStorage flag (`sessionStorage.setItem("admin_default_route","/admin")`) marking the dashboard as the admin landing across sessions (SC#1 / D-11). UX hint ONLY — it MUST NOT gate auth or perform any redirect; the authoritative landing is adminLoginAction's existing redirect to /admin.
  </action>
  <verify>
    <automated>cd frontend; corepack pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `corepack pnpm build` exits 0 (the replaced /admin page + new components compile in the Next app graph).
    - admin-nav.tsx contains a Dashboard link with an EXACT-match active check for `/admin` — `grep -F "pathname === \"/admin\"" frontend/src/components/admin/admin-nav.tsx` matches; both "Dashboard" and "Branding" labels are present.
    - SC#1 landing depends on the existing post-login redirect: `grep -F "redirect(\"/admin\")" frontend/src/lib/auth.ts` matches (the adminLoginAction redirect to /admin is present and unchanged); /admin now IS the dashboard, so the operator lands on the dashboard — this plan adds no new redirect.
    - The sessionStorage flag is set but is never read by any auth/redirect path — `grep -rn "admin_default_route" frontend/src` shows it only in admin-default-route.tsx (a UX hint, not a gate).
  </acceptance_criteria>
  <done>/admin is the KPI dashboard landing (reached via the existing adminLoginAction→/admin redirect); the admin nav has Dashboard (leading, exact-match) + Branding links; the sessionStorage flag is set as a UX hint; the Next build compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client dashboard → use-server fetchKpis | The admin_jwt HttpOnly cookie is read SERVER-SIDE only; never reaches client JS. |
| sessionStorage default-route flag | Client-only UX hint; NOT a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Elevation | sessionStorage default-route flag used to gate auth/landing | mitigate | The flag is a UX hint ONLY (RESEARCH §Security V3); the authoritative landing/redirect is adminLoginAction + the Edge middleware Bearer verify. The flag is written, never read by any auth path (acceptance criterion greps for this). |
| T-10-15 | Information Disclosure | admin Bearer leaking to client JS via the KPI fetch | mitigate | fetchKpis lives in the `"use server"` kpi-api.ts (cookies() server-side); the client KpiDashboard calls the exported async action and cannot read the HttpOnly cookie — same proven pattern as admin-api.ts. |
| T-10-16 | Denial of Service (UX) | Recharts renders blank on React 19 / 0-height container | mitigate | react-is stays pinned via pnpm.overrides (untouched); ResponsiveContainer is wrapped in a fixed h-64 parent; the volume-chart not-blank smoke test is the sentinel (Phase 9 precedent). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH §Standard Stack — all frontend deps already present; zero new installs). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend; corepack pnpm vitest run src/components/admin/volume-chart.test.tsx src/components/admin/kpi-card.test.tsx` — GREEN (react-is sentinel + empty state + KpiCard red-500/emerald-600 color logic).
- `cd frontend; corepack pnpm build` — exits 0.
- `grep -F "dashboard/kpis" frontend/src/lib/kpi-api.ts` — matches (the KPI endpoint is consumed with the Bearer forwarded).
- `grep -F "redirect(\"/admin\")" frontend/src/lib/auth.ts` — matches (SC#1 landing relies on the existing adminLoginAction redirect, unchanged by this plan).
- `grep -rn "admin_default_route" frontend/src` — appears only in admin-default-route.tsx (UX hint, not an auth gate).
</verification>

<success_criteria>
- ADD-01: /admin is the KPI dashboard landing (replacing the placeholder), reached via the EXISTING adminLoginAction → redirect("/admin"); a sessionStorage default-route flag marks it across sessions; the admin nav links to it (leading) + to Branding.
- ADD-02 (frontend): the five cards render the KPI values; money as strings; negative P&L red-500, positive emerald-600 (kpi-card.test.tsx guards this); fresh-deploy zeros show real $0.0000 not N/A.
- ADD-03 (frontend): the Recharts AreaChart renders 30-day daily buckets in a fixed h-64 parent; <1 bucket shows the 30-day-empty empty state at the same height (no blank axis); the DAU toggle refetches by window.
- UI-SPEC fidelity: copy/spacing/typography/color match the contract; react-is pin untouched.
</success_criteria>

<output>
Create `.planning/phases/10-admin-kpi-dashboard-configurable-branding/10-04-SUMMARY.md` when done.
</output>
</content>

---
phase: 04-markets-domain-houseadapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/markets/enums.py
  - backend/app/markets/models.py
  - backend/app/markets/__init__.py
  - backend/app/integrations/__init__.py
  - backend/app/integrations/market_source.py
  - backend/alembic/versions/0003_phase4_markets.py
  - backend/alembic/env.py
  - backend/tests/markets/__init__.py
  - backend/tests/markets/conftest.py
  - backend/tests/markets/test_models.py
  - backend/tests/markets/test_protocol.py
  - backend/pyproject.toml
autonomous: true
requirements:
  - MKT-07
  - MKT-08

must_haves:
  truths:
    - "Market, Outcome, OddsSnapshot ORM models exist with correct column types and relationships"
    - "MarketSource Protocol is defined with fetch_active_markets, fetch_market, detect_resolution"
    - "HouseAdapter implements MarketSource and is registered in REGISTRY[HOUSE]"
    - "markets table has source + source_market_id + condition_id columns (MKT-07)"
    - "Binary-only constraint prevents more than 2 outcomes per market (MKT-08)"
    - "All three tables include tenant_id ghost column (PLT-01)"
    - "Alembic migration 0003 creates markets, outcomes, odds_snapshots tables"
  artifacts:
    - path: "backend/app/markets/enums.py"
      provides: "MarketStatus and MarketSourceEnum str enums"
      contains: "class MarketStatus"
    - path: "backend/app/markets/models.py"
      provides: "Market, Outcome, OddsSnapshot ORM models"
      exports: ["Market", "Outcome", "OddsSnapshot"]
    - path: "backend/app/integrations/market_source.py"
      provides: "MarketSource Protocol + REGISTRY + HouseAdapter"
      exports: ["MarketSource", "REGISTRY", "HouseAdapter", "register_source", "get_adapter"]
    - path: "backend/alembic/versions/0003_phase4_markets.py"
      provides: "Migration creating markets, outcomes, odds_snapshots tables"
      contains: "op.create_table"
    - path: "backend/tests/markets/conftest.py"
      provides: "Test fixtures for market domain"
      contains: "sample_market"
  key_links:
    - from: "backend/app/integrations/market_source.py"
      to: "backend/app/markets/models.py"
      via: "HouseAdapter returns Market ORM instances"
      pattern: "from app.markets.models import"
    - from: "backend/alembic/env.py"
      to: "backend/app/markets/models.py"
      via: "Side-effect import for autogenerate"
      pattern: "from app.markets.models import"
    - from: "backend/app/markets/models.py"
      to: "backend/app/markets/enums.py"
      via: "Status and source enum values in CHECK constraints"
      pattern: "from app.markets.enums import"
---

<objective>
Build the market domain foundation: ORM models (Market, Outcome, OddsSnapshot), Python enums, the MarketSource Protocol with HouseAdapter implementation, Alembic migration 0003, and test infrastructure with unit tests for models and protocol conformance.

Purpose: Establishes the source-agnostic market domain that all later market-touching code (CRUD, bets, settlement, Polymarket sync) builds on. Proves the MarketSource Protocol with a fully-controllable HouseAdapter before any external dependency.

Output: Working ORM models with migration, HouseAdapter registered in REGISTRY, 10+ passing unit/integration tests for model constraints and protocol conformance.
</objective>

<execution_context>
@C:\Users\pobom\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pobom\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\pobom\xpredict\.planning\PROJECT.md
@C:\Users\pobom\xpredict\.planning\ROADMAP.md
@C:\Users\pobom\xpredict\.planning\STATE.md
@C:\Users\pobom\xpredict\.planning\phases\04-markets-domain-houseadapter\04-CONTEXT.md
@C:\Users\pobom\xpredict\.planning\phases\04-markets-domain-houseadapter\04-RESEARCH.md
@C:\Users\pobom\xpredict\backend\app\auth\models.py
@C:\Users\pobom\xpredict\backend\app\db\base.py
@C:\Users\pobom\xpredict\backend\app\db\types.py
@C:\Users\pobom\xpredict\backend\app\core\config.py
@C:\Users\pobom\xpredict\backend\app\core\audit\service.py
@C:\Users\pobom\xpredict\backend\alembic\env.py
@C:\Users\pobom\xpredict\backend\alembic\versions\0002_phase2_auth.py
@C:\Users\pobom\xpredict\backend\tests\conftest.py
@C:\Users\pobom\xpredict\backend\tests\auth\conftest.py
@C:\Users\pobom\xpredict\backend\CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain models, enums, Protocol, HouseAdapter, and migration</name>
  <files>
    backend/app/markets/enums.py,
    backend/app/markets/models.py,
    backend/app/markets/__init__.py,
    backend/app/integrations/__init__.py,
    backend/app/integrations/market_source.py,
    backend/alembic/versions/0003_phase4_markets.py,
    backend/alembic/env.py,
    backend/pyproject.toml
  </files>
  <read_first>
    backend/app/auth/models.py (UUID PK + tenant_id ghost + relationship + timestamp patterns),
    backend/app/db/base.py (Base import),
    backend/app/db/types.py (Money alias — odds columns must NOT use Money; use Numeric(8,6) directly),
    backend/app/core/config.py (get_settings for tenant_id default),
    backend/alembic/env.py (model registration pattern — add Market/Outcome/OddsSnapshot imports),
    backend/alembic/versions/0002_phase2_auth.py (migration revision chain: down_revision must be 0002_phase2_auth; TENANT_DEFAULT constant pattern),
    backend/app/integrations/__init__.py (current stub to replace),
    backend/app/markets/__init__.py (current stub to replace),
    backend/pyproject.toml (dependency section for adding python-slugify)
  </read_first>
  <behavior>
    - Test: MarketStatus enum has exactly 5 values: DRAFT, OPEN, CLOSED, RESOLVED, CANCELLED
    - Test: MarketSourceEnum has exactly 2 values: HOUSE, POLYMARKET
    - Test: Market model has columns id, question, slug, resolution_criteria, category, source, source_market_id, condition_id, status, deadline, bet_count, created_at, updated_at, closed_at, resolved_at, tenant_id (MKT-07)
    - Test: Outcome model has columns id, market_id, label, initial_odds, current_odds, tenant_id
    - Test: OddsSnapshot model has columns id, market_id, outcome_id, probability, snapshot_at, tenant_id
    - Test: Market.outcomes relationship loads with selectinload (lazy="raise" blocks implicit loads)
    - Test: HouseAdapter satisfies isinstance(adapter, MarketSource) check (runtime_checkable)
    - Test: register_source(HOUSE, HouseAdapter()) + get_adapter(HOUSE) returns same instance
    - Test: HouseAdapter.detect_resolution returns None always (house markets resolve via admin action)
    - Test: Migration 0003 creates three tables with correct column types and CHECK constraints
    - Test: Binary-only — inserting a 3rd outcome for a market violates CHECK or trigger (MKT-08)
    - Test: Market source column CHECK rejects values other than HOUSE/POLYMARKET
    - Test: All three tables have tenant_id column with correct default
  </behavior>
  <action>
    Install python-slugify: `cd backend && uv add "python-slugify>=8.0,<9.0"`.

    Create `backend/app/markets/enums.py` with two `str, enum.Enum` classes:
    - `MarketStatus(str, Enum)` with values DRAFT, OPEN, CLOSED, RESOLVED, CANCELLED
    - `MarketSourceEnum(str, Enum)` with values HOUSE, POLYMARKET

    Create `backend/app/markets/models.py` following `app/auth/models.py` patterns verbatim. Three ORM models inheriting from `app.db.base.Base`:

    `Market` — tablename "markets", __table_args__ with two CheckConstraints:
    - `ck_markets_status` constraining status to the 5 MarketStatus values
    - `ck_markets_source` constraining source to HOUSE/POLYMARKET
    Columns: `id` (UUID PK, default uuid4, server_default gen_random_uuid), `question` (Text, not null), `slug` (String(100), unique, index, not null), `resolution_criteria` (Text, not null), `category` (String(100), nullable), `source` (String(20), not null, server_default "HOUSE"), `source_market_id` (String(200), nullable — per MKT-07), `condition_id` (String(200), nullable — Phase 6 forward-compat), `status` (String(20), not null, server_default "OPEN"), `deadline` (DateTime(timezone=True), not null), `bet_count` (Integer, not null, server_default "0", default 0 — Phase 5 criteria-lock mechanism), `created_at` (DateTime(timezone=True), server_default func.now()), `updated_at` (DateTime(timezone=True), server_default func.now(), onupdate func.now()), `closed_at` (DateTime(timezone=True), nullable), `resolved_at` (DateTime(timezone=True), nullable), `tenant_id` (UUID nullable, default lambda get_settings().TENANT_ID_DEFAULT — PLT-01 ghost).
    Relationships: `outcomes: Mapped[list[Outcome]]` with back_populates="market", cascade="all, delete-orphan", lazy="raise". Same for `odds_snapshots: Mapped[list[OddsSnapshot]]`.

    `Outcome` — tablename "outcomes". Columns: `id` (UUID PK), `market_id` (UUID FK "markets.id" ondelete CASCADE, index), `label` (String(50), not null — "YES" or "NO"), `initial_odds` (Numeric(8,6), not null), `current_odds` (Numeric(8,6), not null), `tenant_id` ghost. Relationship: `market: Mapped[Market]` back_populates="outcomes".

    `OddsSnapshot` — tablename "odds_snapshots". Columns: `id` (UUID PK), `market_id` (UUID FK "markets.id" ondelete CASCADE, index), `outcome_id` (UUID FK "outcomes.id" ondelete CASCADE, index), `probability` (Numeric(8,6), not null), `snapshot_at` (DateTime(timezone=True), server_default func.now()), `tenant_id` ghost. Relationship: `market: Mapped[Market]` back_populates="odds_snapshots".

    NOTE: Use `from __future__ import annotations` in models.py (safe — no FastAPI Depends here). Do NOT use it in router files.

    Create `backend/app/integrations/market_source.py` with:
    - `@runtime_checkable class MarketSource(Protocol)` defining three async methods:
      - `fetch_active_markets(self, session: AsyncSession, *, limit: int = 25) -> list[Market]`
      - `fetch_market(self, session: AsyncSession, market_id: UUID) -> Market | None`
      - `detect_resolution(self, session: AsyncSession, market_id: UUID) -> ResolutionResult | None`
    - `ResolutionResult` as a simple dataclass with fields: `winning_outcome_id: UUID`, `source: str`, `confidence: str`
    - Module-level `REGISTRY: dict[MarketSourceEnum, MarketSource] = {}`
    - `register_source(source, adapter)` and `get_adapter(source)` functions
    - `HouseAdapter` class implementing MarketSource:
      - `fetch_active_markets`: SELECT Market WHERE status = OPEN AND source = HOUSE, options(selectinload(Market.outcomes)), limit, ordered by created_at desc
      - `fetch_market`: SELECT Market WHERE id = market_id, options(selectinload(Market.outcomes), selectinload(Market.odds_snapshots))
      - `detect_resolution`: return None always (house markets resolve via explicit admin action)
    - At module bottom: `register_source(MarketSourceEnum.HOUSE, HouseAdapter())`

    Update `backend/app/integrations/__init__.py` to replace the stub comment with: `from app.integrations.market_source import MarketSource, REGISTRY, get_adapter, register_source, HouseAdapter`.

    Update `backend/app/markets/__init__.py` to export the models and enums.

    Create `backend/alembic/versions/0003_phase4_markets.py`:
    - `revision = "0003_phase4_markets"`, `down_revision = "0002_phase2_auth"`
    - `TENANT_DEFAULT = "00000000-0000-0000-0000-000000000001"` (same constant as 0001/0002)
    - upgrade(): create_table "markets" (all columns matching model, plus ck_markets_status and ck_markets_source CHECK constraints), create_table "outcomes" (with FK to markets, index on market_id), create_table "odds_snapshots" (with FKs to markets and outcomes, indexes on market_id and outcome_id)
    - For MKT-08 binary-only enforcement: add a Postgres trigger function `check_binary_outcomes` that raises when outcome count for a market exceeds 2 on INSERT into outcomes. Create trigger `trg_binary_outcomes_only` BEFORE INSERT on outcomes.
    - downgrade(): drop trigger, drop function, drop tables in reverse FK order (odds_snapshots, outcomes, markets)

    Update `backend/alembic/env.py`: add import line `from app.markets.models import Market, Outcome, OddsSnapshot  # noqa: F401  (Plan 04-01)` alongside existing model imports.

    Run `cd backend && uv add "python-slugify>=8.0,<9.0"` to install the dependency.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/markets/test_models.py tests/markets/test_protocol.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from app.markets.enums import MarketStatus, MarketSourceEnum; assert len(MarketStatus) == 5; assert len(MarketSourceEnum) == 2"` exits 0
    - `uv run python -c "from app.markets.models import Market, Outcome, OddsSnapshot"` exits 0 (models importable)
    - `uv run python -c "from app.integrations.market_source import MarketSource, HouseAdapter, REGISTRY, get_adapter; from app.markets.enums import MarketSourceEnum; assert isinstance(get_adapter(MarketSourceEnum.HOUSE), MarketSource)"` exits 0
    - `grep -c "0003_phase4_markets" backend/alembic/versions/0003_phase4_markets.py` returns >= 1
    - `grep -c "from app.markets.models import" backend/alembic/env.py` returns 1
    - `grep -c "tenant_id" backend/app/markets/models.py` returns 3 (one per model)
    - `grep -c "source_market_id" backend/app/markets/models.py` returns >= 1 (MKT-07)
    - `grep -c "condition_id" backend/app/markets/models.py` returns >= 1 (MKT-07 forward-compat)
    - `grep -c "python-slugify" backend/pyproject.toml` returns >= 1
    - `tests/markets/test_models.py` has tests for column existence, CHECK constraints, tenant_id defaults, binary-only trigger (MKT-08)
    - `tests/markets/test_protocol.py` has tests for isinstance check, registry lookup, detect_resolution returns None
    - All tests in tests/markets/ pass green
  </acceptance_criteria>
  <done>
    Market, Outcome, OddsSnapshot models exist with correct types and constraints. MarketSource Protocol defined with 3 async methods. HouseAdapter implements it and is REGISTRY[HOUSE]. Migration 0003 creates all three tables with CHECK constraints and binary-only trigger. Alembic env.py registers the new models. python-slugify installed. All unit and integration tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test infrastructure — fixtures and conftest for market tests</name>
  <files>
    backend/tests/markets/__init__.py,
    backend/tests/markets/conftest.py
  </files>
  <read_first>
    backend/tests/conftest.py (engine, async_session, postgres_container fixtures — session-scoped patterns),
    backend/tests/auth/conftest.py (admin_user fixture pattern — need admin for Phase 4 router tests in Plan 02),
    backend/app/markets/models.py (Market, Outcome, OddsSnapshot for factory functions),
    backend/app/markets/enums.py (MarketStatus, MarketSourceEnum for fixture defaults)
  </read_first>
  <action>
    Create `backend/tests/markets/__init__.py` as empty file.

    Create `backend/tests/markets/conftest.py` with shared fixtures for all market tests:

    - Import the `async_session` and `engine` fixtures from parent conftest (inherited via pytest fixture scoping)
    - `admin_user` fixture: create a User with is_superuser=True, is_verified=True, is_active=True via direct ORM insert (same pattern as tests/auth/conftest.py::admin_user). Use email "market-admin@test.com" to avoid collisions with auth test fixtures.
    - `sample_market` async fixture: factory function that creates a Market with 2 Outcome rows (YES at 0.5, NO at 0.5) and 1 OddsSnapshot per outcome. Parameters: question (default "Will it rain tomorrow?"), status (default OPEN), source (default HOUSE). Returns the Market ORM instance with outcomes eagerly loaded.
    - `market_with_bets` async fixture: calls sample_market then sets bet_count=1 via direct UPDATE to simulate Phase 5 bet placement (for criteria-locking tests).
    - All fixtures use `loop_scope="session"` matching the parent pattern.
    - All test files in this directory use `pytestmark = [pytest.mark.integration, pytest.mark.asyncio(loop_scope="session")]` (matching the auth test pattern).

    Important: the `admin_user` fixture here must be function-scoped (not session-scoped) to work within the session-scoped async_session's transaction scope. Use savepoints (`session.begin_nested()`) if needed for isolation within the session transaction.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/markets/ --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/markets/__init__.py` exists as empty file
    - `tests/markets/conftest.py` exists and defines `admin_user`, `sample_market`, `market_with_bets` fixtures
    - `grep -c "sample_market" backend/tests/markets/conftest.py` returns >= 1
    - `grep -c "admin_user" backend/tests/markets/conftest.py` returns >= 1
    - `grep -c "market_with_bets" backend/tests/markets/conftest.py` returns >= 1
    - `pytest --collect-only tests/markets/` runs without import errors
  </acceptance_criteria>
  <done>
    Test infrastructure for market domain is ready. conftest.py provides admin_user, sample_market, and market_with_bets fixtures. pytest collection succeeds without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API | Admin Bearer JWT required for all /api/v1/admin/markets routes (Plan 02) |
| Database constraints | CHECK constraints and trigger enforce data integrity at DB level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ORM models | mitigate | CHECK constraints on status and source columns enforce valid values at DB level; Python enums enforce at application level |
| T-04-02 | Tampering | Migration 0003 | mitigate | Binary-only trigger prevents >2 outcomes per market (MKT-08); integration test verifies trigger fires |
| T-04-03 | Information Disclosure | tenant_id | mitigate | Ghost column defaulted to TENANT_DEFAULT; not exposed in API responses |
| T-04-SC | Tampering | uv add python-slugify | mitigate | python-slugify is [ASSUMED] — 10yr history, 66M downloads/month, MIT license; verified import path `from slugify import slugify` is unique to python-slugify (not the confusable `slugify` or `awesome-slugify` packages) |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/markets/ -x -q` — all market tests green
- `cd backend && uv run pytest -x -q` — full suite still green (no regressions)
- `cd backend && uv run alembic heads` — shows 0003_phase4_markets as current head
- `cd backend && uv run ruff check app/markets/ app/integrations/` — no lint errors
- `cd backend && uv run mypy app/markets/ app/integrations/ --ignore-missing-imports` — no type errors
</verification>

<success_criteria>
- 3 ORM models (Market, Outcome, OddsSnapshot) with correct column types
- MarketSource Protocol with 3 async methods defined
- HouseAdapter implements MarketSource and is registered as REGISTRY[HOUSE]
- Migration 0003 creates 3 tables with CHECK constraints + binary-only trigger
- markets.source + markets.source_market_id + markets.condition_id columns exist (MKT-07)
- Binary-only enforced at DB level (MKT-08)
- tenant_id ghost column on all 3 tables (PLT-01)
- python-slugify installed
- All tests pass, full suite green
</success_criteria>

<output>
Create `.planning/phases/04-markets-domain-houseadapter/04-01-SUMMARY.md` when done
</output>

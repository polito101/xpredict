---
phase: 11-hardening-operator-demo-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/sentry-alerts.md
autonomous: false
requirements: [PLT-07]
must_haves:
  truths:
    - "The four critical Sentry alert rules are specified (dataset, filter, aggregate, threshold, notification channel) for UI definition"
    - "Each of the four alerts has a documented synthetic-trigger command that reaches its existing in-code emit site"
    - "A human verifies each synthetic event lands in the configured Sentry notification channel against the real staging DSN"
  artifacts:
    - path: "docs/runbooks/sentry-alerts.md"
      provides: "SC#5 — 4 alert-rule definitions + synthetic-trigger procedure + sign-off table"
      contains: "reconcile_wallets"
      min_lines: 60
  key_links:
    - from: "docs/runbooks/sentry-alerts.md"
      to: "backend/app/wallet/reconcile.py"
      via: "reconciliation-drift synthetic trigger"
      pattern: "reconcile_wallets"
    - from: "docs/runbooks/sentry-alerts.md"
      to: "frontend/src/app/api/sentry-test/route.ts"
      via: "settlement/api error synthetic trigger reference"
      pattern: "sentry-test"
user_setup:
  - service: sentry
    why: "SC#5 alert rules live in Sentry org config (not the repo); the round-trip needs the real staging DSN"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry project xpredict-staging -> Settings -> Client Keys (DSN)"
    dashboard_config:
      - task: "Define the 4 alert rules per the runbook and wire the notification channel (Slack #general or email)"
        location: "Sentry -> Alerts -> Create Alert (per docs/runbooks/sentry-alerts.md)"
---

<objective>
SC#5 — Close the PLT-08 deferral: configure Sentry alert rules for the four critical scenarios and
verify each fires to the configured notification channel.

  1. Settlement failure
  2. Polymarket sync error-rate spike
  3. Ledger reconciliation drift
  4. Auth-abuse / failed-login burst

Purpose: PLT-08 was code-completed in Phase 1 with "alert rules explicitly deferred to Phase 11"
(REQUIREMENTS.md). The four error-emit sites ALREADY exist in code; this plan does NOT add or change
them. In this stack, Sentry alert rules are org-side config (Sentry UI/API), not a repo artifact
(RESEARCH A2). The deliverable is a version-controlled RUNBOOK that (a) specifies each rule precisely
enough to define in the Sentry UI, (b) gives a synthetic-trigger command per scenario that reaches
the existing emit site, and (c) has a sign-off table Pol completes against the real `xpredict-staging`
DSN. Verification is a documented manual-verify, consistent with how Phase 1 handled the Sentry
round-trip (RESEARCH Pitfall 5).

Output: ONE markdown runbook under `docs/runbooks/`. No code change, no CI change, no IaC (UI
definition + runbook is the lower-risk choice per RESEARCH Open Question 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-operator-demo-gate/11-CONTEXT.md
@.planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md
@backend/app/core/sentry.py
@backend/app/wallet/reconcile.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/runbooks/sentry-alerts.md (4 rules + synthetic triggers + sign-off)</name>
  <files>docs/runbooks/sentry-alerts.md</files>
  <read_first>
    - backend/app/core/sentry.py  (CONFIRM: `init_sentry(service, settings, integrations)` is a no-op when `SENTRY_DSN` is None; tags every event `service=api|worker|beat|frontend`; `send_default_pii=False`. The runbook's filters key on the `service` tag.)
    - backend/app/wallet/reconcile.py  (CONFIRM: `reconcile_wallets` Celery task name `app.wallet.reconcile.reconcile_wallets`; on drift it logs `wallet_ledger_drift` CRITICAL and calls `sentry_sdk.capture_message(..., level="error")`. This is the reconciliation-drift emit site — the runbook's synthetic trigger injects a drift row then runs this task.)
    - backend/app/main.py  (the FastAPI ServerErrorMiddleware Sentry capture + the 429 rate-limit handler — the settlement-failure and auth-abuse emit surfaces; the executor reads the exact line context to name them accurately)
    - backend/app/integrations/polymarket/tasks.py  (the `capture_exception` sites in the poll/snapshot tasks — the Polymarket sync error-rate emit surface)
    - backend/app/celery_app.py  (the `task_failure` signal capture — the worker-side settlement-failure path)
    - frontend/src/app/api/sentry-test/route.ts  (the existing FastAPI/Next synthetic-error endpoint pattern; reference `GET /api/sentry-test` as the api-surface synthetic trigger)
    - .planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md  (§"Pattern 3: Sentry alert rule"; §"System Architecture Diagram → SENTRY block" — the exact emit sites + synthetic triggers; §"Pitfall 5" — why this is a manual-verify against the real DSN; §"State of the Art" — metric vs issue alerts, static vs percent-change thresholds)
  </read_first>
  <action>
    Create `docs/runbooks/sentry-alerts.md` (English). Structure:
    (1) Header: purpose (closes PLT-08 alert-rule deferral), the single project per env model
        (`xpredict-staging`), the `service` tag filter convention, and that `send_default_pii=False`
        is already set (no PII in synthetic triggers).
    (2) A "Notification channel" note: target Slack `#general` (the existing GitHub<->Slack channel)
        or email — Pol picks one when defining the rules (RESEARCH A6).
    (3) FOUR rule subsections, each a table with: Dataset (errors/events), Filter (the `service:` tag
        + message/transaction match), Aggregate (`count()` or `percentage`/`failure_rate`), Threshold
        type + value (Static `> 0 in 5m` for settlement failure & reconciliation drift & auth-abuse
        burst; Percent-change for the Polymarket "error-rate spike"), and Trigger action (the chosen
        channel). Map each rule to its REAL in-code emit site by name:
          - Settlement failure: FastAPI ServerErrorMiddleware capture (main.py) + Celery `task_failure`
            signal (celery_app.py) on the settlement task.
          - Polymarket sync error-rate spike: `capture_exception` in `app/integrations/polymarket/tasks.py`
            (percent-change threshold).
          - Ledger reconciliation drift: `capture_message(level="error")` in `app/wallet/reconcile.py`
            (the `wallet_ledger_drift` message).
          - Auth-abuse / failed-login burst: the 429 handler in main.py (failed-login rate-limit burst).
    (4) A "Synthetic triggers" section: ONE concrete command per scenario, all reaching the existing
        emit sites (no new code):
          - api/settlement: `GET /api/sentry-test` (Next) and the FastAPI equivalent test route, plus
            the worker path via `celery -A app.celery_app call <settlement-task>` if a safe synthetic
            task exists; otherwise document forcing a settlement exception in staging.
          - Polymarket: induce a poll error (e.g. point `GAMMA_API_BASE_URL` at an unreachable host in
            staging and let `poll_polymarket_top25` fail) — name the task.
          - Reconciliation drift: inject a synthetic drift row (mutate one `accounts.balance` so it
            diverges from its ledger) then run `celery -A app.celery_app call app.wallet.reconcile.reconcile_wallets`
            (or trigger the scheduled task) — confirm `wallet_ledger_drift` + the Sentry message.
          - Auth-abuse: fire >6 failed `POST /auth/login` attempts within the rate-limit window to
            produce the 429 burst.
    (5) A sign-off table (one row per rule): columns = Rule | Synthetic trigger run? | Event in Sentry? |
        Alert fired to channel? | Verified by | Date. Left blank for Pol to complete (this is the
        manual-verify gate). Reference the Phase 1 precedent (PLT-08 manual-verify in 01-04-SUMMARY).
    Do NOT author IaC or add a `sentry-cli` dependency (UI definition + runbook is the chosen path).
    Do NOT modify any emit site (constraint 1: verify-only on the existing code).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f docs/runbooks/sentry-alerts.md && grep -Ec 'Settlement|Polymarket|[Rr]econciliation|[Aa]uth' docs/runbooks/sentry-alerts.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/sentry-alerts.md` exists and is >= 60 lines.
    - It names all four scenarios (grep matches Settlement, Polymarket, reconciliation, auth) — count >= 4.
    - It contains the literal task name `app.wallet.reconcile.reconcile_wallets` AND references
      `GET /api/sentry-test` (both `key_links` patterns resolve).
    - Each of the four rules specifies a dataset, a `service:`-tag filter, an aggregate, a threshold
      type+value, and a notification action.
    - It contains a sign-off table with one row per rule (blank for Pol to fill).
    - No emit-site source file is modified (`git diff --stat` shows only `docs/runbooks/sentry-alerts.md`).
  </acceptance_criteria>
  <done>The runbook fully specifies the 4 rules, gives a synthetic trigger per scenario tied to the real emit sites, and provides the manual-verify sign-off table — with zero code changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Sentry alert round-trip — Pol defines rules + verifies synthetic triggers (real DSN)</name>
  <read_first>
    - docs/runbooks/sentry-alerts.md  (the runbook authored in Task 1 — the exact rule definitions + synthetic-trigger commands + sign-off table to execute)
    - .planning/phases/01-scaffold-foundations/01-04-SUMMARY.md  (the Phase 1 Sentry round-trip manual-verify precedent — same shape: needs the real SENTRY_DSN)
  </read_first>
  <action>
    PAUSE for a blocking human gate. Present the runbook to Pol and request the real-DSN round-trip:
    define the four alert rules in `xpredict-staging`, run each synthetic trigger, confirm each event +
    alert lands in the configured channel, and fill the sign-off table. Nothing can be asserted in CI
    (Sentry is external SaaS — RESEARCH Pitfall 5); block phase closure until the sign-off table is
    complete or the gaps are reported.
  </action>
  <what-built>
    Task 1 produced a version-controlled runbook specifying the four alert rules and a synthetic
    trigger per scenario, all wired to the error-emit sites that already exist in code. Nothing can
    be asserted inside CI — Sentry is an external SaaS and alert delivery requires a live project +
    real DSN (RESEARCH Pitfall 5). This checkpoint is the manual-verify, executed by Pol against the
    real `xpredict-staging` Sentry project.
  </what-built>
  <how-to-verify>
    1. In Sentry (`xpredict-staging`), create the four alert rules per `docs/runbooks/sentry-alerts.md`
       (settlement failure, Polymarket sync error-rate spike, reconciliation drift, auth-abuse burst),
       each pointing at the chosen notification channel (Slack #general or email).
    2. Ensure the staging stack runs with the real `SENTRY_DSN` set.
    3. Run each of the four synthetic-trigger commands from the runbook.
    4. Confirm each event appears in Sentry AND its alert fires to the notification channel.
    5. Fill in the sign-off table in the runbook (commit the completed table).
  </how-to-verify>
  <resume-signal>Type "approved" once all four alerts fired to the channel and the sign-off table is filled, or describe which rule(s) did not fire.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging app -> Sentry SaaS | Error events (no PII — `send_default_pii=False`) cross to an external observability provider |
| operator/dev -> synthetic triggers | The reconciliation-drift trigger deliberately writes a divergent `accounts.balance` row in STAGING only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Information Disclosure | synthetic-trigger payloads to Sentry | mitigate | `send_default_pii=False` is already set in `init_sentry` (sentry.py); synthetic triggers use non-PII placeholder data; runbook instructs staging-only execution |
| T-11-03-02 | Denial of Service / data integrity | reconciliation-drift synthetic row | accept | The drift injection runs against the STAGING DB only and is reverted after the test; production data is never touched (runbook caveat) |
| T-11-03-03 | Repudiation | missed critical alert (gap in coverage) | mitigate | All four critical scenarios from PROJECT.md have an explicit rule + a verified synthetic round-trip; the sign-off table proves each fired |
| T-11-03-SC | Tampering | npm/pip installs | accept | This plan adds NO package installs (no `sentry-cli`/IaC — UI definition chosen); nothing to gate |
</threat_model>

<verification>
- `docs/runbooks/sentry-alerts.md` exists (>=60 lines), names all four scenarios, cites the real
  emit sites (`reconcile_wallets`, `/api/sentry-test`), and includes the sign-off table.
- `git diff --stat` confirms no emit-site code changed.
- Phase-gate: the Task-2 checkpoint sign-off table is filled — all four alerts verified firing to the
  configured channel against the real staging DSN (Pol).
</verification>

<success_criteria>
SC#5 satisfied: the four critical Sentry alert rules are configured and synthetically triggered;
each alert lands in the configured Sentry project's notification channel — documented in a
version-controlled runbook and verified manually against the real staging DSN, with zero code change.
</success_criteria>

<output>
Create `.planning/phases/11-hardening-operator-demo-gate/11-03-SUMMARY.md` when done.
</output>

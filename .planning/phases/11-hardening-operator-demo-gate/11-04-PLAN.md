---
phase: 11-hardening-operator-demo-gate
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/regulatory.md
  - docs/terms-of-service.md
  - docs/operator-agreement.md
  - frontend/src/app/layout.tsx
  - frontend/src/app/admin/layout.tsx
autonomous: false
requirements: [PLT-07]
must_haves:
  truths:
    - "docs/regulatory.md exists as a section SKELETON with counsel-review notes (no authored legal content)"
    - "A ToS placeholder and an operator-agreement template stub are checked into the repo"
    - "The player and admin UI footers link to the ToS / token-policy docs"
    - "Counsel review is represented as a deferred external dependency, never as authored legal text"
  artifacts:
    - path: "docs/regulatory.md"
      provides: "SC#6 regulatory posture skeleton + counsel-review notes"
      contains: "Counsel review"
      min_lines: 30
    - path: "docs/terms-of-service.md"
      provides: "ToS placeholder (structure + non-monetary-value assertion stub)"
      contains: "PLACEHOLDER"
    - path: "docs/operator-agreement.md"
      provides: "operator-agreement template stub (the 7 operator commitments as headers)"
      contains: "operator"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "docs/terms-of-service.md"
      via: "player footer link"
      pattern: "terms"
    - from: "frontend/src/app/admin/layout.tsx"
      to: "docs/terms-of-service.md"
      via: "admin footer link"
      pattern: "terms"
user_setup: []
---

<objective>
SC#6 — Scaffold the regulatory / ToS posture: section skeletons + a ToS placeholder + an
operator-agreement template stub + counsel-review notes, plus footer links in the player and admin
UI.

Purpose: PITFALLS.md §"The Regulatory Line" makes the ToS/regulatory posture a CRITICAL gate before
any operator demo. Per CONSTRAINT 2 (hard scope), this phase delivers STRUCTURE + BASE + NOTES ONLY —
NO deep legal content. The actual Spanish-counsel review is an EXTERNAL deferred dependency
(STATE.md Blockers: "Spanish legal counsel must review ToS and token policy before any demo … this
is a gating dependency"). Counsel review is represented as a `checkpoint:human-verify` deferral,
never as authored legal text.

Output: three markdown skeletons under `docs/` + two minimal footer edits to existing layouts. No
backend change, no new component, no data-flow change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-operator-demo-gate/11-CONTEXT.md
@.planning/phases/11-hardening-operator-demo-gate/11-RESEARCH.md
@frontend/src/app/layout.tsx
@frontend/src/app/admin/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/regulatory.md + docs/terms-of-service.md + docs/operator-agreement.md (skeletons + notes ONLY)</name>
  <files>docs/regulatory.md, docs/terms-of-service.md, docs/operator-agreement.md</files>
  <read_first>
    - .planning/research/PITFALLS.md  (section "The Regulatory Line" lines 563-629 — the three-element gambling test, "What keeps us safe", the "What breaks us" do-not-do list, Cuco's developer checklist, and the seven operator-facing commitments. The skeleton section HEADERS + the operator-agreement stub commitment list derive FROM this — but DO NOT author finished legal prose; reference what counsel must review.)
    - .planning/phases/11-hardening-operator-demo-gate/11-CONTEXT.md  (CONSTRAINT 2 — STRUCTURE + BASE + NOTES ONLY; counsel review is an EXTERNAL deferred dependency; "Do NOT write deep legal content or expand the regulatory scope")
    - .gitleaks.toml  (CONFIRM the docs markdown allowlist entry is present so these docs may contain example env-var-shaped strings without tripping gitleaks; no gitleaks config change is needed)
    - REQUIREMENTS.md  ("Out of Scope" table — token transfers / redemption / monetary value are hard-blocked; the ToS placeholder ASSERTS tokens have no monetary value, matching WAL-09)
  </read_first>
  <action>
    Create three English markdown files under `docs/`.

    (1) `docs/regulatory.md` — a SECTION SKELETON. Headers only + bracketed counsel-review notes.
        Sections: "Regulatory posture (v1: play-money, production-grade)"; "The three-element test
        (Spain Ley 13/2011)" (state the three elements as headers, note "v1 removes the prize +
        consideration elements" — reference PITFALLS, do not re-author the analysis); "What keeps us
        safe" (bulleted facts already true in code: tokens system-granted only, non-transferable at DB
        level per WAL-09, non-redeemable, no monetary-prize leaderboard); "What breaks us — do-not-do
        list" (header + a one-line pointer to the PITFALLS table, not a re-transcription); "Geo-fencing"
        (header + a bracketed "COUNSEL REVIEW REQUIRED: jurisdiction allowlist" note); "Open
        counsel-review items" (a checklist of what Spanish counsel must sign off — ToS, token policy,
        geo-block list — each marked as a bracketed "DEFERRED: external counsel" note). Every
        substantive legal claim is a bracketed "COUNSEL REVIEW REQUIRED: …" / "DEFERRED: external
        counsel" note, NOT finished text.

    (2) `docs/terms-of-service.md` — a ToS PLACEHOLDER. A top blockquote banner stating
        "PLACEHOLDER — NOT LEGALLY REVIEWED. Counsel review is a gating dependency (see
        docs/regulatory.md)." Then the section HEADERS a real ToS will need (Acceptance; Eligibility &
        geo-restrictions; Play-money tokens — no monetary value, non-transferable, non-redeemable;
        Prohibited conduct; Account suspension; Disclaimers; Governing law — Spain), each with a
        one-line "[PLACEHOLDER]" stub. The only concrete assertion: tokens have NO monetary value and
        are non-transferable/non-redeemable (structural, matches WAL-09, the trust signal). No other
        authored legal prose.

    (3) `docs/operator-agreement.md` — an operator-agreement TEMPLATE STUB. A blockquote banner
        stating "TEMPLATE STUB — binding operator policy; finalize with counsel before any operator
        signs." Then the SEVEN operator commitments from PITFALLS "Operator-facing checklist" as
        headers (will not enable token purchase; will not enable token redemption; will not award
        cash/prizes on bet outcomes; will geo-fence per recommended list; will display ToS asserting
        tokens have no monetary value; will not advertise tokens as having monetary value; will obtain
        DGOJ license before any real-money plan), each with a one-line "[TEMPLATE — counsel to
        finalize]" stub, plus a signature-block placeholder.

    Keep all three at SKELETON level: section headers + bracketed notes + the structural token-value
    assertion. Do NOT author finished legal content or expand scope (CONSTRAINT 2).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f docs/regulatory.md && test -f docs/terms-of-service.md && test -f docs/operator-agreement.md && grep -lE 'COUNSEL|PLACEHOLDER|TEMPLATE|DEFERRED' docs/regulatory.md docs/terms-of-service.md docs/operator-agreement.md</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `docs/`.
    - `docs/regulatory.md` is >= 30 lines and contains the phrase "Counsel review" (or "COUNSEL REVIEW").
    - `docs/terms-of-service.md` contains "PLACEHOLDER" and an explicit "no monetary value" /
      "non-transferable" assertion.
    - `docs/operator-agreement.md` contains "TEMPLATE" and lists all seven operator commitments as headers.
    - NONE of the three files contains authored finished legal prose — every legal claim is a bracketed
      COUNSEL/PLACEHOLDER/TEMPLATE/DEFERRED note (spot-checked by the reviewer).
  </acceptance_criteria>
  <done>The three regulatory/ToS/operator-agreement skeletons exist with bracketed counsel-review notes and the structural token-value assertion — no authored legal content, no scope expansion.</done>
</task>

<task type="auto">
  <name>Task 2: Footer links to ToS / token policy in the player and admin layouts</name>
  <files>frontend/src/app/layout.tsx, frontend/src/app/admin/layout.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx  (the player root layout — Server Component; `<body className="min-h-full flex flex-col">` with `{children}` then `<Toaster />`. The footer goes AFTER `{children}` and before `<Toaster />`, so the column layout pushes it to the bottom. `next/link` is NOT imported yet — add the import.)
    - frontend/src/app/admin/layout.tsx  (the admin layout — Server Component; outer `<div className="min-h-screen flex flex-col">` with `<main className="flex-1">{children}</main>`. The footer goes AFTER `</main>`, inside the flex column. `Link` is already imported from `next/link`.)
    - docs/terms-of-service.md, docs/regulatory.md  (the link targets — these are repo docs; link to the GitHub blob paths or relative repo paths since there is no in-app /terms route in v1; document the chosen href form in the SUMMARY)
    - .planning/phases/11-hardening-operator-demo-gate/11-CONTEXT.md  (CONSTRAINT 1 — footer links are "within structure scope"; no new route, no new component beyond an inline footer element; CSS-only/markup-only)
  </read_first>
  <action>
    Add a minimal footer to BOTH layouts linking to the ToS + token policy. Since v1 has no in-app
    `/terms` route, link to the repo docs (the GitHub blob URL for `docs/terms-of-service.md` and
    `docs/regulatory.md`, OR a relative `/docs/...` path if the executor confirms a static-serving
    convention — prefer the GitHub blob URL for determinism and document the choice).

    Player `frontend/src/app/layout.tsx`: import `Link` from `next/link`; insert a `<footer>` AFTER
    `{children}` and before `<Toaster />`. Use the existing responsive container convention
    (`mx-auto w-full max-w-6xl px-4 sm:px-6`) so it is mobile-correct at 360px. The footer contains
    two text links ("Terms of Service" -> docs/terms-of-service.md, "Token policy" -> docs/regulatory.md)
    plus a one-line note "Play-money tokens have no monetary value." (the trust signal). Use
    `text-xs text-zinc-500` styling consistent with the existing zinc palette.

    Admin `frontend/src/app/admin/layout.tsx`: insert the same `<footer>` AFTER the closing `</main>`,
    inside the outer flex-col `<div>`, reusing `Link` (already imported) and the `max-w-6xl px-6`
    container convention already used by the admin nav.

    Keep both edits markup-only (a `<footer>` element + links). Do NOT add a new component file, a new
    route, or any data fetch. Do NOT alter the branding `<style>` injection or any other layout logic
    (CONSTRAINT 1: no refactor).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/frontend" && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/layout.tsx` contains a `<footer>` with a link whose href references
      `terms-of-service` (grep matches the `key_links` `terms` pattern) and the "no monetary value" note.
    - `frontend/src/app/admin/layout.tsx` contains a `<footer>` with a link referencing `terms-of-service`.
    - `pnpm typecheck` and `pnpm build` both exit 0 (NOTE: a pre-existing orphan `middleware.test.ts`
      breaks the FULL `pnpm test` repo-wide typecheck per STATE.md DEF-FE-01 — that is out of scope;
      the `next build` app-graph typecheck must still exit 0, which it does because both layouts are
      type-clean).
    - The footer uses the responsive container convention (`max-w-6xl` + `px-4 sm:px-6` player /
      `px-6` admin) so it renders correctly at 360px (visual confirmation deferred to plan 11-05's QA).
    - No new component file or route was added (`git status` shows only the two layout edits + the
      three docs from Task 1).
  </acceptance_criteria>
  <done>Both layouts render a footer linking to the ToS + token-policy docs with the no-monetary-value note; typecheck + build pass; no new route/component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Counsel-review deferral (external dependency — represented, not authored)</name>
  <read_first>
    - docs/regulatory.md, docs/terms-of-service.md, docs/operator-agreement.md  (the skeletons from Task 1 — what counsel must review)
    - .planning/STATE.md  (Blockers/Concerns: "Spanish legal counsel must review ToS and token policy before any demo … Not deferrable; this is a gating dependency on Phase 11 completion")
  </read_first>
  <action>
    PAUSE for a blocking human gate. Present the three skeletons + the wired footers and ask Pol to
    confirm they are SKELETONS only (no authored legal prose) and to acknowledge Spanish-counsel review
    of the ToS + token policy as a gating EXTERNAL dependency that this phase does not close. Do NOT
    author or expand legal content to "satisfy" the gate (CONSTRAINT 2). Block phase closure until Pol
    accepts the scaffold or specifies required scaffold changes.
  </action>
  <what-built>
    Task 1 produced skeletons + counsel-review notes; Task 2 wired the footers. The actual
    Spanish-counsel review of the ToS + token policy is an EXTERNAL dependency that this phase CANNOT
    satisfy in-repo (CONSTRAINT 2 — no authored legal content). This checkpoint exists to represent
    that deferral explicitly: the regulatory SCAFFOLD ships now; the binding legal review is
    acknowledged as a gating external item, NOT faked with authored legal text.
  </what-built>
  <how-to-verify>
    1. Confirm `docs/regulatory.md`, `docs/terms-of-service.md`, and `docs/operator-agreement.md` are
       SKELETONS only (no authored finished legal prose) — they should read as structure + bracketed
       COUNSEL/PLACEHOLDER/TEMPLATE notes.
    2. Confirm the player + admin footers link to the ToS / token-policy docs.
    3. Acknowledge that Spanish-counsel review of the ToS + token policy remains a gating external
       dependency before any real operator demo (the deferral is recorded, not closed by this phase).
  </how-to-verify>
  <resume-signal>Type "approved — scaffold accepted, counsel review acknowledged as deferred external dependency", or describe required scaffold changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| play-money product -> regulatory definition | Mislabeling tokens as having monetary value would cross into regulated gambling (existential risk) |
| repo docs -> public footer links | The footer exposes ToS/token-policy doc links to every player + admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Repudiation / Compliance | ToS asserting token value incorrectly | mitigate | The ToS placeholder + regulatory skeleton assert tokens have NO monetary value, non-transferable, non-redeemable (matches WAL-09 DB firewall); finished legal text is deferred to counsel (no premature/incorrect claims authored) |
| T-11-04-02 | Tampering | footer links pointing to wrong/missing docs | mitigate | Footer hrefs reference the committed repo docs; `pnpm build` verifies the layouts compile; `key_links` grep asserts the `terms` reference exists |
| T-11-04-03 | Information Disclosure | example secret-shaped strings in docs tripping gitleaks | accept | `.gitleaks.toml` already allowlists docs markdown; no secrets are placed in these files regardless |
| T-11-04-SC | Tampering | npm/pip installs | accept | This plan adds NO package installs (markdown + two markup-only layout edits); nothing to gate |
</threat_model>

<verification>
- `docs/regulatory.md`, `docs/terms-of-service.md`, `docs/operator-agreement.md` exist as skeletons
  with bracketed counsel notes + the structural token-value assertion.
- Player + admin footers link to the ToS / token-policy docs; `pnpm typecheck` + `pnpm build` pass.
- `git status` confirms only the three docs + the two layout edits changed (no new route/component,
  no backend change).
- Phase-gate: the Task-3 checkpoint records counsel review as an acknowledged deferred external dependency.
</verification>

<success_criteria>
SC#6 satisfied (closure-scoped per CONSTRAINT 2): the regulatory posture is documented in
`docs/regulatory.md`, a ToS placeholder + operator-agreement template stub are checked into the repo,
the player and admin UI footers link to the ToS / token policy, and the Spanish-counsel review is
represented as a deferred external dependency — with no authored legal content and no scope expansion.
</success_criteria>

<output>
Create `.planning/phases/11-hardening-operator-demo-gate/11-04-SUMMARY.md` when done.
</output>

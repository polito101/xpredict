---
phase: 16-catalog-event-api-house-event-crud
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - backend/app/catalog/__init__.py
  - backend/app/catalog/schemas.py
  - backend/app/catalog/service.py
  - backend/app/catalog/router.py
  - backend/tests/catalog/test_catalog_router.py
  - backend/tests/catalog/test_categories.py
  - backend/tests/catalog/test_event_detail.py
autonomous: true
requirements: [BRW-01, BRW-02, BRW-03, BRW-04, BRW-05]
tags: [catalog, browse, search, pg-trgm, approach-b, event-detail, categories, derive-event-status]

must_haves:
  truths:
    - "GET /api/v1/catalog returns a bounded (LIMIT 100) list of discriminated {type: market|event} items, never paginated"
    - "Text search q matches local markets.question + market_groups.title via ILIKE bound param (never proxied to Gamma)"
    - "category (exact), status {open, closing_soon, resolved}, and sort {volume, closing_soonest, newest} filters all apply; every filter combination returns 200 + an explicit (possibly empty) list"
    - "GET /events/{slug} serves ≥2-child groups only, returning per-outcome YES-price rows + derived status; 1-child / missing slug → 404"
    - "GET /categories returns the non-empty DISTINCT category union over markets + market_groups (CAT-06), names only"
    - "Money/odds (volume, yes_price) serialize as JSON strings, never floats"
  artifacts:
    - path: "backend/app/catalog/schemas.py"
      provides: "CatalogItem (discriminated type), CatalogOutcome, EventDetail, EventOutcomeRead, CategoryList — money as JSON strings"
      contains: "Literal"
      min_lines: 40
    - path: "backend/app/catalog/service.py"
      provides: "CatalogService.list_catalog (Approach B two bounded queries merged in Python), get_event, list_categories"
      contains: "selectinload"
      min_lines: 90
    - path: "backend/app/catalog/router.py"
      provides: "public_catalog_router (prefix /api/v1): GET /catalog, GET /events/{slug}, GET /categories"
      contains: "@public_catalog_router.get"
      min_lines: 40
    - path: "backend/tests/catalog/test_catalog_router.py"
      provides: "BRW-01/03/04/05 coverage: search-local-only, status filter, sort, every-combo-empty-safe, LIMIT 100"
      min_lines: 60
  key_links:
    - from: "backend/app/catalog/service.py"
      to: "backend/app/settlement/event_service.py"
      via: "derive_event_status + ChildStatus import (Phase 15, unchanged)"
      pattern: "derive_event_status"
    - from: "backend/app/catalog/service.py"
      to: "markets / market_groups tables"
      via: "selectinload(MarketGroup.markets).selectinload(Market.outcomes) + ILIKE bound param"
      pattern: "selectinload\\(MarketGroup.markets\\)"
    - from: "backend/app/catalog/router.py"
      to: "backend/app/catalog/service.py"
      via: "CatalogService calls; status/sort as Literal Query params"
      pattern: "Literal\\["
---

<objective>
Build the public catalog read API (BRW-01..05): a new `app/catalog/` package with `CatalogService` (Approach B — two bounded `LIMIT 100` queries over standalone `markets` and ≥2-child `market_groups`, merged/sorted/sliced in Python), the `public_catalog_router` exposing `GET /api/v1/catalog`, `GET /api/v1/events/{slug}`, `GET /api/v1/categories`, the discriminated `CatalogItem` schema, and integration tests covering search-local-only, status/sort filters, every-combo-empty-safety, event-detail ≥2-child gating, and the categories union.

Purpose: This is the player-facing read surface of the credible catalog — search/filter/sort over the mixed Polymarket+house grid, plus event detail and category navigation. It reuses Phase-15 `derive_event_status` byte-for-byte and the existing pg_trgm GIN + category indexes (no new migration).
Output: `app/catalog/{__init__,schemas,service,router}.py` + three `tests/catalog/test_*.py` files. Router registration in `main.py` is deferred to plan 16-05 (sequenced to avoid a `main.py` edit collision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-RESEARCH.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-PATTERNS.md
@.planning/phases/16-catalog-event-api-house-event-crud/16-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CatalogService (Approach B) + catalog schemas</name>
  <files>backend/app/catalog/__init__.py, backend/app/catalog/schemas.py, backend/app/catalog/service.py</files>
  <read_first>
    - CREATE: backend/app/catalog/__init__.py (new), backend/app/catalog/schemas.py (new), backend/app/catalog/service.py (new)
    - ANALOG (two-bounded-queries-merged): backend/app/markets/service.py — `list_home_markets` (lines 243-272, the two `select().options(selectinload(...)).limit(N)` then concatenate idiom) + `list_markets` conditional WHERE chaining (lines 274-302)
    - ANALOG (by-slug read, use scalar_one_or_none NOT scalar_one): backend/app/markets/service.py `get_market_by_slug` (~lines 321-334)
    - ANALOG (read DTO + money-as-string serializer): backend/app/markets/schemas.py — `OutcomeRead` (lines 102-113), `MarketListItem` (lines 156-191)
    - SOURCE OF TRUTH (derived status — IMPORT UNCHANGED): backend/app/settlement/event_service.py — `derive_event_status` (lines 98-122), `ChildStatus` (lines 69-79), the is_yes_winner construction in `_derive_status` (lines 612-618: `yes_id = next((o.id for o in child.outcomes if o.label.upper()=="YES"), None); is_yes_winner = child.winning_outcome_id is not None and child.winning_outcome_id == yes_id`), the eager-load chain in `_load_group_with_children` (lines 137-143)
    - SOURCE OF TRUTH (columns): backend/app/markets/models.py — `Market` (group_id ~178, group_item_title ~182, volume, deadline, status, created_at), `MarketGroup` (NO volume/status column, ~200-275), `lazy="raise"` on `.markets`/`.outcomes`; backend/app/markets/enums.py — `MarketStatus`, `MarketSourceEnum`
    - RESEARCH detail: §Pattern 1 (Approach B algorithm, lines 193-267), §"Discriminated catalog item schema" (lines 579-610), §Pattern 4 categories (lines 309-335), Open Q2 status mapping (lines 688-690): partially_resolved → public "open"; void → grouped with "resolved"
  </read_first>
  <behavior>
    - list_catalog with no filters returns a list of CatalogItem (≤100), each `type` "market" (standalone, group_id IS NULL) or "event" (group with ≥2 children); a 1-child group is excluded (never an event item).
    - q="foo" filters markets by `Market.question.ilike("%foo%")` and groups by `MarketGroup.title.ilike("%foo%")` (bound param) — no Gamma call.
    - category="X" applies exact equality on both tables; a nonexistent category returns [].
    - status="open" → markets with stored OPEN; events whose derived status maps to public "open" (derived open OR partially_resolved). status="resolved" → markets RESOLVED; events whose derived status is "resolved" OR "void". status="closing_soon" → OPEN AND deadline ≤ now+48h (markets in SQL; events via min open-child deadline).
    - sort="volume" → desc, event volume = sum(child.volume); "closing_soonest" → deadline asc among open; "newest" → created_at desc.
    - get_event(slug) returns the MarketGroup with eager-loaded children+outcomes via scalar_one_or_none (None if missing).
    - list_categories returns sorted DISTINCT non-empty categories union over markets (group_id IS NULL) + market_groups.
  </behavior>
  <action>
    Create `backend/app/catalog/__init__.py` (empty). Create `backend/app/catalog/schemas.py` (MAY keep `from __future__ import annotations`) with: `CatalogOutcome` (`from_attributes`; `label: str`, `yes_outcome_id: UUID | None`, `yes_price: Decimal` serialized to str via `@field_serializer(when_used="json")`); `CatalogItem` (`type: Literal["market","event"]`, `id: UUID`, `slug: str`, `title: str`, `category: str | None`, `source: str`, `status: str` (public), `deadline: datetime | None`, `volume: Decimal` (str serialize), `created_at: datetime`, `outcomes: list[CatalogOutcome]`); `EventOutcomeRead` (per-child row: `label`, `yes_outcome_id`, `yes_price` str, `market_id`, `child_slug`, `child_status`); `EventDetail` (`id`, `slug`, `title`, `category`, `source`, `status` (derived), `deadline`, `created_at`, `outcomes: list[EventOutcomeRead]`); `CategoryList` (or return `list[str]` directly). Follow the recommended shape in RESEARCH lines 579-610. Create `backend/app/catalog/service.py` with `class CatalogService` exposing `@staticmethod async def list_catalog(session, *, q=None, category=None, status=None, sort="volume") -> list[CatalogItem]`, `get_event(session, slug) -> MarketGroup | None`, `list_categories(session) -> list[str]`. Implement `list_catalog` as Approach B per RESEARCH §Pattern 1: query A = `select(Market).where(Market.group_id.is_(None)).where(status IN visible).options(selectinload(Market.outcomes))` + conditional `q`/`category`/`status` filters + `.limit(CATALOG_LIMIT)`; query B = `select(MarketGroup).options(selectinload(MarketGroup.markets).selectinload(Market.outcomes))` + conditional `q`/`category` + `.limit(CATALOG_LIMIT)`; build items, skip groups with `<2` children, derive event status by IMPORTING `derive_event_status` + `ChildStatus` from `app.settlement.event_service` (build ChildStatus exactly as `_derive_status` does, case-insensitive YES), map derived→public status (partially_resolved→"open", void→"resolved"), post-derive Python status filter for events, compute event volume = `sum(child.volume)`, event deadline = min open-child deadline; merge A+B, sort in Python by the requested key, `return items[:CATALOG_LIMIT]`. Define `CATALOG_LIMIT = 100` and `CLOSING_SOON_WINDOW = timedelta(hours=48)` module constants. Use `scalars().all()` (NEVER `scalar_one()`) in the list path. `get_event` mirrors `_load_group_with_children` but `.where(MarketGroup.slug == slug)` + `scalar_one_or_none`. `list_categories` = two `select(col).where(category isnot None, category != "").distinct()` (markets restricted to `group_id IS NULL` + visible status; groups unrestricted) merged with `sorted(set(...) | set(...))`. Use `Market.question.ilike(f"%{q}%")` / `MarketGroup.title.ilike(f"%{q}%")` bound params — NEVER `text(f"... ILIKE '%{q}%'")`, NEVER any Gamma client call. This service module MAY keep `from __future__ import annotations` (only the router must omit it).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/catalog --co -q && cd backend && uv run python -c "from app.catalog.service import CatalogService, CATALOG_LIMIT; from app.catalog.schemas import CatalogItem, EventDetail; assert CATALOG_LIMIT == 100"</automated>
  </verify>
  <acceptance_criteria>
    - `app/catalog/service.py` imports `derive_event_status` and `ChildStatus` from `app.settlement.event_service` (reuses Phase 15, defines no new status logic).
    - `CATALOG_LIMIT == 100` and `list_catalog` slices `[:CATALOG_LIMIT]` (bounded; no pagination params).
    - `app/catalog/service.py` contains `.ilike(` and does NOT contain `text(` nor any `Gamma`/`public-search` reference (search is local pg_trgm only).
    - `list_catalog` uses `scalars().all()` and contains no `scalar_one(` (BRW-05 empty-safety; Pitfall 1).
    - `app/catalog/schemas.py` contains `Literal["market", "event"]` and serializes `volume` + `yes_price` to `str` (money as JSON strings).
    - Both `MarketGroup.markets` and `Market.outcomes` reads go through `selectinload` (no bare relationship access).
  </acceptance_criteria>
  <done>CatalogService implements Approach B with derived event status imported from Phase 15, local-only ILIKE search, bounded LIMIT 100, the public status mapping, and event detail / categories reads; schemas discriminate market vs event with money as strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: public_catalog_router (/catalog, /events/{slug}, /categories)</name>
  <files>backend/app/catalog/router.py</files>
  <read_first>
    - CREATE: backend/app/catalog/router.py (new)
    - ANALOG (public router decl + flat-list endpoint + slug-404): backend/app/markets/router.py — `public_market_router` decl (lines 143-146, NOTE: NO `from __future__ import annotations`), `list_markets_public` flat `list[...]` (lines 149-155), `get_market_public` 404 shape (lines 158-173), the `Literal` allowlist Query idiom (line 180: `window: Annotated[Literal["24h","7d","30d"], Query()] = "7d"`)
    - SOURCE OF TRUTH: backend/app/catalog/service.py (from Task 1) — the `CatalogService` methods to call; backend/app/catalog/schemas.py — `CatalogItem`, `EventDetail`
    - SOURCE OF TRUTH (derived status for the detail response): backend/app/settlement/event_service.py `derive_event_status` (line 98)
    - RESEARCH: §Pattern 3 event detail (lines 282-307), §Security V5 (lines 762, status/sort as Literal → 422 before service)
  </read_first>
  <behavior>
    - GET /api/v1/catalog returns 200 + JSON array of catalog items; an empty filter combo returns 200 + [].
    - status query value outside {open, closing_soon, resolved} → 422 (FastAPI Literal validation, before the service).
    - sort query value outside {volume, closing_soonest, newest} → 422.
    - GET /api/v1/events/{slug} on a ≥2-child group → 200 + EventDetail (per-outcome rows + derived status); on a 1-child group or missing slug → 404.
    - GET /api/v1/categories → 200 + JSON array of category name strings.
  </behavior>
  <action>
    Create `backend/app/catalog/router.py`. **OMIT `from __future__ import annotations`** (FastAPI 3.13 `Annotated[..., Depends()]` gotcha — Pitfall 5; mirror the documented omission in `settlement/router.py:16-18`); add the same load-bearing comment noting why it is absent. Declare `public_catalog_router = APIRouter(prefix="/api/v1", tags=["catalog"])`. Add `@public_catalog_router.get("/catalog", response_model=list[CatalogItem])` taking `session: Annotated[AsyncSession, Depends(get_async_session)]`, `q: Annotated[str | None, Query(max_length=200)] = None`, `category: Annotated[str | None, Query(max_length=100)] = None`, `status: Annotated[Literal["open","closing_soon","resolved"] | None, Query()] = None`, `sort: Annotated[Literal["volume","closing_soonest","newest"], Query()] = "volume"` → returns `await CatalogService.list_catalog(session, q=q, category=category, status=status, sort=sort)` (the service already returns the schema-shaped items, or `model_validate` each). Add `@public_catalog_router.get("/events/{slug}", response_model=EventDetail)`: load `group = await CatalogService.get_event(session, slug)`; `children = list(group.markets) if group else []`; if `group is None or len(children) < 2`: `raise HTTPException(status_code=404, detail="Event not found")`; else assemble per-outcome `EventOutcomeRead` rows (each child's `group_item_title` label + its case-insensitive YES `current_odds` as the `yes_price`) + the derived status via `derive_event_status` and return `EventDetail`. Add `@public_catalog_router.get("/categories", response_model=list[str])` → `await CatalogService.list_categories(session)`. NO `Depends(current_active_admin)` on any route (public reads are intentionally unauthenticated). Expose only outcome label + YES price + derived status in the detail — never resolver identity, per-user payout, or justification author (V4 public-safe projection). Add `__all__ = ["public_catalog_router"]`. Use runtime imports for `AsyncSession`.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.catalog.router import public_catalog_router; paths=[r.path for r in public_catalog_router.routes]; assert '/api/v1/catalog' in paths and '/api/v1/events/{slug}' in paths and '/api/v1/categories' in paths, paths"</automated>
  </verify>
  <acceptance_criteria>
    - `app/catalog/router.py` does NOT contain `from __future__ import annotations` (router omission rule) and carries the comment explaining the omission.
    - The router registers exactly `/api/v1/catalog`, `/api/v1/events/{slug}`, `/api/v1/categories`.
    - `status` and `sort` query params are typed `Literal[...]` (bad values 422 before the service runs).
    - No route on `public_catalog_router` has a `Depends(current_active_admin)` (reads are public).
    - The `/events/{slug}` handler raises 404 when `group is None or len(children) < 2`.
    - The python import smoke check exits 0.
  </acceptance_criteria>
  <done>The public catalog router exposes the three read endpoints under /api/v1 with Literal-validated filters, the ≥2-child 404 gate on event detail, and no auth on reads; it imports without the future-annotations footgun.</done>
</task>

<task type="auto">
  <name>Task 3: Catalog integration tests (BRW-01..05 + categories + event detail)</name>
  <files>backend/tests/catalog/test_catalog_router.py, backend/tests/catalog/test_categories.py, backend/tests/catalog/test_event_detail.py</files>
  <read_first>
    - CREATE: backend/tests/catalog/test_catalog_router.py, backend/tests/catalog/test_categories.py, backend/tests/catalog/test_event_detail.py (all new)
    - DEPENDS ON (import, do NOT redefine): backend/tests/catalog/conftest.py (the `api` AsyncClient fixture) + backend/tests/catalog/_factories.py (make_market / make_event / per-state helpers) — both from plan 16-01
    - ANALOG (public-read assertions): backend/tests/markets/test_public_router.py (whole file; flat-list shape, the back-compat `assert isinstance(body, list)` at line 86)
    - SOURCE OF TRUTH (behaviors to assert): 16-RESEARCH.md §Validation Architecture Test Map (lines 723-737) — test names test_search_local_only / test_status_filter / test_sort / test_empty_combos; §Pitfall 1 (every filter combo → 200 + [])
    - SOURCE OF TRUTH (status mapping under test): 16-RESEARCH.md Open Q2 (lines 688-690): partially_resolved→"open", void→"resolved"
  </read_first>
  <action>
    Create `backend/tests/catalog/test_catalog_router.py` covering: `test_catalog_returns_bounded_list` (seed a mix; assert 200, `isinstance(body, list)`, `len(body) <= 100`, every item has a `type` in {"market","event"}, no 1-child group surfaces as an event); `test_search_local_only` (seed a market whose question contains a unique token + a non-matching one; `GET /api/v1/catalog?q=<token>` returns only the match; assert no outbound Gamma call — the test never patches a Gamma client and the result is purely local); `test_status_filter` (seed open + resolved + a partially_resolved event + a void event; assert `?status=open` includes the open + partially_resolved event, `?status=resolved` includes resolved + void event, `?status=closing_soon` returns only OPEN within 48h); `test_sort` (assert `?sort=volume` desc with an event ranked by `sum(child.volume)`, `?sort=newest` by created_at desc, `?sort=closing_soonest` by deadline asc); `test_empty_combos` (several guaranteed-empty filter combinations e.g. `?category=__none__&status=resolved&q=zzzzz` each return `200` + `[]`, NEVER an error); `test_bad_status_and_sort_422` (`?status=bogus` and `?sort=bogus` each 422). Create `backend/tests/catalog/test_categories.py`: `test_categories_union_nonempty` (seed markets + groups across categories incl. one NULL/empty category; assert the response is the sorted DISTINCT union and excludes NULL/empty — CAT-06). Create `backend/tests/catalog/test_event_detail.py`: `test_event_detail_two_child` (≥2-child group → 200, per-outcome rows carry the `group_item_title` label + YES price as a string, status is the derived value), `test_event_detail_single_child_404` (1-child group slug → 404), `test_event_detail_missing_404` (random slug → 404). All money/odds assertions check JSON string type. Reuse the `api` fixture + factories; use `async_session` from the parent conftest for seeding. Per-module run only (Windows-worktree-safe).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/catalog -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/catalog -x` exits 0 (all catalog tests green on this module).
    - `test_search_local_only` proves a `q` match comes from local rows and asserts no Gamma proxy occurred (BRW-01; the search anti-feature).
    - `test_empty_combos` asserts `200` + `[]` (not an error) for ≥3 guaranteed-empty filter combinations (BRW-05; Pitfall 1).
    - `test_status_filter` asserts the partially_resolved→open and void→resolved public mapping (BRW-03).
    - `test_event_detail_single_child_404` and `test_event_detail_missing_404` both assert status_code 404 (EVT-07 gating).
    - `test_categories_union_nonempty` asserts NULL/empty categories are excluded (CAT-06).
    - yes_price / volume assertions verify the value is a JSON `str`.
  </acceptance_criteria>
  <done>Integration tests prove search is local-only, every filter combination is bounded and empty-safe, status/sort work with the derived-event mapping, event detail gates on ≥2 children, and categories union excludes empties — all green per-module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public client → catalog API (no auth) | Untrusted `q`/`category`/`status`/`sort` query input crosses here; reads are intentionally unauthenticated. |
| catalog service → Postgres | `q` reaches the DB; must be a bound param, never interpolated SQL. |
| catalog service → Gamma (FORBIDDEN) | Search must NOT cross to Gamma `/public-search` — local rows only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | SQL injection via `q` search term | mitigate | SQLAlchemy bound param `.ilike(f"%{q}%")` (the `%…%` wrapper is data); no `text(f"… ILIKE '%{q}%'")`. Acceptance criterion asserts `app/catalog/service.py` contains no `text(`. |
| T-16-02 | Tampering | injection / invalid value via `status`/`sort` | mitigate | `Literal["open","closing_soon","resolved"]` / `Literal["volume","closing_soonest","newest"]` Query params → FastAPI 422 before the service. Test `test_bad_status_and_sort_422`. |
| T-16-03 | DoS | resource exhaustion via unbounded catalog | mitigate | hard `LIMIT 100`, no pagination; each sub-query individually capped at 100. Acceptance criterion asserts `CATALOG_LIMIT == 100` + `[:CATALOG_LIMIT]` slice. |
| T-16-04 | Information Disclosure | leaking resolver identity / per-user payout / justification author in public reads | mitigate | catalog + event schemas expose only outcome label + YES price + derived status; no `resolution_justification` author, no payout, no `actor_user_id`. |
| T-16-05 | Spoofing/coupling | proxying player search to a third party (Gamma) | mitigate | search is local pg_trgm only; `test_search_local_only` asserts no Gamma proxy; acceptance criterion forbids a Gamma reference in the service. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero new deps — RESEARCH §Package Legitimacy Audit "not applicable"). |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/catalog -x` green (search-local-only, status/sort filters, every-combo-empty-safe, LIMIT-100 bound, categories union, event-detail ≥2-child gating).
- `app/catalog/service.py` contains `.ilike(` and `selectinload(`, and contains no `text(` / no Gamma reference / no `scalar_one(` in the list path.
- `app/catalog/router.py` does not contain `from __future__ import annotations`; registers exactly the three `/api/v1` routes; no `Depends(current_active_admin)` on reads.
- No edit to `app/main.py` in this plan (registration is plan 16-05, sequenced).
</verification>

<success_criteria>
- BRW-01: indexed local ILIKE search over `markets.question` + `market_groups.title`, never Gamma.
- BRW-02: `/categories` non-empty union + `/events/{slug}` per-outcome rows + derived status (≥2-child only).
- BRW-03: status filter {open, closing_soon, resolved} with the derived-event mapping.
- BRW-04: sort {volume (event = SUM children), closing_soonest, newest}.
- BRW-05: bounded `LIMIT 100`, no pagination, every filter combo → explicit empty/zero (200 + []).
</success_criteria>

<output>
Create `.planning/phases/16-catalog-event-api-house-event-crud/16-02-SUMMARY.md` when done
</output>

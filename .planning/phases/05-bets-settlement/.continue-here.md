---
context: phase
phase: 05-bets-settlement
owner: Agustin
status: in_progress
last_updated: 2026-05-27
branch: gsd/phase-05-bets-settlement
head: efbacbd
---

# ▶ RESUME HERE — Phase 5 (Bets, Settlement & First E2E Demo)

**30-second orientation:** Phase 5 is mid-flight on branch `gsd/phase-05-bets-settlement`.
Bet placement (SC#1), sign-up bonus (SC#4) and the **pure settlement-core math**
(payout + settlement plan) are SHIPPED-LOCAL and green. **Phase 4 (Markets) is now MERGED
to `origin/main` (PR #6)** — the old "Phase 4 not started" blocker is GONE. The next real
work is the **transactional `SettlementService.resolve_market`** wired to Phase 4's real
models — but it is gated by a **migration-numbering collision** that must be resolved first
(see BLOCKING CONSTRAINTS). Do the integration deliberately; it touches the shared migration
chain and the Phase 4↔5 contract.

**First action when resuming:** read this whole file, then read
`.planning/phases/05-bets-settlement/INTEGRATION.md`-equivalent facts below (§Integration
Coordinates) and decide the migration-reconciliation sequence WITH Pol before writing code.

---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through repo inspection this session. Acknowledge before proceeding.

- [ ] **CONSTRAINT: Alembic migration `0003` COLLISION.** Phase 3 (wallet, unmerged, rides
  inside this branch) and Phase 4 (markets, merged to main) BOTH created a revision numbered
  `0003` off `0002`:
  - `origin/main`: `0001 → 0002 → 0003_phase4_markets`
  - this branch (`HEAD`): `0001 → 0002 → 0003_phase3_wallet_ledger`
  Integrating Phase 5 onto the Phase-4 main produces **two `0003` heads** → alembic
  `Multiple head revisions`. **Mitigation (required, do NOT skip):** when Phase 3 lands on the
  Phase-4 main, RENUMBER `0003_phase3_wallet_ledger` → `0004_phase3_wallet_ledger` with
  `down_revision = "0003_phase4_markets"`; then Phase 5's bets migration becomes
  `0005_phase5_bets` off `0004`. Confirm a single head with
  `uv run --directory backend python -m alembic heads` (expect exactly one).

- [ ] **CONSTRAINT: Phase 3 (Wallet & Ledger) is NOT on `main`.** `app/wallet/` on
  `origin/main` is only the `__init__.py` placeholder. The ENTIRE Phase 3 implementation lives
  on `gsd/phase-03-wallet-double-entry-ledger` AND is carried inside THIS branch (that is why
  HEAD is **37 ahead / 16 behind** origin/main). Phase 5 cannot be merged before Phase 3 is
  merged (Phase 5 depends on the wallet/ledger). The integration order is **Phase 3 → then
  Phase 5** (see §Recommended Path).

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Blind rebase onto new main | Rebasing this branch straight onto `origin/main` will collide on `0003` and may try to re-apply the whole (unmerged) Phase 3 | blocking | Resolve Phase 3's merge + migration renumber FIRST; rebase Phase 5 only after Phase 3 is on main; drop the duplicated Phase 3 commits during rebase |
| Guessing the odds/payout model | Settlement payout depends on Phase 4's odds representation | advisory | Already neutralized: `compute_payout(stake, price)` takes price as an INPUT. At integration, CONFIRM `Outcome.current_odds` is a probability/price in (0,1] before feeding it in (see §Integration Coordinates) |
| Editing shipped place_bet/Bet/port casually | Capturing odds-at-placement edits already-green code + the shared port DTO | advisory | Do it test-first (TDD), keep the `MarketReadPort` change additive, re-run the bets suite |
| Churning STATE.md / PHASES.md on this branch | Both differ from main; editing invites rebase conflicts | advisory | Leave them; this handoff is the source of truth until the Phase 3 merge + Phase 5 rebase reconcile them |

<current_state>
Branch `gsd/phase-05-bets-settlement` @ `efbacbd`, working tree CLEAN. This was a
post-PC-restart resume session. No `/gsd-plan-phase` was ever run for Phase 5 — it is being
built as disciplined, additive, atomic TDD commits (no `.planning/phases/05-*/PLAN.md`; this
`.continue-here.md` is the de-facto plan/handoff). All new code is in NEW modules only
(`app/bets/`, `app/settlement/`) — zero edits to shipped wallet/auth code, migration-free
(single head `0003_phase3_wallet_ledger` on this branch).
</current_state>

<completed_work>

## This session (post-restart) — 2 atomic commits, pure settlement core
- `5d70cbf` feat(05): pure payout & P&L math — `app/settlement/payout.py`
  (`compute_payout(stake,price)=stake/price`, `profit_or_loss(stake,payout)=payout-stake`,
  `quantize_money` 4-dp ROUND_HALF_UP). 15 pure unit tests (no Docker).
- `efbacbd` feat(05): settlement plan — `app/settlement/plan.py`
  (`build_settlement_plan(bets,*,winning_outcome_id)` → per-bet won/lost + payout + pnl +
  status, plus aggregates `total_payout` [winners→wallet] / `total_loser_stake`
  [losers→house_revenue]; `BetToSettle`/`SettledBet`/`SettlementPlan` dataclasses). 6 unit tests.
- Gates green: ruff + ruff format + mypy + money-lint. settlement 21/21 unit + bets 18/18
  testcontainer (Docker). Started Docker Desktop this session to run integration tests.

## Earlier in Phase 5 (pre-restart) — already on the branch
- `fab9eb9` chore: mark phase 05 in progress + Phase 4/5 parallel ownership handshake note.
- `5969072` feat(05): sign-up bonus on email verify — `WalletService.grant_signup_bonus`
  (SC#4 / WAL-02 / ADU-03). idempotency_key `bonus:{user_id}`.
- `da46970` feat(05): `MarketReadPort` Protocol + `MarketView`/`OutcomeView` DTOs + `Bet` model
  (`app/bets/`; market_id/outcome_id are plain UUIDs, NO FK — migration-free).
- `fe3b5d9` feat(05): `BetService.place_bet` — atomic ACID bet placement vs the port (SC#1).
</completed_work>

<remaining_work>

## Phase 5 success criteria (ROADMAP.md §"Phase 5", 9 SC)
- ✅ SC#1 — place-bet ACID + kill-mid-tx (`fe3b5d9`)
- ❌ SC#2 — bet API 403 when email unverified / banned (NO bets router yet)
- ❌ SC#3 — confirm modal + per-market min/max stake (server+client) + sell→405
- ✅ SC#4 — sign-up bonus on email verify (`5969072`)
- 🟡 SC#5 — admin two-step resolve + `SettlementService.resolve_market` ACID
  (**pure core DONE**: payout + plan; transactional ledger-posting NOT done)
- 🟡 SC#6 — idempotent settlement (design chosen: `transfers.idempotency_key='settle:{bet_id}'`
  using the EXISTING UNIQUE — no new constraint/migration; NOT implemented)
- ❌ SC#7 — resolution display + portfolio (open/settled P&L) — frontend + portfolio read API
- 🟡 SC#8 — admin reversal via compensating entries (pure plan reusable; transactional not done)
- ✅ SC#9 — admin manual recharge (delivered in Phase 3: `POST /admin/wallets/{id}/recharge`)

## Concrete units of work left
1. **Capture odds-at-placement on the Bet** (the Phase 4 odds seam): extend `OutcomeView` with
   `price: Decimal`; add `Bet.price` (or `odds_at_placement`) column (bets table is still
   migration-free → the column is free); capture it in `place_bet`. Test-first; re-run bets suite.
2. **`MarketResolvePort` write contract** + transactional **`SettlementService.resolve_market`**
   (SC#5/6/8): consume the shipped `build_settlement_plan`, apply to the ledger via
   `WalletService._post_transfer` (winners market_liability→user_wallet with house_promo top-up
   for the fixed-odds shortfall; losers market_liability→house_revenue), idempotency via
   `transfers.idempotency_key='settle:{bet_id}'`, audit via `AuditService.record`
   (event `settlement.resolved`), reversal via compensating entries (`settlement.reversed`).
   Docker integration tests.
3. **Bet API router** (SC#2): `POST /bets`, gated by verified + not-banned (403), wired to a
   real `MarketReadPort` adapter over Phase 4's `app/markets`.
4. **Stake limits + sell→405** (SC#3) — needs an operator min/max source (TenantConfig).
5. **Portfolio read API + resolution display** (SC#7) using `compute_payout`/`profit_or_loss`
   for unrealized (current odds) and realized (settled) P&L; frontend.
6. **Migration `0005_phase5_bets`** (bets table + FKs to markets/outcomes) — write ONLY after
   the `0003`→`0004` renumber so the chain is `…→0004_wallet→0005_bets`.
7. **First E2E demo** (bet → admin resolve → wallet credited → P&L) — the Phase 5 milestone.
</remaining_work>

<decisions_made>

## Architecture decisions (this session + carried)
- **Payout model = "odds locked at placement"** (`.planning/research/ARCHITECTURE.md`): a
  winning bet's gross payout is `stake / price`; loser pays 0; **no house edge in v1** (v2 MAYBE
  per FEATURES.md). `price` is the chosen outcome's price/probability captured at placement.
- **Pure-core-first split**: settlement is split into pure math (`payout.py`, `plan.py` — no I/O,
  unit-tested, reusable UNCHANGED by Phase 7 Polymarket auto-resolution) and a transactional
  shell (`SettlementService`, gated, integration-tested). The pure core takes `price` as an
  INPUT, so it has ZERO Phase 4 coupling.
- **Money quantization**: 4-dp `ROUND_HALF_UP` (`quantize_money`) to match `NUMERIC(18,4)`
  (`app/db/types.py`); Decimals from strings, never float (WAL-05).
- **Idempotent settlement WITHOUT a new constraint**: reuse the existing
  `transfers.idempotency_key UNIQUE` with key `settle:{bet_id}` instead of adding a new
  `(bet_id, event_type)` UNIQUE to `entries` — preserves the single-head invariant during
  parallel dev. (ROADMAP SC#6 mentions the entries constraint as ONE option; this achieves the
  same "zero double-payouts" guarantee on the existing schema.)
- **Additive, low-risk strategy (preserve)**: new modules only, migration-free, validated Phase 3
  ledger reused (`_post_transfer`/`transfer`) and NOT modified.
- **STATE.md / PHASES.md deliberately NOT churned on this branch** to avoid rebase conflicts with
  Pol's parallel Phase 3/4 work on those shared files.
</decisions_made>

<context_update_phase4_merged>

## ⚠️ CONTEXT UPDATE (2026-05-27): Phase 4 is DONE — re-evaluate dependencies
Per Agustin: **Pol finished Phase 4; Pol is now on Phase 6; Agustin will take Phase 7 next.**
Verified in the repo: **`origin/main` @ `eba7a41` merged Phase 4 via PR #6**
(`gsd/phase-04-markets-domain-houseadapter`). Future sessions must NOT assume "Phase 4 not started".

**Blockers REMOVED by the Phase 4 merge:**
- The real Markets domain now exists on `main` → the Phase 5 `MarketReadPort` stub can be swapped
  for a real adapter, the admin resolution endpoint can be wired, and the E2E demo is buildable.
- Migration `0005_phase5_bets` (bets table + FKs to markets/outcomes) is now writable — it just
  must chain after the renumbered wallet migration (see BLOCKING CONSTRAINTS), not the old "0004".

**Blockers / risks that REPLACED it (the new reality):**
- The `0003` migration collision (BLOCKING CONSTRAINT #1).
- Phase 3 wallet is still unmerged (BLOCKING CONSTRAINT #2) → Phase 5 cannot complete/merge until
  Phase 3 lands on the Phase-4 main.
- `PHASES.md` on THIS branch still shows "Phase 4 ⬜ Not started" — STALE. `origin/main` reflects
  Phase 4 done. Do not trust this branch's PHASES.md for Phase 4 status until rebase reconciles it.
</context_update_phase4_merged>

<integration_coordinates>

## Phase 4 surface to integrate against (from `origin/main` @ `eba7a41`, read-only inspected)
- **Models** `backend/app/markets/models.py`:
  - `Market` (table `markets`): `id`, `question`, `slug`, `resolution_criteria`, `category`,
    `source` (HOUSE|POLYMARKET), `source_market_id`, `condition_id`, `status` (String(20)),
    `deadline` (tz-aware), `bet_count`, `created_at/updated_at/closed_at/resolved_at`, `tenant_id`;
    relationships `outcomes`, `odds_snapshots` (both `lazy="raise"` → must eager-load).
  - `Outcome` (table `outcomes`): `id`, `market_id` (FK→markets, ON DELETE CASCADE), `label`,
    **`initial_odds` Numeric(8,6)**, **`current_odds` Numeric(8,6)**, `tenant_id`.
  - `OddsSnapshot` (table `odds_snapshots`): `id`, `market_id`, `outcome_id`,
    **`probability` Numeric(8,6)**, `snapshot_at`, `tenant_id`.
- **Enums** `backend/app/markets/enums.py`: `MarketStatus` = DRAFT|OPEN|CLOSED|RESOLVED|CANCELLED;
  `MarketSourceEnum` = HOUSE|POLYMARKET.
- **Protocol** `backend/app/integrations/market_source.py` — Pol's real `MarketSource` (compare
  with Phase 5's `app/bets/market_port.py:MarketReadPort`; decide adapt-vs-consume at integration).
- **Service/router/schemas**: `backend/app/markets/{service,router,schemas}.py`.
- **Migration**: `0003_phase4_markets` (down_revision `0002_phase2_auth`) → creates
  `markets`, `outcomes`, `odds_snapshots`.

## ⭐ The odds seam — ONE thing to confirm before wiring payouts
Phase 5's `compute_payout(stake, price)` expects `price` = a **probability/price in (0,1]**
(e.g. 0.5 for "50/50", giving 2× payout). Phase 4 exposes `Outcome.current_odds`/`initial_odds`
as `Numeric(8,6)`, and ADM-02 says default "50/50".
**MUST VERIFY at integration:** is `current_odds` stored as a probability (≈0.5) or as decimal
odds (≈2.0)? If probability → feed it straight into `compute_payout(stake, outcome.current_odds)`.
If decimal odds → use `price = 1 / current_odds`. Check Phase 4's `MarketService`/HouseAdapter
seed + a created-market row before depending on it.

## Status mapping
Phase 5's string constants `MARKET_OPEN/CLOSED/RESOLVED` (`app/bets/market_port.py`) align with
`MarketStatus.OPEN/CLOSED/RESOLVED`. Phase 4 adds DRAFT + CANCELLED — decide bet-eligibility
(only OPEN + before deadline accepts bets; DRAFT/CLOSED/RESOLVED/CANCELLED reject).
</integration_coordinates>

## Required Reading (in order, when resuming)
1. THIS file (top to bottom).
2. `PHASES.md` (repo root) — but treat its Phase 4 row as STALE on this branch (Phase 4 IS merged).
3. `.planning/ROADMAP.md` §"Phase 5: Bets, Settlement & First End-to-End Demo" (the 9 SC).
4. `.planning/REQUIREMENTS.md` — BET-01..07, STL-02..07, ADM-05, WAL-02, ADU-03.
5. `backend/app/settlement/{payout,plan}.py` — the shipped pure core (what the transactional layer consumes).
6. `backend/app/bets/{service,market_port,models,constants,exceptions}.py` — bet placement + the port.
7. `backend/app/wallet/service.py` — the ledger primitives to reuse (`_post_transfer`, `transfer`, `_resolve_user_wallet_id`).
8. On `origin/main`: `backend/app/markets/{models,enums,service}.py` + `app/integrations/market_source.py` (Phase 4 integration surface).
9. `.planning/research/ARCHITECTURE.md` (payout model, "odds locked at placement") + `PITFALLS.md` (settlement idempotency / reversal).

## Infrastructure State
- **Docker Desktop**: started this session; testcontainers Postgres works. Integration tests:
  `cd backend && uv run pytest tests/<dir>` (run per-directory — the whole-suite one-process run
  hits the pre-existing DEF-03-01 audit-immutability session-poisoning; per-dir is green).
- **Toolchain**: Python 3.12/3.13 via `uv` (0.11.x); `uv run pytest|ruff|mypy`; money-lint =
  `uv run python scripts/lint_money_columns.py`. Bare `alembic` is BLOCKED on Windows →
  `uv run --directory backend python -m alembic …`.
- **Session rooting**: this was a HOME-rooted session → NO `mcp__github__*` (the documented
  GOTCHA). PRs are Pol's via GitHub MCP `create_pull_request` from a REPO-ROOTED session. `git`
  (fetch/commit) works from home via Git Credential Manager.
- **Secrets**: NEVER store/commit `GITHUB_PERSONAL_ACCESS_TOKEN` or `LINEAR_API_KEY`.

## ⭐ Recommended next implementation path (do in this order)
1. **Coordinate with Pol** on the Phase 3 merge + the `0003`→`0004` renumber (it touches the
   shared migration chain; Phase 3 is the wallet phase). Decide who renumbers/merges Phase 3.
2. **Land Phase 3 on the Phase-4 main**: `/gsd-verify-work 3` → `/gsd-code-review` →
   `/gsd-secure-phase 3`; renumber `0003_phase3_wallet_ledger`→`0004_phase3_wallet_ledger`
   (`down_revision="0003_phase4_markets"`); PR → Pol merges. Now main = Phase 1+2+3+4, single head `0004`.
3. **Rebase Phase 5** onto the new main: drop the now-merged Phase 3 commits, keep ONLY the
   Phase-5 commits (`fab9eb9`, `5969072`, `da46970`, `fe3b5d9`, `5d70cbf`, `efbacbd`). Verify one
   alembic head.
4. **Odds seam (TDD)**: extend `OutcomeView` with `price`; add `Bet.price`; capture in `place_bet`;
   confirm the `current_odds` probability-vs-decimal-odds question.
5. **Transactional `SettlementService.resolve_market`** (SC#5/6/8) consuming `build_settlement_plan`
   + `MarketResolvePort` (mark Market RESOLVED via `app/markets/service`) + audit + reversal +
   Docker integration tests.
6. **Migration `0005_phase5_bets`** (bets table + FKs to markets/outcomes) off `0004`.
7. **Bet API router** (SC#2) + **portfolio/resolution display** (SC#7) + **stake limits/sell-405**
   (SC#3) + **frontend** + **E2E demo** (the Phase 5 milestone).
8. Mark `PHASES.md` row 5 `👀 In review` + PR#, `/gsd-ship` from a repo-rooted session
   (PR via GitHub MCP — only Pol merges).

<next_action>
Start by reading this file fully, then (with Pol) settle the Phase-3-merge / `0003`→`0004`
renumber sequence. Do NOT write Phase 5 integration code until the migration chain is linearized
and this branch is rebased onto the Phase-4 main. The pure settlement core (`payout.py`,
`plan.py`) is done and green — build the transactional `SettlementService` on top of it next.
</next_action>

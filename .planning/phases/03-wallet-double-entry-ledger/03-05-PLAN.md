---
phase: 03-wallet-double-entry-ledger
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - backend/app/wallet/schemas.py
  - backend/app/wallet/router.py
  - backend/app/wallet/service.py
  - backend/app/main.py
  - backend/tests/wallet/test_money_serialization.py
  - backend/tests/wallet/test_stripe_stub.py
  - frontend/src/app/wallet/page.tsx
  - frontend/src/app/wallet/__tests__/wallet-page.test.tsx
autonomous: true
requirements: [WAL-03, WAL-04, PLT-05]
must_haves:
  truths:
    - "A logged-in player can GET their current wallet balance"
    - "A player can GET a paginated transaction history (kind, amount, direction, timestamp, reason)"
    - "Every money value in the API response is a JSON string, never a float"
    - "A player can only read their OWN wallet (no cross-user read)"
    - "WalletService.recharge(payment_provider='stripe') raises NotImplementedError"
    - "A disabled 'Add funds' button is present in the player wallet UI"
  artifacts:
    - path: "backend/app/wallet/router.py"
      provides: "GET /wallet/me/balance + GET /wallet/me/transactions (player cookie)"
      contains: "current_active_player"
    - path: "backend/tests/wallet/test_money_serialization.py"
      provides: "SC#4 — asserts raw JSON balance/amount is a string, not a float"
      contains: "isinstance"
    - path: "frontend/src/app/wallet/page.tsx"
      provides: "Player wallet page with balance, history, and a DISABLED Add funds button (SC#6)"
      contains: "disabled"
  key_links:
    - from: "backend/app/wallet/router.py"
      to: "current_active_player"
      via: "Depends gate binds reads to the caller's own wallet"
      pattern: "current_active_player"
    - from: "backend/app/wallet/router.py"
      to: "WalletService.get_balance / get_transactions"
      via: "read-only service calls scoped to the authenticated user"
      pattern: "get_balance|get_transactions"
    - from: "frontend/src/app/wallet/page.tsx"
      to: "Add funds disabled button"
      via: "shadcn Button disabled attribute"
      pattern: "disabled"
---

<objective>
Ship the player-facing read slice (SC#4) and the Stripe stub (SC#6). Backend: `GET /wallet/me/balance`
and `GET /wallet/me/transactions?page=` gated by the Phase 2 `current_active_player` cookie dependency,
scoped to the caller's OWN wallet (no cross-user read). All money serialized as JSON strings via the
`MoneyStr` type (RESEARCH Pattern 4) — with an explicit test asserting the raw JSON is a string, not a
float (PITFALLS "Looks Done But Isn't"). Stripe stub: `WalletService.recharge(payment_provider="stripe")`
raises `NotImplementedError` (method signature ready for v2 with no refactor), plus a disabled
"Add funds" button in the player wallet UI.

After this slice a real player can: log in, see their balance, scroll their transaction history, and
see (but not use) the future "Add funds" affordance.

Purpose: complete the user-visible wallet capability (WAL-03 balance, WAL-04 history) with the float
firewall (SC#4) and the Stripe door-open stub (PLT-05 / SC#6).
Output: player read schemas + router, the read methods on WalletService, the wiring in main.py, the
two backend tests, and the frontend wallet page + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-wallet-double-entry-ledger/03-CONTEXT.md
@.planning/phases/03-wallet-double-entry-ledger/03-RESEARCH.md
@.planning/phases/03-wallet-double-entry-ledger/03-VALIDATION.md
@.planning/phases/03-wallet-double-entry-ledger/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Player read schemas + WalletService read methods + GET /wallet/me/* router + main wiring</name>
  <files>backend/app/wallet/schemas.py, backend/app/wallet/router.py, backend/app/wallet/service.py, backend/app/main.py</files>
  <read_first>
    - backend/app/wallet/schemas.py (the FILE BEING EXTENDED — MoneyStr already defined in 03-04; reuse it; add the read response schemas here)
    - backend/app/wallet/service.py (the FILE BEING EXTENDED — add read methods alongside the 03-02 writers; keep them read-only)
    - .planning/phases/03-wallet-double-entry-ledger/03-RESEARCH.md (Pattern 4 MoneyStr + TransactionItem shape lines 295-320; WAL-04 keyset/offset pagination over entries joined to transfers, table line 66; SC#4 the response is JSON string)
    - backend/app/auth/router.py (current_active_player = fastapi_users_player.current_user(active=True, verified=True) — lines 78-79; import it via app.auth.deps)
    - backend/app/auth/deps.py (lazy re-export of current_active_player — lines 82-106)
    - backend/app/wallet/models.py (Account, Transfer, Entry — for the balance + history queries)
    - backend/app/wallet/constants.py (KIND_USER_WALLET, PLAY_USD)
    - backend/app/main.py (the FILE BEING MODIFIED — mount the wallet player router next to the existing includes; the wallet admin router was added in 03-04)
    - backend/app/db/session.py (get_async_session)
  </read_first>
  <behavior>
    - GET /wallet/me/balance (authenticated player) returns 200 with {balance: <string>, currency: "PLAY_USD"}.
    - GET /wallet/me/transactions?page=1 returns a paginated list of entries for the caller's wallet (kind, amount<string>, direction, created_at ISO, reason); page param controls offset; total/has_next surfaced.
    - An unauthenticated request (no cookie) returns 401.
    - A player only ever sees their OWN wallet/entries (the query is scoped by the authenticated user's id — there is no user_id path param).
    - balance and every amount serialize as JSON strings (MoneyStr).
  </behavior>
  <action>
    Extend `app/wallet/schemas.py` with `BalanceResponse(BaseModel)` (`balance: MoneyStr`, `currency: str`),
    `TransactionItem(BaseModel)` (`kind: str`, `amount: MoneyStr`, `direction: str`, `created_at: datetime`,
    `reason: str | None`), and `TransactionPage(BaseModel)` (`items: list[TransactionItem]`, `page: int`,
    `page_size: int`, `total: int`, `has_next: bool`). Reuse the existing `MoneyStr` (do not redefine).

    Extend `app/wallet/service.py` with read-only methods: `get_balance(session, *, user_id) -> Decimal`
    (SELECT the user_wallet account's balance for owner_id == user_id, kind KIND_USER_WALLET) and
    `get_transactions(session, *, user_id, page=1, page_size=50) -> tuple[list[rows], int]` (resolve the
    user's wallet account id, then SELECT entries joined to transfers WHERE entries.account_id == that
    wallet, ORDER BY entries.created_at DESC, with LIMIT/OFFSET pagination; return the page rows + a total
    count). Derive `reason` from `transfer.transfer_metadata.get("reason")` and `kind` from `transfer.kind`.
    These methods MUST NOT mutate or commit.

    Create `app/wallet/router.py` exposing `wallet_router = APIRouter(prefix="/wallet/me",
    tags=["wallet"])` with `GET /balance` and `GET /transactions`. Both take
    `player = Depends(current_active_player)` (imported from app.auth.deps) and
    `session = Depends(get_async_session)`. There is NO user_id parameter — the wallet is always the
    authenticated player's (cross-user read is structurally impossible). `GET /transactions` takes
    `page: int = Query(1, ge=1)` and an optional `page_size: int = Query(50, ge=1, le=200)`. Return
    `BalanceResponse` / `TransactionPage`. If the player somehow has no wallet, return balance 0 / empty
    page (defensive; registration guarantees a wallet).

    Wire `from app.wallet.router import wallet_router` + `app.include_router(wallet_router)` into
    `app/main.py` next to the admin router include from 03-04.
  </action>
  <verify>
    <automated>uv run --directory backend ruff check app/wallet/ app/main.py && uv run --directory backend python -c "from app.main import app; paths=[r.path for r in app.routes]; assert '/wallet/me/balance' in paths and '/wallet/me/transactions' in paths, paths; print('routes OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /wallet/me/balance` and `GET /wallet/me/transactions` are registered and gated by `current_active_player`.
    - Neither endpoint accepts a user_id param (reads are scoped to the authenticated player only — cross-user read impossible).
    - `BalanceResponse` and `TransactionItem` use `MoneyStr`; `TransactionPage` carries page/page_size/total/has_next.
    - `WalletService.get_balance` / `get_transactions` are read-only (grep: no commit/add/update inside their bodies).
    - ruff clean; app imports and lists the two routes.
  </acceptance_criteria>
  <done>Players can read their own balance and paginated history via cookie-gated endpoints; money is MoneyStr; reads are strictly self-scoped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#4 money-as-string test + SC#6 Stripe stub test</name>
  <files>backend/tests/wallet/test_money_serialization.py, backend/tests/wallet/test_stripe_stub.py</files>
  <read_first>
    - .planning/phases/03-wallet-double-entry-ledger/03-RESEARCH.md (SC#4 must assert raw JSON contains a quoted string line 320; SC#6 recharge(payment_provider="stripe") raises NotImplementedError line 585)
    - .planning/research/PITFALLS.md (Pitfall 4 lines 114-141 — Decimal serialized as string, never float; "Looks Done" Decimal-precision check line 475)
    - backend/tests/auth/test_login.py (how a player logs in + the cookie is set on the test client; reuse to authenticate the read endpoints)
    - backend/tests/wallet/conftest.py (funded wallet fixture — from 03-01; or register + recharge to create history)
    - backend/tests/conftest.py (client fixture; async_session)
    - backend/app/wallet/service.py (recharge — the stripe branch from 03-02)
    - backend/app/wallet/router.py + schemas.py (the read surface under test — from Task 1)
  </read_first>
  <behavior>
    - test_balance_is_json_string: GET /wallet/me/balance for a funded player returns a body whose balance field is a JSON string (isinstance(response.json()["balance"], str) AND a quoted-substring check on response.text), NEVER a float.
    - test_transaction_amounts_are_json_strings: after a recharge produces history, GET /wallet/me/transactions returns items whose amount is a string for every item.
    - test_transactions_paginated: with > page_size entries, page 1 and page 2 return disjoint slices and has_next behaves correctly.
    - test_player_cannot_read_without_auth: unauthenticated GET returns 401.
    - test_recharge_stripe_raises_not_implemented (SC#6): calling WalletService.recharge(..., payment_provider="stripe") raises NotImplementedError (pure unit, no DB).
  </behavior>
  <action>
    Create `tests/wallet/test_money_serialization.py` with `pytestmark = [pytest.mark.integration,
    pytest.mark.asyncio(loop_scope="session")]`. Authenticate a player (register or fixture + login to get
    the cookie), give them history (recharge via the admin endpoint from 03-04, or seed entries directly),
    then assert on the RAW JSON (both `isinstance(..., str)` and a `'"' in response.text` style check that a
    quoted number is present and a bare float is not). Cover the pagination + no-auth cases.

    Create `tests/wallet/test_stripe_stub.py` as a fast UNIT test (no Docker) asserting
    `pytest.raises(NotImplementedError)` around `WalletService.recharge(session=None_or_dummy, user_id=...,
    amount=Decimal("1"), reason="x", idempotency_key="k", payment_provider="stripe")` — the stripe branch
    must raise BEFORE touching the session, so a dummy/None session is acceptable. This keeps SC#6 in the
    Nyquist quick (non-integration) run.
  </action>
  <verify>
    <automated>uv run --directory backend pytest tests/wallet/test_money_serialization.py tests/wallet/test_stripe_stub.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_balance_is_json_string GREEN: balance is a JSON string in the raw response (SC#4 / WAL-03).
    - test_transaction_amounts_are_json_strings GREEN: every history amount is a string (SC#4 / WAL-04).
    - test_transactions_paginated GREEN: disjoint pages + correct has_next (WAL-04).
    - test_player_cannot_read_without_auth GREEN: 401 without a cookie.
    - test_recharge_stripe_raises_not_implemented GREEN as a unit test (SC#6 / PLT-05), runnable without Docker.
  </acceptance_criteria>
  <done>SC#4 (money-as-string) and SC#6 (Stripe stub raises) are proven, with SC#6 + the no-auth guard in the fast unit run.</done>
</task>

<task type="auto">
  <name>Task 3: Player wallet page with balance, history, and a DISABLED "Add funds" button (SC#6)</name>
  <files>frontend/src/app/wallet/page.tsx, frontend/src/app/wallet/__tests__/wallet-page.test.tsx</files>
  <read_first>
    - frontend/src/app/page.tsx (the minimal page shape + Tailwind classes — the house style)
    - frontend/src/components/ui/button.tsx (the shadcn Button — supports a `disabled` prop)
    - frontend/src/components/ui/card.tsx (Card for the balance/history layout)
    - frontend/src/lib/auth.ts (server-action + getBackendUrl pattern; how server-side fetch reads BACKEND_URL and forwards the session cookie — lines 49-86; mirror for a server-side wallet fetch)
    - frontend/src/app/admin/page.tsx (an existing authenticated page shape to mirror for layout/structure)
    - frontend/src/app/(auth)/__tests__/login.test.tsx (the Vitest + Testing Library render-test pattern to mirror)
    - frontend/vitest.config.ts + frontend/vitest.setup.ts (test runner config)
  </read_first>
  <action>
    Create `frontend/src/app/wallet/page.tsx` — a player wallet page (Server Component is fine) that renders:
    a balance card (it MAY fetch `GET /wallet/me/balance` server-side using the `getBackendUrl()` +
    cookie-forwarding pattern from lib/auth.ts, OR render a static shell that a follow-up phase wires to
    live data — but DO display a balance area and the currency PLAY_USD), a transaction-history section
    (list/table of recent entries; an empty-state is fine if not wired to live data yet), and a DISABLED
    "Add funds" button using the shadcn `Button` with the `disabled` attribute and a tooltip/caption like
    "Coming soon" (SC#6 / PLT-05 — the Stripe affordance is present but inert). Keep copy in ENGLISH and
    avoid the word "deposit" (PITFALLS #3 — say "Add funds" / "play balance", never "deposit").

    Create `frontend/src/app/wallet/__tests__/wallet-page.test.tsx` (Vitest + Testing Library, mirror
    login.test.tsx) asserting the page renders an "Add funds" button that is DISABLED (e.g.,
    `expect(screen.getByRole("button", {name: /add funds/i})).toBeDisabled()`), and that a balance/currency
    region is present. If the page does a server-side fetch, mock it so the test runs offline.

    Do NOT wire real Stripe — this is the inert stub only. Note in a comment that v2 enables the button
    behind the `stripe_recharge_enabled` feature flag (already seeded FALSE in Phase 1).
  </action>
  <verify>
    <automated>cd frontend && pnpm test:run -- src/app/wallet 2>/dev/null || pnpm test -- --run src/app/wallet</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/wallet/page.tsx` renders a balance/currency region, a transaction-history section, and an "Add funds" button.
    - The "Add funds" button is DISABLED (the Vitest test asserts `toBeDisabled()`) — SC#6 / PLT-05.
    - The wallet-page Vitest test passes (offline; any backend fetch is mocked).
    - Copy is English and avoids "deposit" (PITFALLS #3).
  </acceptance_criteria>
  <done>The player wallet page exists with a disabled "Add funds" button, satisfying the SC#6 UI half; the page test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player cookie -> read endpoints | Untrusted session crosses here; reads must bind to the caller's own wallet |
| API response -> client | Money must leave as a string, never a float |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Information Disclosure | Cross-user balance/history read | mitigate | `/wallet/me/*` bound to `current_active_player`; no user_id param; query scoped to authenticated user; test_player_cannot_read_without_auth |
| T-03-19 | Tampering | Money exposed as float (precision tampering) | mitigate | `MoneyStr` PlainSerializer on all money fields; test_balance_is_json_string + test_transaction_amounts_are_json_strings (SC#4) |
| T-03-20 | Elevation / regulatory | A live "Add funds" path enabling token purchase | mitigate | button is DISABLED + recharge(stripe) raises NotImplementedError; gated behind `stripe_recharge_enabled` (FALSE) for v2 (PLT-05; PITFALLS #3 no purchase) |
</threat_model>

<verification>
- `uv run --directory backend pytest tests/wallet/test_money_serialization.py tests/wallet/test_stripe_stub.py -q` green.
- `uv run --directory backend pytest -m "not integration" tests/wallet -q` green and INCLUDES the Stripe-stub unit test.
- `cd frontend && pnpm test:run` green (wallet-page test).
- `uv run --directory backend ruff check app/wallet/ app/main.py` clean; app lists `/wallet/me/balance` + `/wallet/me/transactions`.
</verification>

<success_criteria>
- Player can read own balance + paginated history; money is a JSON string everywhere — WAL-03, WAL-04, SC#4.
- Reads strictly self-scoped (no cross-user read) — Information Disclosure mitigated.
- `WalletService.recharge(payment_provider="stripe")` raises NotImplementedError — SC#6 / PLT-05.
- Disabled "Add funds" button present in the player UI — SC#6 / PLT-05.
</success_criteria>

<output>
Create `.planning/phases/03-wallet-double-entry-ledger/03-05-SUMMARY.md` when done.
</output>

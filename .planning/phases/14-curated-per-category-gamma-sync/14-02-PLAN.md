---
phase: 14-curated-per-category-gamma-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/integrations/polymarket/client.py
  - backend/tests/polymarket/test_client.py
autonomous: true
requirements: [CAT-01, CAT-05]
nyquist_compliant: true

must_haves:
  truths:
    - "GammaClient.fetch_events(tag_id=...) issues exactly one GET /events with params active=true, closed=false, order=volume24hr, ascending=false, the tag_id, and limit/offset."
    - "fetch_events caps the limit at 500 — calling with limit=999 sends limit=500 (CAT-05 hard ceiling)."
    - "fetch_events reuses the exact tenacity retry decorator (NetworkError/TimeoutException, 3 attempts, exponential jitter) already on fetch_top_markets."
    - "The client.py docstring no longer claims the stale 300 req/10s rate (that is the /markets limit; /events is 500 req/10s)."
  artifacts:
    - path: "backend/app/integrations/polymarket/client.py"
      provides: "GammaClient.fetch_events(tag_id, limit, offset)"
      contains: "async def fetch_events"
    - path: "backend/tests/polymarket/test_client.py"
      provides: "fetch_events param + limit-cap unit tests (mock httpx)"
      contains: "fetch_events"
  key_links:
    - from: "backend/app/integrations/polymarket/client.py::fetch_events"
      to: "GET /events"
      via: "self._get_client().get('/events', params=...)"
      pattern: "get\\(\\s*[\"']/events[\"']"
---

<objective>
Add the HTTP boundary for the curated sync: `GammaClient.fetch_events()`, mirroring `fetch_top_markets` (same tenacity decorator, same bounded pool) but hitting `GET /events` with the per-category curation params and the CAT-05 limit cap + short-page-capable signature. Fix the stale `/markets`-only rate-limit docstring. This plan owns ONLY `client.py` + its unit test, so it runs in parallel (Wave 1) with the parser/config plan (14-01) — zero file overlap.

Purpose: Implements the fetch half of CAT-01 (sync via `GET /events`) and the `limit`≤500 hard cap of CAT-05. The short-page stop (consuming `offset`) lives in the task loop (14-04); this method exposes the `offset` param so that loop can page.
Output: `client.py` gains `fetch_events`; `test_client.py` gains param + limit-cap unit tests (mock httpx, no network, no Docker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-curated-per-category-gamma-sync/14-CONTEXT.md
@.planning/phases/14-curated-per-category-gamma-sync/14-RESEARCH.md
@.planning/phases/14-curated-per-category-gamma-sync/14-VALIDATION.md

# The method to mirror (decorator + _get_client + param style) and the stale docstring
@backend/app/integrations/polymarket/client.py
# The mock-httpx test harness to mirror (_make_mock_response, TestGammaClientFetchTopMarkets)
@backend/tests/polymarket/test_client.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GammaClient.fetch_events + fix the stale rate-limit docstring</name>
  <read_first>
    - backend/app/integrations/polymarket/client.py (FULL): the module docstring "Rate limit is 300 req/10s" (lines 7-8 — STALE, that is /markets), `_get_client()` bounded pool (33-45, max_connections=10, 15s timeout), the `@retry(...)` decorator on `fetch_top_markets` (47-52 — NetworkError/TimeoutException, stop_after_attempt(3), wait_exponential_jitter), and the `fetch_top_markets` body shape (53-75): `client.get("/markets", params={...})`, `resp.raise_for_status()`, `resp.json()`, `log.info(...)`.
    - 14-RESEARCH.md "GammaClient.fetch_events()" (lines 462-483) — the exact decorator + params (`active`/`closed`/`tag_id`/`order=volume24hr`/`ascending=false`/`limit=str(min(limit,500))`/`offset`) and the docstring fix note.
  </read_first>
  <behavior>
    - fetch_events(*, tag_id: str, limit: int = 10, offset: int = 0) -> list[dict[str, object]].
    - Sends GET /events with params: active="true", closed="false", tag_id=tag_id, order="volume24hr", ascending="false", limit=str(min(limit, 500)), offset=str(offset).
    - limit=999 → the sent `limit` param is "500" (CAT-05 cap).
    - Decorated with the SAME tenacity retry as fetch_top_markets (NetworkError/TimeoutException, 3 attempts, exponential jitter, reraise=True).
    - Calls resp.raise_for_status(); returns resp.json(); log.info("gamma.fetch_events", tag_id=..., event_count=len(data), limit=limit).
  </behavior>
  <action>
    Add `async def fetch_events(self, *, tag_id: str, limit: int = 10, offset: int = 0) -> list[dict[str, object]]` below `fetch_top_markets`, copying the `@retry(...)` decorator verbatim. Use `self._get_client()` (reuses the bounded pool + 15s timeout). GET `/events` with params exactly per the behavior block; the `min(limit, 500)` is the CAT-05 ceiling. `raise_for_status()` then return `resp.json()` typed `list[dict[str, object]]`; `log.info("gamma.fetch_events", tag_id=tag_id, event_count=len(data) if isinstance(data, list) else 0, limit=limit)`. Update the module docstring (lines 7-8): the 300 req/10s figure is the `/markets` limit — `/events` is 500 req/10s; the curated events poll fires ~7 req / 5 min (far under budget). Keep `fetch_top_markets` and `fetch_market_by_id` untouched (back-compat).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/polymarket/test_client.py -m unit -x</automated>
  </verify>
  <acceptance_criteria>
    - client.py contains `async def fetch_events` and a `get("/events"` call.
    - `grep -v '^#' backend/app/integrations/polymarket/client.py | grep -c "300 req/10s"` is 0 (stale docstring removed).
    - client.py contains `"order": "volume24hr"` and `min(limit, 500)` within fetch_events.
    - `cd backend && uv run pytest tests/polymarket/test_client.py -m unit -x` exits 0 (Task 2 supplies the param + cap assertions).
  </acceptance_criteria>
  <done>fetch_events issues a single ranked /events GET with the curation params and a 500 cap, reusing the proven retry/pool; the rate-limit docstring is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Add fetch_events param + limit-cap unit tests</name>
  <read_first>
    - backend/tests/polymarket/test_client.py (lines 1-60): `pytestmark = [pytest.mark.unit]`, `_make_mock_response(json_data, status_code)` helper (20-35), `class TestGammaClientFetchTopMarkets` + `test_single_batch_call` (38-55) showing the `gamma._client = mock_client` injection pattern and `mock_client.get.call_count`/`mock_client.get.call_args` assertions.
    - 14-VALIDATION.md SC#2 row — `test_fetch_events_caps_limit` is the named CAT-05 cap proof.
  </read_first>
  <action>
    Add a `TestGammaClientFetchEvents` class. `test_fetch_events_params`: inject a mock `httpx.AsyncClient` (set `gamma._client = mock_client`, `is_closed = False`, `get` returns `_make_mock_response([...])`), call `await gamma.fetch_events(tag_id="21", limit=10)`, assert exactly 1 GET, that the path arg is "/events", and that `call_args` params include `tag_id="21"`, `order="volume24hr"`, `ascending="false"`, `active="true"`, `closed="false"`. `test_fetch_events_caps_limit`: call `fetch_events(tag_id="2", limit=999)` and assert the sent `limit` param equals `"500"` (CAT-05). Mirror the existing mock style (no real network). Keep both under `@pytest.mark.unit` / `@pytest.mark.asyncio`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/polymarket/test_client.py -m unit -x</automated>
  </verify>
  <acceptance_criteria>
    - test_client.py contains `test_fetch_events_params` and `test_fetch_events_caps_limit`.
    - `cd backend && uv run pytest tests/polymarket/test_client.py::TestGammaClientFetchEvents -m unit -x` exits 0.
    - The cap test asserts the outgoing `limit` param is `"500"` when called with `limit=999`.
  </acceptance_criteria>
  <done>fetch_events param shape + the 500 limit cap are proven by mock-httpx unit tests with no network call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GammaClient ↔ gamma-api.polymarket.com | Outbound HTTP to a public, unauthenticated third-party API; responses are untrusted JSON |

## STRIDE Threat Register

(Applicable ASVS: V5 Input Validation. No auth/session — Gamma is public read-only, `client.py:7`.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Denial of Service | malformed/oversized /events response or 5xx | mitigate | Bounded httpx pool (max_connections=10) + 15s timeout (reused `_get_client`); tenacity retry (3 attempts, jittered backoff) on transient NetworkError/Timeout; `raise_for_status` surfaces 5xx to the caller's per-category keep-last-good (14-04). |
| T-14-06 | Denial of Service | unbounded `limit` flooding Gamma | mitigate | `min(limit, 500)` hard cap (CAT-05) — the client can never request more than the documented ceiling regardless of caller input. |
| T-14-07 | Spoofing | impersonated Gamma host | accept | HTTPS base URL (`GAMMA_API_BASE_URL`) with httpx default cert verification; public read-only data, no secrets transmitted — low value target. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/polymarket/test_client.py -m unit -x` exits 0 (param + cap, all mocked).
- No real network call in tests (httpx mocked).
- Windows-worktree policy: unit run only; the full suite is the Linux-CI gate.
</verification>

<success_criteria>
- fetch_events performs a single ranked `GET /events` with the curation params and a 500 hard cap, on the same retry/pool as the existing fetch.
- The stale 300 req/10s docstring is corrected to the /events 500 req/10s reality.
- Param shape + cap are unit-proven without touching the network.
</success_criteria>

<output>
Create `.planning/phases/14-curated-per-category-gamma-sync/14-02-SUMMARY.md` when done.
</output>

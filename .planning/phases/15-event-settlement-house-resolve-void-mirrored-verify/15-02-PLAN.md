---
phase: 15-event-settlement-house-resolve-void-mirrored-verify
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - backend/app/settlement/event_service.py
  - backend/tests/settlement/test_event_service.py
autonomous: true
requirements: [EVA-03, EVA-04, EVT-06]
must_haves:
  truths:
    - "Resolving a house event settles the winning child on its YES outcome and every other child on its NO outcome, each on its own fresh session"
    - "Re-running resolve over the same event is a true no-op (idempotent replay; no double-credit)"
    - "Voiding a house event settles EVERY child on its NO outcome (YES bettors lose, NO bettors win) — not a stake refund"
    - "A child failure leaves already-settled siblings intact, surfaces the failed child(ren), and the event derives to partially_resolved; re-run finishes"
    - "EventService raises on a source=POLYMARKET group and on a blank/whitespace justification"
    - "An event.resolved / event.voided audit row is written in its own transaction with actor + group_id + justification"
    - "spike-004 _reconcile_async drift_count == 0 after resolve / void / partial / replay"
  artifacts:
    - path: "backend/app/settlement/event_service.py"
      provides: "EventService.resolve_event + void_event classmethods (loop SettlementService per child on fresh sessions)"
      contains: "class EventService"
    - path: "backend/tests/settlement/test_event_service.py"
      provides: "Integration tests (testcontainers) for resolve/void/partial/replay/mirrored-reject/blank-justification + _seed_house_event synthesizer"
      contains: "_seed_house_event"
  key_links:
    - from: "backend/app/settlement/event_service.py"
      to: "app.settlement.service.SettlementService.resolve_market"
      via: "per-child loop on a fresh _get_session_maker() session"
      pattern: "SettlementService\\.resolve_market"
    - from: "backend/app/settlement/event_service.py"
      to: "app.db.session._get_session_maker"
      via: "fresh session per child (23505 dangling-tx avoidance)"
      pattern: "_get_session_maker"
    - from: "backend/app/settlement/event_service.py"
      to: "app.core.audit.service.AuditService.record"
      via: "event-level audit row in its own begin()"
      pattern: "AuditService\\.record"
---

<objective>
Implement the house-event RESOLVE and VOID orchestration (EVA-03, EVA-04) by EXTENDING `backend/app/settlement/event_service.py` with an `EventService` class whose `resolve_event` / `void_event` classmethods LOOP the UNCHANGED `SettlementService.resolve_market` over a `MarketGroup`'s child markets — ONE FRESH `AsyncSession` per child (Option A; the 23505 dangling-tx landmine forbids chaining two self-committing settle calls on one session). Plus the integration test suite in `backend/tests/settlement/test_event_service.py` covering resolve, void, partial-failure, idempotent replay, the mirrored-reject gate, and the blank-justification guard — every path asserting the spike-004 ledger-integrity check green.

This phase COMPOSES; it does NOT reinvent settlement. The payouts, loser sweep, bet flips, market-status flip, per-bet idempotency keys, FOR-UPDATE lock ordering, and per-child `settlement.resolved` audit rows are ALL already inside `SettlementService` and stay byte-for-byte unchanged. The event service only orchestrates the loop, maps winning-outcome→child YES/NO, rejects mirrored groups, enforces a non-blank justification, and writes ONE additional event-level audit row.

Purpose: Deliver EVA-03/EVA-04 by reuse — every transactional hazard is solved one level down; the only new risk surface is session-per-child discipline + correct YES/NO mapping.
Output: `EventService.resolve_event` + `void_event` (in the same module as Wave 1's pure layer) + `test_event_service.py` with the `_seed_house_event` synthesizer (no house events exist pre-Phase-18).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-CONTEXT.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-RESEARCH.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-PATTERNS.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-VALIDATION.md
@.claude/skills/spike-findings-xpredict/SKILL.md
@.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventService.resolve_event + void_event — fresh-session-per-child loop, YES/NO mapping, mirrored-reject, justification guard, event audit (EVA-03, EVA-04)</name>
  <files>backend/app/settlement/event_service.py</files>
  <read_first>
    - backend/app/settlement/event_service.py  (Wave 1 pure layer — EXTEND it; keep `derive_event_status`/`ChildStatus` intact)
    - backend/app/settlement/service.py  (THE primitive being looped — `SettlementService.resolve_market` signature at lines 83-93, opens its OWN `async with session.begin()` at line 110; idempotent via PENDING filter + `settle:{bet_id}:{leg}` keys. DO NOT modify.)
    - backend/app/settlement/market_port.py  (MarketResolvePort Protocol — pass the SAME HouseMarketResolveAdapter() per child; writes on caller session, never commits)
    - backend/app/settlement/adapters.py  (HouseMarketResolveAdapter — the concrete port the loop injects)
    - backend/app/integrations/polymarket/tasks.py  (lines 408-518 `_run_detect_resolutions` — the `_get_session_maker()` per-unit-of-work session idiom + `selectinload(Market.outcomes)` + `HouseMarketResolveAdapter()` + `SettlementService.resolve_market(actor_user_id=...)` call shape)
    - backend/app/markets/models.py  (MarketGroup lines 200-275 — NO status/winner col, `markets` relationship `lazy="raise"` at :272; Market `group_id`/`group_item_title`/`source`/`status`/`winning_outcome_id`, `outcomes`+`group` `lazy="raise"` at :184-197; Outcome.label)
    - backend/app/markets/enums.py  (MarketStatus, MarketSourceEnum — `MarketSourceEnum.POLYMARKET.value` for the reject gate, `MarketStatus.RESOLVED.value`)
    - backend/app/markets/service.py  (lines 374-378 / 182 — the canonical IN-01 `func.upper(Outcome.label) == "YES"` case-insensitive match)
    - backend/app/core/audit/service.py  (AuditService.record signature — caller owns tx, only flushes; actor `f"user:{actor_user_id}"` / `"system"`)
    - backend/app/settlement/router.py  (lines 185-199 force_settle — the action-THEN-audit "separate begin()" analog for the event-level audit row)
    - backend/app/db/session.py  (_get_session_maker — @lru_cache async_sessionmaker, expire_on_commit=False — THE fresh-session factory)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-PATTERNS.md  (every CORE IDIOM block: fresh-session-per-child, selectinload load, mirrored-reject, YES/NO mapping, void-reuses-loop, event audit)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-RESEARCH.md  (Pitfalls 1/2/4; Pattern 1/2/4; Open Q2 winning-outcome defensive guard)
  </read_first>
  <behavior>
    - resolve_event(group_id, winning_outcome_id, justification, actor_user_id): loads group+children+outcomes; raises if group.source==POLYMARKET; raises if justification blank; confirms winning_outcome_id maps to exactly one child; loops resolve_market per child (winner child on its YES==supplied outcome, every other child on its NO outcome), each on a FRESH session, best-effort; writes one event.resolved audit row.
    - void_event(group_id, justification, actor_user_id): same loop but EVERY child settles on its NO outcome (no winner); writes event.voided.
    - Both: a child settle failure is caught, the failed child id recorded, loop continues (siblings intact -> derives partially_resolved); winning child is settled FIRST in resolve.
    - Mirrored (source=POLYMARKET) group -> raise ValueError (admin read-only; EVA-06).
    - Blank/whitespace justification -> raise ValueError.
  </behavior>
  <action>
    EXTEND `backend/app/settlement/event_service.py` (keep Wave 1's `ChildStatus`/`derive_event_status`). Add the imports per 15-PATTERNS "Imports pattern" (`from __future__ import annotations`; `TYPE_CHECKING`; `UUID`; `func, select` from sqlalchemy; `selectinload`; `AuditService`; `MarketSourceEnum, MarketStatus`; `Market, MarketGroup, Outcome`; `HouseMarketResolveAdapter`; `SettlementService`; the in-function/local import of `_get_session_maker` from `app.db.session`). Define `class EventService` with classmethods mirroring `SettlementService.resolve_market`'s keyword-only + `actor_user_id: UUID | None = None` style (service.py:83-93), returning a small summary dataclass (e.g. `EventSettleResult(group_id, child_count, children_settled, children_failed: tuple[UUID, ...], status: str)` — shape at discretion; include the failed-child list and the derived status).

    Shared private helpers (module-level or classmethods):
    1. `_load_group_with_children(session, group_id)` — `select(MarketGroup).where(id==group_id).options(selectinload(MarketGroup.markets).selectinload(Market.outcomes))`, `.scalar_one_or_none()` (15-PATTERNS load idiom — dodges `lazy="raise"` at models.py:272/184). Raise (e.g. `NoResultFound`/`ValueError`) if the group is missing.
    2. `_yes_outcome_id(session, market_id)` and `_no_outcome_id(session, market_id)` — `select(Outcome.id).where(Outcome.market_id==..., func.upper(Outcome.label) == "YES")` and `... != "YES"` respectively (15-PATTERNS YES/NO mapping; Pitfall 2 — case-insensitive so mirrored title-case "Yes" never silently misses). `.scalar_one()`.

    `resolve_event(cls, *, group_id, winning_outcome_id, justification, actor_user_id=None)`:
    - Open a read session via `_get_session_maker()`; load the group via helper #1. If `group is None` -> raise. If `group.source == MarketSourceEnum.POLYMARKET.value` -> `raise ValueError("Mirrored (Polymarket) events are admin read-only; use force-settle (ADM-06).")` (15-PATTERNS mirrored-reject; EVA-06). If `not justification or not justification.strip()` -> `raise ValueError(...)` (CONTEXT V5; the two-step confirm is Phase 16).
    - Defensive winning-outcome guard (Open Q2): confirm `winning_outcome_id` belongs to exactly one child of the group (query `Outcome` joined to the group's child market ids); raise if zero/ambiguous. This is a service-layer guard, NOT a substitute for the Phase-16 endpoint validation.
    - Build `ordered_children` as a list of `(child_market_id, child_winning_outcome_id)`: the ONE child that owns `winning_outcome_id` settles on `winning_outcome_id` (its YES); EVERY OTHER child settles on its `_no_outcome_id` (event-of-binaries: exactly one child can be YES — Assumption A1). ORDER = winning child FIRST, then losing children by `market.id` (CONTEXT — winners paid before any loser hiccup).
    - Loop EXACTLY per 15-PATTERNS "CORE IDIOM — fresh session per child": `session_maker = _get_session_maker()`; `resolver = HouseMarketResolveAdapter()`; for each `(cid, woid)` -> `async with session_maker() as child_session:` call `SettlementService.resolve_market(child_session, market_id=cid, winning_outcome_id=woid, market_resolver=resolver, justification=justification, actor_user_id=actor_user_id)`; on `except Exception` append `cid` to `failed` and `continue`. NEVER wrap two settle calls in one `with`/`begin()` (Pitfall 1 — the 23505 dangling-tx landmine).
    - After the loop, write ONE event-level audit row in its own tx (15-PATTERNS event-audit idiom; router.py:185-199 analog): `async with session_maker() as audit_session, audit_session.begin():` `await AuditService.record(audit_session, actor=f"user:{actor_user_id}" if actor_user_id is not None else "system", event_type="event.resolved", payload={"group_id": str(group_id), "winning_outcome_id": str(winning_outcome_id), "child_count": ..., "children_settled": ..., "children_failed": [str(x) for x in failed], "justification": justification})`. Money is never in this payload (only ids+counts); any string field stays a str.
    - Project the final status: load each child's `status` + whether its `winning_outcome_id` is the YES outcome, build `ChildStatus` rows, call `derive_event_status` (Wave 1) for the returned summary `status`.

    `void_event(cls, *, group_id, justification, actor_user_id=None)`:
    - Same group-load + mirrored-reject + blank-justification guards. NO `winning_outcome_id` parameter and NO defensive winning-outcome guard.
    - `ordered_children` = EVERY child on its `_no_outcome_id` (15-PATTERNS void-reuses-loop; EVA-04 all-children-NO, NOT a refund — there is no refund path in the ledger). Order by `market.id`.
    - Same fresh-session-per-child loop calling `SettlementService.resolve_market` with each child's NO outcome. Same best-effort `failed` handling.
    - Event audit `event_type="event.voided"`, payload omits `winning_outcome_id` (or sets it `None`).
    - Return the same summary with the derived status (all-children-NO all-resolved -> `derive_event_status` yields `"void"`).

    DO NOT: modify `service.py`/`adapters.py`/`market_port.py`; add a `market_groups` status column or a migration (EVT-06); add FOR UPDATE/lock logic (already inside SettlementService — 15-RESEARCH Anti-Patterns); implement a refund path; settle a child via `closed=true` alone (the spike-002 guard inside the adapter/service is the only path to a child MarketStatus).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.settlement.event_service import EventService, derive_event_status, ChildStatus; assert hasattr(EventService, 'resolve_event') and hasattr(EventService, 'void_event'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `event_service.py` contains `class EventService` with `async def resolve_event(` and `async def void_event(` (both classmethods, keyword-only, `actor_user_id: UUID | None = None`)
    - The loop body calls `SettlementService.resolve_market(` inside `async with session_maker() as` ... — a FRESH session per child (grep both `_get_session_maker` and `SettlementService.resolve_market` present; the two settle calls are NOT in one shared `with`)
    - The mirrored-reject gate `group.source == MarketSourceEnum.POLYMARKET.value` raises (grep `MarketSourceEnum.POLYMARKET`)
    - A blank-justification guard raises on `not justification.strip()` (grep `justification.strip`)
    - The YES/NO mapping uses `func.upper(Outcome.label) == "YES"` and `!= "YES"` (case-insensitive — grep `func.upper`)
    - An event-level `AuditService.record(... event_type="event.resolved" ...)` and `event_type="event.voided"` are written in their own `begin()` block (grep both event types)
    - `event_service.py` does NOT modify or re-implement payout/reverse logic (no `compute_payout`/`_post_transfer`/`build_settlement_plan` call — it delegates to SettlementService)
    - `from app.settlement.event_service import EventService, derive_event_status, ChildStatus` imports cleanly (Wave 1 pure layer preserved)
  </acceptance_criteria>
  <done>`EventService.resolve_event` and `void_event` compose `SettlementService` over a group's children on per-child fresh sessions, with the mirrored-reject gate, non-blank justification guard, case-insensitive YES/NO mapping, and an additional event-level audit row — zero new settlement primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — resolve / void / partial-failure / idempotent-replay / mirrored-reject / blank-justification + _seed_house_event synthesizer + spike-004 integrity gate (EVA-03, EVA-04)</name>
  <files>backend/tests/settlement/test_event_service.py</files>
  <read_first>
    - backend/app/settlement/event_service.py  (the EventService under test — Task 1)
    - backend/tests/settlement/test_resolve_market.py  (THE committed-session integration analog — copy header lines 58-76, `_seed_wallet` :158-178, `_balance` :181-187, `_liability_id` :189-202, `_bets_for_user` :204-208, `_place` :210-221, `_seed_real_market` :244-277, `_audit_for_market` :287-302, `FakeMarketResolver`/`RaisingMarketResolver` :93-135; the happy-path delta-assert style :308-351)
    - backend/tests/settlement/test_force_settle.py  (lines 171-196 — the reject-pattern assertions for the mirrored-reject + the source=POLYMARKET seeding)
    - backend/app/wallet/reconcile.py  (lines 61-85 `_reconcile_async(session)` -> `{"accounts_checked","drift_count"}`; house_promo excluded by design at :50 — the spike-004 integrity gate)
    - backend/tests/wallet/test_reconcile.py  (how `_reconcile_async` is invoked in a test)
    - backend/tests/conftest.py  (the `engine` fixture — testcontainers + `alembic upgrade head`; `async_session` is rolled-back and MUST NOT be used for the act per Pitfall 5)
    - backend/app/markets/models.py  (MarketGroup :200-275 + Market.group_id stamping :176-182 — the new `_seed_house_event` builds a group + N children)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-PATTERNS.md  (`test_event_service.py` section: committed-vs-rolled-back model, `_seed_house_event` builder, integrity-check idiom, mirrored-reject test, idempotent-replay canary)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-RESEARCH.md  (Pitfalls 1/3/5/6; Open Q1 integrity-assertion style; Validation Architecture Wave 0 gaps)
    - .planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-VALIDATION.md  (Wave 0 + spike-004 sign-off; per-task verification map)
  </read_first>
  <behavior>
    - test_resolve_event_settles_all_children: 3-child house event, winner child on YES + 2 losers on NO; winners paid stake/price, losers swept, each child liability drains to 0; bets flip; integrity drift_count==0.
    - test_resolve_event_is_idempotent: resolve, then resolve the SAME group again -> second pass settles nothing, no double-credit, integrity drift_count==0 (the Pitfall-1 canary).
    - test_void_event_settles_every_child_on_no: void -> every child resolves on NO (YES bettors lose, NO bettors win), NOT a refund; integrity drift_count==0.
    - test_resolve_event_partial_failure_lands_partially_resolved: inject one child failure -> settled siblings intact, failed child surfaced in the result, derive_event_status -> "partially_resolved"; re-run finishes (no double-credit); integrity drift_count==0.
    - test_resolve_event_rejects_mirrored_group: source=POLYMARKET group -> EventService.resolve_event raises (EVA-06).
    - test_resolve_event_rejects_blank_justification: whitespace justification -> raises.
  </behavior>
  <action>
    Create `backend/tests/settlement/test_event_service.py` as an INTEGRATION module mirroring `test_resolve_market.py` (15-PATTERNS `test_event_service.py` section). CRITICAL (Pitfall 5): `SettlementService`/`EventService` commit internally, so the act MUST use committed `_get_session_maker()` sessions and assert COMMITTED state — do NOT use the rolled-back `async_session` fixture. Copy the header block verbatim (`pytestmark = [pytest.mark.integration, pytest.mark.asyncio(loop_scope="session")]`, the `_require_testcontainer(engine)` autouse fixture, and the `_bets_table(engine)` autouse fixture that runs `Bet.__table__.create(checkfirst=True)`).

    Reuse the committed-session helpers from `test_resolve_market.py` (copy or import): `_seed_wallet`, `_balance`, `_liability_id`, `_bets_for_user`, `_place`, the `StubMarketSource`, `FakeMarketResolver`, `RaisingMarketResolver`. NOTE: `EventService` uses the REAL `HouseMarketResolveAdapter` internally (it does not accept an injected resolver the way the helpers do), so the children MUST exist as real `markets` rows — seed them committed (extend the `_seed_real_market` idiom).

    NEW helper `_seed_house_event(n_children, winner_index, ...) -> (group_id, [(child_market_id, yes_outcome_id, no_outcome_id), ...])` (15-PATTERNS `_seed_house_event` — no house-event seed exists pre-Phase-18): on a committed `async with sm() as s, s.begin():`, INSERT a `MarketGroup(id=uuid4(), title=..., slug=f"evt-{uuid4().hex[:8]}", source=MarketSourceEnum.HOUSE.value)`, flush, then N HOUSE child `Market` rows (each `group_id=group.id`, `group_item_title=...`, `status=MarketStatus.OPEN.value`, a future `deadline`) each with a `"YES"` and a `"NO"` `Outcome` (record both outcome ids per child). Place bets on the children (use `_place` + the in-memory `StubMarketSource` mirroring each child so the bet path + per-market liability exist) so there is real money to settle. For the mirrored-reject test, seed an analogous group with `source=MarketSourceEnum.POLYMARKET.value`.

    Integrity gate (Open Q1 — do BOTH where cheap): add `_assert_ledger_clean()` calling `reconcile._reconcile_async(fresh_session)` and asserting `summary["drift_count"] == 0` (house_promo excluded by design); ALSO assert the per-child liability drains to `Decimal("0.0000")` and house deltas match (the precise `test_resolve_market.py:339-342` style, using before/after DELTAS on `HOUSE_PROMO_ACCOUNT_ID`/`HOUSE_REVENUE_ACCOUNT_ID` and absolute values on per-test fresh accounts). Call `_assert_ledger_clean()` at the END of resolve / void / partial / replay tests (the literal spike-004 gate — 15-VALIDATION cross-cutting invariant).

    Write the test functions per <behavior>. For idempotency, run `EventService.resolve_event` twice over the same group and assert the second pass moved no money (winner/loser balances unchanged, bets already SETTLED) and `drift_count == 0` — this is the 23505 dangling-tx canary (Pitfall 1). For partial failure, force exactly one child to fail (e.g. seed one child WITHOUT a ledger liability / outcomes so its settle raises, or point its NO mapping at a non-existent outcome) so the loop's `except` records it; assert the other children settled, the failed child id is in the result, the event derives `partially_resolved`, then re-run and assert it completes with no double-credit. For mirrored-reject, `with pytest.raises(ValueError): await EventService.resolve_event(group_id=mirrored_group_id, winning_outcome_id=..., justification="x")`. For blank-justification, `with pytest.raises(ValueError): await EventService.resolve_event(..., justification="   ")`.

    Do NOT write a resolve -> reverse -> RE-resolve test (that is the KNOWN deferred Pitfall-6 gap — `reverse_idempotency_key` collision; reverse lives in Plan 03 and is scoped to restore+audit only).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/settlement/test_event_service.py tests/settlement/test_derive_event_status.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/settlement/test_event_service.py -x` exits 0 (Linux CI is the source of truth; the Windows worktree may flake — verify per-module)
    - The file defines `_seed_house_event` building a `MarketGroup` (source=HOUSE) + N child markets each with YES/NO outcomes + placed bets
    - The file is marked `pytest.mark.integration` and uses committed `_get_session_maker()` sessions for the act (NOT the rolled-back `async_session` fixture)
    - At least one resolve test and one void test call `reconcile._reconcile_async(...)` and assert `drift_count == 0`
    - An idempotent-replay test resolves the same group twice and asserts no double-credit (balances unchanged on the second pass)
    - A partial-failure test asserts the event derives `partially_resolved` and the failed child id is surfaced
    - A `pytest.raises(ValueError)` test covers the source=POLYMARKET mirrored-reject and another covers blank/whitespace justification
    - NO test resolves -> reverses -> re-resolves (the deferred Pitfall-6 gap is not exercised)
  </acceptance_criteria>
  <done>The resolve/void integration suite is green: winners paid, losers swept, liability drained, idempotent replay a no-op, partial failure recoverable, mirrored + blank-justification rejected, and spike-004 `drift_count == 0` after every resolution path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin actor -> EventService (service layer) | The resolve/void request carries an `actor_user_id` + a `winning_outcome_id` + a `justification`. The HTTP/admin-Bearer gate is Phase 16; at this layer the service must record an actor, refuse a blank justification, refuse a mirrored group, and never offer an unauthenticated settle path. |
| EventService -> ledger (per-child money movement) | Each child settlement crosses into double-entry money movement (via the UNCHANGED SettlementService). The integrity invariant (`drift_count == 0`, `CHECK(balance>=0)`) must hold after every path. |
| EventService -> UMA-owned mirrored events | A `source=POLYMARKET` group is owned by the oracle path; an admin must NOT resolve/void it here (only the existing emergency force-settle, ADM-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | Double-credit / double-refund via repeated `resolve_event` / `void_event` | mitigate | Idempotency is inherited for free from SettlementService's `status==PENDING` filter + per-bet `settle:{bet_id}:{leg}` keys; the event loop adds no bookkeeping. The `test_resolve_event_is_idempotent` replay test (the 23505 dangling-tx canary) asserts the second pass moves no money and `drift_count == 0`. |
| T-15-03 | Tampering | Money drift (balance != ledger) after a multi-child resolve/void | mitigate (HIGH) | spike-004 `_reconcile_async(session)` asserted `drift_count == 0` after resolve/void/partial/replay; per-child liability asserted to drain to 0 + house deltas match. Append-only double-entry (WAL-06) + `CHECK(balance>=0)` enforced inside SettlementService (unchanged). |
| T-15-04 | Elevation / Tampering | Admin mutating a mirrored (UMA-owned) event out from under the oracle | mitigate (HIGH) | Mirrored-reject gate: `group.source == MarketSourceEnum.POLYMARKET.value` => raise, in `resolve_event` AND `void_event` (EVA-06). `test_resolve_event_rejects_mirrored_group` asserts the raise. The only mirrored mutation path stays the existing audited force-settle (ADM-06, Phase 16). |
| T-15-05 | Repudiation | Denial of who resolved/voided an event and why | mitigate | One immutable `event.resolved` / `event.voided` audit row per operation (actor + group_id + justification + child counts) via AuditService.record in its own tx, IN ADDITION to the per-child `settlement.resolved` rows. |
| T-15-06 | Tampering / DoS | Partial failure leaving inconsistent money state | mitigate | Per-child ACID transactions (Option A): a failed child rolls back alone; settled siblings stay correct; the event derives a valid recoverable `partially_resolved`; idempotent re-run finishes. `test_resolve_event_partial_failure_lands_partially_resolved` asserts this + `drift_count == 0`. |
| T-15-07 | Tampering / Input Validation | Blank justification erasing the non-repudiation reason; misrouted resolution (winning_outcome belongs to no child) | mitigate | Service-layer guards: raise on `not justification.strip()` (V5); defensively confirm `winning_outcome_id` maps to exactly one child before looping (Open Q2). Both asserted by tests. UUID-typed ids + parameterized SQLAlchemy (no string interpolation) close the SQLi vector. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (zero new dependency — 15-RESEARCH "Package Legitimacy Audit: Not applicable"). slopcheck moot; no `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/settlement/test_event_service.py tests/settlement/test_derive_event_status.py -x -q` GREEN per-module (trust Linux CI for the full suite — Windows worktree flakes).
- spike-004 `_reconcile_async` `drift_count == 0` asserted after resolve / void / partial / replay (15-VALIDATION cross-cutting invariant).
- `EventService` composes `SettlementService` on per-child FRESH sessions (no two settle calls share a `with`/`begin()` — the 23505 landmine avoided).
- Mirrored (`source=POLYMARKET`) groups and blank justifications are rejected with a raise.
- No `market_groups` status/winning_outcome column or migration added (EVT-06 preserved); `service.py`/`adapters.py` unchanged.
</verification>

<success_criteria>
- EVA-03: Resolving a house event settles every child (winner→YES, losers→NO) as per-child transactions; re-running is a safe idempotent no-op; spike-004 integrity green after every path.
- EVA-04: Voiding a house event settles every child on NO (YES bettors lose, NO bettors win) — explicitly not a stake refund; integrity green.
- EVT-06: The returned event status is the derived `derive_event_status` projection (no stored authoritative column).
- Partial failure lands `partially_resolved` with the failed child surfaced and re-run finishing cleanly.
</success_criteria>

<output>
Create `.planning/phases/15-event-settlement-house-resolve-void-mirrored-verify/15-02-SUMMARY.md` when done.
</output>

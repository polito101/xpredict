---
phase: 02-auth-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/core/config.py
  - backend/app/auth/__init__.py
  - backend/app/auth/models.py
  - backend/app/auth/schemas.py
  - backend/alembic/versions/0002_phase2_auth.py
  - .env.example
  - backend/tests/auth/__init__.py
  - backend/tests/auth/test_models.py
  - backend/tests/auth/test_migration_0002.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-09
user_setup: []

tags:
  - auth
  - fastapi-users
  - alembic
  - sqlalchemy
  - argon2

must_haves:
  truths:
    - "Alembic head is 0002_phase2_auth and revision applies cleanly on a fresh DB"
    - "users table exists with fastapi-users base columns (id UUID PK, email UNIQUE, hashed_password, is_active DEFAULT TRUE, is_superuser DEFAULT FALSE, is_verified DEFAULT FALSE) plus display_name nullable, banned_at nullable, token_version INT DEFAULT 0, tenant_id UUID DEFAULT '00000000-...-0001'"
    - "refresh_tokens table exists with token_hash UNIQUE, user_id FK CASCADE, expires_at, revoked_at nullable, reuse_count INT DEFAULT 0, token_version INT DEFAULT 0, created_at"
    - "User ORM class inherits from BOTH SQLAlchemyBaseUserTableUUID AND app.db.base.Base"
    - "Settings(BaseSettings) exposes SECRET_KEY, JWT_ALGORITHM, ACCESS_TOKEN_LIFETIME_SECONDS, REFRESH_TOKEN_LIFETIME_SECONDS, RESEND_API_KEY, RESEND_FROM_ADDRESS, SMTP_HOST, SMTP_PORT, FIRST_ADMIN_EMAIL, FIRST_ADMIN_PASSWORD, FRONTEND_BASE_URL, ADMIN_JWT_PUBLIC_SECRET"
    - ".env.example mirrors all new Phase 2 env vars with placeholder values; gitleaks scan stays clean"
    - "UserRead Pydantic schema exposes is_admin (mapped from is_superuser) and never leaks is_superuser through model_dump()"
  artifacts:
    - path: "backend/app/auth/models.py"
      provides: "User + RefreshToken ORM models (D-02, D-08)"
      contains: "class User(SQLAlchemyBaseUserTableUUID, Base)"
    - path: "backend/app/auth/schemas.py"
      provides: "Pydantic schemas with is_superuser→is_admin mapping (D-09)"
      contains: "class UserRead(schemas.BaseUser[uuid.UUID])"
    - path: "backend/alembic/versions/0002_phase2_auth.py"
      provides: "Migration creating users + refresh_tokens"
      contains: "down_revision: str | None = \"0001_phase1_foundations\""
    - path: "backend/app/core/config.py"
      provides: "Settings extended with Phase 2 env vars"
      contains: "SECRET_KEY: str"
    - path: ".env.example"
      provides: "Placeholder env vars for Phase 2"
      contains: "SECRET_KEY="
  key_links:
    - from: "backend/app/auth/models.py"
      to: "backend/app/db/base.py"
      via: "Base import"
      pattern: "from app.db.base import Base"
    - from: "backend/app/auth/models.py"
      to: "fastapi_users_db_sqlalchemy"
      via: "SQLAlchemyBaseUserTableUUID mixin"
      pattern: "SQLAlchemyBaseUserTableUUID"
    - from: "backend/alembic/versions/0002_phase2_auth.py"
      to: "backend/alembic/versions/0001_phase1_foundations.py"
      via: "down_revision chain"
      pattern: "down_revision.*0001_phase1_foundations"
---

<objective>
Ship the schema foundation for Phase 2 auth: pyproject deps (fastapi-users, resend, aiosmtplib), `Settings` env-var expansion, SQLAlchemy ORM models (`User` + `RefreshToken`), Pydantic API schemas with `is_superuser→is_admin` mapping (D-09), and Alembic migration 0002 that creates both tables under testcontainers.

Purpose: All downstream plans (02-02 player surface, 02-03 admin + create-admin) import the `User` model and read `Settings` keys defined here. Pinning the schema + config + deps in one focused plan unblocks both Wave 2 plans to run in parallel.

Output: A clean `alembic upgrade head` produces the Phase 2 tables, `from app.auth.models import User` works in tests, and `Settings(BaseSettings)` validates every Phase 2 env var. No router wiring yet — that is 02-02 / 02-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-identity/02-CONTEXT.md
@.planning/phases/02-auth-identity/02-RESEARCH.md
@.planning/phases/02-auth-identity/02-PATTERNS.md
@backend/app/db/base.py
@backend/app/core/config.py
@backend/app/core/audit/models.py
@backend/app/core/feature_flags/models.py
@backend/alembic/versions/0001_phase1_foundations.py
@backend/pyproject.toml
@.env.example
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend dep additions + Settings expansion + .env.example</name>
  <files>backend/pyproject.toml, backend/app/core/config.py, .env.example, backend/tests/auth/__init__.py, backend/tests/auth/test_settings_phase2.py</files>
  <read_first>
    - backend/pyproject.toml (current Phase 1 pins — slowapi already at >=0.1.9,<0.2; DO NOT alter Phase 1 deps)
    - backend/app/core/config.py (existing `Settings(BaseSettings)` class; `extra="ignore"`; `is_dev`/`is_prod` properties — APPEND only)
    - .env.example (mirror format; Phase 1 comments at lines 50-54 already reserve Phase 2 space)
    - .planning/phases/02-auth-identity/02-RESEARCH.md §"Runtime State Inventory" lines 944-958 (full env-var list)
    - .planning/phases/02-auth-identity/02-PATTERNS.md §"Settings Extension Pattern" lines 763-773
    - backend/tests/test_settings.py (Phase 1 pattern for asserting Settings shape)
    - .gitleaks.toml (allowlist scope — `.env.example` placeholders must not trigger detection)
  </read_first>
  <behavior>
    - Test 1: `Settings()` instantiates with `SECRET_KEY="x"*32` env override and exposes the 12 new Phase 2 keys (SECRET_KEY, JWT_ALGORITHM, ACCESS_TOKEN_LIFETIME_SECONDS, REFRESH_TOKEN_LIFETIME_SECONDS, RESEND_API_KEY, RESEND_FROM_ADDRESS, SMTP_HOST, SMTP_PORT, FIRST_ADMIN_EMAIL, FIRST_ADMIN_PASSWORD, FRONTEND_BASE_URL, ADMIN_JWT_PUBLIC_SECRET).
    - Test 2: `Settings()` validates `JWT_ALGORITHM` as the `Literal["HS256"]` type and defaults to `"HS256"`.
    - Test 3: defaults are correct — `ACCESS_TOKEN_LIFETIME_SECONDS == 900`, `REFRESH_TOKEN_LIFETIME_SECONDS == 2_592_000`, `SMTP_HOST == "mailpit"`, `SMTP_PORT == 1025`, `RESEND_FROM_ADDRESS == "noreply@xpredict.local"`, `FRONTEND_BASE_URL == "http://localhost:3000"`.
    - Test 4: `RESEND_API_KEY`, `FIRST_ADMIN_EMAIL`, `FIRST_ADMIN_PASSWORD`, `ADMIN_JWT_PUBLIC_SECRET` accept `None` (optional in dev).
    - Test 5: `extra="ignore"` is preserved — adding an unknown env var does not raise `ValidationError`.
  </behavior>
  <action>
    Add three runtime deps to `backend/pyproject.toml` `[project.dependencies]` per D-01 + RESEARCH §"Standard Stack" lines 149-156 (using v15.0.5 per researcher correction; surface this delta to Pol in execute-plan checkpoint via SUMMARY note since CONTEXT D-01 says "v14"):
      - `fastapi-users[sqlalchemy] >=15.0.5,<16.0.0`
      - `resend[async] >=2.30.0,<3.0`
      - `aiosmtplib >=4.0,<5.0`
    Run `uv lock` and `uv sync` after editing pyproject.toml.

    APPEND to `class Settings(BaseSettings)` in `backend/app/core/config.py` (do NOT redefine the class; preserve `extra="ignore"`) per D-09 + PATTERNS §"Settings Extension Pattern":
      - `SECRET_KEY: str` (no default — must be set in env; min length 32 enforced via pydantic `Field(min_length=32)` if practical; otherwise documented in `.env.example`)
      - `JWT_ALGORITHM: Literal["HS256"] = "HS256"` (Assumption A8: HS256 with shared secret in v1; RS256 is a Phase 11 hardening item)
      - `ACCESS_TOKEN_LIFETIME_SECONDS: int = 900`
      - `REFRESH_TOKEN_LIFETIME_SECONDS: int = 2_592_000`
      - `RESEND_API_KEY: str | None = None`
      - `RESEND_FROM_ADDRESS: str = "noreply@xpredict.local"`
      - `SMTP_HOST: str = "mailpit"`
      - `SMTP_PORT: int = 1025`
      - `FIRST_ADMIN_EMAIL: str | None = None`
      - `FIRST_ADMIN_PASSWORD: str | None = None`
      - `FRONTEND_BASE_URL: str = "http://localhost:3000"`
      - `ADMIN_JWT_PUBLIC_SECRET: str | None = None`

    Mirror in `.env.example` under a new section `# Phase 2 — Auth & Identity (AUTH-01..09)`; replace the existing Phase 2 placeholder block at lines 50-54 with concrete placeholder values (per D-11, FIRST_ADMIN_PASSWORD must NOT be a real-looking secret — use literal `change-me-min-12-chars-A1!` so gitleaks stays clean). For SECRET_KEY use `change-me-32+chars-of-random-secret`. RESEND_API_KEY left empty (dev uses Mailpit).

    Create `backend/tests/auth/__init__.py` (empty file — test package marker).
    Create `backend/tests/auth/test_settings_phase2.py` per PATTERNS §"Settings Extension Pattern"; tests use `monkeypatch.setenv(...)` to inject values and assert defaults / types / optional handling.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/auth/test_settings_phase2.py -x</automated>
    <automated>cd backend &amp;&amp; uv lock --check</automated>
    <automated>cd backend &amp;&amp; uv run python -c "from app.core.config import Settings; s = Settings(SECRET_KEY='x'*32); assert s.JWT_ALGORITHM == 'HS256' and s.SMTP_HOST == 'mailpit' and s.ACCESS_TOKEN_LIFETIME_SECONDS == 900"</automated>
    <automated>pre-commit run gitleaks --files .env.example</automated>
  </verify>
  <done>pyproject locked with fastapi-users[sqlalchemy]>=15.0.5, resend[async]>=2.30, aiosmtplib>=4.0; Settings exposes all 12 Phase 2 keys with correct defaults/types; .env.example mirrors every key with safe placeholders; `pytest tests/auth/test_settings_phase2.py` is green; gitleaks scan clean on .env.example.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: User + RefreshToken ORM models + Pydantic schemas</name>
  <files>backend/app/auth/__init__.py, backend/app/auth/models.py, backend/app/auth/schemas.py, backend/tests/auth/test_models.py</files>
  <read_first>
    - backend/app/db/base.py (the `DeclarativeBase` `Base` class — User must inherit from this AND from SQLAlchemyBaseUserTableUUID per D-02)
    - backend/app/core/audit/models.py (UUID PK with dual default + tenant_id ghost column pattern — replicate verbatim on RefreshToken.id and User.tenant_id)
    - backend/app/core/feature_flags/models.py (Integer column with server_default pattern)
    - .planning/phases/02-auth-identity/02-RESEARCH.md §"Common Operation 1" lines 1062-1147 (full model body to copy)
    - .planning/phases/02-auth-identity/02-RESEARCH.md §"Common Operation 2" lines 1149-1192 (schema body)
    - .planning/phases/02-auth-identity/02-PATTERNS.md §"backend/app/auth/models.py" lines 33-99 and §"backend/app/auth/schemas.py" lines 103-138
  </read_first>
  <behavior>
    - Test 1: `User` is importable from `app.auth.models`, inherits from BOTH `SQLAlchemyBaseUserTableUUID` and `app.db.base.Base` (verified via `User.__mro__`).
    - Test 2: `User` declares columns `display_name (Text, nullable)`, `banned_at (TIMESTAMPTZ, nullable)`, `token_version (Integer, NOT NULL, server_default '0', python default 0)`, `tenant_id (UUID, nullable, python default from Settings.TENANT_ID_DEFAULT)`.
    - Test 3: `User.refresh_tokens` relationship resolves to `list[RefreshToken]` with `cascade="all, delete-orphan"`.
    - Test 4: `RefreshToken` has `id (UUID PK with both default=uuid4 AND server_default=func.gen_random_uuid())`, `token_hash (Text UNIQUE NOT NULL)`, `user_id (UUID FK users.id ondelete=CASCADE NOT NULL)`, `expires_at (TIMESTAMPTZ NOT NULL)`, `revoked_at (TIMESTAMPTZ nullable)`, `reuse_count (Integer NOT NULL server_default '0')`, `token_version (Integer NOT NULL server_default '0')`, `created_at (TIMESTAMPTZ server_default func.now() NOT NULL)`.
    - Test 5: `UserRead.model_dump(...)` of an instance with `is_superuser=True, display_name="A"` returns a dict containing `is_admin == True` and does NOT contain `is_superuser` (use `Field(exclude=True)` per PATTERNS line 137).
    - Test 6: `UserCreate` and `UserUpdate` accept an optional `display_name: str | None`.
  </behavior>
  <action>
    Create `backend/app/auth/__init__.py` (empty) and `backend/app/auth/models.py`. Module docstring locks the schema (per PATTERNS lines 38-44 audit/models.py shape) and references D-02, D-08, AUTH-01, AUTH-09.

    `User` class signature per D-02: `class User(SQLAlchemyBaseUserTableUUID, Base):` — multiple inheritance. Import `SQLAlchemyBaseUserTableUUID` from `fastapi_users_db_sqlalchemy`. Add custom columns (display_name per D-10 — nullable so Phase 3 wallet + Phase 8 CRM avoid ALTER TABLE, banned_at per CONTEXT line 23 — column shipped now, ban logic in Phase 8, token_version, tenant_id) and the `refresh_tokens` relationship per RESEARCH §"Common Operation 1" lines 1080-1106. Copy `tenant_id` definition verbatim from `audit/models.py` lines 47-51 (PATTERNS §"Tenant ID Ghost Column Pattern" lines 730-739).

    `RefreshToken(Base)` class per RESEARCH lines 1109-1146 — copy verbatim. Include the `token_version` snapshot column per RESEARCH line 1137 (used by `DatabaseStrategy.read_token` for password-reset invalidation, AUTH-06 belt-and-suspenders).

    Create `backend/app/auth/schemas.py` per RESEARCH §"Common Operation 2" lines 1156-1191. `UserRead` extends `schemas.BaseUser[uuid.UUID]`, adds `display_name: str | None = None`. Use `Field(exclude=True)` on `is_superuser` AND a `computed_field` for `is_admin` (defense-in-depth per PATTERNS line 137 — the `Field(exclude=True)` is the authoritative hider; the `computed_field` is the convenience mapping). `UserCreate(schemas.BaseUserCreate)` and `UserUpdate(schemas.BaseUserUpdate)` each add `display_name: str | None = None`.

    Create `backend/tests/auth/test_models.py` per PATTERNS §"backend/tests/auth/test_*.py" lines 539-593; tests verify class hierarchy, column properties via `User.__table__.columns`, relationship config, and `UserRead.model_dump()` exclusion. Use `pytest.mark.asyncio(loop_scope="session")` + `pytest.mark.integration` so the `async_session` fixture applies the migration before the test runs. Per AUTH-01 (D-08, D-09): tests are the truth checker for the model shape.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/auth/test_models.py -x</automated>
    <automated>cd backend &amp;&amp; uv run python -c "from app.auth.models import User, RefreshToken; from fastapi_users_db_sqlalchemy import SQLAlchemyBaseUserTableUUID; from app.db.base import Base; assert SQLAlchemyBaseUserTableUUID in User.__mro__ and Base in User.__mro__; assert {'token_hash', 'user_id', 'expires_at', 'revoked_at', 'reuse_count', 'token_version', 'created_at'}.issubset(RefreshToken.__table__.columns.keys())"</automated>
    <automated>cd backend &amp;&amp; uv run python -c "from app.auth.schemas import UserRead; import uuid; r = UserRead(id=uuid.uuid4(), email='a@b.co', is_active=True, is_verified=False, is_superuser=True); d = r.model_dump(); assert d['is_admin'] is True and 'is_superuser' not in d, d"</automated>
    <automated>cd backend &amp;&amp; uv run ruff check app/auth/ tests/auth/test_models.py</automated>
    <automated>cd backend &amp;&amp; uv run mypy app/auth/models.py app/auth/schemas.py</automated>
  </verify>
  <done>Both ORM classes and three Pydantic schemas exist; `UserRead` hides `is_superuser` and exposes `is_admin`; `pytest tests/auth/test_models.py` green; ruff + mypy clean on `app/auth/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Alembic migration 0002_phase2_auth (users + refresh_tokens)</name>
  <files>backend/alembic/versions/0002_phase2_auth.py, backend/tests/auth/test_migration_0002.py</files>
  <read_first>
    - backend/alembic/versions/0001_phase1_foundations.py (TENANT_DEFAULT constant; revision header pattern; downgrade ordering — drop indexes before tables, drop FK-bearing tables before parents)
    - .planning/phases/02-auth-identity/02-RESEARCH.md §"Common Operation 4" lines 1247-1306 (full migration body — copy verbatim)
    - .planning/phases/02-auth-identity/02-PATTERNS.md §"backend/alembic/versions/0002_phase2_auth.py" lines 383-463
    - backend/tests/conftest.py lines 124-198 (`engine` fixture runs `alembic upgrade head` via testcontainers — verify 0002 chains correctly)
    - backend/app/auth/models.py (created in Task 2 — migration columns MUST match ORM declarations verbatim)
  </read_first>
  <behavior>
    - Test 1: After `alembic upgrade head`, `users` table exists with all 11 columns (id, email, hashed_password, is_active, is_superuser, is_verified, display_name, banned_at, token_version, tenant_id, and any fastapi-users implicit columns).
    - Test 2: `users.id` is UUID PK with `server_default = gen_random_uuid()`; `users.email` has UNIQUE index `ix_users_email`; `users.tenant_id` defaults to `'00000000-0000-0000-0000-000000000001'::uuid` (the TENANT_DEFAULT constant from migration 0001).
    - Test 3: `refresh_tokens` table exists with all 8 columns; `token_hash` is UNIQUE-indexed (ix_refresh_tokens_token_hash); `user_id` has FK to `users.id` ON DELETE CASCADE; `ix_refresh_tokens_user_id` index exists.
    - Test 4: `refresh_tokens.reuse_count` and `refresh_tokens.token_version` both default to `0` server-side.
    - Test 5: `alembic heads` after upgrade returns exactly one head = `0002_phase2_auth`.
    - Test 6: `downgrade()` correctly drops both tables and all indexes; `alembic downgrade -1` returns the DB to revision `0001_phase1_foundations`.
  </behavior>
  <action>
    Create `backend/alembic/versions/0002_phase2_auth.py` per PATTERNS lines 387-455 + RESEARCH §"Common Operation 4" lines 1247-1306:
      - Revision header: `revision: str = "0002_phase2_auth"`, `down_revision: str | None = "0001_phase1_foundations"`, `branch_labels: str | None = None`, `depends_on: str | None = None`.
      - Reuse `TENANT_DEFAULT = "00000000-0000-0000-0000-000000000001"` (single source of truth, Pitfall 10 — declare at module level same as 0001 does).
      - `upgrade()`: create `users` table with columns matching `app/auth/models.py` declarations exactly. UUID PK uses `server_default=sa.text("gen_random_uuid()")`. Email = `sa.String(320)` (fastapi-users base size). `hashed_password = sa.String(1024)`. Booleans use `server_default=sa.text("TRUE"|"FALSE")`. `display_name = sa.Text nullable`. `banned_at = sa.TIMESTAMP(timezone=True) nullable`. `token_version = sa.Integer NOT NULL server_default "0"`. `tenant_id = postgresql.UUID(as_uuid=True) nullable server_default sa.text(f"'{TENANT_DEFAULT}'::uuid")`.
      - Create `users` indexes: `op.create_index("ix_users_email", "users", ["email"], unique=True)`.
      - `upgrade()`: create `refresh_tokens` with columns from RESEARCH lines 1276-1296. FK `user_id` uses `sa.ForeignKey("users.id", ondelete="CASCADE")`.
      - Create `refresh_tokens` indexes: `ix_refresh_tokens_token_hash UNIQUE`, `ix_refresh_tokens_user_id`.
      - `downgrade()`: drop `refresh_tokens` indexes → drop `refresh_tokens` table → drop `users` indexes → drop `users` table (FK-bearing first, then parent). DO NOT use `CASCADE` (let FK CASCADE on the table-create do it).

    Create `backend/tests/auth/test_migration_0002.py` per PATTERNS §"Integration Test Pattern" lines 796-805. Use `engine` + `async_session` fixtures; use `sqlalchemy.inspect(sync_conn).get_columns("users")` (via `engine.sync_engine`) to assert column shapes; assert FK + indexes via `inspect.get_foreign_keys` / `get_indexes`. Verify `alembic_version.version_num == "0002_phase2_auth"` after `upgrade()`.

    Run the migration manually inside testcontainers to verify (this is what `engine` fixture does on first request).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/auth/test_migration_0002.py -x --tb=short</automated>
    <automated>cd backend &amp;&amp; uv run alembic check 2&gt;&amp;1 | head -20</automated>
    <automated>cd backend &amp;&amp; uv run alembic heads | grep -c "0002_phase2_auth"</automated>
    <automated>cd backend &amp;&amp; uv run python -c "from alembic.script import ScriptDirectory; from alembic.config import Config; cfg = Config('alembic.ini'); s = ScriptDirectory.from_config(cfg); rev = s.get_revision('0002_phase2_auth'); assert rev.down_revision == '0001_phase1_foundations'"</automated>
  </verify>
  <done>Migration 0002 applies cleanly on a fresh DB, both tables exist with correct schema, downgrade returns to 0001; `pytest tests/auth/test_migration_0002.py` green; `alembic heads` reports exactly `0002_phase2_auth`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings env vars (host environment → Python process) | SECRET_KEY, RESEND_API_KEY, FIRST_ADMIN_PASSWORD cross from `.env.local`/host into the app at boot |
| Migration SQL → Postgres | Alembic upgrade applies DDL; FK + UNIQUE + DEFAULT constraints become the schema-layer truth |
| User input → ORM persistence (deferred to 02-02) | Phase 2 register/login routes will pass form data into `User`; this plan only locks the column shapes that constrain that input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `.env.example` accidentally leaking real secret values | mitigate | All Phase 2 placeholders use literal `change-me-*` strings and pass `gitleaks protect --staged` (Phase 1 pre-commit + CI gate) |
| T-02-02 | Tampering | New runtime deps (fastapi-users, resend, aiosmtplib) — supply-chain | mitigate | Versions pinned `>=15.0.5,<16` / `>=2.30,<3` / `>=4,<5`; RESEARCH §"Package Legitimacy Audit" lines 184-203 ran slopcheck [OK] on all three (fastapi-users 6yrs/700K+ DLs, resend 2yrs/800K+, aiosmtplib 9yrs/5M+) — Approved |
| T-02-03 | Tampering | `users.tenant_id` ghost column missing or wrong default | mitigate | Migration 0002 reuses TENANT_DEFAULT constant from 0001 (Pitfall 10 single-source); integration test asserts `server_default = '00000000-...-0001'::uuid` |
| T-02-04 | Tampering | SQL injection via raw f-string in migration | accept | Only `TENANT_DEFAULT` (a literal constant, not user input) is interpolated into `sa.text(...)`; same pattern as Plan 01-03 |
| T-02-05 | Information Disclosure | `users.hashed_password` column type wrong (e.g. `VARCHAR(60)` truncates Argon2id ~95-char hash) | mitigate | Column is `sa.String(1024)` per fastapi-users base contract — verified against `SQLAlchemyBaseUserTableUUID` source; integration test asserts column length |
| T-02-06 | Repudiation | UserRead schema leaks `is_superuser` through `model_dump()` → admin escalation footprint visible to player | mitigate | Use BOTH `Field(exclude=True)` on `is_superuser` AND `computed_field` on `is_admin` (defense-in-depth per PATTERNS line 137); unit test asserts `'is_superuser' not in r.model_dump()` |
| T-02-07 | Elevation of Privilege | Migration creates user table BUT no audit trail of who created the schema | accept | Schema migrations are operator-controlled (Pol or Cuco runs `alembic upgrade`); audit-log captures runtime mutations only (per PLT-02 charter) |
| T-02-08 | Denial of Service | `refresh_tokens` table grows unbounded → query slowdown | accept | Phase 2 ships indexes (`token_hash`, `user_id`); a periodic cleanup task is a Phase 11 hardening item (PITFALLS.md "Looks Done But Isn't" #11) — not blocking demo |
| T-02-SC | Tampering | npm/pip installs from PyPI mid-plan | mitigate | All three new packages are `[OK]` per RESEARCH §"Package Legitimacy Audit" lines 184-203; no `[ASSUMED]` / `[SUS]` / `[SLOP]` — no human-verify checkpoint required for this plan |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/auth/ -x` exits 0 (3 new test files green)
- `cd backend && uv lock --check` exits 0 (lockfile in sync with pyproject)
- `cd backend && uv run ruff check app/auth/ tests/auth/` exits 0
- `cd backend && uv run mypy app/auth/` exits 0 (strict mode)
- `cd backend && uv run alembic heads` returns exactly `0002_phase2_auth`
- `pre-commit run gitleaks --files .env.example` exits 0
- `python -c "from app.auth.models import User, RefreshToken"` succeeds at import time (no circular imports, no missing deps)
</verification>

<success_criteria>
- ✅ All 12 Phase 2 env vars added to `Settings` with documented defaults/types; `.env.example` mirrors with safe placeholders.
- ✅ `User(SQLAlchemyBaseUserTableUUID, Base)` and `RefreshToken(Base)` ORM classes exist in `backend/app/auth/models.py`; column definitions match the migration verbatim.
- ✅ Pydantic schemas (`UserRead`, `UserCreate`, `UserUpdate`) live in `backend/app/auth/schemas.py`; `UserRead` exposes `is_admin` and never serializes `is_superuser`.
- ✅ Alembic migration 0002 chains from `0001_phase1_foundations`, creates `users` + `refresh_tokens` tables with all indexes, constraints, and TENANT_DEFAULT; downgrade restores prior state.
- ✅ Three new test files (`test_settings_phase2.py`, `test_models.py`, `test_migration_0002.py`) green under `pytest -x`.
- ✅ Ruff + mypy clean on `app/auth/`; gitleaks clean on `.env.example`.
- ✅ One atomic git commit per task on the `gsd/phase-02-auth-identity` branch.
</success_criteria>

<output>
Create `.planning/phases/02-auth-identity/02-01-SUMMARY.md` when done with: deps locked + lockfile diff hashes, exact columns + indexes on both tables (from `psql \d users` and `psql \d refresh_tokens`), Settings keys + types + defaults, deviations from CONTEXT D-01 ("v14" → v15.0.5 per RESEARCH researcher correction; needs Pol review), test counts (X new tests, all green), and atomic commit hashes (one per task).
</output>

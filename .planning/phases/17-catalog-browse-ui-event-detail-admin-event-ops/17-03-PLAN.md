---
phase: 17-catalog-browse-ui-event-detail-admin-event-ops
plan: 03
type: execute
wave: 3
depends_on: ["17-01"]
files_modified:
  - frontend/src/app/events/[slug]/page.tsx
  - frontend/src/app/events/[slug]/error.tsx
  - frontend/src/components/event/event-detail-view.tsx
  - frontend/src/components/event/outcome-row.tsx
  - frontend/src/components/event/event-detail-view.test.tsx
autonomous: true
requirements: [EVT-02, EVT-03, EVT-05, BRW-06]
tags: [event-detail, per-outcome-rows, framing-lock, bet-reuse, price-history, ws-cap, white-label]

must_haves:
  truths:
    - "Event detail renders every outcome as an INDEPENDENT row with its OWN YES price/odds — never a single bar summing to 100% (the framing LOCK)"
    - "Selecting an outcome mounts the reused OrderEntryForm targeting that child (full YES+NO via fetchMarket(child_slug)) + the reused PriceHistorySection (keyed child_slug) + a SINGLE live socket for the selected child only (WS cap)"
    - "GET /events/{slug} 404 -> the 'Event not found' state; the page is a Server Component shell mirroring app/markets/[slug]/page.tsx; default-selected outcome is SSR-ready (no flash)"
    - "All new surfaces are white-label (brand ring on the selected row, brand bars)"
  artifacts:
    - path: "frontend/src/app/events/[slug]/page.tsx"
      provides: "event detail Server Component (SSR fetchEvent + default child detail + history; 404 state; isAuthenticated)"
      min_lines: 70
    - path: "frontend/src/components/event/event-detail-view.tsx"
      provides: "client island: independent outcome rows + selected-child panel (OrderEntryForm + chart + single socket)"
      min_lines: 90
    - path: "frontend/src/components/event/outcome-row.tsx"
      provides: "one independent outcome row (own YES%, own bar, status chip, selectable)"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/event/event-detail-view.tsx"
      to: "frontend/src/lib/api.ts"
      via: "fetchMarket(child_slug)+fetchPriceHistory on select (full YES+NO outcomes for OrderEntryForm)"
    - from: "frontend/src/components/event/event-detail-view.tsx"
      to: "frontend/src/components/{order-entry-form,price-history-section,market-detail-live-odds}.tsx"
      via: "reuse verbatim for the selected child; single useMarketSocket mount (cap)"
---

<objective>
Build the multi-outcome event detail page (`/events/[slug]`): the per-outcome framing LOCK made visual (independent rows, own YES odds, never sum-to-100; EVT-02), bet-on-one-outcome by reusing `OrderEntryForm` against the selected child binary market (EVT-03), per-outcome price history reusing `PriceHistorySection` (EVT-05), and the storm-proof live-odds cap (one socket = selected child only). Clones `app/markets/[slug]/page.tsx`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: OutcomeRow (independent per-outcome row)</name>
  <files>frontend/src/components/event/outcome-row.tsx</files>
  <analog>frontend/src/components/odds-display.tsx (own YES/NO bar) ; market-card row</analog>
  <action>
    Create `outcome-row.tsx` (`"use client"`) `{label:string; yesPct:number; status:string; selected:boolean; onSelect:()=>void}`. Render a `<button type="button" onClick={onSelect} aria-pressed={selected} aria-label={`${label}, ${yesPct}% YES`}>` full-width, `flex items-center justify-between gap-3 rounded-lg border p-3 text-left transition`, with `ring-2 ring-brand-primary` when `selected`. Left: label (`font-medium truncate`) + a small status chip when status is not OPEN (Closed/Resolved). Right: the YES % (`tabular-nums font-semibold`) + a thin own-bar (reuse `OddsDisplay` with `yes={yesPct} no={100-yesPct}`, OR an inline `h-1.5` bar with `bg-brand-primary` width = yesPct%). This row shows ONLY this outcome's own YES vs its own NO — never a shared cross-outcome bar.
  </action>
  <verify><automated>cd frontend && pnpm exec tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - OutcomeRow is a keyboard-accessible button (`aria-pressed`, `aria-label`) showing its own YES% + own bar + selected brand ring.
    - No cross-outcome/summed bar.
  </acceptance_criteria>
  <done>An independent, accessible, brand-styled per-outcome row exists.</done>
</task>

<task type="auto">
  <name>Task 2: EventDetailView (rows + selected-child panel + single socket)</name>
  <files>frontend/src/components/event/event-detail-view.tsx, frontend/src/components/event/event-detail-view.test.tsx</files>
  <analog>app/markets/[slug]/page.tsx body + price-history-section.tsx (client state+refetch)</analog>
  <action>
    Create `event-detail-view.tsx` (`"use client"`) `{event:EventDetail; defaultChild:MarketDetail; defaultHistory:PricePoint[]; isAuthenticated:boolean}`. State: `selectedMarketId` (init `defaultChild.id`), `child` (init defaultChild), `history` (init defaultHistory), `loading`. `grid grid-cols-1 gap-8 lg:grid-cols-3`. LEFT (`lg:col-span-2 min-w-0`): map `event.outcomes` → `<OutcomeRow key={o.market_id} label={o.label} yesPct={Math.round(parseFloat(o.yes_price)*100)} status={o.child_status} selected={o.market_id===selectedMarketId} onSelect={()=>select(o)} />`. RIGHT (`lg:col-span-1`): a `Card lg:sticky lg:top-8` titled with the selected child's label, containing — when not loading — `MarketDetailLiveOdds` (marketId=child.id, yes/no outcome ids from child.outcomes, initialOdds map) [the ONLY socket], the reused `OrderEntryForm` (marketId=child.id, outcomes=normalizeOutcomes(child) [YES/NO], marketStatus=child.status, isAuthenticated, minStake/maxStake), and `<PriceHistorySection key={child.slug} slug={child.slug} initialPoints={history} />`. `select(o)`: if same id return; set loading; `Promise.allSettled([fetchMarket(o.child_slug), fetchPriceHistory(o.child_slug,"7d")])` → update child+history+selectedMarketId; clear loading (keep prior child on failure). Include a `normalizeOutcomes` helper (map child.outcomes label→YES/NO, like the market page). `event-detail-view.test.tsx`: mock `@/lib/api` (`fetchMarket`/`fetchPriceHistory`), mock the socket hook + bet action; render with a 3-outcome event; assert 3 independent rows each with its own percent, assert NO element represents a single 100%-sum bar, assert the default panel shows the OrderEntryForm for the default child, assert clicking a second row triggers `fetchMarket(child_slug)` and updates the panel.
  </action>
  <verify><automated>cd frontend && pnpm exec vitest run src/components/event/event-detail-view.test.tsx</automated></verify>
  <acceptance_criteria>
    - Renders one independent OutcomeRow per outcome (own YES%); no single summed bar.
    - The selected panel mounts the reused OrderEntryForm + PriceHistorySection + exactly one MarketDetailLiveOdds (single socket).
    - Selecting another outcome client-fetches that child and re-renders the panel.
    - Test green.
  </acceptance_criteria>
  <done>The event detail interaction (independent rows + reused bet/chart/live-odds for the selected child, capped to one socket) works and is tested.</done>
</task>

<task type="auto">
  <name>Task 3: Event detail page + error boundary</name>
  <files>frontend/src/app/events/[slug]/page.tsx, frontend/src/app/events/[slug]/error.tsx</files>
  <analog>app/markets/[slug]/page.tsx (SSR shell, 404 state, cookies, header) + app/markets/[slug]/error.tsx</analog>
  <action>
    Create `app/events/[slug]/page.tsx` (async Server Component) mirroring the market detail shell. `const {slug} = await params`. In a Suspense-wrapped body: `let event:EventDetail` via `fetchEvent(slug)` in try/catch — on `EventNotFound` render the "Event not found" state (clone the market 404 state: heading "Event not found", body "This event doesn't exist or is no longer available.", "Back to markets" → `/`); other errors → an "Unable to load this event" state. Pick the default outcome: first outcome whose `child_status === "OPEN"` sorted by yes_price desc, else `event.outcomes[0]`. SSR `const [mRes,hRes] = await Promise.allSettled([fetchMarket(def.child_slug), fetchPriceHistory(def.child_slug,"7d")])`; if `mRes` rejected → "Unable to load this event"; defaultHistory = hRes fulfilled ? points : []. `cookies()→isAuthenticated`. Header: `<h1 class="text-3xl font-semibold tracking-tight">{event.title}</h1>` + `SourceBadge` + a derived-status badge (reuse `MarketStatusBadge` mapped, or a small inline badge for open/partially_resolved/resolved/void). Render `<EventDetailView event={event} defaultChild={mRes.value} defaultHistory={defaultHistory} isAuthenticated={isAuthenticated} />`. Page shell `w-full max-w-6xl mx-auto px-4 sm:px-6 py-12`. Create `app/events/[slug]/error.tsx` (clone the market error boundary copy → "Unable to load this event").
  </action>
  <verify><automated>cd frontend && pnpm exec tsc --noEmit && cd frontend && pnpm exec eslint "src/app/events/**" src/components/event</automated></verify>
  <acceptance_criteria>
    - The page SSR-fetches the event + default child detail/history, 404→not-found state, derives `isAuthenticated`, renders the header + EventDetailView.
    - Default outcome is SSR-ready (no client-fetch flash on load).
    - Typecheck + lint clean for the new files.
  </acceptance_criteria>
  <done>The event detail route renders the framing-LOCK page server-side with the default outcome ready and a 404/error path.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` clean; `pnpm exec eslint` clean for `src/app/events` + `src/components/event`.
- `cd frontend && pnpm exec vitest run src/components/event` green.
- Code audit: exactly one `MarketDetailLiveOdds`/`useMarketSocket` mount in `event-detail-view.tsx` (the selected child); no stacked/summed outcome bar anywhere.
</verification>

<success_criteria>
- EVT-02: independent per-outcome rows, own YES odds, never sum-to-100.
- EVT-03: bet on one outcome via the reused OrderEntryForm against the constituent binary child.
- EVT-05: per-outcome price history via the reused chart (keyed child_slug).
- Criterion 3: live-odds WS subscription capped to the single on-screen selected child.
- BRW-06: brand-styled selected row + bars.
</success_criteria>

<output>Create 17-03-SUMMARY.md when done.</output>
</content>

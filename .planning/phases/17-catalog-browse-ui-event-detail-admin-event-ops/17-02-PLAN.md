---
phase: 17-catalog-browse-ui-event-detail-admin-event-ops
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/components/catalog/catalog-controls.tsx
  - frontend/src/components/catalog/event-card.tsx
  - frontend/src/components/catalog/event-card.test.tsx
  - frontend/src/components/catalog/catalog-controls.test.tsx
  - frontend/src/components/market-list.tsx
  - frontend/src/components/market-list.test.tsx
autonomous: true
requirements: [BRW-06, EVT-04]
tags: [browse, search, filters, event-card, white-label, empty-state, framing-lock]

must_haves:
  truths:
    - "Homepage / is the curated catalog browse: Server Component reads searchParams, fetchCatalog(params)+fetchCategories(), renders CatalogControls + a grid of (EventCard | adapter->MarketCard) + an explicit empty state"
    - "CatalogControls debounces search (~300ms) to the URL, renders an 'All' chip + only the returned (non-empty) category chips, and status/sort Selects that update the URL"
    - "EventCard is visually distinct from MarketCard: top 2-4 outcomes each with its OWN YES %, '+N more' when >4, an 'Event · N outcomes' badge, links to /events/{slug} — NEVER a single bar summing to 100%"
    - "Every new surface uses brand utilities (bg-/text-/ring-brand-primary), no hardcoded hue (BRW-06)"
  artifacts:
    - path: "frontend/src/components/catalog/event-card.tsx"
      provides: "the distinct multi-outcome catalog card (EVT-04)"
      min_lines: 50
    - path: "frontend/src/components/catalog/catalog-controls.tsx"
      provides: "debounced search + category chips + status/sort selects -> URL"
      min_lines: 70
    - path: "frontend/src/app/page.tsx"
      provides: "catalog browse homepage (searchParams-driven SSR + grid + empty state)"
      min_lines: 40
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/lib/catalog.ts"
      via: "fetchCatalog(params) + fetchCategories() + catalogMarketToMarketItem"
    - from: "frontend/src/components/catalog/catalog-controls.tsx"
      to: "next/navigation"
      via: "useRouter().replace + useSearchParams + usePathname (debounced search)"
---

<objective>
Upgrade the homepage `/` from the plain `MarketList` (`GET /markets`) to the curated catalog browse (`GET /catalog`): search + category chips + status/sort + explicit empty states (BRW-01..05 UI), plus the distinct multi-outcome `EventCard` (EVT-04), all white-label (BRW-06). Reuse `MarketCard` (via the Plan-01 adapter) for `type:"market"` items and `MarketGrid` for the grid. Retire the now-orphaned `MarketList`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: EventCard (distinct multi-outcome card)</name>
  <files>frontend/src/components/catalog/event-card.tsx, frontend/src/components/catalog/event-card.test.tsx</files>
  <analog>frontend/src/components/market-card.tsx (Card + stretched link + OddsDisplay + footer) ; frontend/src/components/__tests__/market-card.test.tsx</analog>
  <action>
    Create `event-card.tsx` (Server Component) taking `{event: CatalogItem}` (type:"event"). Sort `event.outcomes` by `yes_price` desc; take top 4 as `shown`, `more = outcomes.length - shown.length`. Card with the same hover/focus affordances as MarketCard (`group relative … focus-within:ring-2 focus-within:ring-brand-primary`). Header: title `<h3 class="text-base font-semibold leading-snug line-clamp-2">` wrapping a stretched `Link href={`/events/${event.slug}`} className="after:absolute after:inset-0"` + a `Badge` (`relative z-10`, top-right) reading `Event · {outcomes.length} outcomes`. Content: each `shown` outcome as a row — label (`truncate text-sm`) + its own YES `Math.round(parseFloat(yes_price)*100)`% (`tabular-nums text-sm`, right) + a thin own-bar (a div `h-1.5 rounded-full bg-zinc-200` with an inner `bg-brand-primary` width = that pct%). When `more > 0`, a final muted `+{more} more` row (`text-xs text-zinc-500`). Footer: `Vol: {formatVolume(event.volume)}` + `formatDeadline(event.deadline ?? "")` + `SourceBadge` (`relative z-10`). Each outcome bar represents ONLY that outcome's own YES — never a shared 100% bar. `event-card.test.tsx`: mock `next/link`; assert the badge text, that exactly ≤4 outcome labels render + the "+N more" row when >4, each shows its own percent, and the link href is `/events/{slug}`.
  </action>
  <verify><automated>cd frontend && pnpm exec vitest run src/components/catalog/event-card.test.tsx</automated></verify>
  <acceptance_criteria>
    - EventCard renders top ≤4 outcomes each with its own YES %, "+N more" when >4, the "Event · N outcomes" badge, and links to `/events/{slug}`.
    - No single stacked/100%-sum bar; each outcome has its own independent bar.
    - Brand accent via `bg-brand-primary` (no hardcoded hue).
    - Test green.
  </acceptance_criteria>
  <done>The distinct multi-outcome event card exists, is framing-LOCK-correct, and is tested.</done>
</task>

<task type="auto">
  <name>Task 2: CatalogControls (debounced search + category chips + status/sort -> URL)</name>
  <files>frontend/src/components/catalog/catalog-controls.tsx, frontend/src/components/catalog/catalog-controls.test.tsx</files>
  <analog>frontend/src/components/admin/markets-data-table.tsx (filter bar Selects + URL) ; order-entry-form.tsx (shadcn Select)</analog>
  <action>
    Create `catalog-controls.tsx` (`"use client"`) `{categories:string[]; q?:string; category?:string; status?:string; sort?:string}`. Use `useRouter`, `usePathname`, `useSearchParams`. A `setParam(key, value)` helper builds a new `URLSearchParams` from the current ones, sets/deletes the key, and `router.replace(`${pathname}?${sp}`)`. Filter bar `flex flex-wrap items-end gap-4`: (a) a search `Input` (`Search` icon, label "Search markets", controlled by local state seeded from `q`, debounced ~300ms via a `useRef<timeout>` cleared on unmount → `setParam("q", value)`); (b) a category chip row (`flex gap-2 overflow-x-auto`): an "All" chip (active when no `category`) + one chip per `categories` entry — active chip `bg-brand-primary text-brand-primary-foreground`, inactive `bg-zinc-100 text-zinc-700 hover:bg-zinc-200`, each a `<button aria-pressed>` calling `setParam("category", c)` (or delete for "All"); (c) a status `Select` (All/Open/Closing soon/Resolved → `setParam("status", …)`); (d) a sort `Select` (Volume/Closing soonest/Newest, default volume). `catalog-controls.test.tsx`: mock `next/navigation` (`useRouter.replace` spy, `useSearchParams`→`new URLSearchParams()`, `usePathname`→"/"); assert typing in search (after the debounce, using fake timers) calls `replace` with `q=`; assert only the provided categories render (no empty category chip); assert clicking a category + changing status call `replace` with the right param.
  </action>
  <verify><automated>cd frontend && pnpm exec vitest run src/components/catalog/catalog-controls.test.tsx</automated></verify>
  <acceptance_criteria>
    - Search debounces to `router.replace` with `q`; status/sort/category update the URL.
    - Only the provided (non-empty) categories render as chips (+ an "All" chip).
    - Active chip uses `bg-brand-primary` (BRW-06).
    - Test green.
  </acceptance_criteria>
  <done>The browse controls drive the URL (shareable, SSR-fresh) with brand-styled chips and an explicit category set.</done>
</task>

<task type="auto">
  <name>Task 3: Homepage catalog browse + retire MarketList</name>
  <files>frontend/src/app/page.tsx, frontend/src/components/market-list.tsx, frontend/src/components/market-list.test.tsx</files>
  <analog>existing app/page.tsx + app/markets/[slug]/page.tsx (Promise.allSettled, degrade-to-empty)</analog>
  <action>
    Rewrite `app/page.tsx` as an async Server Component `Home({searchParams}:{searchParams:Promise<{q?;category?;status?;sort?:string}>})`. `const sp = await searchParams`. `const [catRes, catalogRes] = await Promise.allSettled([fetchCategories(), fetchCatalog({q:sp.q, category:sp.category, status:sp.status as PublicCatalogStatus|undefined, sort:sp.sort as CatalogSort|undefined})])`. categories → `[]` on reject. If catalog rejects → render an error block (reuse the shipped "Failed to load" copy). Shell unchanged (`w-full max-w-6xl mx-auto px-4 sm:px-6 py-12`, H1 "Markets"). Render `<CatalogControls categories={categories} q={sp.q} category={sp.category} status={sp.status} sort={sp.sort} />`. Then: if `items.length === 0` → the empty block (centered, "No markets found" / "No markets match your current filters…"); else `<MarketGrid>{items.map(it => it.type === "event" ? <EventCard key={it.id} event={it}/> : <MarketCard key={it.id} market={catalogMarketToMarketItem(it)}/>)}</MarketGrid>`. Verify (Grep) `MarketList` has no other importer; then DELETE `components/market-list.tsx` + `components/market-list.test.tsx` (orphaned by this change; `market-card.tsx` stays — reused). Keep `MarketListSkeleton` if still used elsewhere; if it was only used by the old homepage Suspense, leave it (harmless) or remove if orphaned.
  </action>
  <verify><automated>cd frontend && pnpm exec tsc --noEmit && cd frontend && pnpm exec eslint src/app/page.tsx src/components/catalog</automated></verify>
  <acceptance_criteria>
    - `/` server-fetches the catalog + categories, renders controls + grid (events as EventCard, markets as adapted MarketCard) + an explicit empty state.
    - `MarketList` (+ its test) is removed and no longer imported anywhere; the app typechecks.
    - No hardcoded brand hue introduced.
  </acceptance_criteria>
  <done>The homepage is the curated, white-label catalog browse; the orphaned MarketList is retired.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` clean; `pnpm exec eslint src` clean for the new files.
- `cd frontend && pnpm exec vitest run src/components/catalog` green.
- Grep: no `summing|stacked|100%` outcome bar in `event-card.tsx`; no remaining import of `market-list`.
</verification>

<success_criteria>
- BRW-01..05 (UI): search/category/status/sort drive a bounded browse with an explicit empty state.
- EVT-04: distinct multi-outcome event card (top 2-4 + %, "+N more").
- BRW-06: brand utilities throughout.
</success_criteria>

<output>Create 17-02-SUMMARY.md when done.</output>
</content>

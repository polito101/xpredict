---
phase: 08-admin-crm-user-management-audit-log-viewer
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/src/components/ui/table.tsx
  - frontend/src/components/ui/tabs.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/textarea.tsx
  - frontend/src/components/ui/separator.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/lib/admin-api.ts
  - frontend/src/app/admin/layout.tsx
  - frontend/src/app/admin/users/page.tsx
  - frontend/src/app/admin/users/[id]/page.tsx
  - frontend/src/app/admin/audit-log/page.tsx
  - frontend/src/components/admin/users-data-table.tsx
  - frontend/src/components/admin/user-status-badge.tsx
  - frontend/src/components/admin/user-detail-tabs.tsx
  - frontend/src/components/admin/profile-tab.tsx
  - frontend/src/components/admin/wallet-tab.tsx
  - frontend/src/components/admin/bets-tab.tsx
  - frontend/src/components/admin/recharge-form.tsx
  - frontend/src/components/admin/ban-confirm-dialog.tsx
  - frontend/src/components/admin/unban-confirm-dialog.tsx
  - frontend/src/components/admin/audit-log-table.tsx
  - frontend/src/components/admin/audit-payload-viewer.tsx
  - frontend/src/components/admin/export-csv-button.tsx
  - frontend/src/components/admin/admin-search-input.tsx
  - frontend/src/components/admin/date-range-filter.tsx
  - frontend/src/components/admin/pagination-controls.tsx
  - frontend/src/app/layout.tsx
autonomous: false
requirements:
  - ADU-01
  - ADU-02
  - ADU-04
  - ADU-05
  - ADU-06
  - ADD-04

user_setup: []

must_haves:
  truths:
    - "Admin sees paginated user list at /admin/users with search, status filter, date filter, and sort"
    - "Admin can click a user row to navigate to /admin/users/{id} detail page"
    - "Admin user detail page has Profile, Wallet, and Bets tabs"
    - "Admin can ban a user via dialog with mandatory reason"
    - "Admin can unban a user via dialog with optional reason"
    - "Recharge form on wallet tab calls existing Phase 3 recharge endpoint"
    - "Recharge form is disabled when user is banned"
    - "Admin can export users/transactions/bets to CSV via dropdown"
    - "Audit log page at /admin/audit-log shows paginated entries filterable by event type, actor, and date"
    - "Audit log JSONB payload is collapsible/expandable"
    - "Admin nav links Users and Audit log are active; Markets is still placeholder"
  artifacts:
    - path: "frontend/src/app/admin/users/page.tsx"
      provides: "User list page with DataTable"
    - path: "frontend/src/app/admin/users/[id]/page.tsx"
      provides: "User detail page with tabs"
    - path: "frontend/src/app/admin/audit-log/page.tsx"
      provides: "Audit log viewer page"
    - path: "frontend/src/lib/admin-api.ts"
      provides: "Server Action helper for admin API calls with Bearer forwarding"
      exports: ["adminApiFetch"]
    - path: "frontend/src/components/admin/users-data-table.tsx"
      provides: "TanStack Table v8 data table for user list"
  key_links:
    - from: "frontend/src/app/admin/users/page.tsx"
      to: "frontend/src/lib/admin-api.ts"
      via: "adminApiFetch for initial data load"
      pattern: "adminApiFetch.*admin/users"
    - from: "frontend/src/components/admin/recharge-form.tsx"
      to: "frontend/src/lib/admin-api.ts"
      via: "adminApiFetch POST to recharge endpoint"
      pattern: "adminApiFetch.*admin/wallets"
    - from: "frontend/src/components/admin/ban-confirm-dialog.tsx"
      to: "frontend/src/lib/admin-api.ts"
      via: "adminApiFetch POST to ban endpoint"
      pattern: "adminApiFetch.*ban"
---

<objective>
Build the complete frontend admin CRM surface: user list page with TanStack Table v8 (ADU-01 per D-06), user detail page with tabbed layout (ADU-02 per D-07), ban/unban dialogs (ADU-04/ADU-05 per D-01/D-04), recharge form calling Phase 3 primitive, CSV export dropdown (ADU-06 per D-08), and audit log viewer with collapsible JSONB payload (ADD-04 per D-11/D-12/D-13). Activate admin nav links.

Purpose: Deliver the "I can manage my customers" demo surface for the operator. This is the visible CRM that makes the operator trust the platform.
Output: 3 new admin pages + 15 new components + admin API helper + shadcn component installs + nav link activation.
</objective>

<execution_context>
@C:\Users\pobom\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pobom\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-crm-user-management-audit-log-viewer/08-CONTEXT.md
@.planning/phases/08-admin-crm-user-management-audit-log-viewer/08-RESEARCH.md
@.planning/phases/08-admin-crm-user-management-audit-log-viewer/08-PATTERNS.md
@.planning/phases/08-admin-crm-user-management-audit-log-viewer/08-UI-SPEC.md
@.planning/phases/08-admin-crm-user-management-audit-log-viewer/08-01-SUMMARY.md
@.planning/phases/08-admin-crm-user-management-audit-log-viewer/08-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Package installs: @tanstack/react-table and sonner (both [ASSUMED] per slopcheck unavailability in RESEARCH.md Package Legitimacy Audit)</what-built>
  <how-to-verify>
    Verify these packages are legitimate before installation:

    1. @tanstack/react-table:
       - Visit https://www.npmjs.com/package/@tanstack/react-table
       - Confirm: publisher is "tanstack", repo is github.com/TanStack/table, weekly downloads 2M+
       - Locked by D-06

    2. sonner:
       - Visit https://www.npmjs.com/package/sonner
       - Confirm: publisher is "emilkowalski", repo is github.com/emilkowalski/sonner, weekly downloads 1M+
       - Referenced in UI-SPEC

    Both are well-known packages with high star counts. This checkpoint is required by the Package Legitimacy Gate protocol because slopcheck was unavailable during research.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installation, or flag any concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install frontend dependencies and shadcn components</name>
  <files>
    frontend/package.json,
    frontend/pnpm-lock.yaml,
    frontend/src/components/ui/table.tsx,
    frontend/src/components/ui/tabs.tsx,
    frontend/src/components/ui/dialog.tsx,
    frontend/src/components/ui/dropdown-menu.tsx,
    frontend/src/components/ui/select.tsx,
    frontend/src/components/ui/textarea.tsx,
    frontend/src/components/ui/separator.tsx,
    frontend/src/components/ui/tooltip.tsx,
    frontend/src/components/ui/sonner.tsx,
    frontend/src/app/layout.tsx
  </files>
  <read_first>
    frontend/package.json,
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/app/layout.tsx
  </read_first>
  <action>
    Install new npm dependencies in frontend/:
    - `pnpm add @tanstack/react-table` (v8.21.x)
    - `pnpm add sonner` (v2.x, if not already a shadcn dependency)

    Install shadcn UI components (each via `pnpm dlx shadcn@latest add`):
    - table, tabs, dialog, dropdown-menu, select, textarea, separator, tooltip, sonner

    After installation, verify `pnpm build` and `pnpm typecheck` still pass.

    Add the Toaster provider to the root layout (`frontend/src/app/layout.tsx`). Import `Toaster` from `@/components/ui/sonner` and render it inside the body, after the children. Follow the sonner/shadcn pattern for Next.js App Router.

    Verify Next.js version: run `pnpm list next` to confirm actual installed version. Adapt patterns if needed (Next 15 vs 16 -- the key difference is whether `cookies()` is async).
  </action>
  <verify>
    <automated>cd frontend && pnpm build && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "@tanstack/react-table" in dependencies
    - frontend/src/components/ui/table.tsx exists
    - frontend/src/components/ui/tabs.tsx exists
    - frontend/src/components/ui/dialog.tsx exists
    - frontend/src/components/ui/sonner.tsx exists
    - frontend/src/app/layout.tsx contains Toaster import and render
    - `cd frontend && pnpm build` exits 0
    - `cd frontend && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    All shadcn components installed, @tanstack/react-table added, Toaster wired in root layout, build + typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Admin API helper, nav links, user list page, and export button</name>
  <files>
    frontend/src/lib/admin-api.ts,
    frontend/src/app/admin/layout.tsx,
    frontend/src/app/admin/users/page.tsx,
    frontend/src/components/admin/users-data-table.tsx,
    frontend/src/components/admin/user-status-badge.tsx,
    frontend/src/components/admin/admin-search-input.tsx,
    frontend/src/components/admin/date-range-filter.tsx,
    frontend/src/components/admin/pagination-controls.tsx,
    frontend/src/components/admin/export-csv-button.tsx
  </files>
  <read_first>
    frontend/src/lib/auth.ts,
    frontend/src/lib/api.ts,
    frontend/src/app/admin/layout.tsx,
    frontend/src/app/admin/page.tsx,
    frontend/src/app/admin/login/admin-login-form.tsx,
    frontend/src/components/ui/table.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/input.tsx,
    frontend/src/components/ui/select.tsx,
    frontend/src/components/ui/dropdown-menu.tsx
  </read_first>
  <action>
    **admin-api.ts** -- Server Action module ("use server") for admin API calls. Read the existing `auth.ts` pattern for admin_jwt cookie forwarding. Create:
    - `adminApiFetch(path: string, init?: RequestInit)`: reads `admin_jwt` cookie via `(await cookies()).get("admin_jwt")?.value`, constructs `Authorization: Bearer {token}` header, fetches `BACKEND_URL + path` with `cache: "no-store"`. Returns parsed JSON. Throws on !res.ok with status code in message.
    - `adminApiExport(path: string)`: Same Bearer forwarding but returns raw Response (for CSV blob download). The frontend component will extract the blob.
    - Type definitions: `PaginatedResponse<T>` (items, total, page, page_size, pages), `UserListItem` (id, email, display_name, status, banned_at, created_at, last_activity, balance).

    **admin/layout.tsx** -- Replace placeholder `<span>` elements per UI-SPEC Layout Contract. Change "Users" to `<Link href="/admin/users">Users</Link>` and "Audit log" to `<Link href="/admin/audit-log">Audit log</Link>`. "Markets" stays as disabled placeholder. Use `usePathname()` to highlight active link with `text-zinc-900 font-semibold underline underline-offset-4` (dark: `text-zinc-50`). Inactive: `text-zinc-500 hover:text-zinc-900` (dark variants).

    **users/page.tsx** -- Server Component that does initial data fetch via `adminApiFetch("/api/v1/admin/users?page=1&page_size=20")` and passes data to the `UsersDataTable` client component. Layout: `max-w-6xl mx-auto px-6 py-12`, heading "Users" (`text-xl font-semibold`), `ExportCsvButton` top right.

    **users-data-table.tsx** -- "use client" component using TanStack Table v8 with `useReactTable`, `getCoreRowModel`, `manualPagination: true`, `manualSorting: true`. Columns per D-06: email, display_name (truncated with tooltip), status (UserStatusBadge), signup date (formatted), last activity (relative time like "2h ago"), balance (MoneyStr formatted). Inline "View" link in last column. Entire row clickable with `cursor-pointer` and `router.push('/admin/users/{id}')`. State: `pagination` (pageIndex, pageSize), `sorting` (SortingState). On state change, call `adminApiFetch` with updated query params (search, status, sort_by, sort_order, page, page_size). Search and filters reset page to 1. Use `AdminSearchInput` for debounced search (300ms, per UI-SPEC). Use `Select` dropdown for status filter ("All statuses", "Active", "Banned"). Use `DateRangeFilter` for signup date range. Render shadcn `Table` component. Include loading skeleton (5 rows) and empty state per UI-SPEC copywriting contract.

    **user-status-badge.tsx** -- Simple badge component: "Active" with `bg-emerald-100 text-emerald-700` (dark variants), "Banned" with `bg-red-100 text-red-700` (dark variants). Include `aria-label` per UI-SPEC accessibility.

    **admin-search-input.tsx** -- Debounced search input (300ms) with `Search` lucide icon. Calls onChange callback with debounced value. Width `min-w-[280px]`.

    **date-range-filter.tsx** -- Two date inputs (type="date") labeled "From" / "To". Calls onChange with { from, to } pair.

    **pagination-controls.tsx** -- Reusable pagination: "< Prev" / "Next >" buttons + "Page X of Y" text. Prev disabled on page 1, Next disabled on last page. `aria-label` per UI-SPEC.

    **export-csv-button.tsx** -- "use client" component. Button with `outline` variant labeled "Export". DropdownMenu with 3 items: "Export Users (CSV)", "Export Transactions (CSV)", "Export Bets (CSV)". Each triggers `adminApiExport("/api/v1/admin/export/{type}?{current_filters}")`, creates blob URL, triggers download. Shows spinner during export. Toast "Export started" on click via sonner.
  </action>
  <verify>
    <automated>cd frontend && pnpm build && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/admin-api.ts contains adminApiFetch function that reads admin_jwt cookie
    - frontend/src/app/admin/layout.tsx contains Link elements for "/admin/users" and "/admin/audit-log"
    - frontend/src/app/admin/layout.tsx does NOT contain Link for Markets (stays as disabled span)
    - frontend/src/app/admin/users/page.tsx exists and renders UsersDataTable
    - frontend/src/components/admin/users-data-table.tsx imports from "@tanstack/react-table"
    - frontend/src/components/admin/users-data-table.tsx uses manualPagination: true
    - frontend/src/components/admin/user-status-badge.tsx renders "Active" or "Banned" badge
    - frontend/src/components/admin/export-csv-button.tsx renders DropdownMenu with 3 CSV options
    - `cd frontend && pnpm build` exits 0
    - `cd frontend && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    User list page renders with TanStack DataTable, search/filter/sort/pagination all trigger server-side refetch, export CSV dropdown works, admin nav links active. Build and typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: User detail page with tabs + ban/unban dialogs + audit log page</name>
  <files>
    frontend/src/app/admin/users/[id]/page.tsx,
    frontend/src/components/admin/user-detail-tabs.tsx,
    frontend/src/components/admin/profile-tab.tsx,
    frontend/src/components/admin/wallet-tab.tsx,
    frontend/src/components/admin/bets-tab.tsx,
    frontend/src/components/admin/recharge-form.tsx,
    frontend/src/components/admin/ban-confirm-dialog.tsx,
    frontend/src/components/admin/unban-confirm-dialog.tsx,
    frontend/src/app/admin/audit-log/page.tsx,
    frontend/src/components/admin/audit-log-table.tsx,
    frontend/src/components/admin/audit-payload-viewer.tsx
  </files>
  <read_first>
    frontend/src/lib/admin-api.ts,
    frontend/src/components/ui/tabs.tsx,
    frontend/src/components/ui/dialog.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/form.tsx,
    frontend/src/components/ui/textarea.tsx,
    frontend/src/components/ui/separator.tsx,
    frontend/src/components/ui/tooltip.tsx,
    frontend/src/components/ui/sonner.tsx,
    frontend/src/components/admin/users-data-table.tsx,
    frontend/src/components/admin/pagination-controls.tsx,
    frontend/src/components/admin/user-status-badge.tsx,
    frontend/src/components/admin/admin-search-input.tsx,
    frontend/src/components/admin/date-range-filter.tsx,
    frontend/src/app/admin/login/admin-login-form.tsx
  </read_first>
  <action>
    **users/[id]/page.tsx** -- Server Component. Fetches user detail via `adminApiFetch("/api/v1/admin/users/{id}")`. Layout per UI-SPEC: back link ("Back to users" with ArrowLeft icon), user email as heading, display name, metadata row (joined date, last active), ban/unban button top right. Passes data to `UserDetailTabs` client component.

    **user-detail-tabs.tsx** -- "use client" wrapper using shadcn Tabs. Three tabs: "Profile", "Wallet", "Bets". Each tab renders its respective component. Tab switch is client-side only (no URL change per UI-SPEC). Each tab manages its own state independently.

    **profile-tab.tsx** -- Two-column key-value layout per UI-SPEC. Fields: Email, Display Name, Verified (green check + date or red X + "Not verified"), Status (UserStatusBadge, with ban date if banned), Signup Date, Last Activity. Label column 160px. Uses `text-sm font-medium text-zinc-500` for labels.

    **wallet-tab.tsx** -- "use client". Shows balance (`text-2xl font-semibold` with $ prefix, 4 decimal places). Contains RechargeForm (disabled when banned, with tooltip "Cannot recharge a banned user"). Contains transaction history table using PaginationControls. Fetches transactions via `adminApiFetch("/api/v1/admin/users/{id}/transactions?page=...")`. Credits in `emerald-700` with + prefix, debits in `red-700` with - prefix. Columns: Type, Amount, Reason, Date.

    **bets-tab.tsx** -- "use client". Paginated bets table. Fetches via `adminApiFetch("/api/v1/admin/users/{id}/bets?page=...")`. Columns per UI-SPEC: Market (truncated 40 chars with tooltip), Outcome, Stake, Status (badge: Won=emerald, Lost=red, Open=zinc), P&L (positive emerald, negative red, open "--"). Uses PaginationControls.

    **recharge-form.tsx** -- "use client". Inline card form with Amount ($ prefix, numeric input) and Reason (required text). Uses react-hook-form + zod validation (amount > 0, reason min 1 char). Submit calls `adminApiFetch` as POST to `/api/v1/admin/wallets/{user_id}/recharge` with body `{amount, reason}` and `Idempotency-Key` header (UUID v4 generated per submission). On success: toast "Wallet recharged successfully" (sonner), refresh balance + transaction table. On error: toast "Failed to recharge wallet". Entire form disabled (greyed out) when user is banned.

    **ban-confirm-dialog.tsx** -- "use client". shadcn Dialog. Title "Ban user", description per UI-SPEC copywriting. Mandatory reason Textarea (min 1 char, validated before submit). "Confirm ban" button in `destructive` variant, shows spinner during submission. Calls `adminApiFetch` POST to `/api/v1/admin/users/{id}/ban` with `{reason}`. On success: toast "User has been banned", dialog closes, parent refetches user data. "Cancel" button closes dialog.

    **unban-confirm-dialog.tsx** -- "use client". Same pattern but reason is optional. "Confirm unban" button in `default` variant. Calls POST `/api/v1/admin/users/{id}/unban`. Toast "User has been unbanned".

    **audit-log/page.tsx** -- Server Component. Fetches first page via `adminApiFetch("/api/v1/admin/audit-log?page=1&page_size=50")` and event types via `adminApiFetch("/api/v1/admin/audit-log/event-types")`. Passes to AuditLogTable client component. Layout: heading "Audit Log", filter bar (event type Select dropdown populated from event-types endpoint + "All events" option, AdminSearchInput for actor, DateRangeFilter), table, pagination.

    **audit-log-table.tsx** -- "use client". TanStack Table with manualPagination. Columns per UI-SPEC: Timestamp (formatted "MMM DD HH:mm:ss"), Event Type (Badge secondary variant, `font-mono text-xs`), Actor (truncated 20 chars with tooltip), Payload (AuditPayloadViewer). Default page_size=50. Filter changes refetch with server-side params.

    **audit-payload-viewer.tsx** -- "use client". Collapsed by default: one-line preview (JSON.stringify, truncated 80 chars, muted text). Chevron toggle (ChevronDown/ChevronUp from lucide-react). Expanded: `<pre>` block with `bg-zinc-50 dark:bg-zinc-900 rounded-md p-4 text-xs font-mono`, `JSON.stringify(payload, null, 2)`. Toggle tracks `aria-expanded`. Content region has `role="region"` with `aria-label="Audit event payload"`.

    Follow all UI-SPEC states (loading skeleton, empty state, error state, populated) for each page. Loading skeletons use `aria-hidden="true"` on skeleton elements and `aria-busy="true"` on parent.
  </action>
  <verify>
    <automated>cd frontend && pnpm build && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/admin/users/[id]/page.tsx exists and fetches user detail
    - frontend/src/components/admin/user-detail-tabs.tsx renders 3 tabs: Profile, Wallet, Bets
    - frontend/src/components/admin/recharge-form.tsx calls /api/v1/admin/wallets/{user_id}/recharge with Idempotency-Key header
    - frontend/src/components/admin/recharge-form.tsx is disabled when user is banned
    - frontend/src/components/admin/ban-confirm-dialog.tsx requires reason (min 1 char) before submit
    - frontend/src/components/admin/unban-confirm-dialog.tsx has optional reason field
    - frontend/src/app/admin/audit-log/page.tsx exists and renders AuditLogTable
    - frontend/src/components/admin/audit-log-table.tsx uses manualPagination with page_size=50
    - frontend/src/components/admin/audit-payload-viewer.tsx has aria-expanded toggle
    - `cd frontend && pnpm build` exits 0
    - `cd frontend && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    User detail page with tabs, ban/unban dialogs, recharge form, and audit log viewer are all built. All states (loading, empty, error, populated) handled. Build and typecheck clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Admin CRM surface: user list with search/filter/sort/pagination, user detail with profile/wallet/bets tabs, ban/unban with dialogs, recharge form, CSV export, and audit log viewer with collapsible JSONB payload. All backed by the API endpoints from Plans 01 and 02.</what-built>
  <how-to-verify>
    Start the dev stack and verify the following:

    1. Log in as admin at /admin/login
    2. Navigate to /admin/users -- verify:
       - User list loads with columns (email, name, status badge, signup date, last activity, balance)
       - Search by email works (type a partial email, results filter after 300ms)
       - Status filter dropdown works ("Active" / "Banned" / "All statuses")
       - Column sorting works (click a header)
       - Pagination works (if enough users)
       - Click "Export" dropdown -- 3 CSV options appear

    3. Click a user row to navigate to /admin/users/{id} -- verify:
       - "Back to users" link at top
       - Email and display name shown
       - 3 tabs: Profile, Wallet, Bets
       - Profile tab shows key-value fields
       - Wallet tab shows balance and recharge form
       - Bets tab shows bets table (may be empty)

    4. Test recharge:
       - Enter amount and reason in recharge form, submit
       - Toast "Wallet recharged successfully" appears
       - Balance updates, transaction appears in history

    5. Test ban:
       - Click "Ban User" button
       - Dialog opens with mandatory reason textarea
       - Enter reason, confirm
       - Toast "User has been banned" appears
       - Button changes to "Unban User", status badge shows "Banned"
       - Recharge form becomes disabled with tooltip

    6. Test unban:
       - Click "Unban User" button
       - Dialog opens with optional reason
       - Confirm
       - Status returns to "Active", recharge form re-enabled

    7. Navigate to /admin/audit-log -- verify:
       - Entries load with timestamp, event type badge, actor, payload preview
       - Event type dropdown filter works
       - Actor search works
       - Click payload chevron to expand -- formatted JSON appears
       - Pagination works (page size 50)

    8. Admin nav: "Users" and "Audit log" are clickable links; "Markets" is greyed out
  </how-to-verify>
  <resume-signal>Type "approved" if the CRM surface works as described, or list issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Next.js Server Actions | admin_jwt cookie forwarded as Bearer to FastAPI |
| Client Components -> Backend API | All requests go through adminApiFetch which adds Bearer |
| CSV blob download | Export triggers browser download; CSV was sanitized server-side |
| User input (recharge amount, ban reason) | Validated client-side via zod + server-side via Pydantic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Spoofing | Admin API calls from frontend | mitigate | adminApiFetch reads admin_jwt HttpOnly cookie via Server Action and forwards as Bearer; no client-side token exposure |
| T-08-11 | Tampering | Recharge double-submit | mitigate | Idempotency-Key UUID v4 header per submission; Phase 3 recharge endpoint enforces idempotency |
| T-08-12 | Information Disclosure | Admin JWT in client code | mitigate | admin_jwt is HttpOnly; adminApiFetch is "use server" -- token never reaches browser JS |
| T-08-13 | Denial of Service | Repeated export requests | accept | No rate limit on export in v1; batch CSV capped at 10000 rows server-side (Plan 02) |
| T-08-SC | Tampering | npm installs (@tanstack/react-table, sonner) | mitigate | Human checkpoint (Task 1) verifies package legitimacy on npmjs.com before install |
</threat_model>

<verification>
1. `cd frontend && pnpm build` -- build succeeds
2. `cd frontend && pnpm typecheck` -- no type errors
3. Manual verification: admin CRM pages load, search/filter/sort/pagination work, ban/unban/recharge work, audit log displays and filters, CSV export downloads
</verification>

<success_criteria>
Complete admin CRM frontend surface operational: 3 pages (user list, user detail, audit log) with all interactions (search, filter, sort, pagination, ban/unban, recharge, CSV export, JSONB expand/collapse) working against the backend API. Build and typecheck clean. Human verification approved.
</success_criteria>

<output>
Create `.planning/phases/08-admin-crm-user-management-audit-log-viewer/08-03-SUMMARY.md` when done
</output>

---
phase: 12-admin-market-operations-ui-and-player-resolution-display
plan: 06
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-04", "12-05"]
files_modified:
  - frontend/src/components/admin/resolve-market-dialog.tsx
  - frontend/src/components/admin/reverse-settlement-dialog.tsx
  - frontend/src/components/admin/force-settle-dialog.tsx
  - frontend/src/components/admin/close-market-dialog.tsx
  - frontend/src/components/admin/__tests__/settlement-dialogs.test.tsx
  - frontend/src/app/admin/markets/[id]/page.tsx
  - frontend/src/components/admin/kpi-card.tsx
autonomous: false
requirements: [STL-02, STL-07, ADM-05, ADM-06, ADM-04]
must_haves:
  truths:
    - "From the admin market detail page an admin can resolve a house market via a two-step confirm + mandatory justification (with a YES/NO outcome Select)"
    - "An admin can reverse a settlement (justification only) and force-settle a stuck Polymarket market (outcome + justification), each two-step + mandatory justification"
    - "An admin can close an OPEN market early; the action buttons are gated by market status + source"
    - "Each settlement dialog blocks empty justification client-side (matching backend min_length=1), stays open during submit, toasts on success, and the parent refetches"
    - "The KPI 'Pending resolutions' card deep-links to /admin/markets?status=CLOSED"
    - "The reverse dialog copy does NOT promise clean re-resolution (Pitfall 5)"
  artifacts:
    - path: "frontend/src/components/admin/resolve-market-dialog.tsx"
      provides: "two-step resolve with outcome Select + mandatory justification"
      contains: "Confirm resolve"
    - path: "frontend/src/app/admin/markets/[id]/page.tsx"
      provides: "admin market detail: edit form + status/source-gated action buttons"
      contains: "MarketForm"
  key_links:
    - from: "frontend/src/components/admin/resolve-market-dialog.tsx"
      to: "resolveMarket"
      via: "bare /admin/markets/{id}/resolve via the 12-02 wrapper"
      pattern: "resolveMarket\\("
    - from: "frontend/src/app/admin/markets/[id]/page.tsx"
      to: "resolve/reverse/force-settle/close dialogs"
      via: "status + source gating (OPEN/CLOSED house→resolve; OPEN/CLOSED poly→force-settle; RESOLVED→reverse; OPEN→close)"
      pattern: "(ResolveMarketDialog|ReverseSettlementDialog|ForceSettleDialog)"
    - from: "frontend/src/components/admin/kpi-card.tsx"
      to: "/admin/markets?status=CLOSED"
      via: "Pending resolutions card Link"
      pattern: "status=CLOSED"
---

<objective>
Wire the admin settlement actions (BLOCKER-2): resolve / reverse / force-settle two-step confirm dialogs + the early-close action, hosted on a new `/admin/markets/[id]` detail page that also embeds the edit form (from 12-05), with the KPI "pending resolutions" card deep-linking in. This is the last seam that makes the operator path reachable through the product, and it completes SC#5 (the end-to-end-through-UI acceptance).

Purpose: `/admin/markets/{id}/resolve|reverse|force-settle` and `/close` all exist, are admin-gated, and are tested — but no frontend calls them. With 12-05's CRUD surface live and 12-01 persisting the winner, this plan adds the three settlement dialogs (clones of `ban-confirm-dialog.tsx`) and the detail page that hosts them, completing Flows 1 & 3. The two-step + mandatory-justification UX is a client concern over the existing endpoints (which enforce `min_length=1`).

Output: the four dialogs (resolve / reverse / force-settle / close), their test, the `[id]/page.tsx` detail page (edit form + status/source-gated action buttons), and the KPI deep-link.

This plan is NOT autonomous: Task 3 is a blocking human-verify gate (SC#5 walk-through).

Reverse copy guard (Pitfall 5): re-resolving after a reversal collides on reused settlement idempotency keys (a known, deferred v1 limitation). The reverse dialog body MUST set the expectation that reversal restores the pre-settlement state for audit/correction and MUST NOT promise "you can resolve again afterward." Use the UI-SPEC §Reverse copy guard verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-admin-market-operations-ui-and-player-resolution-display/12-RESEARCH.md
@.planning/phases/12-admin-market-operations-ui-and-player-resolution-display/12-PATTERNS.md
@.planning/phases/12-admin-market-operations-ui-and-player-resolution-display/12-UI-SPEC.md
@.planning/phases/12-admin-market-operations-ui-and-player-resolution-display/12-02-SUMMARY.md
@.planning/phases/12-admin-market-operations-ui-and-player-resolution-display/12-05-SUMMARY.md
@C:\Users\pobom\xpredict\CLAUDE.md
@C:\Users\pobom\xpredict\.claude\skills\spike-findings-xpredict\SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve / reverse / force-settle / close dialogs + test</name>
  <files>frontend/src/components/admin/resolve-market-dialog.tsx, frontend/src/components/admin/reverse-settlement-dialog.tsx, frontend/src/components/admin/force-settle-dialog.tsx, frontend/src/components/admin/close-market-dialog.tsx, frontend/src/components/admin/__tests__/settlement-dialogs.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/ban-confirm-dialog.tsx  (the WHOLE 133-line clone target: the validate-then-submit core L59-76, the close-guard `onOpenChange={(o) => !submitting && onOpenChange(o)}` L79, the reset-on-open effect L51-57, the mandatory Textarea + role="alert" error L89-107, the destructive Confirm button + Loader2 L118-128)
    - frontend/src/lib/admin-markets-api.ts  (resolveMarket/reverseSettlement/forceSettle + closeMarket from 12-02 — note resolve/reverse/force-settle hit the BARE prefix)
    - frontend/src/lib/admin-markets-types.ts  (ResolveMarketBody {winning_outcome_id, justification}; ReverseBody {justification}; ForceSettleBody {winning_outcome_id, justification})
    - frontend/src/components/ui/select.tsx  (the Select primitive for the YES/NO outcome picker)
    - .planning/phases/12-admin-market-operations-ui-and-player-resolution-display/12-UI-SPEC.md  (§Surface 3 + §Copywriting: dialog titles, "Confirm resolve/reversal/force-settle", "A justification is required.", the per-action success/failure toasts, the §Reverse copy guard body, the close-dialog consequence copy "This stops the market from accepting new bets...")
  </read_first>
  <behavior>
    - resolve-market-dialog: a YES/NO outcome Select (fed from the market's outcomes) ABOVE a mandatory justification Textarea; title "Resolve market"; destructive "Confirm resolve"; on confirm validates outcome selected + justification.trim().length >= 1, then calls resolveMarket(id, {winning_outcome_id, justification}); success toast "Market resolved.", parent refetches; dialog stays open during submit; Loader2 spinner.
    - reverse-settlement-dialog: justification ONLY (no outcome Select); title "Reverse settlement"; body uses the §Reverse copy guard (does NOT promise re-resolution); destructive "Confirm reversal"; calls reverseSettlement(id, {justification}); toast "Settlement reversed."
    - force-settle-dialog: outcome Select + justification; title "Force-settle market"; destructive "Confirm force-settle"; calls forceSettle(id, {winning_outcome_id, justification}); toast "Market force-settled."
    - close-market-dialog: NO reason field (the API takes none); title "Close market"; consequence copy "This stops the market from accepting new bets. Players keep their open positions until resolution."; destructive "Close market"; calls closeMarket(id); toast "Market closed. It's no longer accepting bets."
    - All four block close while submitting; success toast + parent refetch; failure toast per UI-SPEC; 401/403 → "Your session expired. Please sign in again."
    - Empty justification (resolve/reverse/force-settle) is blocked client-side with role="alert" "A justification is required."
  </behavior>
  <action>
    Clone `ban-confirm-dialog.tsx` into each dialog, keeping the structure verbatim (reset-on-open effect, `onOpenChange` close-guard, mandatory Textarea + `role="alert"` `text-sm font-medium text-red-500` error, destructive Button + `Loader2`, stays-open-during-submit). Per dialog:

    - `resolve-market-dialog.tsx`: props `{ open, onOpenChange, marketId, outcomes, onResolved }`. Add a YES/NO outcome `<Select>` above the justification (value = chosen `winning_outcome_id`, items from `outcomes` — label + id). In `handleConfirm`, first guard `!winningOutcomeId` → set a "Select the winning outcome" validation error; then guard `justification.trim().length < 1` → "A justification is required."; then `await resolveMarket(marketId, { winning_outcome_id: winningOutcomeId, justification: justification.trim() })`; `toast.success("Market resolved.")`; `onResolved()`; close. Failure: "Couldn't resolve the market. Please try again." Title "Resolve market"; confirm "Confirm resolve". Reset BOTH outcome + justification on open.

    - `reverse-settlement-dialog.tsx`: props `{ open, onOpenChange, marketId, onReversed }`. Justification only. DialogDescription = the §Reverse copy guard body verbatim ("Reverse this settlement. This posts compensating ledger entries that return every affected player to their pre-settlement balance and writes an audit entry. It does not re-open the market for a clean re-resolution."). `await reverseSettlement(marketId, { justification })`; toast "Settlement reversed."; failure "Couldn't reverse the settlement. Please try again." Title "Reverse settlement"; confirm "Confirm reversal".

    - `force-settle-dialog.tsx`: props `{ open, onOpenChange, marketId, outcomes, onForceSettled }`. Same outcome Select + justification as resolve. `await forceSettle(marketId, { winning_outcome_id, justification })`; toast "Market force-settled."; failure "Couldn't force-settle the market. Please try again." Title "Force-settle market"; confirm "Confirm force-settle".

    - `close-market-dialog.tsx`: props `{ open, onOpenChange, marketId, onClosed }`. NO Textarea (drop the reason field entirely). DialogDescription = "This stops the market from accepting new bets. Players keep their open positions until resolution." `await closeMarket(marketId)`; toast "Market closed. It's no longer accepting bets."; failure "Couldn't close the market. Please try again." Title "Close market"; confirm "Close market".

    For all four, map a 401/403 thrown by the wrapper to the `toast.error("Your session expired. Please sign in again.")` branch (mirror the ban dialog's catch; if the thrown Error message carries the status like "API error: 401", branch on it; otherwise a generic failure toast).

    Create `frontend/src/components/admin/__tests__/settlement-dialogs.test.tsx` (jsdom): (1) resolve dialog — confirming with no justification shows "A justification is required." and does NOT call resolveMarket (mock it); confirming with an outcome + justification calls `resolveMarket(id, {winning_outcome_id, justification})` once; (2) reverse dialog — the body contains "does not re-open the market for a clean re-resolution" and confirming with empty justification blocks; (3) force-settle — empty justification blocks; (4) close dialog — has no reason field and calls closeMarket on confirm. Mock the wrapper functions from `@/lib/admin-markets-api`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- src/components/admin/__tests__/settlement-dialogs.test.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The four dialog files exist; resolve/force-settle contain a `<Select>` for the outcome; reverse has NO outcome Select; close has NO Textarea.
    - `reverse-settlement-dialog.tsx` contains "does not re-open the market for a clean re-resolution" (Pitfall 5 copy guard).
    - Each settlement dialog calls its 12-02 wrapper (`resolveMarket` / `reverseSettlement` / `forceSettle` / `closeMarket`) and blocks close while submitting (`!submitting && onOpenChange`).
    - The test asserts the empty-justification block + the correct wrapper call for each; `cd frontend && pnpm test -- src/components/admin/__tests__/settlement-dialogs.test.tsx` exits 0.
    - `cd frontend && pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>The four action dialogs (two-step + mandatory justification where required, reverse copy guard, close with no reason) are wired to the bare/CRUD endpoints via the 12-02 layer; the dialogs test is green.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/markets/[id] detail page (edit form + status/source-gated action buttons) + KPI deep-link</name>
  <files>frontend/src/app/admin/markets/[id]/page.tsx, frontend/src/components/admin/kpi-card.tsx</files>
  <read_first>
    - frontend/src/app/admin/users/[id]/page.tsx  (the shipped detail-page-hosts-actions convention — Server Component fetches the entity, hands it to a client island that hosts the action buttons + dialogs; mirror its structure)
    - frontend/src/components/admin/market-form.tsx  (the edit form from 12-05 — mode="edit", takes initialValues + betCount; embed it in edit-mode)
    - frontend/src/lib/admin-markets-api.ts  (fetchMarketAdmin(id) → MarketDetail; the action wrappers used by the dialogs)
    - frontend/src/components/admin/{resolve-market-dialog,reverse-settlement-dialog,force-settle-dialog,close-market-dialog}.tsx  (the dialogs from Task 1)
    - frontend/src/components/admin/market-status-badge.tsx  (status chip in the detail header)
    - frontend/src/components/admin/kpi-card.tsx  (KpiGrid L156-182 — the "Pending resolutions" KpiCard L172-175 to wrap in a Link to /admin/markets?status=CLOSED; keep the card visual identical)
    - .planning/phases/12-admin-market-operations-ui-and-player-resolution-display/12-UI-SPEC.md  (§Surface 3 action-button gating: OPEN/CLOSED house→Resolve; OPEN/CLOSED Polymarket→Force-settle; RESOLVED→Reverse; OPEN→Close early; A-KPI-LINK = status=CLOSED)
  </read_first>
  <action>
    1. Create `frontend/src/app/admin/markets/[id]/page.tsx`: a Server Component (`dynamic = "force-dynamic"`) that `await fetchMarketAdmin(id)` (degrade gracefully on failure — show a not-found / error state mirroring the admin/users detail page), inside the `mx-auto max-w-6xl px-6 py-12` shell. Render a header with the question H1 + `<MarketStatusBadge status={market.status} />` + `<SourceBadge>`. Hand the market to a `"use client"` island (define it in this file or a sibling) that:
       - renders `<MarketForm mode="edit" marketId={market.id} initialValues={...} betCount={market.bet_count} />` (the 12-05 form; criteria auto-disables when bet_count > 0);
       - renders the status/source-gated action buttons + their dialogs: a **Resolve** button (opens `ResolveMarketDialog`, passing `market.outcomes`) when `market.status` is OPEN/CLOSED AND `market.source === "HOUSE"`; a **Force-settle** button (opens `ForceSettleDialog`) when OPEN/CLOSED AND `market.source === "POLYMARKET"`; a **Reverse settlement** button (opens `ReverseSettlementDialog`) when `market.status === "RESOLVED"`; a **Close market** `destructive` button (opens `CloseMarketDialog`) when `market.status === "OPEN"`;
       - on any dialog success (`onResolved`/`onReversed`/`onForceSettled`/`onClosed`) calls `router.refresh()` (or re-fetches) so the page reflects the new status.
       Use the shipped Button variants per UI-SPEC §Copywriting (resolve/reverse/force-settle = `destructive`; close = `destructive`; save = `default` inside the form).

    2. `frontend/src/components/admin/kpi-card.tsx`: wrap the "Pending resolutions" `KpiCard` (L172-175 inside `KpiGrid`) in a `next/link` `<Link href="/admin/markets?status=CLOSED">` so the card navigates to the markets-needing-action filter. Keep the card visual identical — only add the navigation affordance (the existing card markup unchanged inside the Link). (A-KPI-LINK: status=CLOSED is the closest queryable proxy for "past-deadline awaiting action".)
  </action>
  <verify>
    <automated>cd frontend && pnpm typecheck && pnpm test -- src/components/admin/__tests__/settlement-dialogs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/admin/markets/[id]/page.tsx` contains `fetchMarketAdmin`, `MarketForm`, and references all four dialogs gated by `market.status` + `market.source` (grep finds `ResolveMarketDialog`, `ForceSettleDialog`, `ReverseSettlementDialog`, `CloseMarketDialog`).
    - The Resolve button is gated to `source === "HOUSE"` and OPEN/CLOSED; Force-settle to `source === "POLYMARKET"` and OPEN/CLOSED; Reverse to RESOLVED; Close to OPEN.
    - `frontend/src/components/admin/kpi-card.tsx` contains `href="/admin/markets?status=CLOSED"` wrapping the Pending resolutions card; the card markup is otherwise unchanged.
    - `cd frontend && pnpm typecheck` exits 0; the settlement-dialogs test still passes.
  </acceptance_criteria>
  <done>The admin market detail page hosts the edit form + status/source-gated resolve/reverse/force-settle/close actions; the KPI Pending-resolutions card deep-links to the action filter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC#5 end-to-end-through-the-UI acceptance (operator → player resolution)</name>
  <action>
    This is a BLOCKING human-verify gate — NOT autonomous work. All code is already shipped + machine-verified by Tasks 1-2 and the prior waves (12-01..05). The executor's job here is to boot the stack, present the SC#5 walk-through below to Pol, and PAUSE for sign-off — do NOT auto-approve (this checkpoint ignores any auto-advance setting). Run the documented local stack command (or use staging), then guide the human through the numbered loop in <how-to-verify>, recording the per-step outcome in the SUMMARY. Resume only on an explicit "approved" (or capture the failing step + surface + error if it breaks).
  </action>
  <what-built>
    The full operator + player resolution loop is now reachable through the product UI: admin market CRUD (12-05) + resolve/reverse/force-settle (this plan) + the per-market stake limits (12-03) + the player resolution display (12-04), over the winner-persisting backend (12-01). This checkpoint verifies SC#5 — the integration acceptance that spans all slices — with NO raw-API step anywhere in the loop.

    All autonomous work is complete and machine-verified before this gate: backend suites (settlement/markets/bets/admin) green, frontend unit tests green (URL-contract guard, status badge, resolution panel, market form, settlement dialogs), and typecheck clean. This gate is the human walk-through that the audit's Flows 1 & 3 now pass through the UI.
  </what-built>
  <how-to-verify>
    Run the stack (the documented local command, or staging). Then, signed in as an admin (Bearer) and as a verified player (cookie), walk the loop entirely through the UI — no curl/raw API:

    1. **Create (ADM-02):** Admin → /admin/markets → "Create market" → fill question / resolution criteria / a future deadline / leave odds at 50/50 / optionally set Min stake + Max stake → submit. Expect the toast "Market created." and the market appears in the list.
    2. **List + filter (ADM-01):** confirm the new market shows with a HOUSE source badge + OPEN status chip; exercise the source/status/category filters.
    3. **Bet (BET-06):** as the player, open the market detail, try a stake below Min and above Max → expect the inline "Stake must be between {min} and {max} PLAY_USD." message; place a valid in-range bet → expect the confirm dialog → success.
    4. **Criteria lock (ADM-07):** back as admin, open the market detail (/admin/markets/{id}) → the resolution-criteria field is DISABLED with the locked helper (the market now has a bet); odds/deadline stay editable.
    5. **Resolve (ADM-05 / STL-02):** click "Resolve" → pick the winning outcome → enter a justification → "Confirm resolve" → toast "Market resolved." The status chip flips to RESOLVED.
    6. **Player resolution display (STL-06):** as the player, reload the market detail → the right panel now shows "Resolution": winning outcome, "Resolved by Operator", the public justification, the settled date, and YOUR Won/Lost + payout + realized P&L. The page does NOT 404.
    7. **Reverse (STL-07):** back as admin, on the RESOLVED market click "Reverse settlement" → confirm the body says it does NOT re-open for clean re-resolution → enter a justification → "Confirm reversal" → toast "Settlement reversed."
    8. **Force-settle (ADM-06):** on an OPEN/CLOSED Polymarket-mirrored market, confirm the "Force-settle" button + two-step + justification flow works (if no mirrored market is past deadline, verify the button appears + the dialog opens).
    9. **KPI deep-link:** /admin dashboard → click the "Pending resolutions" card → lands on /admin/markets?status=CLOSED.
  </how-to-verify>
  <resume-signal>Type "approved" if the loop passes end-to-end through the UI, or describe the step that broke (with the surface + error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> bare /admin/markets/{id}/resolve|reverse|force-settle | settlement mutations are admin-Bearer-gated server-side; the dialogs forward the admin_jwt via the use-server layer |
| admin justification input -> audit/settlement | the justification is mandatory + persisted; the backend enforces min_length=1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-18 | Repudiation | resolve/reverse/force-settle without a justification | mitigate | Backend enforces `min_length=1` (tested: `test_resolve_422_when_justification_blank`); each dialog also blocks empty justification client-side with `role="alert"` before calling the wrapper |
| T-12-19 | Tampering / Repudiation | wrong-prefix settlement call silently 404s | mitigate | The 12-02 wrappers (guarded by `admin-markets-api.test.ts`) target the BARE `/admin/markets/{id}/...` prefix; the dialogs only call those wrappers |
| T-12-20 | Elevation of Privilege | non-admin reaching resolve/reverse/force-settle | accept (backend-owned) | `current_active_admin` gates every endpoint (401/403, tested in `test_settlement_router.py`); this plan adds no new endpoint |
| T-12-21 | Tampering | re-resolve after reverse corrupts the ledger (idempotency-key collision, Pitfall 5) | mitigate (UX) | The reverse dialog copy explicitly does NOT promise re-resolution; the known v1 limitation is surfaced, not silently invited (a clean re-resolve would need a backend per-bet epoch — out of scope, flagged) |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs (RESEARCH §Package Legitimacy Audit); no install task present |
</threat_model>

<verification>
- `cd frontend && pnpm test -- src/components/admin/__tests__/settlement-dialogs.test.tsx` → green.
- `cd frontend && pnpm typecheck` → exit 0.
- **Wave merge (full):** `cd backend && uv run pytest` (CI-graded for the integration tier — scan `origin/main..HEAD`, the ~4 pre-existing Windows-only failures are NOT regressions) + `cd frontend && pnpm test && pnpm typecheck`.
- **SC#5 human-verify gate (Task 3):** the operator→player resolution loop passes end-to-end through the UI with no raw-API step.
</verification>

<success_criteria>
- STL-02/ADM-05: two-step resolve with outcome + mandatory justification, wired to bare /admin/markets/{id}/resolve.
- STL-07: reverse (justification only) with the no-re-resolution copy guard.
- ADM-06: force-settle (outcome + justification) for Polymarket markets.
- ADM-04: early close from the detail page.
- The KPI Pending-resolutions card deep-links to /admin/markets?status=CLOSED.
- SC#5: the full operator→player loop passes through the UI (human-verify gate).
</success_criteria>

<output>
Create `.planning/phases/12-admin-market-operations-ui-and-player-resolution-display/12-06-SUMMARY.md` when done. Record the SC#5 walk-through outcome and any limitation surfaced (esp. the reverse→re-resolve v1 limitation per Pitfall 5).
</output>

"""Health endpoint stubs — actual handlers live in ``app.routers.health``.

This module is kept as the D-07 layout placeholder; future phases that want to
add operational endpoints (e.g., ``/metrics`` in Phase 11 hardening) can wire
helpers here.
"""
